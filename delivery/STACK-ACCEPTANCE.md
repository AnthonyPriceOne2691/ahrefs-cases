# Stack acceptance

**Date:** 2026-09-10
**Stack:** delivery@1.88 · cqg@absent · okf@absent
**Где лежат каноны:** вне репо: `~/Documents/Prepare` (вариант D, см. «Остатки»)

checked_at: 2026-09-10
stale_after: 2026-10-10

## Что развёрнуто

Развёртывание идёт **волнами** по `docs/CONTOUR_ROLLOUT.md`: ось приходит, когда
канон нужен, с названным предельным сроком. Пройдена волна В0.

| Слой | Состояние | Примечание |
|---|---|---|
| ① delivery/ | deployed | дерево §2.3 полностью, constitution заполнен под продукт |
| ② гейты | absent | волна В1, предельный срок — начало Ф2 (появится код, который гейтам есть что просматривать) |
| ③ knowledge/ | absent | волна В2, предельный срок — начало Ф8 (до калибровки порогов) |
| ④ CI + гейт мержа | weak | приходит волной В1 вместе с ②; merge_guard: нет; branch protection: нет |
| ⑤ поведение модели | n/a | волна В3; в MVP текст кейса шаблонный, модель не вызывается |

## Карта ролей гейтов

Пусто до волны В1: гейтов формы в проекте пока нет. Заполняется при развёртывании ②
по CQG «Применимость → Карта ролей».

| Роль | Канонический скрипт | В этом проекте | Статус |
|---|---|---|---|
| — | — | — | n/a: волна В1 не пройдена |

## Полнота, не только зелёность

- [ ] `check_gate_coverage.sh` — n/a: приезжает с ② (волна В1)
- [ ] `pre-commit run --all-files` — n/a: приезжает с ② (волна В1)
- [x] `stack_selftest.py` — гоняется вне репозитория, где лежат каноны (вариант D):
      OKF_KNOWLEDGE_BUNDLE.md: 7 executable block(s) checked, 13 skipped
      
      stack_selftest: 112 block(s) checked, 51 skipped (markdown 35, text 11, <none> 2, javascript 2, ini 1), 0 failure(s)
- [x] Непустое число просмотренных файлов — **проверено там, где это применимо
      сегодня:** `delivery_check.py` читает и судит 15 файлов дерева `delivery/` и
      `.claude/settings.json`, а не пропускает их мягко. Гейты формы, у которых
      это число главный признак настройки (CQG §6), в проекте отсутствуют — и это
      записано состоянием `② absent`, а не закрыто галочкой.

## Механика в чистом клоне

```
$ git clone . <clone> && python3 scripts/delivery_check.py
  0 error(s), 1 warning(s) — warning: ci-oracles weak (волна В1)
```

Клон несёт механику и не несёт текстов канонов — проверяемое поведение варианта D.
Полный вывод: `delivery/active/verify-report.md`.

## Остатки (не закрыто — и почему)

| Инвариант | Почему не закрыт | Компенсация |
|---|---|---|
| force-push / обход админом | CI и гейт мержа не развёрнуты (волна В1) | до В1 — правило в constitution; после — `merge_guard.sh` |
| «payload канонов не сломан» в CI проекта | вариант D: текстов канонов в репозитории нет по построению | `stack-selftest: external (~/Documents/Prepare)`, руками при каждой правке канона |
| `git add -f` обходит `.git/info/exclude` | на репозитории, который станет чужим, закрыть нечем | остаток назван в STATUS; проверка `--check-local` даёт два нуля |
| скрининг состава у доктора (`cqg@2.25`) | читает `docs/canon/extract_payload.py`, которого под D в репо нет | локальный прогон, ручной |
| гейты формы (ruff / mypy / pre-commit) | волна В1 не пройдена | `shape-oracles: weak` в STATUS с названной причиной |

## Разбор: где эта процедура подвела

Записывать КАЖДЫЙ случай, когда приёмка показала зелено, а дыра была.
Формат: симптом → корневая причина → что добавлено в harness → как проверено.

### 2026-09-10 — клиентские документы уехали в первый коммит

- **Симптом:** первый коммит (до публикации) содержал ТЗ клиента и замеры
  внутренней системы агентства, хотя решение «клиентское — локально» было принято.
- **Причина:** механизм, а не невнимательность: `.git/info/exclude` действует
  только на **неотслеживаемые** пути. Файлы уже были в индексе, и следующий
  `git add -A` вернул их после `git rm --cached` — то есть правильная команда,
  выполненная в неправильном порядке, выглядела как выполненная.
- **Что добавлено:** порядок закреплён в `AGENTS.md` («класть строки игнора до
  первого коммита контура»), проверка — `extract_payload.py --check-local`, у
  которой два числа: «уже уехало» и «уедет при следующем add».
- **Как проверено:** `git log --all --name-only` по именам клиентских документов —
  пусто; `--check-local` даёт два нуля. Переписано до пуша, в опубликованной
  истории их не было.
