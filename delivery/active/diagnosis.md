# Diagnosis journal

**Симптом:** три наблюдения живого прогона, не пойманные ни одним из 468 тестов.
**Repro:** прогон Ф7 12.09.2026 на пяти доменах, ключ владельца.

- H1: остаток квоты по ключу и есть потолок → проверил ответ
  `subscription-info`: лимитов **два**, ключа (2 000 000) и воркспейса
  (8 000 000), и воркспейс делится с другими сервисами агентства →
  ОПРОВЕРГНУТА: потолок — минимум
- H2: `collect` собирает то, что просили → проверил: команда берёт **все**
  проекты базы (`select(Project)` без условия), и на стенде это 16 проектов
  вместо пяти названных → ПОДТВЕРЖДЕНА как дыра процедуры
- H3: подпись значения обрезана шрифтом или длиной числа → проверил геометрию:
  `_RIGHT = 46` при подписи из семи знаков с пробелами-разделителями; метка
  рисуется правее последней точки и выходит за `_WIDTH` → ПОДТВЕРЖДЕНА

**Корневая причина** у каждого своя, общее одно: все три видны только на
исполнении. Квота — потому что второй лимит появляется лишь в живом ответе;
область прогона — потому что на фикстурах «вся база» и «мой список» совпадают;
подпись — потому что в разметке SVG она не обрезана, обрезает её `viewBox` при
растеризации.

**Почему прошло мимо гейтов:** ни один из трёх не выражается в коде как ошибка.
Первый — недостающее сравнение, второй — отсутствующий параметр, третий —
геометрия. Оракулы добавлены на первые два; третий проверяется рендером PDF.
