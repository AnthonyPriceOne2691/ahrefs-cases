# Eval smoke — this shipment

- [ ] E1 — `delete(Model)` без условия краснеет с файлом и строкой
- [ ] E2 — `delete(Model).where(...)` проходит
- [ ] E3 — помеченный выход проходит
- [ ] E4 — контрольный проект с точками и вердиктом пережил прогон
- [ ] E5 — контрольный прогон и расход units пережили прогон
- [ ] E6 — все тесты зелёные
