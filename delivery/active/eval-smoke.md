# Eval smoke — this shipment

Derived from spec acceptance. Run during verify.

- [ ] E1/E2 — короткий период собирается историей, длинный точками; обе цены и причина видны в решении
- [ ] E3 — при равной цене выбрана история
- [ ] E5 — перекрытые окна дают историю независимо от цены
- [ ] E6 — при `pre_start_baseline_months: 0` до старта работ не запрашивается ни один месяц
- [ ] E7 — golden-таблица классификации зелёная после ухода `org_cost` из шага 1
- [ ] E8 — смета совпадает с числом точек, которое вернул провайдер, в обеих схемах
- [ ] E9 — сквозной прогон: разбивка по способу и «стоимость запуска на 100 URL» в отчёте, шаг 1 в пределах 10 000 units
- [ ] E10 — повторный прогон: кэш и строка `cached` на месте
- [ ] E11 — `history_truncated` сохраняется в обеих схемах
