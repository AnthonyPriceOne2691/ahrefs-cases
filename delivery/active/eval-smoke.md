# Eval smoke — this shipment

- [x] E1 — потолок берётся по меньшему из двух остатков
- [x] E2 — нет полей воркспейса: остаток по ключу, без ошибки
- [x] E3 — `--only` собирает ровно названное
- [x] E4 — неизвестный домен в списке останавливает прогон
- [x] E5 — длинная подпись развёрнута влево и внутри рамки
- [x] E6 — короткая остаётся справа
