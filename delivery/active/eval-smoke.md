# Eval smoke — this shipment

- [x] E1 — `Retry-After: 7` даёт паузу 7 с, а не 1 с из лесенки
- [x] E2 — `Retry-After` HTTP-датой считается от текущего времени
- [x] E3 — заголовка нет или он мусорный — работает прежняя лесенка
- [x] E4 — просят дольше потолка: отказ сразу, величина названа в ошибке
- [x] E5 — 503 с `Retry-After` слушается так же, как 429
