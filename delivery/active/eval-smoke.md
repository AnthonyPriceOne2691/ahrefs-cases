# Eval smoke — this shipment

- [x] E1 — несколько выбранных уезжают одним архивом, в запросе их номера
- [x] E2 — один выбранный уезжает PDF, а не архивом из одного файла
- [x] E3 — «выбрать всё» берёт только то, у чего есть файл
- [x] E4 — пока ничего не выбрано, панели выбора нет
- [x] E5 — значок темы в левой колонке и не внутри списка разделов
- [x] E6 — клик меняет схему и подпись значка
- [x] E7 — выбор темы переживает перезагрузку
