# Eval smoke — this shipment

- [x] раскладка «пакет в корне» → ставится `-e .`, предупреждения нет
- [x] раскладка «backend/» → ставится `-e backend`, поведение прежнее
- [ ] прогон CI после пуша: аннотаций нет
