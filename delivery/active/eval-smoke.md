# Eval smoke — this shipment

Сквозной путь: собранные серии → вердикт с объяснением. Ahrefs не трогается.

```
$ python3 scripts/run_collect.py classify
$ python3 scripts/run_collect.py explain example.com
```

- [ ] `classify` печатает версию порогов и распределение по четырём группам
- [ ] сумма по группам равна числу классифицированных проектов
- [ ] `explain` показывает каждое условие: факт, порог, сработало ли, решает оно или справочно
- [ ] у «хороших» видно, что сработали и главная метрика, и подтверждающая
- [ ] повторный `classify` не плодит вердикты: один проект на одну версию порогов
