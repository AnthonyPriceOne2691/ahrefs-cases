# Eval smoke — this shipment

Derived from spec acceptance. Run during verify.

- [ ] E1 — `up -d` поднимает шесть сервисов
- [ ] E2 — `/api/health` отвечает, миграция применена
- [ ] E3 — экран входа открывается, вход работает через прокси
- [ ] E4 — правка `src/` подхватывается без пересборки
- [ ] E5 — правка `web/src/` подхватывается сама
- [ ] E6 — контекст сборки без `.venv` и `node_modules`
- [ ] E7 — `node_modules` машины не подменяет контейнерные
- [ ] E8 — api ждёт готовности базы
