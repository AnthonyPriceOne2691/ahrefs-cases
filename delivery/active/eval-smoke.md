# Eval smoke — this shipment

Сквозной путь поставки: файл → проекты в базе → серии → отчёт. Прогоняется на
verify целиком, командой, а не по частям: части уже проверены тестами, а здесь
проверяется, что они соединены.

```
$ python3 scripts/run_collect.py all config/projects.example.csv
```

- [x] печатает `провайдер: fixture` — прогон не трогает квоту заказчика
- [x] `принято: 3 (создано 3, обновлено 0)`, отклонённых нет
- [x] повторный запуск той же команды: `создано 0, обновлено 3` — проекты не удвоились
- [x] `прогон N: done`, `проектов: 3 (собрано 3, ...)`
- [x] `точек записано: 100` — серии в базе, а не пустой прогон
- [x] `units потрачено: 150` = 3 × 50 по модели стоимости `metrics-history`
- [x] `python3 scripts/run_collect.py intake docs/нет-такого.csv` — внятный отказ, не трассировка

Прогнано 2026-09-10 на чистой дев-базе: оба запуска `all`, затем негативные
случаи. Наблюдалось ровно то, что перечислено выше; отказ по несуществующему
файлу и по `.pdf` печатает строку и возвращает код 2, а не трассировку —
это было починено по ходу smoke, а не объявлено пройденным.
