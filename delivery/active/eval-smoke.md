# Eval smoke — this shipment

Derived from spec acceptance. Run during verify.

- [ ] E2 — список «было → стало» есть, в базе ни одной новой строки
- [ ] E3 — предпросмотр и пересчёт дают одинаковые группы
- [ ] E8 — «изменений нет» сказано словами
- [ ] E8a — проект без вердикта показан как «нет вердикта → группа»
- [ ] E9b — нехватка покрытия показана отдельной строкой с месяцами
