# Eval smoke — this shipment

Derived from spec acceptance. Run during verify.

- [ ] E1 — вердикты новой версии появились, старые остались
- [ ] E4 — активна ровно одна версия, вердикты прежней не тронуты
- [ ] E5 — повторный пересчёт обновляет, а не удваивает
- [ ] E6 — сотня проектов и ни одного HTTP-запроса
- [ ] E7 — несуществующая версия даёт ошибку со списком доступных
- [ ] E9 — версия просит окно шире купленного: нехватка названа, вердикт не выдан
- [ ] E10 — окно совпадает с купленным: пересчёт проходит молча
