# Eval smoke — this shipment

- [ ] W1 — две кампании одного сайта: два PDF с разными sha256, две строки `кейсы.csv` со своими файлами и группами
- [ ] W2 — у каждой кампании своя строка `cases` со своими числами, её артефакт — её файл
- [ ] W3 — номер сборки в имени файла — своей кампании, и он равен версии её строки `cases`
- [ ] W4 — «(2)» получает поздняя кампания, сколько бы раз ни собирали
- [ ] W5 — контент-запрет одной кампании не отнимает кейс у другой и не даёт запрещённой строку `cases`
- [ ] W6 — исполнение: копия дев-базы, `nordvpn.com` — два PDF и две строки списка, `zeta-clinic.example` — две строки «не попал»; до правки — один PDF и одна строка
