# Eval smoke — this shipment

Derived from spec acceptance. Run during verify.

- [ ] E1 — XLSX на десять строк: принято 10
- [ ] E2 — битая строка отклонена с причиной, годные приняты
- [ ] E3 — повторная загрузка обновляет, не удваивает
- [ ] E4 — неизвестный формат: `400`, не `500`
- [ ] E5 — превышен предел: `413` с названным пределом
- [ ] E6 — закрытая Google Sheet: `400` про страницу входа
- [ ] E7 — без права `run`: `403` с названием права
- [ ] E8 — смета совпадает с расчётом прогона
- [ ] E9 — не хватает квоты: `may_start=false`, числа в причине
- [ ] E10 — остаток неизвестен: вердикт `unknown`
- [ ] E11 — `/api/runs/estimate` не перехвачен `/{run_id}`
- [ ] E12 — окна точек у сметы и у задачи одни
