# Eval smoke — this shipment

Журнал прогонов на боевой сборке ветки (`vite build`, `vite preview` с прокси на
API ветки), копия дев-базы (29 прогонов: 8 live, 20 fixture, 1 без режима),
headless Chrome 154 по CDP, обе темы; 24.09.2026. Два прохода: до переноса на
main (сборка `index-DMXXwa7e.js`, копия `cases_journal_check`) и после — с #25
(`index-sG87de7E.js`, `cases_journal_check2`). Результаты совпали; ниже —
второй проход. Копии удалены.

- [x] W1 — раскрытие прогона 1088 (18 строк): `проверка-провал.example`,
      `контрольный.example`, `проверка-слабый.example`, `müller-shop.de`,
      `проверка-рост.example`; в колонке домена ни одного `xn--`. После
      удаления проекта `müller-shop.de` на копии — «müller-shop.de (проект
      удалён)»: пометка #25 и правило 4а вместе
- [x] W2 — тестом (на стенде нет хоста, который обратно не разбирается)
- [x] W3 — все 20 fixture-прогонов (обе страницы журнала): под «смета →
      факт» — «условные units: прогон без живого ключа (fixture)»
- [x] W4 — все 8 живых (1089, 1088, 577, 565, 564, 327, 286, 285): без пометки
- [x] W5 — прогон 132: «режим не записан — units не считаются расходом»
- [x] W6 — тестом: `live` журнала совпал с приращением «потрачено»
