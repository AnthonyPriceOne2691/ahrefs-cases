# Eval smoke — this shipment

Derived from spec acceptance. Run during verify.

- [x] E1 — вердикты новой версии появились, старые остались: у проекта два вердикта с разными `ruleset_id`
- [x] E4 — активна ровно одна версия; число вердиктов прежней не изменилось
- [x] E5 — повторный пересчёт по той же версии оставил один вердикт, а не два
- [x] E6 — десять проектов, ноль HTTP-запросов (транспорт подменён падающим)
- [x] E7 — несуществующая версия даёт ошибку с перечислением доступных
- [x] E9 — версия с `pre_start_baseline_months: 3` на купленной без запаса истории: пропуск с месяцами 2024-10…12, вердикт не записан
- [x] E10 — окно 6 месяцев внутри купленной истории: пересчёт молчит, «пропущено 0»
- [x] E9-наоборот — домен без истории получает `insufficient_data`, а не пропуск
- [x] Различитель — дыра внутри собранного отрезка не считается нехваткой покупки
