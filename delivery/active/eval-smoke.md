# Eval smoke — this shipment

- [x] E1 — IDN-домен из файла находится в базе
- [x] E2 — `WWW.Example.COM/path` приводится к `example.com`
- [x] E3 — не-домен даёт отказ с причиной, а не «в базе нет»
- [x] E4 — цепочка исключений: в журнале первопричина
- [x] E5 — одиночная ошибка: текст прежний
