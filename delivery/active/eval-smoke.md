# Eval smoke — this shipment

Derived from spec acceptance. Run during verify.

- [ ] E1 — боевые образы собираются
- [ ] E2 — шесть сервисов поднимаются, migrate до api
- [ ] E3 — `/` отдаёт приложение через nginx
- [ ] E4 — `/api/health` проходит через прокси
- [ ] E5 — вход через nginx возвращает токен
- [ ] E6 — маршрут SPA напрямую не даёт 404
- [ ] E7 — без `JWT_SECRET` сервис не поднимается молча
- [ ] E8 — в образе нет dev-зависимостей и исходников фронта
- [ ] E9 — убитый контейнер поднимается сам
