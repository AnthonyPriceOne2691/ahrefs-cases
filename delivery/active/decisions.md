# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-15 · выбрал определять источник остатка по `config.ahrefs.api_key` вместо `config.ahrefs.provider` — решение владельца; потому что режим рядов отвечает на вопрос «откуда брать метрики», а остаток принадлежит ключу: на стенде с фикстурными рядами строка «Остаток квоты» показывала зашитые в `FixtureQuota.left` 10 000 при настоящем остатке около 1,46 млн, и по ней решают, запускать ли прогон
- 2026-09-15 · выбрал признаком наличие ключа вместо безусловного живого источника — потому что без ключа запрос невозможен физически, и `LiveQuota` упёрлась бы в отказ на каждой чистой установке; с ключом же запрос бесплатен (`subscription-info`, 0 units), то есть цена решения нулевая
- 2026-09-15 · выбрал снимать ключ в `tests/conftest.py` присваиванием вместо `setdefault` — потому что `setdefault` уступает переменной окружения, а боевой ключ приходит из `.env` разработчика: правка `build_quota` тут же отправила в сеть 16 тестов и уронила ещё 15 ошибками, и это при обещании докстроки «ни один тест не берёт живой ключ»
- 2026-09-15 · выбрал оставить `FixtureQuota` с её десятью тысячами вместо бесконечности — потому что мягкий стоп и смета обязаны срабатывать в разработке: бесконечный остаток означал бы, что первое их настоящее срабатывание случится на живых деньгах
