# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.
- 2026-09-11 · выбрал открыть предпросмотр порогов правом `read` вместо `edit_thresholds` — потому что пересчёт бесплатен (Ahrefs не дёргается), а последствия смены порогов полезно видеть тем, кто потом спорит о группах: смотреть и применять разные действия (§8 документа реализации)
- 2026-09-11 · выбрал неизменяемость версии (повтор имени — 409) вместо правки на месте — потому что вердикты ссылаются на версию, и правка задним числом делает прошлые решения необъяснимыми
- 2026-09-11 · выбрал отдавать алерты состоянием (`GET /api/alerts`) вместо отправки в Telegram — потому что доставка требует настройки каналов и токенов заказчика, а состояние можно отдать сейчас и показать на экране Ф6
