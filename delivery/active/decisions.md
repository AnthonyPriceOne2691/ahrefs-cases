# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-11 · выбрал скачивать файл запросом `download()` в `api/client.ts` вместо ссылки `<a href>` — потому что браузер отправляет ссылку без заголовка `Authorization`, а токен у нас не в куке: человек получил бы `401` вместо кейса
- 2026-09-11 · выбрал раскодировать имя из `filename*=utf-8''…` в `nameFromHeaders` вместо имени по умолчанию — потому что имена кейсов кириллические («сайт в нише финтех — Кейс.pdf»), и без раскодирования человек нашёл бы в загрузках файл из процентов
- 2026-09-11 · выбрал отзывать ссылку сразу после клика (`revokeObjectURL` в `pages/cases/save.ts`) вместо отзыва при уходе со страницы — потому что blob держится в памяти вкладки, а экран открыт целый день: каждая скачанная пачка оставалась бы в нём мегабайтом
- 2026-09-11 · выбрал метку «домен скрыт» в `CasesTable.tsx` вместо показа поля `anonymized` — потому что поле верно и бесполезно тому, кто решает, что отправить клиенту: решение принимают по следствию, а не по флагу
- 2026-09-11 · выбрал развести `PackSection` и `CaseLibrary` вместо одного `CasesPage` — потому что гейт длины функции покраснел на 92 строках и сложности 11: в одном компоненте оказались две разные работы, «что можно забрать целиком» и «что собрано»
