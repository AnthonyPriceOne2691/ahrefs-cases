# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-11 · выбрал второй тип «замечание» вместо флага внутри отказа — потому что флаг обязан помнить каждый читатель отчёта, а забывший посчитает замечания отклонёнными строками: число «отклонено строк» начало бы врать
- 2026-09-11 · выбрал оставить объём неизвестным вместо вытаскивания первого числа из текста — потому что «12 статей и 214 ссылок» дало бы 12, и неверное число уехало бы в кейс клиенту; отсутствующее число видно, неверное — нет
- 2026-09-11 · выбрал смягчить **только** `work_volume` вместо всех полей — потому что он единственный, которому разрешено быть пустым и который не участвует в расчётах: у гео и флага публикации отказ и есть защита
