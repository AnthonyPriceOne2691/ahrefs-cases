# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.

- 2026-09-11 · выбрал узкий набор полей для кривой позиций (`top3`, `top4_10`) вместо всех пяти корзин — потому что цена строки это `10 × поля + 1`: 21 против 51, середина периода 315 units против 765 на кейс, а распределение топ-3/10/100 уже куплено шагом 2 в точках А и Б
- 2026-09-11 · выбрал докупать середину периода вместо всей истории — потому что границы куплены шагом 2, и повторная покупка четырёх строк стоит 84 units на кейс за данные, которые есть
- 2026-09-11 · выбрал покупать `traffic value` двумя точками вместо истории — потому что в кейсе это число «трафик стоимостью $X», а не кривая: 100 units против 209 за серию, которую никто не покажет
- 2026-09-11 · выбрал месячное разрешение кривой вместо квартального — потому что ТЗ прямо называет гранулярность месячной, а после сужения полей разница стала 105 units на кейс: информативность здесь дороже экономии
- 2026-09-11 · выбрал форсировать историю для кривой вместо выбора схемы по цене — потому что дешевле по-прежнему две точки (100 против 399), но ступень существует ровно ради серии: цена не может решать там, где решает назначение данных
- 2026-09-11 · выбрал перенести DR из флага шага 2 в ступень кейса вместо того, чтобы включить флаг — потому что DR в must-have списке ТЗ, а выключенный флаг откладывает требование молча (урок L33): тридцати кандидатам историей он стоил бы 3000 units, десяти кейсам двумя точками — 1000
- 2026-09-11 · выбрал переснять снимок дублей вместо схлопывания двух публичных входов в одну функцию — потому что `collect_stage2` и `collect_case_data` обязаны иметь одинаковую сигнатуру по построению, а функция с параметром-ступенью вынесла бы магическое число 3 в публичный API; тело у них уже общее (`_run_by_ids`), и настоящий дубль поставки — фильтр окна в `cache.py` — вынесен в `_in_window`, а не переснят
