# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-24 · выбрал отказ `400` для брака файла (`UnfitSourceError` из `validate.check_fit`) вместо строк `missing_column`/`empty_source` в отчёте `200` — потому что `200` говорит «принято» всем потребителям сразу (экран пишет «Принято из …», CLI отвечает кодом 1, лог — `intake_accepted`), и это та же ложь успехом, что L10 у закрытой таблицы
- 2026-09-24 · выбрал реляционный тест над общим `listFormat.json` вместо эндпоинта `GET /api/intake/columns` — потому что описания и примеры колонок всё равно живут на фронте и сверка нужна в любом случае, а эндпоинт добавил бы подсказке состояние загрузки и каждому тесту экрана подменный ответ; колонки — константы кода, сервер и фронт уезжают одним коммитом (роли из L101 меняются в базе на ходу — там справочник с сервера оправдан)
- 2026-09-24 · выбрал не вводить отказ по числу строк вместо предела в 100 из ТЗ — потому что предел объявлен как объём загрузки, а не правило приёма: смета и прогон считают всю базу, второй файл обходит предел на загрузку, и он не ограничил бы ни одного unit; траты держит смета с квотой. Вопрос оставлен владельцу
- 2026-09-24 · выбрал ловить любое исключение openpyxl при чтении книги (`xlsx_source.read_xlsx_stream`) вместо списка известных типов — потому что на 4 видах брака нашлось 3 разных типа (`BadZipFile` на PDF и CSV, `KeyError` на DOCX, `ParseError` на обрезанном листе), и каждый не пойманный — пятисотка человеку; приведение ячеек, то есть наш код, вынесено из блока и так не прячется
- 2026-09-24 · выбрал подсказку ближайшего имени в тексте отказа («period_start — в шапке «period start», переименуйте») вместо того, чтобы принимать `Period Start` как `period_start` — потому что владелец просил не брать неподходящее, а не расширять формат; тихое прощение ввело бы второе правило соответствия имён (урок L124) и поменяло бы контракт и для `--only`
- 2026-09-24 · выбрал ставить отказ под полем, которым давали список (`SourceForm` помнит последний способ), вместо общей плашки под обоими полями — потому что отказ целиком чинится в источнике, и плашка, общая на 2 поля, не говорит, в каком (`asked` в `SourceForm.tsx`); `RunsPage` при этом не тронут: ошибка по-прежнему одна строкой, место выбирает форма
- 2026-09-24 · выбрал сбрасывать поле выбора файла сразу после выбора (`resetRef` у `FileButton`) вместо сброса после ответа — потому что браузер не шлёт `change` на тот же файл, и поправленный после отказа список иначе не уходит вовсе (замер в Chrome: на `origin/main` второй выбор того же файла дал 0 запросов, на ветке — 1); к моменту сброса файл уже взят
- 2026-09-24 · выбрал проверять размер файла на экране до отправки вместо того, чтобы полагаться только на `413` сервера — потому что через прокси ответ `413` до человека не доходит: сервер рвёт соединение на пределе, прокси ловит `EPIPE` и отвечает `500` (замер в Chrome через `vite preview`); предел берётся из `listFormat.json`, который тест сверяет с `MAX_UPLOAD_BYTES`, так что второй копией правила это не стало. Остальные проверки остались только на сервере
- 2026-09-24 · выбрал убрать 2 кода строк `empty_source` и `missing_column` (`RejectReason` и `REASONS` в `IntakeOutcome.tsx`) и плашку «в источнике не нашлось ни одной строки» вместо того, чтобы оставить их «на всякий случай» — потому что сервер их больше не шлёт, а отчёт с нулём принятых и нулём отклонённых стал недостижим; мёртвый перевод — приглашение снова прислать брак файла строкой отчёта

