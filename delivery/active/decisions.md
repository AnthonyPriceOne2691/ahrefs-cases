# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-12 · выбрал убрать умолчание `source` у девяти функций (`classify_all`, `recalc`, `preview`, `diagnose_poor`, `build_cases` и соседи) вместо правки двух вызовов — потому что это третье повторение L53 подряд, и умолчание здесь и есть дефект: на фикстурах оно совпадает с истиной, и ни один из 459 тестов промах поймать не может
- 2026-09-12 · выбрал оракул на **сигнатуры** (`tests/test_source_is_required.py`) вместо теста на конкретные вызовы — потому что вызовов становится больше с каждым экраном, а свойство «источник называет вызывающий» одно и проверяется у функции, а не у места
- 2026-09-12 · выбрал `configured_source()` из `api/routers/projects.py` в роутере порогов вместо второй такой же функции — потому что она уже спрашивает источник у фабрики провайдера, а вторая копия разошлась бы с первой ровно тогда, когда режим станет сложнее одного флага
