# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-24 · выбрал строгое правило «себя не лишить `manage_users` никогда» (`_refuse_losing_own_management` в `api/routers/users.py`) вместо правила «последнего управляющего» с подсчётом по базе — потому что так решил владелец («Строже: себе нельзя вообще»), и строгое даёт инвариант без подсчёта: API отвечает только управляющему, а он себя не лишит права, не выключит и не удалит; подсчёт по всей базе к тому же не изолируется тестом через API (урок L68)
- 2026-09-24 · выбрал смотреть на итог прав после правки через `rights_of_user` вместо разбора полей запроса (`personal_rights`, `group`) по отдельности — потому что право теряется 2 путями (личное «Нет» и смена своей группы), и правило перекрытия группы личным решением живёт в одной функции `deps.rights_of_user`; разбор полей завёл бы его вторую копию, которая разошлась бы при первом новом пути
- 2026-09-24 · выбрал проверять после записи полей в объект и полагаться на откат сессии (`storage/session.py`: rollback на ошибке) вместо предварительного расчёта «как будет» на копии — потому что копия `User` потребовала бы второй сборки прав; откат проверен исполнением: после отказа в базе-двойнике строка осталась `ENGINEER`, `{}`
- 2026-09-24 · выбрал оставить защиту «последний администратор» (`_refuse_last_admin`) как есть вместо того, чтобы растворить её в новой — потому что это отдельное решение из CRM с отдельным тестом `test_last_admin_is_not_deleted`: удалить последнего админа нельзя даже при сотруднике с личным `manage_users`; растворение в новом правиле сломало бы этот тест и отменило бы решение, которого владелец не отменял
- 2026-09-24 · выбрал не трогать экран (`ManageUser.tsx`) вместо того, чтобы прятать «Нет» у своего права заранее — потому что экран устроен показывать отказы сервера его словами (докстринг `ManageUser.tsx`), так же он показывает «себя не выключить»; проверено кнопкой в браузере на 2 путях — плашка с текстом отказа, переключатель остался на «Да»; во фронте 0 изменённых строк
