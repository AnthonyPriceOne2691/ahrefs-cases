# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-11 · выбрал останавливать прогон один раз вместо падения каждого зависимого теста — потому что причина одна, а сто одинаковых красных прячут её среди самих себя
- 2026-09-11 · выбрал останавливать только тогда, когда выбранные тесты действительно требуют базы, вместо безусловной проверки на старте — потому что прогон чистых модульных тестов базы не требует и не должен требовать докера
- 2026-09-11 · выбрал явный выход переменной окружения вместо полного запрета — потому что пропуск сам по себе не зло: злом было умолчание. Набранная руками переменная делает выбор видимым
- 2026-09-11 · выбрал назвать команду подъёма в тексте отказа вместо того, чтобы поднимать базу из тестов самим — потому что у прогона нет права запускать докер на чужой машине, и «тест поднял мне контейнер» — сюрприз хуже отказа
