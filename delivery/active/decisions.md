# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.
- 2026-09-11 · выбрал строить меню по списку прав из `/api/auth/me` вместо названия группы — потому что с Ф5.5 право выдаётся и лично, поверх группы: меню по группе не показало бы раздел тому, кому право выдали точечно
- 2026-09-11 · выбрал хранить токен в `localStorage` вместо памяти вкладки — потому что перезагрузка страницы иначе выбрасывает человека посреди работы; цена (токен виден XSS) названа в спеке, а не умолчана: сервис внутренний и живёт за VPN агентства
- 2026-09-11 · выбрал различать `401`, `403` и `503` тремя сообщениями вместо одного «что-то пошло не так» — потому что действия человека разные: войти заново, попросить право, сказать инженеру про настройку
