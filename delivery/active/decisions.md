# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-14 · выбрал допуск 0,01 % (`NOISE_PCT` в `scripts/probe_next_day.py`) вместо сравнения на точное равенство — потому что замер 14.09.2026 дал расхождение 0,00007 % (три визита из 4 550 157), и приговор по нему стоил бы 50 units за домен на каждом обновлении; выбранная граница на три порядка выше замеренного шума и на шесть порядков ниже порогов, решающих группу
- 2026-09-14 · выбрал отдельный исход «подтверждена по существу» с печатью `max(drifted)` в `_compare` вместо того, чтобы молча приравнять шум к «ничего не изменилось» — потому что следующий замер иначе сравнивал бы с «ничего не менялось» и увидел бы несуществующую поломку: 2 закрытых месяца из 3 в снимке 14.09.2026 дрейфовали, и все три остались бы в выводе неотличимы от неподвижных
- 2026-09-14 · выбрал считать значимым любое изменение нулевого значения (`_drift_pct` возвращает `inf` при `before == 0`) вместо деления на ноль или пропуска строки — потому что процента у роста с нуля нет, а ноль в закрытом месяце, ставший числом, — это новость при любом допуске
