# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-24 · выбрал отвести ветку от `bugfix/two-campaigns-two-cases` (PR #27) вместо свежего `origin/main` — потому что судьба собранного и запрещённого кейса берётся из исходов `pack` по `project_id`, которые вводит та поставка; от `main` пришлось бы повторить её правку в тех же строках `pack_cases` (гарантированный конфликт) или вернуть ключ по домену, который она чинит (Z39); после слияния #27 `main` вливается обычным merge
- 2026-09-24 · выбрал пять новых значений `RunItemOutcome` с миграцией вместо старых значений (`skipped_no_data`, `skipped_invalid`) — потому что слово исхода на экране берётся из значения, и «не положен по группе» читалось бы «пропущен: строка не принята»; расшифровка по ступени прогона стала бы вторым словарём поверх первого
- 2026-09-24 · выбрал собранному кейсу исход `ok` вместо отдельного `case_built` — потому что `finish_run` считает «собрано» по `ok`, экран сворачивает `ok` в «без замечаний собрано: N», и отдельное значение потребовало бы правки `run_journal.finish_run` и `RunFates.COLLECTED` — обоих мест, которые соседняя поставка держит в работе
- 2026-09-24 · выбрал исключать исходы ступени кейсов из `cache.empty_since` по множеству значений (`CASE_OUTCOMES`) вместо фильтра по `params_snapshot["stage"]` — потому что константа ступени живёт в `run_journal`, а `cache → run_journal → fetch → plan → cache` — цикл импорта; `ok` собранного кейса в подсчёте остаётся, и это безвредно: кейс собирают только проекту с данными, а память о пустоте нужна проекту без них
- 2026-09-24 · выбрал совет «переклассифицируйте по рядам вердикта» (`classify --source <ряды вердикта>`) вместо прежнего «по этому источнику» — потому что прежний читался как «по текущему режиму», а на проде в `fixture` такой пересчёт переписал бы живые вердикты фикстурными (слово координатора); пересчёт по рядам вердикта не переписывает ничего ни в каком режиме
- 2026-09-24 · выбрал гонять фазовый гейт с `--diff-base bugfix/two-campaigns-two-cases` вместо `origin/main` — потому что против `main` стартовый дифф поставки непуст (7 файлов кода #27, «поставка не стартует поверх них»), а предохранитель считал бы 399 строк чужого диффа в свои 800; база сравнения — ветка, от которой поставка отведена, и после слияния #27 она совпадёт с `main`
