# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-14 · выбрал хранить источник в вердикте (`verdicts.source`, пишется в `classify/verdicts.py::store` из `Computed.source`) вместо того, чтобы выводить его из рядов при сборке — потому что выводить неоткуда: на стенде в `metric_points` лежат 1 134 и 2 463 точки двух источников одновременно, и какие из них видел вердикт, знает только тот, кто считал
- 2026-09-14 · выбрал отказ сборки при расхождении (`cases/builder.py::_attempt` → исход `verdict_mismatch` с `CaseAttempt.detail`) вместо чтения рядов по источнику вердикта — потому что второе тише и хуже: `build_cases(source=…)` молча вернул бы фикстурный кейс на запрос живого, и человек узнал бы об этом из чисел в PDF, а не из строки отчёта; на стенде отказ назвал 14 доменов поимённо
- 2026-09-14 · выбрал допускающее `NULL` поле (`migrations/versions/e6f7a8b9c0d1`) вместо обратного заполнения или удаления старых вердиктов — потому что на стенде 20 вердиктов без источника и 40 с ним, часть посчитана по живым рядам: одно значение на всех было бы выдумкой, а `DELETE` упёрся бы в `cases.verdict_id ON DELETE RESTRICT` (`storage/models/case.py`) и снёс бы записи о выданных клиенту артефактах
- 2026-09-14 · выбрал сверять числа пересчётом через `classify.points` (`cases/builder.py::numbers_mismatch`) вместо сравнения таблицы с хвостом кривой — потому что точка Б по построению не равна последнему измеренному месяцу: замер L46 — 113 312 против 116 190 на одном проекте, то есть такое сравнение краснело бы на исправных кейсах
- 2026-09-14 · выбрал брать окна из версии порогов вердикта вместо действующей сейчас — потому что кейс объясняется своими порогами (инвариант 6 канона кейса), и сверка по чужим окнам отказывала бы всем прошлым кейсам после первой же правки окон
- 2026-09-14 · выбрал один исход `verdict_mismatch` с деталью (`cases/model.py::CaseOutcome`, поле `CaseAttempt.detail`) вместо трёх отдельных исходов — потому что действие человека во всех трёх случаях одно и то же (`classify` по этому источнику), а исходы в этом перечислении различаются именно действием: так их развели L32 и L34 — «не положен по группе», «данных не хватило» и «вердикта нет» ведут к разным шагам, а эти три к одному
- 2026-09-14 · выбрал сверять только метрики, которые в вердикте есть, вместо полного равенства вердикта и рядов — потому что ступень кейса докупает DR и стоимость трафика **после** классификации (L41): полное равенство не давало бы ни одного кейса в нормальном порядке прогона
- 2026-09-14 · выбрал поставить обе проверки в `builder._attempt` вместо вызывающих команд — потому что через него проходят все пути сборки (показать, собрать PDF, собрать пачку), а правило в обёртке действует только на тех, кто зовёт обёртку (L127)
