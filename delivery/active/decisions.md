# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-12 · выбрал потолок `min(остаток ключа, остаток воркспейса)` в `LiveQuota.units_left` вместо остатка ключа — потому что воркспейс делится с другими сервисами агентства: при остатке ключа 1 469 188 и воркспейса 100 000 preflight обещал бы полтора миллиона и упёрся бы в отказ на середине прогона
- 2026-09-12 · выбрал считать поля воркспейса необязательными (`_has_workspace`) вместо строгой проверки — потому что у тарифа без воркспейса их не будет вовсе, и строгость превратила бы «второго потолка нет» в «остаток неизвестен», то есть в вечный запрет прогонов
- 2026-09-12 · выбрал отказ при неизвестном домене в `--only` (`ValueError` в `collect_all`) вместо молчаливого пропуска — потому что собрать меньше названного молча значит показать человеку успешный прогон с половиной списка без данных (класс урока L1)
- 2026-09-12 · выбрал выбирать сторону подписи по расчёту ширины (`_DIGIT_WIDTH` в `export/charts.py`) вместо увеличения правого поля — потому что поле сжало бы график во всех кейсах ради редкого семизначного числа, а сторона решает вопрос ровно там, где он возникает
