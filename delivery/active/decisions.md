# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-25 · выбрал верхнюю границу шага 2 «нынешние кандидаты плюс проекты, за которых шаг 1 заплатит впервые» вместо «все проекты» — потому что «все проекты» требовали бы в смету шаг 2 для 41 «плохого» проекта прода, чьи группы шаг 1 не поменяет (история у них уже куплена), и при лимите ключа 10 000 смета отказывала бы циклу, который стоит сотни units
- 2026-09-25 · выбрал ступени цепочки отдельными строками журнала с общим ключом задачи вместо одной строки «цикл» — потому что одна строка требовала бы заново собрать судьбы, счётчики и units четырёх ступеней в одну и сломала бы «Скачать» у сборки и «Собрать кейсы» у данных под кейс, которые держатся за ступень строки
- 2026-09-25 · выбрал признак `continues` от сервера (самый новый прогон цепочки, кончился не сборкой, меньше двух минут назад) вместо того, чтобы экран сам ждал следующую строку — потому что экран не знает, кончилась цепочка или идёт, а часы браузера и сервера расходятся; сервер решает по своим часам и по своему журналу
- 2026-09-25 · выбрал проверку сметы цикла на сервере в `POST /api/runs/chain` (409) вместо одной недоступной кнопки на экране — потому что кнопка — это вёрстка, а деньги ключа заказчика сторожит только сервер (решение владельца: «просто не давать запускать прогон»)
- 2026-09-25 · выбрал вынести построение строки журнала в `api/run_rows.py` вместо waiver на длину `runs.py` — потому что `runs.py` дорос до 599 строк при планке 500, а строка журнала — отдельная работа (итоги, пачка, цепочка), которую журнал и карточка делят
