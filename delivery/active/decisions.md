# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-13 · выбрал показывать человеческий вид домена в заголовке кейса (`cases/builder._title` через `intake.normalize.to_unicode`) вместо хранения второго поля в базе — потому что имя для человека выводится из канона однозначно, а второе поле потребовало бы миграции и разошлось бы с первым при первой правке нормализации
- 2026-09-13 · выбрал возвращать канон при неразбираемом punycode вместо исключения — потому что такой хост приезжает из чужой системы, и падение сборки кейса остановило бы пачку целиком: показать `xn--битый.example` некрасиво, а `to_unicode` с исключением потребовал бы обработки в каждом вызывающем — не собрать ничего хуже обоих
- 2026-09-13 · выбрал приводить домен к канону во **всех** командах (`render`, `explain`, `diagnose`, `cases`) вместо одного только `--only` — потому что `render «ПРОВЕРКА-Рост.example»` отвечал «проект не найден» на проект, который есть: тот же урок L124, просто в другой команде
- 2026-09-13 · выбрал оракул на текст вызовов (`test_every_command_taking_a_domain_canonicalises_it`) вместо теста на каждую команду — потому что команд с доменом станет больше, а свойство одно: забывчивость повторится ровно в новой команде, и тест на четыре существующих вызова `args.domain` её не поймает: оракул читает исходник `scripts/run_collect.py` и требует `_canonical` у каждого
