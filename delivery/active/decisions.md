# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-24 · выбрал отказ `400` для брака файла (`UnfitSourceError` из `validate.check_fit`) вместо строк `missing_column`/`empty_source` в отчёте `200` — потому что `200` говорит «принято» всем потребителям сразу (экран пишет «Принято из …», CLI отвечает кодом 1, лог — `intake_accepted`), и это та же ложь успехом, что L10 у закрытой таблицы
- 2026-09-24 · выбрал реляционный тест над общим `listFormat.json` вместо эндпоинта `GET /api/intake/columns` — потому что описания и примеры колонок всё равно живут на фронте и сверка нужна в любом случае, а эндпоинт добавил бы подсказке состояние загрузки и каждому тесту экрана подменный ответ; колонки — константы кода, сервер и фронт уезжают одним коммитом (роли из L101 меняются в базе на ходу — там справочник с сервера оправдан)
- 2026-09-24 · выбрал не вводить отказ по числу строк вместо предела в 100 из ТЗ — потому что предел объявлен как объём загрузки, а не правило приёма: смета и прогон считают всю базу, второй файл обходит предел на загрузку, и он не ограничил бы ни одного unit; траты держит смета с квотой. Вопрос оставлен владельцу
