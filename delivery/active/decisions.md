# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.
- 2026-09-11 · выбрал личные права поверх роли (`users.permissions`, приём из CRM агентства) вместо новой группы под каждое новое право — потому что в CRM эта развилка уже пройдена: там роль даёт набор, а личное право перекрывает её, и выдача права одному человеку не требует ни новой роли, ни правки кода
- 2026-09-11 · выбрал заводить первого администратора командой `useradd` на сервере вместо открытого bootstrap-эндпоинта из CRM — потому что у их bootstrap своя опора (флаг `AUTH_API_ENFORCEMENT` и их окружение), а у нас это была бы дверь в интернет ради одноразового действия (урок L24: переносить паттерн вместе с опорой)
- 2026-09-11 · выбрал генерировать пароль и показывать его один раз вместо отправки почтой — потому что почтовый ящик переживает пароль, а `secrets` вместо `random` — потому что предсказуемый генератор это открытая дверь, а не неудобство
