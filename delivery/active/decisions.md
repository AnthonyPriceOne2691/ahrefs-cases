# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.
- 2026-09-10 · выбрал держать пороги только в `Ruleset` (БД) и читать файл лишь сидом вместо чтения `config/thresholds.example.yml` в рантайме — потому что ТЗ **требует** правки порогов людьми без деплоя и версии у каждой правки: файл истории не хранит, а вердикт обязан ссылаться на версию, иначе через месяц «почему тут medium» необъяснимо
- 2026-09-10 · выбрал `extra="forbid"` в модели порогов вместо `extra="ignore"`, как в конфиге приложения — потому что этот документ человек правит руками: опечатка `growth_pct_mim` при `ignore` дала бы порог по умолчанию и молча сдвинула границу группы, тогда как в `.env` лишние ключи законны (там живут переменные всех доменов)
- 2026-09-10 · выбрал считать точки А и Б средним по окну вместо первого и последнего значения серии — потому что одна аномальная точка на границе **ломает** вывод: на плоской серии месяц с удвоением даёт +100 % по last-точке против +50 % по окну в два месяца; при этом окно лишь делит выброс пополам, и это ограничение записано в спеке, а не выдано за защиту
- 2026-09-10 · выбрал `medium.supporting_required: 0` вместо буквального чтения Приложения А — потому что при обязательной подтверждающей для «средних» проект с ростом трафика +80 % и без ссылочного **не поддерживается** ни одной группой: «хорошие» не пускают, «средние» тоже, а определение «плохих» (трафик < +10 % или падение) к нему не подходит. Конфликт описан в сиде и вынесен на калибровку
- 2026-09-10 · выбрал различать решающие условия и объяснительные факты (`Reason.decisive`) вместо `all(passed)` по всем записям — потому что записи о подтверждающих метриках **ломали** решение при `supporting_required=0`: «топ-10 вырос на 12 % при ориентире 15 %» считалось провалом условия, которого никто не требовал, и `weak_growth` уходил в `poor`
- 2026-09-10 · выбрал не включать `weak_growth` в строгую golden-таблицу вместо фиксации удобного ожидания — потому что 6 доменов из 12 дают `poor`, а 6 `medium`: ожидание было бы зелёным по случайности выбранного домена, а тест — сторожем видимости проблемы, которой он не видит
- 2026-09-10 · выбрал `insufficient_data` считать до групп вместо «не дотянул до порога → poor» — потому что «нет данных» и «плохой результат» **требуют** разных действий: докупить историю против не делать кейс; слитые вместе, они теряют проекты, у которых просто мало месяцев
- 2026-09-10 · выбрал сид порогов идемпотентным по `version` вместо идемпотентности по содержимому — потому что иначе правка файла без смены версии молча переписывала бы пороги, по которым уже вынесены вердикты, и они **потеряли** бы объяснимость
