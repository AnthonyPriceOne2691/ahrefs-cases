# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-13 · выбрал генератор таблицы с маркерами в документе (`scripts/units_profile.py --write` + сверка в `tests/test_units_profile.py`) вместо правки чисел руками — потому что руками правили дважды, 11.09 и 12.09, и оба раза документ расходился с кодом в тот же день: в обращении одновременно жили «18 310», «27 330» и «33 560»
- 2026-09-13 · выбрал звать `choose_scheme` в профиле вместо умножения цены строки на месяцы — потому что схему выбирает цена на каждый endpoint: у `keywords-history` и `refdomains-history` дешевле две точки (100 против 114), и умножение посчитало бы другой прогон, дороже на 840 units
- 2026-09-13 · выбрал посчитать и второй маршрут (`--stage1 auto`, 18 070) вместо одной цены принятого — потому что разница 10 900 и есть цена решения 11.09: за неё куплены график, диагностика Ф3в и пересчёт окон, и в разговоре с заказчиком это число придётся называть
- 2026-09-13 · выбрал в оракуле E3 подменять `endpoints.FIELD_PRICES` вместо поля спеки — потому что `EndpointSpec` заморожен (`frozen=True`) и `monkeypatch.setattr(METRICS_HISTORY, "select", ...)` потребовал бы снять `frozen`, то есть разрешить менять цену endpoint'а в рантайме ради одного теста: цена endpoint'а не должна меняться в рантайме, а таблица цен — модульная величина
