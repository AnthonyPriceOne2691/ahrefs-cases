# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-11 · выбрал ветвление по `backend/pyproject.toml` в шаге «Install backend deps» вместо безусловного `pip install -e backend` — потому что условие проверяло корневой `pyproject.toml`, а команда ставила каталог `backend`, которого в этом репозитории нет: предупреждение печаталось каждый прогон и гасилось только сменой раскладки
- 2026-09-11 · выбрал оставить в корневой ветке `pip install -e .` вместо того, чтобы ничего не делать, — потому что ветка «есть requirements-dev.txt» проекта не ставит вовсе, и на таком проекте mypy снова остался бы без рантайм-зависимостей; при нынешней раскладке это повтор-no-op
- 2026-09-11 · выбрал не глушить предупреждение, а сделать его достижимым вместо удаления строки — потому что оно единственный канал, которым шаг сообщает о настоящей беде: удалив его, беду увидели бы по падению mypy, а не по причине
