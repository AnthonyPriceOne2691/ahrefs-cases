# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-13 · выбрал вычитать собственный журнал (`budget.uncounted_spend`, окно `AHREFS_UNITS_COUNTER_LAG_HOURS` = 24 ч) вместо продления резерва после прогона — потому что `reserved_units` фильтрует по `Run.status in (queued, running)` и означает «прогон идёт»: удерживая резерв сутки, поле получило бы второй смысл, а комментарий в нём прямо запрещает компенсирующие записи по той же причине
- 2026-09-13 · выбрал считать вычет только по живым прогонам (`params_snapshot['provider'] == 'live'`) вместо всех строк журнала — потому что fixture-прогон пишет условные units по той же формуле, и на стенде 3 194 units разработки вычитались бы из настоящего остатка Ahrefs: разработка молча запрещала бы боевые прогоны
- 2026-09-13 · выбрал снять умолчание и у `reserved` тоже вместо добавления одного `uncounted=0` — потому что `reserved: int = 0` прожил три поставки и означал «прогон видит только свою квоту»: это тот же урок L53, и оракул `test_preflight_guards_have_no_default` держит теперь оба аргумента
- 2026-09-13 · выбрал показать вычет отдельным числом вместо уменьшения `remaining` — потому что остаток обязан совпадать с кабинетом Ahrefs: расхождение экрана с кабинетом читалось бы как ошибка сервиса, а не как отставание счётчика. Разница названа словами: «прогон считается по остатку 7 238 units»
- 2026-09-13 · выбрал вынести числа расхода в `usage/UsageTotals.tsx` вместо четвёртого бейджа на месте — потому что eslint насчитал странице сложность 12 при пороге 10, а ratchet запрещает рост предупреждений: сигнал верный, читать вперемешку загрузку, отказ и арифметику остатка уже неудобно
