# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.

- 2026-09-11 · выбрал отказ (`ThresholdsError`) на ненулевой `normalize_after_months` вместо тихого игнорирования — потому что поле разбирается моделью порогов и выглядит рабочим: поставив 12 на калибровке, заказчик увидит прежние группы и сделает вывод о порогах, а не о нереализованном блоке; отказ стоит одной строки в `Duration`, а молчание — итерации калибровки
- 2026-09-11 · выбрал правило «пик и снижение после него» вместо поиска точки перелома статистикой — потому что диагноз обязан воспроизводиться человеком по двенадцати числам серии: на калибровке с ним спорят, и «так посчитал алгоритм» не аргумент
