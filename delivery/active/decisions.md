# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.

- 2026-09-11 · выбрал восемь концептов об инвариантах вместо переноса существующих документов в bundle — потому что `docs/IMPLEMENTATION_V3.md` и планы фаз клиентские и по варианту D вне git, а канон обязан жить в репозитории, который передаётся компании; перенос дал бы bundle, который нельзя отдать
- 2026-09-11 · выбрал развилку в `entry` хука (`--staged` либо `--base origin/main`) вместо правки канонного скрипта — потому что канонный `--staged` по замыслу отказывается работать с пустым индексом, а сверка контура делается прогоном `--all-files`, где индекс пуст всегда: хук краснел бы на каждой сверке, а такой хук начинают обходить (Delivery §4.3b); оба режима скрипта остались канонными, адаптация записана в `scripts/lint/adapted.json`
- 2026-09-11 · выбрал `implementation:` из структурных якорей для карты репозитория вместо перечисления модулей — потому что при `src/ahrefs_cases/` гейт требовал бы правки карты на каждой поставке и был бы снят через месяц; критерий из канона: тронули файл — предложение в карте стало ложным?
- 2026-09-11 · выбрал `stale_after` +90 дней на карте вместо доверия гейту — потому что самый ценный её раздел, «грабли», не привязан ни к одному пути: грабли перестают быть граблями молча, когда кто-то починил причину, и ни один гейт этого не увидит
