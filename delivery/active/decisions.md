# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-16 · выбрал проверять наслоение настоящим PDF через пробник вместо чтения разметки глазами — потому что подписи режет и сдвигает растеризация, а не разметка: в SVG наложения «не видно» ни одним поиском по строке, и первый же осмотр готового листа показал вдобавок, что нынешние кейсы собраны до правок рисунка и подписей старта в них нет вовсе
- 2026-09-16 · выбрал собирать место каждой подписи в `_Mark` и раскладывать их вместе вместо того, чтобы чинить каждую подпись отдельно — потому что столкновение принадлежит паре, а не подписи: правило «сторону выбирай по месту» в `_end_label` (`fits_right`) у каждой уже было и от наложения не спасало — до правки сталкивались 20 рисунков из 72
- 2026-09-16 · выбрал симметричное расхождение пары (`_pushed_apart`, по половине нехватки каждой) вместо сдвига одной подписи — потому что сдвинутая целиком уезжает от своей точки вдвое дальше: при нехватке 5,5 единиц это 11 против 5,5, а порог «подпись рядом со своей точкой» в оракуле — 12
- 2026-09-16 · выбрал обходить край холста как препятствие (`_FIELD` в `_free_spot`) вместо обрезки по краю (`_inside_canvas` первой версии) — потому что обрезка возвращала подпись ровно туда, откуда её увела раскладка: у `ebsco.com` и `legal-partners.pl` кривые лежат на дне шкалы, и подпись садилась на деление «0». Первая версия оставила 5 рисунков со столкновениями из 72, вторая — 0; найдено живым прогоном, а не тестом
- 2026-09-16 · выбрал восстанавливать порядок подписей перестановкой уже найденных мест (`_in_order`) вместо запрета обгона при поиске места — потому что запрет оставил бы нижнюю подпись вовсе без места: у `ebsco.com` обе точки старта лежат ниже деления «0» и свободной высоты под ними нет. Перестановка между подписями одного кегля и одной стороны (`shares_column`, равные `size` и `anchor`) безопасна: геометрия одинаковая, новых столкновений появиться неоткуда
- 2026-09-16 · выбрал мерить коробки подписей в оракулах СВОЕЙ меркой (0,52 кегля на знак) вместо вызова функции рисовальщика — потому что оракул, считающий ту же формулу, зеленеет вместе с ошибкой в ней; заниженная мерка ловит настоящие столкновения и не спорит с законной теснотой
- 2026-09-16 · выбрал ослабить порог «подпись рядом со своей точкой» в `test_spread_labels_stay_close_to_their_points` с девяти единиц до двенадцати вместо уменьшения `_LINE_GAP` и множителя в `required_gap` — потому что замер показал сдвиг 10,3 единицы там, где подпись обходит деление шкалы (строка 9 кеглем плюс просвет — уже 9,7), а сжатый зазор вернул бы слипание в PDF: `_DIGIT_WIDTH` = 5,8 это оценка сверху для разметки, а ширину меряет растеризатор
- 2026-09-16 · выбрал разделить рисунок на `charts`, `labels` и `axis` вместо того, чтобы просить исключение у гейта длины — потому что после правки `charts.py` вырос с 480 до 721 строки при пороге 500, и растили его две самостоятельные темы: где встанет подпись (`spread`, `Mark`, `_free_spot`) и как подписана ось (`label_step`, `month_ticks`, `month_labels`). Границы видно по зависимостям: раскладке хватает полосы `field` параметром, оси — готовых координат `xs`, и ни той, ни другой не нужны размеры холста
