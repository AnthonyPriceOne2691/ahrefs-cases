# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-11 · выбрал свежесть по `func.max(Case.id)` в `list_cases` вместо максимума `created_at` — потому что время в базе одинаково у кейсов одной сборки (`pack_cases` пишет их в одной транзакции), и по времени у проекта оказалось бы два «свежих» кейса
- 2026-09-11 · выбрал спрашивать историю у сервера параметром `all_versions` вместо фильтрации полученной страницы — потому что страница отдаёт 50 строк: при девяти версиях фильтрация показала бы историю пяти проектов и молча спрятала остальные
- 2026-09-11 · выбрал показывать по умолчанию свежее, а историю за тумблером, вместо обратного порядка в `CaseLibrary.tsx` — потому что библиотеку открывают, чтобы отправить кейс клиенту, а не изучать, что когда собиралось: шесть проектов давали пятьдесят строк
