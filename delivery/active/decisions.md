# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-24 · выбрал дописать `web/node_modules` в умолчание `BORROW_DIRS` (`scripts/merge_guard.sh`) вместо вывода каталога фронта из `LINT_FE_DIR` в `.pre-commit-config.yaml` — потому что упавший хук `LINT_FE_DIR` не читает: `cd web && npx --no-install eslint` и `files: ^web/src/` написаны в нём буквально, а первое вхождение `LINT_FE_DIR=web` в конфиге — строка комментария 8. Разбор YAML грепом дал бы вторую копию знания хуже первой и 5–6 строк расхождения с каноном вместо одного слова; дрейф раскладки ловит тест, читающий настоящие `package.json`
- 2026-09-24 · выбрал править умолчание в скрипте вместо `MERGE_GUARD_BORROW` в окружении — потому что переменная живёт один вызов: постоянного места для неё в проекте нет (ни обёртки, ни конфига гейта мержа), и её пришлось бы набирать на каждом мерже; правило, которое держится на памяти вызывающего, уже отказало 24.09
- 2026-09-24 · выбрал тест, исполняющий настоящий `merge_guard.sh` на полигоне (`tests/test_merge_guard_environment.py`), вместо сверки текста строки `BORROW_DIRS` — потому что сверка текста пропустила бы порчу, при которой гейт гоняется в клоне, а не во временном дереве (там окружение своё, и любой список «работает»); из трёх порч скрипта — список без `web`, гейт в клоне, гейт пропущен — тест поймал 3 из 3, каждую своим сообщением
- 2026-09-24 · выбрал оставить второй слой — `npx --no-install` без локальной установки идёт в кэш npx, ратчеты TS без инструмента выходят 0 — за рамками вместо правки хуков в `.pre-commit-config.yaml` — потому что это поведение канона, а не проекта: оффлайн-профиль «гейт без инструмента предупреждает, а не падает» объявлен законным в `cqg@1.23`, и правка здесь потребовала бы пяти адаптаций канона (хуки eslint и prettier, `check_eslint_warnings.sh`, `check_jscpd_gate.sh`, половины TS у слоёв и сложности) в одной S-поставке; адресат — канон
