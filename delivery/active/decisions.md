# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.
- 2026-09-11 · выбрал сверять числа текста с числами кейса программно вместо доверия шаблону — потому что ревью черновика ТЗ не предусматривает, а конституция проекта требует прямо: цифра, которую никто не проверил механикой, это обещание
- 2026-09-11 · выбрал версионировать кейс при повторной сборке вместо перезаписи по `(project_id, verdict_id)` — потому что ТЗ требует перегенерации через полгода-год, и сравнивать её будет не с чем, если прошлая версия затёрта
- 2026-09-11 · выбрал перенести форматирование чисел в `cases/format.py` вместо оставления в `export` — потому что текст живёт слоем ниже рендера и не может его импортировать: два форматтера разошлись бы между предложением и таблицей
