# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-25 · выбрал охват «проекты загруженного файла» вместо «вся база» — потому что так решил владелец («только проекты файла»): человек думает файлом, а счётчики и пачка по базе («54 из 54», 11 кейсов при одном проекте) читались как ответ про файл и врали
- 2026-09-25 · выбрал строку цикла с дочерними ступенями под одним ключом задачи вместо одной строки, в которую ступени пишут сами — потому что ступени открывают и закрывают свои прогоны внутри сбора (`collect_projects`, `collect_case_data`), и одна строка потребовала бы переписать сбор; вместо этого журнал прячет ступени, а строка цикла идёт до конца
- 2026-09-25 · выбрал ребёнка по ключу задачи вместо метки «родитель» в снимке ступени — потому что данные под кейс открывает шаг 2 через слой сбора, и метка потребовала бы тащить номер цикла сквозь `collect`; ключ задачи у всех ступеней и так общий — его спрашивает реапер
- 2026-09-25 · выбрал архив цикла сразу в `runs/<номер>/` вместо пачки дня с копией — потому что пачка дня — вся база и лежит на «Кейсах»; сборка по файлу переписала бы её одним кейсом
- 2026-09-25 · выбрал верхнюю границу шага 2 «кандидаты среди проектов файла плюс те, за кого шаг 1 заплатит впервые» вместо «все проекты файла» — потому что у проектов, уже собранных и «плохих», шаг 1 группу не поменяет, а смета, раздутая ими, упёрлась бы в лимит ключа 10 000 раньше времени
- 2026-09-25 · выбрал проверку сметы цикла на сервере (409) вместо одной недоступной кнопки — потому что кнопка — вёрстка, а деньги ключа заказчика сторожит только сервер («просто не давать запускать прогон»)
- 2026-09-25 · выбрал вынести строку журнала и сметы в `api/run_rows.py` и `api/run_estimates.py` вместо waiver на длину `runs.py` — потому что `runs.py` дорос до 599 строк при планке 500, а это отдельные работы, которые журнал, карточка и три сметы делят
