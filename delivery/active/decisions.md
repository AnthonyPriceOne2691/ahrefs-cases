# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-11 · выбрал отдавать справочник прав эндпоинтом `rights_catalog` из `_GROUP_RIGHTS` вместо списка прав в коде фронта — потому что права проверяют роутеры по той же таблице, и вторая копия расходится молча: неделей раньше это стоило экрану предпросмотра латинских `good → poor` (урок L97)
- 2026-09-11 · выбрал показывать право вместе с происхождением (`rightStates` в `pages/users/rights.ts`) вместо итогового списка прав — потому что «админ, у которого лично отобрали `manage_users`» с итоговым списком выглядит как обычный админ: причина исчезает ровно там, где её ищут
- 2026-09-11 · выбрал не показывать права, которых нет ни у группы, ни лично, вместо полной матрицы «можно/нельзя» — потому что строка «нельзя, как у всех в группе» не несёт решения, а длина ячейки утраивается: у инженера пять прав из пяти, у пользователя два
- 2026-09-11 · выбрал зачёркивать отобранное право в `RightsCell.tsx` вместо того, чтобы его скрыть, — потому что «было и отняли» это факт о человеке: скрытое право неотличимо от никогда не выдававшегося
- 2026-09-11 · выбрал задать `minWidth` колонкам «Группа» и «Вход» в `UsersTable.tsx` вместо ширины по содержимому — потому что прогон живого экрана показал обрезанные бейджи («ПОЛЬЗО…», «ВХОД ЗАКР…»): Mantine обрезает бейдж многоточием, и группа становится нечитаемой
