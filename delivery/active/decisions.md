# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.

- 2026-09-11 · выбрал общий проход `verdicts.evaluate` для пересчёта и предпросмотра вместо копии цикла в каждом — потому что иначе в двух местах оказались бы чтение серии, проверка покрытия и вызов правил, и гейт копипаста поймал бы это уже на третьем совпадающем блоке; расхождение таких копий заметно только на калибровке, когда предпросмотру уже доверяют
- 2026-09-11 · выбрал «было» брать из вердикта по **активной** версии вместо последнего по времени — потому что оператор сравнивает с тем, что видит на экране сегодня, а последний по времени вердикт может быть от чужого эксперимента с третьей версией
