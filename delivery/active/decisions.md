# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.
- 2026-09-11 · выбрал класть внутрь архива список `кейсы.csv` вместо голой пачки PDF — потому что ревью черновика ТЗ не предусматривает (конфликт №7 реестра), и человек, открывающий ZIP перед публикацией, — последний предохранитель: ему нужны группа и пометка «можно публиковать»
- 2026-09-11 · выбрал отказ собирать пустой архив вместо пустого ZIP — потому что пустой архив читается как поломка выгрузки, а не как «кейсов нет» (тот же класс, что урок L1 про пустой и недоступный robots.txt)
- 2026-09-11 · выбрал кириллицу в именах вместо транслитерации — потому что ТЗ называет правило именем сайта и ниши, а `zipfile` пишет имена в UTF-8 и читается на обеих системах
