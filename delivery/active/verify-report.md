# Verify report

**Date:** 2026-09-13
**Verifier:** human:anthony (живая часть), agent:claude (оракулы)
**asserts_reviewed_by:** pending — утверждения пробника про сам Ahrefs проверяются
только вторым запуском с живым ключом; механика толкования снимков проверена шестью
примерами без сети
**CI run:** пуш после приёмки
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — ruff, ruff format, mypy strict чисты
- [x] PASS — pre-commit: длина файлов, секреты, OKF-синхронизация, проводка гейтов
- [x] PASS — `delivery_check`: 0 ошибок, 0 предупреждений

## Behavior oracles
- [x] PASS — `pytest`: 480 passed, 1 skipped (было 474 — прибавились шесть примеров пробника)
- [x] PASS — E1–E5 из `eval-smoke.md`

## Ревью рисковых мест

**Деньги.** Квота бесплатна. Снимок — один запрос на домен: три строки по 11
units не добирают до минимума, значит 50 units, и число месяцев в пределах
четырёх цену не меняет.

**Безопасность.** Ключ читается конфигом и не печатается. Снимки ложатся в
`data/raw/` — каталог, исключённый из git ещё в Ф1 как «сырые выгрузки Ahrefs с
доменами клиентов».

**Фикстурный режим.** Пробник отказывается работать и **не пишет снимок**:
фикстура ответила бы собственной формулой, снимок сравнивался бы сам с собой и
подтвердил бы любую гипотезу. Это проверено отдельным примером (E5).

## Spec coverage gaps
- Сам ответ по H2 и H8 — за человеком: нужен живой ключ и два дня.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 0 code (+0 process docs) / +0/-0 (net +0) |
| commits | 0 |
| time_to_accepted_spec | n/a (no spec.md in history — class S?) |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | no |
| implement_retries | MANUAL — fills from session log |
| verify_fails_before_green | MANUAL — count red verify runs (CI run list) |
| est_token_or_cost | MANUAL / n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
