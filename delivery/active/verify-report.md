# Verify report

**Date:** 2026-09-12
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (by=human:anthony at=2026-09-12) — непривязанное
утверждение одно и показано на артефакте: пересобранный кейс `ahrefs.com`
показывает подпись «3 596 467» целиком, слева от точки
**CI run:** пуш после приёмки
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — ruff, ruff format, mypy чисты
- [x] PASS — предохранители: диф в пределах 800, файлов 8 из 25

## Behavior oracles
- [x] PASS — pytest 474 passed, 1 skipped (+6)
- [x] PASS — E1–E6 закрыты тестами

## Product oracles (артефакт и стенд)
- [x] PASS — кейс пересобран из **уже купленных** рядов (сети нет, units не
      тратятся): подпись значения видна целиком и развёрнута влево, короткие
      подписи второй кривой остались справа
- [x] PASS — прогон с `--only` на стенде собирает ровно названные проекты;
      ряды появляются только у них

## Ревью рисковых мест

**Деньги.** Правка квоты меняет ответ preflight в **строгую** сторону: остаток
может стать меньше, прогон — не начаться. Это верный знак ошибки: у отказа цена
— отложенный прогон, у обещания — прерванный на середине и потраченные units.

**Безопасность.** `--only` читает файл со списком доменов; путь приходит от
человека в командной строке, наружу ничего не уходит. Прав и доступов правка не
трогает.

**Транзакция БД.** Записи те же. Фильтр сужает выборку проектов (`where domain
in (...)`), а не меняет, что пишется.

**Интеграция.** Подпись значения — единственное место, где рисунок знает про
ширину шрифта. Ширина знака взята с запасом: ошибка в сторону «не влезет»
разворачивает подпись влево, где места заведомо хватает.

## Spec coverage gaps
- Подпись «старт работ» стороны не выбирает: она стоит у начала периода.
  Вернётся вопросом, если появится кейс, где период начинается у правого края.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD~1 -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 0 code (+10 process docs) / +0/-0 (net +0) |
| commits | 1 |
| time_to_accepted_spec | n/a (no spec.md in history — class S?) |
| rework_after_done | 0 commit(s) after first phase: handoff |
| harness_hardened | no |
| implement_retries | MANUAL — fills from session log |
| verify_fails_before_green | MANUAL — count red verify runs (CI run list) |
| est_token_or_cost | MANUAL / n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
