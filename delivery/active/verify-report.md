# Verify report

**Date:** 2026-09-11
**Verifier:** human:anthony
**asserts_reviewed_by:** n/a (все утверждения ведут к одобренным примерам — см. дайджест)
**CI run:** https://github.com/AnthonyPriceOne2691/ahrefs-cases/actions/runs/34599796059
**Commit:** ee0eb5b

## Shape oracles
- [x] PASS — pre-commit, 29 хуков, упавших 0 (ESLint, Prettier, ratchet предупреждений)
- [x] PASS — предохранители: net_loc 658 при пороге 800
- [x] PASS — `tsc --noEmit` чист

## Behavior oracles
- [x] PASS — vitest 12 тестов, 3 файла; pytest 384 passed, 1 skipped
- [x] PASS — примеры E1–E9 закрыты тестами; E10 (тёмная тема) — глазами

## Product oracles
- [x] PASS — без токена видна форма входа, а не пустой каркас
- [x] PASS — меню собирается по правам: пользователь без `edit_thresholds` и
      `manage_users` не видит «Пороги» и «Люди»
- [x] PASS — протухший токен возвращает на вход и стирает сохранённый

## Ревью рисковых мест

**Скрытая кнопка — не защита, и это записано.** Разделы фильтруются правами, но
серверные проверки стоят и остаются. Строка в спеке и комментарий в `nav.ts`
нужны ровно затем, чтобы на следующем экране никто не «сэкономил» на серверной
проверке, увидев, что кнопки и так не видно.

**Права, а не роли.** Меню строится по списку прав из `/api/auth/me`: право,
выданное лично поверх группы (Ф5.5), открывает раздел без смены группы. Меню по
названию группы этого бы не показало — и человек с выданным правом видел бы
пустое меню при работающем API.

**Безопасность.** Токен лежит в `localStorage`: перезагрузка страницы не
выбрасывает человека посреди работы. Цена названа в спеке — токен доступен XSS;
сервис внутренний, за VPN агентства, и это осознанный размен, а не умолчание.

**Отказы.** Три разных исхода и три разных действия: `401` — войти заново,
`403` — попросить право (текст называет какое), `503` — сказать инженеру про
настройку. Проверяются тестами клиента.

**Транзакция БД и деньги.** Фронт не ходит в базу и не тратит units.

**Новая зависимость.** Testing Library и jsdom — только dev: DoD Ф6 требует
vitest на формы и таблицы, а проверять их без библиотеки нечем. Заодно
выяснилось, что в vitest 5 секция `test` живёт в своём конфиге, а не в
`vite.config.ts`.

**Канон.** Карта репозитория пополнилась слоем `web/src` и правилом про
скрытые пункты меню.

## Assertion digest (ревью ожиданий, не кода)

База: `0bdadee` · сгенерировано `assert_digest.sh`

Новых/изменённых утверждений: **21**, из них без ссылки на пример спеки:
**2**. Вопрос к каждому непривязанному один: **откуда взято ожидаемое
значение — из спеки или придумано под реализацию?**

```
-	expect(headers.Authorization).toBe('Bearer токен-для-теста');
E7	await expect(request('/api/projects')).rejects.toMatchObject({ needsLogin: true });
E8	await expect(request('/api/rulesets')).rejects.toThrow(/edit_thresholds/);
E9	expect(failure).toBeInstanceOf(ApiError);
E9	expect((failure as ApiError).misconfigured).toBe(true);
E9	expect((failure as ApiError).message).toContain('активной версии порогов');
-	await expect(request('/api/projects')).rejects.toThrow(/502/);
E4	expect(seen).toContain('Проекты');
E4	expect(seen).not.toContain('Пороги');
E4	expect(seen).not.toContain('Люди');
E5	expect(seen).toContain('Пороги');
E5	expect(seen).toContain('Люди');
E6	expect(labels(['read', 'manage_users'])).toContain('Люди');
E6	expect(labels(['read', 'manage_users'])).not.toContain('Пороги');
E1	expect(await screen.findByRole('heading', { name: 'Вход' })).toBeInTheDocument();
E1	expect(screen.queryByText('Проекты')).not.toBeInTheDocument();
E2	expect(await screen.findByText(ME.email)).toBeInTheDocument();
E2	expect(localStorage.getItem(TOKEN_KEY)).toBe('свежий');
E3	expect(await screen.findByText('неверная почта или пароль')).toBeInTheDocument();
E7	expect(await screen.findByRole('heading', { name: 'Вход' })).toBeInTheDocument();
E7	await waitFor(() => expect(localStorage.getItem(TOKEN_KEY)).toBeNull());
```

Привязаны к примерам: **E1 E2 E3 E4 E5 E6 E7 E8 E9**. Остальные 2 — нет.

Читать нужно **только строки с `-` в первой колонке**: их ожидание
ничем не подписано. Подпись: `asserts_reviewed_by: human:… at=…`.

asserts_without_example: 2

## Spec coverage gaps
- **Экраны данных** (проекты, карточка, кейсы, прогоны, расход, пороги, люди) —
  следующие поставки Ф6. Сейчас на их месте честные заглушки, которые говорят,
  что экран приедет, а не пустые страницы.
- **Переключатель темы руками** не делался: следуем системной настройке.
- **Мобильная вёрстка** — только то, что даёт `AppShell`.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base 0bdadee -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 16 code (+10 process docs) / +1524/-53 (net +1471) |
| commits | 1 |
| time_to_accepted_spec | 0.0h |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | yes — web/src/api/__tests__/client.test.ts (новый оракул), web/src/app/__tests__/nav.test.ts (новый оракул), web/src/auth/__tests__/login.test.tsx (новый оракул), web/src/test/render.tsx (новый оракул) |
| implement_retries | MANUAL — fills from session log |
| verify_fails_before_green | MANUAL — count red verify runs (CI run list) |
| est_token_or_cost | MANUAL / n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
