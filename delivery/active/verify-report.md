# Verify report

**Date:** 2026-09-11
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (by=human:anthony at=2026-09-11) — непривязанное
утверждение одно: на живом стенде экран показал действующие пороги теми же
числами, что лежат в `config/thresholds.example.yml` (100 %, 2 000 визитов,
6 месяцев)
**CI run:** пуш после приёмки
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — pre-commit, 29 хуков, упавших 0
- [x] PASS — предохранители: net_loc 587 при пороге 800, файлов 7 из 25
- [x] PASS — eslint без предупреждений (сложность `groupSentences` разрезана), tsc чист

## Behavior oracles
- [x] PASS — vitest 68 (+7), pytest не затронут (фронтовая поставка)
- [x] PASS — E1, E2, E5–E8 закрыты тестами
- [x] PASS — оракул `nav.test.ts` **покраснел** на попытке открыть раздел праву
      `read` и вернул решение к таблице доступа ТЗ

## Product oracles (живой стек, Chrome по CDP)
- [x] PASS — экран открыт на настоящих порогах дев-стенда: условия «хорошего» и
      «среднего» словами, пригодность, окна точек
- [x] PASS — обе темы читаются (снимки сняты в каждой)

## Найдено прогоном живого экрана

**«База отсчёта: period».** Внутреннее значение показывалось как есть —
латиницей посреди русского экрана. Переведено (`baselineWords`), незнакомое
значение по-прежнему показывается как есть: подменять его знакомым словом
значило бы соврать про версию, заведённую руками.

## Ревью рисковых мест

**Безопасность.** Раздел остаётся под правом `edit_thresholds` — как в таблице
доступа ТЗ. Экран только читает; сохранение и активация в него не входят вовсе,
поэтому обойти право через интерфейс нечем.

**Транзакция БД.** Риска нет: поставка фронтовая, запрос один и читающий.

**Интеграция.** `Ruleset.payload` приходит из базы и разбирается на экране
защищённо: любое поле может отсутствовать, и отсутствие показывается как «—»
или «не задано», а не как ноль. Форма payload проверяется сервером при
сохранении (`thresholds.parse`), экран её не валидирует и не притворяется, что
валидирует.

## Spec coverage gaps
- Список версий и предпросмотр последствий — поставка `thresholds-preview`
  (код написан и проверен, ждёт своей очереди).
- Правка порогов — поставка `thresholds-edit`.

## Verdict
- [x] READY FOR HANDOFF
