# Verify report

**Date:** 2026-09-11
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (by=human:anthony at=2026-09-11) — утверждение
проверено числами на живом стенде: шесть строк по умолчанию, пятьдесят с
включённой историей
**CI run:** пуш после приёмки
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — pre-commit, 29 хуков, упавших 0
- [x] PASS — предохранители: класс S, 5 файлов, net_loc в пределах S

## Behavior oracles
- [x] PASS — pytest 440 passed, 1 skipped; vitest 62 (+1)
- [x] PASS — repro-тест `test_library_shows_one_case_per_project` краснел до
      правки (возвращал версии всех сборок) и зелен после

## Product oracles (живой стек, Chrome по CDP)
- [x] PASS — библиотека на дев-стенде: **6 строк вместо 50**, по одной на проект,
      версии 10, 9, 10, 10, 10, 8
- [x] PASS — тумблер «показать все версии» вернул 50 строк и ушёл **запросом**
      (`/api/cases?limit=50&all_versions=true`), а не фильтрацией страницы

## Ревью рисковых мест

**Безопасность.** Риска нет: параметр булев, в SQL не подставляется, права
роутера не меняются — `read` как был.

**Транзакция БД.** Подзапрос `select(func.max(Case.id)).group_by(project_id)`
читает и ничего не пишет. На пустой таблице `in_` пустого множества даёт пустой
список — библиотека отвечает «кейсов ещё нет», а не падает.

**Интеграция.** Экран и API договариваются одним параметром; тест E9 проверяет,
что фронт именно просит сервер, потому что фильтрация страницы из 50 строк
показала бы историю пяти проектов и молча спрятала остальные.

## Spec coverage gaps
- Нет: поставка закрывает один дефект целиком.

## Verdict
- [x] READY FOR HANDOFF
