# Verify report

**Date:** 2026-09-13
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (agent:claude at=2026-09-13) — проверено на стенде:
карточка прогона 385 отдала 18 судеб с причинами и расходом по каждому домену

## Shape oracles
- [x] PASS — ruff, ruff format, mypy strict (109 файлов)
- [x] PASS — tsc, eslint (0 ошибок, 0 предупреждений), prettier
- [x] PASS — `okf_sync_gate`, `delivery_check`: чисто

## Behavior oracles
- [x] PASS — pytest: 518 passed, 1 skipped (было 515)
- [x] PASS — vitest: 104 passed (было 102)
- [x] PASS — E1–E5

## Ревью рисковых мест

**Граница выдачи.** Список судеб ограничен `MAX_FATES` = 200: у прогона столько
проектов, сколько в списке заказчика (сто по ТЗ), и выдача без границы ломается
ровно на большом прогоне. Гейт `unbounded-list` проверяет это правило и на
новом эндпоинте.

**Производительность.** Класс подняли `.all()` в `run_journal.fates`. Риска
нет: одна выборка по индексу `run_id` на открытие карточки, свёртка в памяти по
числу записей прогона; в списке прогонов запросов не прибавилось вовсе —
`projects_skipped` считается арифметикой по уже прочитанной строке.

**Права.** Новый ответ отдаётся тем же `require_right("read")`, что и остальные
чтения: отдельной двери у судеб нет.

**Совместимость.** `RunCard` наследует `RunRow`, поле `fates` добавочное —
клиент, который его не знает, продолжает работать. Экран прогонов ходит за
судьбами только по нажатию.

## Spec coverage gaps
- «Перезапустить пропущенные» кнопкой не делается: это трата units, и решение
  принимает владелец после первого боевого прогона.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 0 code (+0 process docs) / +0/-0 (net +0) |
| commits | 0 |
| time_to_accepted_spec | n/a (no spec.md in history — class S?) |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | no |
| implement_retries | MANUAL — fills from session log |
| verify_fails_before_green | MANUAL — count red verify runs (CI run list) |
| est_token_or_cost | MANUAL / n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
