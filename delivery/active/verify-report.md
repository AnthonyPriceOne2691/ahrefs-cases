# Verify report

**Date:** 2026-09-13
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (agent:claude at=2026-09-13) — проверено на стенде до и
после: было «19 проектов, 9 без группы», стало «19 проектов, без группы 0», а
предпросмотр той же версии говорит «не хватает данных 0» вместо девяти

## Shape oracles
- [x] PASS — ruff, ruff format, mypy strict (109 файлов)
- [x] PASS — `okf_sync_gate`: оба задетых концепта тронуты вместе с кодом
- [x] PASS — `delivery_check`: 0 ошибок, 0 предупреждений

## Behavior oracles
- [x] PASS — pytest: 515 passed, 1 skipped (было 511)
- [x] PASS — E1–E5, включая новый оракул согласия трёх путей

## Ревью рисковых мест

**Смысл вердикта.** Поставка снимает запрет: проекты, которым раньше отказывали
в вердикте, теперь его получают. Это ослабление, и оно осознанное — запрет
стоял по неверному признаку (край собранной серии вместо периода работ). Взамен
появилась справочная запись `point_a_months_used`: случай не замолчан, он
назван словами и виден в карточке.

**Калибровка.** Прямое следствие для Ф8: экран порогов и карточка проекта
теперь отвечают об одном проекте одинаково. Прежнее расхождение сделало бы
калибровку невозможной — эксперт сверяет группы, а половина списка исчезала
после каждой активации версии.

**Транзакция БД.** `recalc` пишет вердикт каждому проекту вместо части — число
записей растёт на число прежних пропусков (на стенде 9 из 19). Это одна
`INSERT`-строка на проект в той же транзакции, что и раньше; отдельного
коммита не добавляется.

**Производительность.** Покрытие считается по уже прочитанной серии, второй
выборки нет: `evaluate` теперь **не** считает `gap` повторно, а берёт готовый
из `Computed`. Запросов в цикле не появилось.

**Совместимость.** Поведение проекта с полным покрытием не меняется — это
закреплено примером E5 (ни одной новой записи в объяснении).

## Что показала живая проверка

До поставки один и тот же проект получал три разных ответа: `classify` —
«good», `preview` — «не хватает данных: точка А 2024-06», `recalc` — пропуск без
записи. После активации версии карточка отдавала `verdict: null` у девяти
проектов из девятнадцати. После поставки: без группы — ноль, предпросмотр
согласен с классификацией, в карточке видна строка «точка посчитана по 1 мес.
из 2».

## Spec coverage gaps
- Докупка недостающих месяцев по кнопке не делается: сейчас нехватка названа,
  действие остаётся за человеком.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 0 code (+0 process docs) / +0/-0 (net +0) |
| commits | 0 |
| time_to_accepted_spec | n/a (no spec.md in history — class S?) |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | no |
| implement_retries | MANUAL — fills from session log |
| verify_fails_before_green | MANUAL — count red verify runs (CI run list) |
| est_token_or_cost | MANUAL / n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
