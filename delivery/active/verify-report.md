# Verify report: curves-compare-months-not-bucket-sizes

## Чем проверено

| Что | Чем | Результат |
|---|---|---|
| Свёртка | `pytest tests/test_export_grouping.py -q` | 11 passed |
| Метка старта падала до правки | тот же тест на `charts.py` из HEAD | 1 failed |
| Имена файлов | `pytest tests/test_cases_archive.py -q` | 10 passed |
| Сторож анонимности | `pytest tests/test_cases_builder.py -q` | passed, сужен до полей листа |
| Бэкенд целиком | `pytest -q` | 584 passed, 1 skipped |
| Место под панель | `vitest run src/pages/__tests__/cases.test.tsx` | 17 passed; проверка падала до правки |
| Типы и стиль | `mypy`, `ruff`, `tsc --noEmit` | чисто |

## Подтверждение живыми числами

Правка проверена не только тестами. Числа владельца сошлись с механизмом до
единицы: квартал IV.25 по фикстурному ряду `etymonline.com` —
74 081 + 76 243 + 78 531 = **228 855**, ровно то, что показывал экран. Это и
доказало, что складывались именно месяцы.

## Что сделано со стендом и чем это отличается от сказанного

Владелец выбрал пересчитать вердикты по живым рядам. Прогон
`classify --source live` пересчитал **все 75** проектов: 24 с живыми рядами
получили настоящие вердикты (good 5, medium 5, poor 14), остальные 51 —
`insufficient_data`, потому что живых рядов для них не покупали.

**Описание выбора было неточным:** вариант обещал, что 51 проект «останется
фикстурным», тогда как `classify` перезаписывает вердикт проекта целиком, и
фикстурных вердиктов на стенде не осталось. Возврат — `classify --source
fixture`. Сказано владельцу прямо в тот же час.

`etymonline.com` после пересчёта — **good** по живым рядам (был `medium` по
фикстуре).

## Чего проверка НЕ доказывает

Кейсы на стенде собраны по прежним, фикстурным вердиктам и теперь с ними
расходятся. Пересборка не делалась: это действие над стендом, и его решает
владелец. До пересборки библиотека показывает кейсы, которых текущие вердикты
не порождали, — сам сервис это видит (`numbers_mismatch`) и на сборке скажет.

asserts_reviewed_by: human:anthony at=2026-09-15
