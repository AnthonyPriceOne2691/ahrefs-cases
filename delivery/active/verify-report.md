# Verify report

**Date:** 2026-09-14
**Verifier:** human:anthony (снял запрет на live и вернул его), agent:claude (прогон и оракулы)
**asserts_reviewed_by:** yes (at=2026-09-14) — проверено живьём на калибровочном
наборе: до правки `wickes.co.uk` и `wineverygame.com` имели ноль точек
подтверждающих метрик, после — 43,1 % и 121,5 % у первого, 598,4 % и 52,3 % у второго

## Shape oracles
- [x] PASS — ruff, ruff format, mypy strict (109 файлов)
- [x] PASS — import-linter: `collect` по-прежнему не знает про `classify`
- [x] PASS — `delivery_check`, `okf_sync_gate`

## Behavior oracles
- [x] PASS — pytest: 529 passed, 1 skipped (было 527)
- [x] PASS — E1–E5

## Ревью рисковых мест

**Область действия платящих команд.** В той же поставке `stage2` и `case-data`
получили `--only` через общий помощник `_scoped_projects`: до неё они брали
**все** проекты базы, и живой прогон заплатил бы за отладочные домены стенда
(урок L111, закрытый прежде только для `collect`). Оракул
`test_every_paying_command_has_a_scope` смотрит на разбор аргументов, поэтому
поймает и следующую платящую команду.

**Деньги.** Кандидатов стало больше (на калибровочном наборе 10 вместо 8), и это
осознанное подорожание: прежняя экономия достигалась тем, что часть проектов
теряла шанс на кейс. Догрузка двоих стоила 312 units. Отбор по-прежнему не
делает ни одного запроса — считает по уже купленному шагу 1, а список групп,
за которые платим, назван явно константой `_WORTH_PAYING`: `insufficient_data`
в него не входит, потому что покупать подтверждающие там, где не хватило
трафика, — значит платить за заведомо неполный кейс.

**Контракт слоёв.** Правило переехало в `classify/candidates.py`, а не
протащило пороги в `collect`: сбор обязан оставаться тестируемым без базы
порогов. `scripts/run_collect.py::_stage2` теперь зовёт `stage2_candidates`, а
`collect/runner.py::collect_stage2` по-прежнему принимает готовый список —
сигнатура не изменилась. Проверяет import-linter.

**Удалённый модуль.** `collect/funnel.py` убран целиком вместе с
`preliminary_candidates` и `_traffic_growth`, а не оставлен «на всякий случай»:
вторая точка входа означала бы два ответа на один вопрос. Ссылки в докстроках
`collect/plan.py` и `collect/runner.py` переписаны на
`classify.candidates.stage2_candidates`, тесты `tests/test_collect_funnel.py`
переведены на новое правило через помощник `_candidates`.

**Настройка-сирота.** `AHREFS_STAGE2_MIN_GROWTH` больше ни на что не влияет.
Оставлена в конфиге до следующей поставки и названа в `spec.md`: молчащая
настройка опаснее удалённой, но удалять её в этой поставке — расширение области.

## Что показала живая проверка

Калибровочный прогон на 25 доменах: 9 из 10 проигравших HCU → `poor`, 10 из 10
победителей IndexWatch выше `poor`, ложных срабатываний ноль. Два победителя не
могли стать `good` ни при каких порогах — это и вскрыло дефект. После правки оба
получили подтверждающие метрики, и `wineverygame.com` переходит в `good` на
порогах Приложения А.

## Spec coverage gaps
- Где именно проходит граница «хорошего» — не вопрос этой поставки: пороги
  утверждают Head of Link Building и Owner. Материал для решения собран в
  `docs/CALIBRATION_SET.md`.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 0 code (+0 process docs) / +0/-0 (net +0) |
| commits | 0 |
| time_to_accepted_spec | n/a (no spec.md in history — class S?) |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | no |
| implement_retries | MANUAL — fills from session log |
| verify_fails_before_green | MANUAL — count red verify runs (CI run list) |
| est_token_or_cost | MANUAL / n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
