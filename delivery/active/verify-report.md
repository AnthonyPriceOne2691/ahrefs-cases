# Verify report: next-step-hint

## Чем проверено

| Что | Чем | Результат |
|---|---|---|
| N1–N6: правило «что дальше» по ступени и статусу | `vitest src/pages/__tests__/next-step.test.tsx -t 'правило'` | 6 passed; до правки файл падал на импорте — модуля не было |
| N7: экран целиком ведёт на «Кейсы» | `vitest -t 'N7'` | passed; **падал, пока компонент не подключён к экрану** («Unable to find … №10 — данные под кейс — готов») |
| Фронт целиком | `vitest run` | 208 passed |
| Типы и стиль | `tsc --noEmit`, `eslint --max-warnings 0`, `prettier --check` | чисто |

## Исполнение рисковых путей

`web/src/pages/runs/NextStep.tsx` — боевая сборка (`vite build`) и API на копии дев-базы (`cases_hint`),
headless Chrome 154 по CDP, тёмная и светлая тема, at=2026-09-25. Прогоны заводились в базе по ходу,
страница **не перезагружалась** между шагами 1–3.

| Состояние базы | Что на экране |
|---|---|
| №2001 — шаг 2, идёт | «Прогон №2001 — шаг 2 — идёт. Когда он закончится, здесь появится следующий шаг.» |
| №2001 готов, №2002 — данные под кейс, идёт | «Прогон №2002 — данные под кейс — идёт…» — сменилось опросом |
| №2002 готов | «…— готов. Осталось собрать кейсы: на экране «Кейсы» кнопка «Пересобрать кейсы», когда сборка закончится — «Скачать ZIP».» и ссылка «Перейти к кейсам» (`/cases`) |
| клик по ссылке | адрес `/cases`, на экране «Пачка целиком» |

Контраст текста подсказки (`audit.js`, как для Z36): 0 находок в обеих темах.

## Ревью рисковых мест

**Последний прогон — любого автора.** Следующий шаг общий, потому что общие группы, данные и пачка;
если двое работают одновременно, подсказка назовёт шаг по самому свежему прогону — это и есть
состояние сервиса.

**Опрос.** Раз в 5 с и только пока последний прогон в очереди или идёт — как у журнала; готовый,
упавший и отклонённый не опрашиваются. Прогон, запущенный с этой страницы, освежает подсказку сразу:
запуск шага 1 и шага 2 уже инвалидирует ключ `['runs']`, под которым лежит и подсказка. Прогон с чужого
компьютера появится при возврате на вкладку (`staleTime` 30 с) или обновлении.

**Нагрузка.** Плюс один `GET /api/runs?limit=1` при открытии экрана: база, без Ahrefs и без units.

**Слова.** Ступень и статус — из общего словаря `status.ts` (`stageWord`, `runWord`, L222); кнопки
названы так, как подписаны на экранах. Экран «Прогоны» открыт только праву `run` — у всех, кто видит
подсказку, есть и «Пересобрать кейсы».

**Транзакция БД — риска нет**, потому что дифф не касается сервера: правка живёт в
`web/src/pages/runs/NextStep.tsx`, двух строках `RunsPage.tsx` и тесте `next-step.test.tsx`.

**Цепочка гейтов — риска нет**, гейты не менялись.

## Чего проверка НЕ доказывает

Что человек, впервые открывший прод, дойдёт по подсказке до PDF без вопросов: это `observe_signal` —
следующий путь от шага 1 до «Скачать ZIP» у владельца или пользователя.

## Verdict
- [ ] READY FOR HANDOFF — оракулы зелёные; ждёт подписи human:anthony (verifier)
- [ ] NEED CONVERGE (new tasks)
- [ ] BLOCKED
