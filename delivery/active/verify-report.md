# Verify report

**Date:** 2026-09-11
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (by=human:anthony at=2026-09-11) — утверждение
проверено на стенде числами: при заведомо недостижимой версии предпросмотр
показал четыре проекта «хороший → плохой», при действующей — ни одного
**CI run:** пуш после приёмки
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — pre-commit, 29 хуков, упавших 0
- [x] PASS — предохранители: net_loc 522 при пороге 800, файлов 9 из 25
- [x] PASS — eslint без предупреждений, tsc чист

## Behavior oracles
- [x] PASS — vitest 74 (+6), pytest не затронут (фронтовая поставка)
- [x] PASS — E1, E3–E6, E9 закрыты тестами

## Product oracles (живой стек, Chrome по CDP)
- [x] PASS — список версий и выбор строкой: карточка сверху переключилась на
      выбранную версию с её условиями
- [x] PASS — предпросмотр действующей версии на настоящих данных: посчитано
      5 из 11, шести не хватает данных, никто не двигается
- [x] PASS — **предпросмотр действительно считает**: заведомо недостижимая
      версия (рост от 100 000 %) дала «Сменят группу: 4», все «хороший → плохой»
- [x] PASS — проверочные версии со стенда убраны, осталась одна действующая

## Найдено прогоном живого экрана

**Группы латиницей.** Предпросмотр показывал `good → poor` там, где остальной
интерфейс говорит «хороший» и «плохой»: `PreviewChange` несёт значения
перечисления, и переводить их было некому. Словарь названий вынесен из
`GroupBadge` в `pages/groups.ts` и используется обоими экранами.

## Ревью рисковых мест

**Безопасность.** Раздел под правом `edit_thresholds` (таблица доступа ТЗ, L94).
Предпросмотр ничего не записывает — это `POST` ради расчёта, а не изменения;
имя версии уходит через `encodeURIComponent`, и это не украшение: версии
кириллические, и запрос без кодирования падает ещё в клиенте (проверено —
собственный python-скрипт прогона упал на этом же).

**Транзакция БД.** Экран не пишет. Предпросмотр на сервере читает и считает,
вердикты не трогая, — этим он и отличается от пересчёта.

**Интеграция.** Расчёт идёт по всей базе и занимает секунды: кнопка занята, пока
считает, клик по строке и клик по кнопке разведены остановкой всплытия.

## Spec coverage gaps
- Сравнение «версия А против версии Б» не делается: предпросмотр отвечает
  относительно действующих вердиктов. Появится — будет вопрос к API.
- Правка порогов — поставка `thresholds-edit`.

## Verdict
- [x] READY FOR HANDOFF
