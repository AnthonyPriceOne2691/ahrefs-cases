# Verify report

**Date:** 2026-09-10
**Verifier:** <human:anthony — заполняется при приёмке>
**asserts_reviewed_by:** n/a (класс S — дайджест обязателен для M/L)
**Commit:** см. `git log`

## Что было сломано

Реапер добивал живой прогон. Считается, а не предполагается:

```
худшая задача = 60 с таймаут × 3 попытки + backoff (1+5+30) = 216 с
худший прогон = 216 с × 100 доменов ÷ 3 параллельно      = 120 минут
порог реапера = COLLECT_RUN_STALE_SEC                     = 60 минут
```

Прогон помечался `failed`, его резерв units освобождался, а сам он продолжал
писать в базу, не зная об этом. На фикстурах не проявлялось и не проявилось бы
до Ф7 — там задача выполняется мгновенно.

## Как починено

1. **Прогон получил жёсткий лимит** (`COLLECT_RUN_TIMEOUT_SEC`, 4 часа) и сам
   останавливается по нему. Утверждение «живой прогон не идёт дольше лимита»,
   на котором держится весь подход реапера, стало правдой: в CRM его давал
   таймаут RQ-джобы, у нас до Ф5 очереди нет.
2. **Конфиг отказывается стартовать**, если порог реапера не строго больше
   лимита прогона. Это и есть защита от возврата: несогласованные числа
   больше нельзя подобрать молча.
3. Умолчания посчитаны от худшего случая, а не взяты на глаз.

## Что нашлось по ходу починки

Слабое утверждение в моём же тесте (`status == done or status == partial`)
оказалось признаком непродуманного поведения: прогон, где **ни одна** задача
не выполнена, отчитывался `done`. Невыполненные задачи теперь делают прогон
`partial` наравне с упавшими.

## Ещё два случая того же класса, найденные тем же способом

Расчёт по собственным настройкам вскрыл Z4 и Z5. Оба вошли в эту поставку:
тема одна («на фикстурах зелено, вживую не работает»), класс S, объём мал.

**Z4 — прогон не влезал в бюджет заказчика.**

```
шаг 1: 50 units × 100 доменов                       = 5000
шаг 2: (keywords 50 + refdomains 5 + pages 50
        + search_volume 50) × 30 кандидатов          = 4650
итого                                                = 9650  при бюджете 10 000
неснижаемый запас AHREFS_UNITS_MIN_LEFT              = 5000  ← половина бюджета
```

Из 9650 три тысячи уходили на `pages-history` и `total-search-volume-history`
— метрики, которых нет ни в правилах Ф3, ни в блоках кейса по ТЗ. Данные без
читателя. Выключены флагами, запас снижен до 1000:

```
шаг 1 5000 + шаг 2 1650 = 6650 + запас 1000 = 7650, остаётся 2350
```

**Z5 — период длиннее 21 месяца обрезался молча.** `max_history_months` 24
минус запас 3: точка А бралась не от старта работ, «рост А → Б» описывал не
весь срок, и узнать было неоткуда. Добавлена справочная запись
`history_truncated` в вердикт: группу не меняет, но человек видит.

## Что нашли гейты в моей же правке

Первая версия фильтра endpoint'ов читала конфиг через `getattr` по имени
поля. Правило проекта это запрещает, и гейт `config-access` его поймал:
опечатка в имени при отражении не видна ни mypy, ни на ревью — а стоит здесь
трети бюджета прогона. Переписано явным соответствием.

## Oracles
- [x] PASS — repro-тест: несогласованные числа не дают собрать конфиг
- [x] PASS — умолчания покрывают худший случай (посчитано в тесте, не в комментарии)
- [x] PASS — прогон на дедлайне закрывается `partial`, остаток не запрашивается
- [x] PASS — обычный прогон дедлайном не задет
- [x] PASS — Z4: тест «первичный прогон влезает в 10 000», считает по `stage2_specs()`,
      то есть по реальному набору запросов, а не по воображаемому
- [x] PASS — Z5: `history_truncated` появляется при обрезанной истории и не появляется,
      когда история совпадает с периодом
- [x] PASS — `pytest`: 209 тестов, 1 пропущен; `ruff`, `mypy --strict`, 27 хуков чисты

## Verdict
- [ ] READY FOR HANDOFF
- [ ] NEED CONVERGE (new tasks)
- [ ] BLOCKED

<!-- Вердикт ставит Verifier (human:anthony), §5.2. -->
