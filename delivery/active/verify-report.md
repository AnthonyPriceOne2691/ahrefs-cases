# Verify report

**Date:** 2026-09-13
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (agent:claude at=2026-09-13) — собран живой кейс:
`render «ПРОВЕРКА-Рост.example»` печатает превращение и кладёт файл
`проверка-рост.example — Кейс.pdf` вместо `xn----7sbfmzvfbjddnn.example — Кейс.pdf`

## Shape oracles
- [x] PASS — ruff, ruff format, mypy strict
- [x] PASS — `okf_sync_gate` (тронуты `product/case-content.md` и `product/overview.md`)
- [x] PASS — `delivery_check`

## Behavior oracles
- [x] PASS — pytest: 524 passed, 1 skipped (было 518)
- [x] PASS — E1–E4

## Ревью рисковых мест

**Артефакт клиенту.** Меняется имя файла и заголовок PDF — то, что видит
клиент агентства. Проверено на собранном артефакте, а не в разметке: имя файла
складывается в `export/pdf_renderer.filename` из заголовка кейса.

**Канон не тронут.** В базе, в запросах к Ahrefs и в журнале домен остаётся
каноном: `to_unicode` зовётся только там, где строку читает человек. Это
проверяется тем, что `Project.domain` нигде не переписывается.

**Анонимность.** Правило «домена нет нигде» сильнее и стоит раньше:
анонимный кейс по-прежнему получает «сайт в нише X» (пример E3 прежний).

**Производительность.** Чистая функция над строкой, вызывается по разу на кейс.

## Spec coverage gaps
- Экраны (список проектов, карточка) показывают канон. Для оператора это
  верно — он сверяет с Ahrefs, — но если владелец захочет человеческий вид и
  там, правка однострочная.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 0 code (+0 process docs) / +0/-0 (net +0) |
| commits | 0 |
| time_to_accepted_spec | n/a (no spec.md in history — class S?) |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | no |
| implement_retries | MANUAL — fills from session log |
| verify_fails_before_green | MANUAL — count red verify runs (CI run list) |
| est_token_or_cost | MANUAL / n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
