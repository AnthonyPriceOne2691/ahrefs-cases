# Plan: project-card-hands-over-the-case

Уроки, прочитанные перед работой (`archive/INDEX.md`): **L41** (числа экрана —
из записанного вердикта), **L161–L162** (что живёт в адресе, а что нет),
**L170** (признак без потребителя — надпись), **L79** (уборка за собой в тестах
по своим строкам).

1. **Бэкенд, `api/routers/cases.py`.** Фильтр `project_id` в `list_cases` —
   рядом с `all_versions`, потому что вопрос тот же. Ручка выборки —
   **до** `/{case_id}/download`, иначе слово `selection` уйдёт в номер.
   Архив пишется в каталог выгрузки: `FileResponse` отдаёт файл после возврата
   обработчика, временный файл к тому моменту уже удалён.
2. **Тесты бэкенда** на оба: фильтр отвечает пустым списком на чужой проект,
   отказ выборки называет номера, `selection` не становится номером.
3. **Фронт, `card/CasePdf.tsx`.** Свой запрос и своя кнопка; `obstacle()`
   различает два случая «нечего качать». Монтируется под шапкой карточки.
4. **Фронт, кривые.** `keepPreviousData` у запроса; `stale` в `Charts`;
   переключатель вне приглушаемой области.
5. **Тесты фронта** на оба, с задержанным ответом сервера: без паузы новые
   кривые приезжают в том же тике, и проверять «что видно во время загрузки»
   становится нечего.
6. **Разбор на части** там, где ратчет ESLint покажет рост: планка 0, и
   перерастание восьмидесяти строк — не повод её поднимать.
