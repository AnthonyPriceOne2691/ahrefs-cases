# Plan: library-by-selection-and-a-theme-you-choose

Уроки, прочитанные перед работой (`archive/INDEX.md`): **L161** (что живёт в
адресе, а что нет — отметки сознательно не живут), **L170** (признак без
потребителя), **L79** (уборка за собой в тестах по своим строкам),
грабли Mantine в jsdom (попапы рендерятся секундами — брать `ActionIcon` и
`Checkbox`, не выпадающее меню).

1. **Отметки** — своим модулем `cases/selection.ts`: состояние со своими
   правилами (отмечать можно только строки с файлом), а не три строки внутри
   экрана. Там же скачивание выборки: один — PDF, несколько — архив.
2. **Таблица** получает колонку отметок и шапку «выбрать всё», считающую по
   строкам с файлом.
3. **Панель выбора** — `cases/SelectionBar.tsx`, появляется только когда есть
   что скачивать.
4. **Значок темы** — `app/ThemeToggle.tsx`, `ActionIcon` с обработчиком.
   Действующая тема берётся `useComputedColorScheme`, иначе значок показывал бы
   строку `auto`.
5. **Оформление**: тёмные правила на атрибут, медиазапрос оставить только на
   кадр до запуска скриптов — иначе белая вспышка при загрузке.
6. **Читаемость светлой темы** — проверять глазами на стенде, а не по коду.
