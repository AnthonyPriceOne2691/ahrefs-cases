# Plan: curves-compare-months-not-bucket-sizes

Уроки, прочитанные перед работой (`archive/INDEX.md`): **L41** (экран и PDF
отвечают то же, что вердикт — среднее совпадает с тем, как считаны А и Б),
**L30** (не дорисовывать ноль там, где не мерили — делитель по измеренным
месяцам), **L172** (жалоба на движение экрана — жалоба на следствие).

1. **`export/grouping.py`** — `_mean` вместо `sum` для потоков; делитель по
   измеренным месяцам.
2. **`export/charts.py`** — дата старта сворачивается тем же `period_start`.
3. **Тесты свёртки** переписать под новое правило вместе с именами: старые
   держали сумму и обязаны были покраснеть.
4. **Оракул на корзины разной полноты** — плоский ряд обязан оставаться
   плоским на любом шаге.
5. **`cases/model.py`** — поля `domain` и `version` в конец класса (у dataclass
   поля с умолчанием идут последними).
6. **`export/pdf_renderer.py`** — имя по домену и версии, с откатом на
   заголовок у старых кейсов.
7. **`cli/case_commands.py`** — номер сборки спрашивается до рисования.
8. **Сторож анонимности сузить**, не ослабив: домен запрещён в полях листа.
