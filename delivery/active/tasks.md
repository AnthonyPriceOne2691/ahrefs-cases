# Tasks: gates-judge-or-say-why-not

Уроки по затронутым путям (`archive/INDEX.md`): **L73** (пропуск честен в выводе
и лжив в итоге — именно её лекарство, остановка сьюта без базы, и создало Z18
в джобе, где базы нет), **L8, L58, L68** (зелёный по причине окружения — тот же
класс, здесь в самой оснастке), **L143** (объявленный оракул, которого не
существует, хуже отсутствующего — Z20 его родной брат: оракул существует и не
судит).

- [x] Z18: шаг покрытия стоит там, где база есть, и гоняет сьют **один** раз.
- [x] Z18: оракул — ни одна джоба не зовёт полный сьют без сервиса Postgres.
- [x] Z19: гейт показывает вывод сьюта, когда `coverage.json` не появился.
- [x] Z19: оракул — вывод сьюта не уходит в `/dev/null` безвозвратно.
- [x] Z20: mutmut получает область мутаций там, где его запускают (`src/`).
- [x] Z20: мёртвый ключ `paths_to_mutate` убран из корневого `pyproject.toml`.
- [x] Z20: оракул — конфиг мутаций лежит рядом с cwd гейта и назван ключом 3.x.
- [ ] **Z20: вердикт мутационного гейта — НЕ закрыт.** Область читается, но сьют
      из `src/mutants` не стартует: он предполагает корень репозитория. Замером
      подтверждено, что мутации до тестов доходят; остальное — отдельная
      поставка. Гейт при этом молчит честно и выходит нулём.
- [x] Z21: удалённые файлы исключены из диффа покрытия.
- [x] Покрытие собирается и с подпроцессов; проверено числом (+2,7 пункта).
- [x] Z22: долг CLI назван числами, отсрочка со сроком 29.09.2026 и будильником.
- [x] Z18–Z22 записаны в `docs/FINDINGS.md`.
- [x] pytest (574), vitest (126), mypy, okf-гейты зелёные.
- [x] Прогон CI на этих правках: `tests` зелёная, покрытие впервые посчитано в CI
      и напечатано таблицей (прогон 34974838478).
- [x] Гейт новых зависимостей: манифест без зависимостей больше не требует
      объявления; адаптация в `scripts/lint/adapted.json`, оракул в сьюте.
- [ ] Джоба `delivery` на пробном PR красная брейкерами — артефакт базы:
      `origin/main` отстаёт на 27 коммитов, и гейт меряет их все. При прямом
      пуше в main база совпадёт с HEAD и дифф будет пуст. Проверить пушем.
