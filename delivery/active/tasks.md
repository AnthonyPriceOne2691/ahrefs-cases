# Tasks: the-dash-says-which-case-it-is

Уроки по затронутым путям (`archive/INDEX.md`): **L30** (пустые оси сказали бы
неправду — «не собрано» там, где собрано, такая же неправда), **L75** (подписи
и арифметика не повторяются на фронте), **L23** (чужая форма не притворяется
нашей), **L189** (один текст на два разных «пусто» отправляет чинить не то).

- [x] Z25: журнал расхода units отвечает, покупали метрику или нет
      (`collect/purchases.py`); отдельной записи о покупке в базе нет.
- [x] Журнал молчит про домен — экран молчит тоже: прочерк, а не догадка.
- [x] Связь условия с метрикой живёт в одном месте (`SUPPORTING_METRICS`).
- [x] В колонке «Факт» вместо «—» стоит «не покупали» или «нет данных».
- [x] Z26: колонка «Группа» снята, группы стали заголовками блоков.
- [x] Условия вне групп собраны первым блоком «Общие условия».
- [x] Заголовки взяты из словаря, а не склеены («Условия хороший»).
- [x] Одинаковые формулировки разных групп остаются различимыми.
- [x] Живой замер по всем вердиктам стенда: 160 «не покупали», 16 «нет данных».
- [x] Канон обновлён: инвариант 4а в `groups-and-thresholds.md` и `log.md`.
- [x] Слово на экране — «не собирали», а не «не покупали»: покупаются данные
      Ahrefs, а читалось как покупка ссылок и ключей (владелец).
- [x] Счёт снят из шапки карточки: он сравнивает соседей, а в карточке одного
      проекта соседей нет; в сортировке списка остаётся.
- [x] Z30: файл кейса сверяется с действующим вердиктом ПО ЧИСЛАМ — ссылка
      `verdict_id` лгала, потому что вердикт перезаписывается на месте.
- [x] Устаревший файл не отдаётся ни из карточки, ни из библиотеки и не
      попадает в выборку ZIP; причина названа словами.
- [x] Сверено по всем 75 проектам: 11 совпадают, 51 от прежней группы, 13 без
      файла — «куда ни зайди», экран и файл говорят одно.
- [x] Z31: место под содержимое занимается заранее — «Динамика», библиотека
      кейсов, карточка пачки, плитки расхода. Мигание было вторым заходом Z23.
- [x] Z32: карточка объясняет, почему «стало» и конец кривой — разные числа;
      фраза одна на лист и на экран.
- [x] Рисунок выделяет сами точки А и Б их значениями — теми же, что в
      таблице, и на обеих кривых графика позиций.
- [x] Отметка остаётся в поле графика на всех трёх шагах оси.
- [x] Подписи не сталкиваются на всех 12 кейсах × 3 шага × 2 графика.
- [x] pytest (613), vitest (173), tsc, eslint (0 при планке 0), prettier, mypy.
