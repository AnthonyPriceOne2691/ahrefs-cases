# Tasks: expired-session-sends-you-to-the-door

Уроки по затронутым путям (`archive/INDEX.md`): **L163** (гейт, который не
судил, неотличим от согласного — здесь то же про признак, которого никто не
спрашивает), **L143** (объявленный оракул, которого нет, хуже отсутствующего —
докстрока обещала выход по 401 и обещание читалось как поведение).

- [x] Конец сессии объявляется из ОДНОГО места — слоя обращений к API.
- [x] Токен выбрасывается сразу, а не ждёт следующего запроса.
- [x] Форма входа называет причину: «сессия истекла», а не «неверный пароль».
- [x] На экране входа `401` по-прежнему означает неверный пароль (сторож от перегиба).
- [x] Повторы на `401` отключены — мигание белым было от них.
- [x] Z23 записан в `docs/FINDINGS.md`.
- [x] 5 проверок в `session-expiry.test.tsx`, 4 из них падали до правки.
- [x] vitest (140) и pytest (580) зелёные, tsc чистый.
