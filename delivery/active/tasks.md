# Tasks: Ф3б-1 — пересчёт по версии порогов

Снизу вверх: чистые функции, потом слой базы, потом команда. Уроки архива,
применимые к диффу: L18, L21, L13, L19, L8 — см. plan.md.

## Окна как вопрос, а не только как расчёт
- [x] `classify/points.py`: `window_a`, `window_b`, `baseline_window` публичны
- [x] арифметика остаётся в одном месте — второй экземпляр ловится гейтом копипаста

## Проверка покрытия
- [x] `classify/coverage.py`: каких месяцев не хватает под требования версии
- [x] различитель: месяц внутри собранного отрезка — дыра у Ahrefs, вне — не покупали
- [x] пустая серия — не вопрос покрытия: вердикт даёт классификация
- [x] тест: дыра внутри отрезка не считается нехваткой

## Расчёт отдельно от записи
- [x] `classify/verdicts.py`: `compute_verdict` ничего не пишет, `store` пишет
- [x] `classify_project` собран из них двоих — поведение Ф3а не изменилось
- [x] тест: golden-таблица классификации зелёная без правок

## Пересчёт и активация
- [x] `classify/recalc.py`: пересчёт по версии, пропуск по нехватке, отчёт
- [x] `ruleset_by_version`: ошибка со списком доступных версий вместо пустоты
- [x] `activate`: активна ровно одна версия, прошлые вердикты не тронуты
- [x] тесты E1, E4–E7, E9, E10 + случай «истории нет вовсе»

## Команда
- [x] `scripts/run_collect.py`: `recalc <версия> [--activate]`
- [x] ошибка версии печатается сообщением, а не трассировкой
