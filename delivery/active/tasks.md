# Tasks: очередь и запуск прогона

Уроки архива, применимые к диффу: L53, L11, L24, L2, L13, L54 — см. plan.md.

## Очередь
- [ ] `JobQueue` с двумя реализациями
- [ ] выбор по конфигу, одно место
- [ ] аргументы задач — простые значения

## Задачи
- [ ] прогон сбора со своей сессией
- [ ] сборка пачки кейсов
- [ ] упавшая задача оставляет прогон `failed` с причиной

## Роутер
- [ ] `POST /api/runs` за правом `run`
- [ ] `GET /api/runs` и `GET /api/runs/{id}`
- [ ] замок: второй прогон — 409 с номером активного

## Проверки
- [ ] E1–E10
