# Tasks: вход в сервис

Уроки архива, применимые к диффу: L23, L1, L33, L21, L8, L50 — см. plan.md.

## Пароли и токены
- [ ] bcrypt: хеш и проверка
- [ ] JWT: выпуск с TTL и разбор с причиной в логе
- [ ] пустой секрет — токен не выпускается

## Зависимости
- [ ] `current_user` из заголовка `Authorization`
- [ ] группа берётся из пользователя, а не из заглушки
- [ ] `require_right` не переписан — подставлен источник

## Роутеры
- [ ] `POST /api/auth/login`
- [ ] `GET /api/auth/me` с группой и правами

## Чистка
- [ ] свойства `can_*` у модели убраны: правило прав одно

## Команда
- [ ] `useradd <почта> <группа>` с запросом пароля

## Проверки
- [ ] E1–E10
- [ ] пароль не появляется ни в логе, ни в ответе
