# Tasks: остановка прогона без базы

Уроки архива: **L8**, **L58**, **L68** — все три про «зелёный по причине
окружения»; здесь тот же класс, но окружение убирает проверки, а не подменяет
данные. **L23** — причина отказа обязана называть следующий шаг (команду).

Не применимы: L69–L72 — контейнеров поставка не трогает, только знание о том,
что база живёт в них.

## Остановка
- [ ] проба базы один раз на прогон
- [ ] остановка, если выбраны тесты, которым база нужна
- [ ] в тексте — команда подъёма дев-стека
- [ ] `AHREFS_TESTS_ALLOW_NO_DB=1` возвращает пропуск

## Проверка
- [ ] E1–E4 исполнением
