# Tasks: читающие роутеры

Уроки архива, применимые к диффу: L41, L32/L34, L1, L13, L51, L8 — см. plan.md.

## Схемы
- [ ] проект в списке, карточка, вердикт с объяснением
- [ ] кейс в библиотеке, расход units

## Роутеры
- [ ] `GET /api/projects` с фильтрами и границей выдачи
- [ ] `GET /api/projects/{id}` — вердикт, точки, ряды
- [ ] `GET /api/cases` и `GET /api/cases/{id}/download`
- [ ] `GET /api/usage`
- [ ] все за правом `read`

## Границы
- [ ] лимит с потолком, а не «сколько попросят»
- [ ] нет проекта — 404; нет файла — 404

## Проверки
- [ ] E1–E10
