# Tasks: приём по HTTP и смета

Уроки архива, применимые к диффу: L53, L23, L10, L50, L32/L34 — см. plan.md.

## Приём
- [ ] байты → таблица: XLSX из потока, CSV через уже существующий разбор текста
- [ ] `POST /api/intake/file`: имя параметром, предел размера, отказ по формату
- [ ] `POST /api/intake/link`: Google Sheet, причина закрытой таблицы доносится
- [ ] ответ: принято / создано / обновлено / построчные отказы с причиной

## Смета
- [ ] `GET /api/runs/estimate`: units, запросы, кэш, разбивка по схеме
- [ ] вердикт preflight тремя состояниями, остаток и резерв — числами
- [ ] маршрут объявлен до `/{run_id}`

## Окна точек
- [ ] один источник окон для CLI, задачи и сметы
- [ ] тест: прогон из очереди и смета берут одинаковые окна

## Проверки
- [ ] E1–E12
- [ ] карта репозитория обновлена вместе с кодом
