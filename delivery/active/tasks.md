# Tasks: Ф3а — вердикт классификации

Снизу вверх: чистые функции, потом слой базы, потом команда. Уроки архива,
применимые к диффу: L9, L8, L12 — см. plan.md.

## Пороги как типы
- [ ] `classify/thresholds.py`: модель порогов (окна, eligibility, группы, score)
- [ ] разбор `payload` из `Ruleset` с внятной ошибкой на кривой структуре
- [ ] сид из `config/thresholds.example.yml`, идемпотентный по версии
- [ ] тест: пороги читаются из БД, правка файла на живом сервисе ничего не меняет
- [ ] тест: конструирование по имени поля (урок L4), дефолты не подменяют значения

## Точки и дельты
- [ ] `classify/series.py`: `MetricSeries` из `MetricPoint` (чтение, без решений)
- [ ] `classify/points.py`: среднее по окну вокруг границы периода
- [ ] тест: выброс на границе не меняет группу (D1)
- [ ] `classify/deltas.py`: абсолютная и относительная дельта по метрикам
- [ ] тест: дельта от нулевой базы не даёт бесконечности и не роняет расчёт

## Правила
- [ ] `classify/rules.py`: eligibility (месяцы, дыра) → `insufficient_data` (D6, D9)
- [ ] правила групп таблицей условий; каждое условие пишет запись `reasons`
- [ ] «главная И минимум одна подтверждающая» (D7, D8)
- [ ] порог месяцев понижает группу, а не отсекает проект (D5)
- [ ] `score` и сортировка по абсолютному приросту (D11)
- [ ] тест: `reasons` содержит значение, порог и вердикт по каждой метрике (D10)

## Golden-таблица
- [ ] `tests/test_classify_golden.py`: семь сценариев Ф2а → группа (D13)
- [ ] таблица считается без базы и без сети
- [ ] канарейка: подмена порога меняет группу — таблица действительно судит

## Слой базы и команда
- [ ] `classify/verdicts.py`: запись `Verdict` с `ruleset_id`, `reasons`, точками
- [ ] тест: вердикт хранит версию порогов; другая версия — другой вердикт (D12)
- [ ] `scripts/classify_projects.py` или подкоманда: распределение по группам
- [ ] тест: классификация не делает ни одного HTTP-запроса
- [ ] `eval-smoke.md`: прогон на данных Ф2 и объяснение по одному проекту

## Закрытие
- [ ] покрытие ядра ≥ 80 % (не ниже 94 %, достигнутых в Ф2)
- [ ] `decisions.md` по ходу, не перед handoff
- [ ] `assert_digest` → 0 непривязанных; новые ожидания дописываются в спеку
