# Tasks: file-import-explains-and-refuses

Уроки по затронутым путям — в `plan.md`.

## Slice 1 — сервер: граница «брак файла / брак строки»

- [ ] T1: тесты API на примеры V1–V15 и кода CLI (V21) — красные до правки
      по правильной причине (`200`/`500` вместо `400`, код 1 вместо 2).
- [ ] T2: `UnfitSourceError` и `validate.check_fit`: пусто, дубль колонки,
      нет колонок (с прочитанным и подсказкой похожего имени), только шапка.
- [ ] T3: читатели — книга не читается, двоичный CSV, `csv.Error`; какой лист
      прочитан (`RawTable.where`) у книги и у ссылки.
- [ ] T4: роутер — отказ `400` с подлежащим на обоих путях, лог
      `intake_refused`; пустое тело — «файл пуст»; CLI — код 2.
- [ ] T5: прежние тесты B15 переписаны под новую границу; pytest целиком.

## Slice 2 — экран: подсказка у файла и отказ у поля

- [ ] T6: тесты экрана V16–V18, V20 — красные до правки.
- [ ] T7: `listFormat.json` + реляционный тест с сервером (V19).
- [ ] T8: `ListHelp` — одна карточка, `SheetHelp` и `FileHelp`; `SourceForm` —
      подписи соседями, отказ под своим полем, `accept` из данных, `resetRef`.
- [ ] T9: `IntakeOutcome` — убраны переводы кодов, которых сервер больше не
      шлёт, и плашка «не нашлось ни одной строки» (состояние недостижимо).

## Slice 3 — исполнение и сдача

- [ ] T10: браузер: боевая сборка + API на копии дев-базы; снимки подсказок и
      отказов в обеих темах; случаи V1–V17, V20 настоящими файлами; ссылка —
      подменённым транспортом. Копия базы удалена, процессы остановлены.
- [ ] T11: гейты: pytest, vitest, tsc, eslint, prettier, mypy, ruff,
      `delivery_check`; канон (`knowledge/product/overview.md`) и реестр
      находок (Z42).
- [ ] T12: verify-report, метрики, архив, урок L212, PR, зелёный CI.
