# Tasks: сервис в контейнерах (дев)

Уроки архива, применимые к диффу: L58/L8, L24, L7 — см. plan.md.

## Контекст и образы
- [ ] `.dockerignore`: ни `.venv`, ни `node_modules`, ни `.git`
- [ ] дев-стадия питона без `COPY . .`
- [ ] стадия фронта на `node`, версия прибита цифрой

## Компоуз
- [ ] `migrate` отрабатывает до api и worker
- [ ] `web` с прокси на контейнер `api` через переменную
- [ ] healthcheck у api: «поднялся» = «отвечает»
- [ ] том под `node_modules` контейнера

## Проверка исполнением
- [ ] E1–E8
- [ ] README: запуск одной командой
