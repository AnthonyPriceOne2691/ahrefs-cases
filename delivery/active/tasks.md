# Tasks: Ф2б — экономия units

Порядок снизу вверх: каждый следующий шаг опирается на проверенный предыдущий
(plan.md → Approach). Уроки архива, применимые к диффу: L8, L9, L4 — см. plan.md.

## Кэш: что уже куплено
- [ ] `collect/cache.py`: `covered_until(session, project_id, metrics, source)` — дата последней точки
- [ ] граница закрытого месяца: `now` параметром, а не `date.today()` внутри
- [ ] `plan.plan_stage1(..., now, coverage)`: задача не создаётся, если запрашивать нечего
- [ ] тест: таблица «что в базе → какой `date_from`», включая пустую базу (C2)
- [ ] тест: текущий месяц перезапрашивается, закрытые — нет (C3)

## Кэш: экономия предъявлена числом
- [ ] `budget.record_cached(session, run_id, ...)`: строка `kind=cached`, цена 0
- [ ] `RunReport`: поля «запросов сделано» и «запросов сэкономлено»
- [ ] `CountingProvider` в тестах: обёртка, считающая вызовы
- [ ] тест: первый прогон — 100 запросов, второй — **0**; оба числа в одном тесте (C1)
- [ ] тест: журнал второго прогона состоит из строк `cached` (C1, C10)

## Single-flight
- [ ] `collect/single_flight.py`: обёртка `AhrefsProvider` с `dict[key, asyncio.Task]`
- [ ] ключ = `(endpoint, target, mode, country, date_from, date_to)`
- [ ] тест: два одновременных запроса по одному домену → один вызов провайдера (C4)
- [ ] тест: разные домены не блокируют друг друга (обратная сторона C4)

## Квота: fail-closed
- [ ] `collect/quota.py`: `QuotaSource(Protocol)`, `FixtureQuota`, `LiveQuota`
- [ ] `LiveQuota`: `subscription-info/limits-and-usage` (0 units), разбор остатка
- [ ] `preflight(quota, estimate)`: три исхода — можно, мало, не знаем
- [ ] тест: остаток ниже `AHREFS_UNITS_MIN_LEFT` → прогон не стартует, причина с числами (C6)
- [ ] тест: квота недоступна → прогон не стартует (C7), и это **не** тот же код, что C6

## Смета и резервы
- [ ] `budget.estimate(tasks)`: сумма по задачам **после** кэша
- [ ] `budget.reserve(session, run)`: строка `kind=reserve`
- [ ] `budget.available(session, quota_left)`: остаток минус резервы незавершённых прогонов
- [ ] тест: 60 из 100 доменов в базе → смета на 40 запросов, не на 100 (C5)
- [ ] тест: смета сверена с фактом того же прогона, расхождение ≤ 10 % (C5)
- [ ] тест: второй прогон видит остаток минус резерв первого (C8)

## Шаг 2 воронки
- [ ] `collect/funnel.py`: `preliminary_candidates(session, project_ids, now)` — префильтр по росту трафика
- [ ] порог префильтра в конфиг (`AHREFS_STAGE2_MIN_GROWTH`) + `.env.example`
- [ ] `runner.collect_stage2(session, project_ids, provider)`: `STAGE2_SPECS`, DR под флагом
- [ ] тест: кандидатов 30 из 100 → запросов шага 2 ровно по 30 (C9)
- [ ] тест: по «плохим» проектам дорогих метрик нет ни в журнале, ни в `MetricPoint` (C9)

## Сквозное и закрытие
- [ ] `scripts/run_collect.py`: флаг `--refresh` (принудительное обновление, по умолчанию выключен)
- [ ] отчёт прогона печатает «запросов сделано / сэкономлено / units» (C10)
- [ ] `eval-smoke.md`: два прогона подряд, второй — ноль запросов
- [ ] покрытие ядра ≥ 80 % (не ниже 94 %, достигнутых в Ф2а)
- [ ] `decisions.md` — по ходу, не перед handoff
- [ ] проверить, что примеры C1–C10 помечены id в тестах (`assert_digest` → 0 непривязанных)
