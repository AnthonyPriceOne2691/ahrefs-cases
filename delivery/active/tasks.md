# Tasks: cases-pack-api

- [x] `PackView` в `api/schemas.py`: есть ли архив, имя, размер, когда собран, пояснение.
- [x] `GET /api/cases/pack` и `/pack/download` в `api/routers/cases.py`; маршруты
      объявлены **выше** `/{case_id}/download` — иначе `pack` уедет в параметр и
      выдача станет `422` (та же ловушка, что поймала смету прогона).
- [x] Свежий архив — по времени файла; пропавший между перечислением и опросом
      файл пропускается.
- [x] Тесты E1–E6 в `tests/test_api_cases_pack.py`, каталог выгрузки — временный.
- [x] pytest зелёный на дев-стеке; ruff, format и mypy по тронутым файлам чисты.
- [x] Живая проверка: пачка забирается по HTTP из поднятого стека — 267 319 байт, пять PDF и список внутри.
