# Tasks: file-import-explains-and-refuses

Уроки по затронутым путям — в `plan.md`.

## Slice 1 — сервер: граница «брак файла / брак строки»

- [x] T1: тесты API на примеры V1–V15 и кода CLI (V21) — красные до правки
      по правильной причине: 23 падения — `200` вместо `400` у файлов и таблиц
      без колонок и у одной шапки, `500` у нечитаемой книги и длинной двоичной
      строки, код 1 вместо 2 у CLI, «пустое тело запроса» у пустого файла.
- [x] T2: `UnfitSourceError` и `validate.check_fit`: пусто, пустая первая
      строка, нет колонок (с прочитанным и подсказкой похожего имени), дубль
      колонки — разом с нехваткой, только шапка.
- [x] T3: читатели — книга не читается, двоичный CSV, `csv.Error`; какой лист
      прочитан (`RawTable.where`) у книги и у ссылки.
- [x] T4: роутер — отказ `400` с подлежащим на обоих путях, лог
      `intake_refused`; пустое тело — «файл пуст»; CLI — код 2.
- [x] T5: прежние тесты B15 переписаны под новую границу; pytest целиком.

## Slice 2 — экран: подсказка у файла и отказ у поля

- [x] T6: тесты экрана V16, V18, V20 — красные до правки (плашка не над полем
      ссылки, нет «?» у файла, поле выбора не сброшено); V17 — страж, зелёный и
      до правки.
- [x] T7: `listFormat.json` + реляционный тест с сервером (V19).
- [x] T8: `ListHelp` — одна карточка, `SheetHelp` и `FileHelp`; `SourceForm` —
      подписи соседями, отказ под своим полем, `accept` из данных, `resetRef`.
- [x] T9: `IntakeOutcome` — убраны переводы кодов, которых сервер больше не
      шлёт, и плашка «не нашлось ни одной строки» (состояние недостижимо).
- [x] T9a: раскрытие карточки в jsdom — 22 с (L76): тест проверяет значок и
      данные, раскрытие — в браузере.

## Slice 3 — исполнение и сдача

- [x] T10: браузер: боевая сборка + API на копии дев-базы; снимки подсказок и
      отказов в обеих темах; 18 настоящих файлов и 6 ответов таблицы на ветке и
      на `origin/main`. Копия базы удалена, процессы остановлены.
- [x] T10a (converge): файл больше 2 МБ через прокси давал «сервер ответил
      500» — отказ на экране до отправки, предел из данных подсказки (V22);
      `?usp=sharing` рвался переносом — `nowrap`.
- [x] T11: гейты: pytest, vitest, tsc, eslint, prettier, mypy, ruff,
      `delivery_check`; канон (`knowledge/product/intake.md`) и реестр
      находок (Z42).
- [ ] T12: verify-report, метрики, архив, урок L212, PR, зелёный CI.
