# Tasks: web-cases

- [x] `CaseRow` и `PackView` в `api/types.ts`.
- [x] `download()` в `api/client.ts`: токен заголовком, имя из `Content-Disposition`.
- [x] `api/cases.ts`: библиотека, пачка, два скачивания, запуск сборки.
- [x] `pages/cases/save.ts`: сохранение и немедленный `revokeObjectURL`.
- [x] `pages/cases/CasesTable.tsx`, `PackCard.tsx`, `PackSection.tsx`, `CaseLibrary.tsx`.
- [x] `pages/CasesPage.tsx` и регистрация в `SCREENS` (`routes.tsx`).
- [x] Тесты E1–E8 в `web/src/pages/__tests__/cases.test.tsx`.
- [x] vitest, eslint и tsc зелёные.
- [x] **Живьём**: экран открыт в Chrome, PDF и ZIP скачаны настоящими кликами, пересборка дошла до воркера, обе темы сняты.
