# Tasks: графики проекта по HTTP

Уроки архива, применимые к диффу: L53, L41, L45/L46, L50 — см. plan.md.

## Рисунок
- [ ] компоновка «два графика» в одном месте, зовут оба
- [ ] ряды под кривые доступны API, а не только сборке кейса

## Эндпоинт
- [ ] `GET /api/projects/{id}/charts` под правом `read`
- [ ] полосы окон из записанного вердикта; нет вердикта — нет полос
- [ ] нет рядов — пустой список, а не пустые оси

## Источник
- [ ] ряды читаются источником из конфига, а не умолчанием обработчика
- [ ] то же для карточки

## Проверки
- [ ] E1–E8
