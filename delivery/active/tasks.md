# Tasks: ci-editable-root

- [x] Шаг установки в `.github/workflows/quality.yml` выясняет раскладку:
      `backend/pyproject.toml` — ставится `backend`, иначе корень (`-e .`).
- [x] Логика ветвления проверена на обеих раскладках локально (bash, без pip).
- [x] YAML разбирается, шаги job'ов на месте.
- [ ] **Проверка исполнением**: прогон CI после пуша без этой аннотации.
