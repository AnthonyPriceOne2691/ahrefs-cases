# Tasks: сервис в контейнерах (дев)

Уроки архива, применимые к диффу: L58/L8, L24, L7 — см. plan.md.

## Контекст и образы
- [x] `.dockerignore`: ни `.venv`, ни `node_modules`, ни `.git`
- [x] дев-стадия питона без `COPY . .`
- [x] стадия фронта на `node`, версия прибита цифрой

## Компоуз
- [x] `migrate` отрабатывает до api и worker
- [x] `web` с прокси на контейнер `api` через переменную
- [x] healthcheck у api: «поднялся» = «отвечает»
- [x] том под `node_modules` контейнера

## Проверка исполнением
- [x] E1–E8
- [x] README: запуск одной командой
