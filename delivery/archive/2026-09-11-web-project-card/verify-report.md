# Verify report

**Date:** 2026-09-11
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (by=human:anthony at=2026-09-11) — оба непривязанных
утверждения показаны словами и подписаны: половина сравнения не показывается,
рост от нулевой базы пишется словами, а не процентами
**CI run:** https://github.com/AnthonyPriceOne2691/ahrefs-cases/actions/runs/34614427212
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — pre-commit, 29 хуков, упавших 0
- [x] PASS — предохранители: net_loc 741 при пороге 800, файлов 16 из 25
- [x] PASS — `tsc --noEmit` чист, ESLint без роста предупреждений
- [x] PASS — DRY-гейт: два дубля вынесены (форматирование периода, заготовка тестов)

## Behavior oracles
- [x] PASS — vitest 43 теста (было 33); pytest 416 passed, 1 skipped
- [x] PASS — E1–E10 закрыты тестами

## Product oracles (живой прогон)
- [x] PASS — карточка открыта на настоящем вердикте: «хороший», счёт 10 770,
      пороги 0.0.0-default, рост трафика +160,2 % (22 261 → 57 916)
- [x] PASS — условия вердикта читаются словами, решающие отмечены
- [x] PASS — обе кривые на месте, с полосами окон А и Б и отметкой старта работ
- [x] PASS — до классификации карточка честно предлагает её запустить

## Ревью рисковых мест

**Числа карточки — из записанного вердикта.** Ни одного пересчёта на лету
(L41): экран обязан отвечать то же, что таблица и PDF. Спор сотрудника с
печатным листом не должен решаться тем, какой из двух расчётов свежее.

**Подписи и арифметика не продублированы.** Словарь подписей метрик живёт в
кейсе, формула роста — в классификации, и карточка получает готовые строки с
сервера. Ради этого `_delta` стала публичной `delta_of`. Цена копии названа
числом: рост от нулевой базы у нас `None`, и вторая формула показала бы «100 %»
там, где роста измерить нельзя. Закрыто отдельным тестом («с нуля»).

**Инвариант записан в канон.** `points-a-b.md` пополнился пунктом про нулевую
базу и один источник формулы — иначе следующий потребитель напишет её заново.

**Разметка SVG вставляется как есть, и это осознанная граница.** Рисунок собран
нашим бэкендом из наших чисел; чужая разметка сюда не попадает ни на каком
пути. Записано докстрингом в компоненте, чтобы не выглядело недосмотром.

**Версия порогов стоит рядом с группой.** Вердикты разных версий живут в базе
одновременно; число без версии ничего не значит, а по другой версии тот же
проект может оказаться в другой группе.

**Половина сравнения не показывается.** Метрика, купленная только к одной
точке, в таблицу не попадает: сравнить число с пустотой нельзя, а показать
пустую ячейку значит предложить именно это.

**Безопасность.** Карточка за тем же правом `read`; отдельного пункта меню у
неё нет, приходят из таблицы. Серверные проверки стоят и остаются.

**Деньги.** Экран только читает. Кривые рисуются по уже собранным точкам,
классификация бесплатна — текст на экране это и предлагает.

**Транзакция БД.** Фронт в базу не ходит; на сервере добавлено только чтение.

## Найдено по дороге, не чинилось этой поставкой

**Тесты стирают данные дев-базы.** Четыре модуля (`test_api_read`,
`test_api_runs`, `test_api_intake`, `test_api_thresholds`) в уборке за собой
выполняют `delete(MetricPoint)`, `delete(Verdict)`, `delete(Run)` и
`delete(UnitsLedger)` **без фильтра** — то есть удаляют всё, а не своё. При
показе экрана это стёрло собранные ряды и вердикты дважды: карточка честно
показала «не классифицирован» на проекте, который час назад был «хорошим».
На общей дев-базе агентства любой `pytest` будет так же стирать чужую работу.
Отдельная поставка — следующая.

## Что осталось за границей

Тумблер «месяц/квартал/год» — отдельной поставкой: рисунок строится на сервере,
значит переключателю нужен параметр эндпоинта и агрегация перед рисованием.
Диагностика «плохих» блоком на карточке не делалась — решение не принималось.
## Assertion digest (ревью ожиданий, не кода)

База: `HEAD~1` · сгенерировано `assert_digest.sh`

Новых/изменённых утверждений: **24**, из них без ссылки на пример спеки:
**2**. Вопрос к каждому непривязанному один: **откуда взято ожидаемое
значение — из спеки или придумано под реализацию?**

```
L75	assert traffic["label"] == "органический трафик"
L75	assert traffic["before"] == 1000.0
L75	assert traffic["after"] == 2400.0
L75	assert traffic["absolute"] == 1400.0
L75	assert traffic["pct"] == pytest.approx(140.0)
L75	assert rows["kw_top10"]["label"] == "ключи в топ-10"
-	assert "refdomains" not in subjects
E1	expect(await screen.findByText('klinika.example')).toBeInTheDocument();
E1	expect(screen.getByText('хороший')).toBeInTheDocument();
E1	expect(screen.getByText('счёт 1 400')).toBeInTheDocument();
E1	expect(screen.getByText('пороги 0.0.0-default')).toBeInTheDocument();
E2	expect(cells).toEqual(['органический трафик', '1 000', '2 400', '+140 %']);
-	expect(screen.getByText('с нуля')).toBeInTheDocument();
E3	expect(screen.getByText('решающее')).toBeInTheDocument();
E3	expect(screen.getByText('прошло')).toBeInTheDocument();
E3	expect(screen.getByText('не прошло')).toBeInTheDocument();
E4	expect(document.querySelector('[data-test="traffic"]')).not.toBeNull();
E4	expect(document.querySelector('[data-test="positions"]')).not.toBeNull();
E5	expect(await screen.findByText(/Запустите классификацию/)).toBeInTheDocument();
E6	expect(await screen.findByText(/Кривых нет/)).toBeInTheDocument();
E6	expect(screen.getByText('klinika.example')).toBeInTheDocument();
E7	expect(await screen.findByText('проекта 7 нет')).toBeInTheDocument();
E8	expect(await screen.findByText(/нет права read/)).toBeInTheDocument();
E9	expect(window.location.pathname).toBe('/projects');
```

Привязаны к примерам: **E1 E2 E3 E4 E5 E6 E7 E8 E9 L75**. Остальные 2 — нет.

Читать нужно **только строки с `-` в первой колонке**: их ожидание
ничем не подписано. Подпись: `asserts_reviewed_by: human:… at=…`.

asserts_without_example: 2

**Откуда взяты ожидания двух непривязанных утверждений.** Оба — следствия
правил, которые спека называет словами, но отдельными примерами не оформляет:

- `"refdomains" not in subjects` — метрика, купленная только к одной точке, в
  сравнение не попадает. Это правило спеки «сравнивать половину нельзя»,
  проверенное на данных засева (ссылающиеся домены есть в точке Б и нет в А);
- `getByText('с нуля')` — рост от нулевой базы у нас `None`, а не «100 %»
  (инвариант `points-a-b.md`, пункт 6). Проверяется то, что фронт не подменяет
  отсутствие числа числом.

Владелец прочитал оба и подписал (11.09.2026). Гейт прав в том, что ожидание,
придуманное исполнителем, подписывает не исполнитель.

## Spec coverage gaps
- **Тумблер агрегации** не делался (объявлен вне границ поставки).
- **Скачивание PDF с карточки** не делалось: это раздел «Кейсы».

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD~1 -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 16 code (+10 process docs) / +756/-15 (net +741) |
| commits | 1 |
| time_to_accepted_spec | 0.0h |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | yes — web/src/pages/__tests__/card.test.tsx (новый оракул) |
| implement_retries | 3 — карточка без маршрута в тесте, неразрывный пробел в сравнении чисел, два дубля по DRY-гейту |
| verify_fails_before_green | 0 |
| est_token_or_cost | n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
