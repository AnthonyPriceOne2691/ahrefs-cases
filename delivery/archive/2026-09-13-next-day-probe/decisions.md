# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-13 · выбрал снимок в файле (`data/raw/снимок-месяцев-<дата>.json`) вместо сравнения с рядами в базе — потому что в базе живых точек за текущий месяц нет вовсе: сбор ограничен периодом работ проекта, и у `ahrefs.com` он кончается 30.06.2025. Сравнивать было бы не с чем, а «нет данных» выглядело бы как «ничего не изменилось»
- 2026-09-13 · выбрал сверять счётчик Ahrefs **с журналом** (`units_ledger` живых прогонов) вместо числа из памяти поставки — потому что журнал и есть то, чем мы объясняем счёт: `_live_spend` в `scripts/probe_next_day.py` складывает `UnitsLedger.units_actual` по прогонам с `params_snapshot->>'provider' = 'live'` и даёт 2 262 units за 12.09 — число, которое иначе пришлось бы помнить наизусть и переписывать из поставки в поставку
- 2026-09-13 · выбрал три исхода сравнения вместо двух — потому что «ничего не изменилось» соблазнительно прочитать как «правило верно», а это разные вещи: сутки Ahrefs мог не пересчитывать домен. Третью ветку держит отдельный пример `test_nothing_changed_leaves_the_question_open`, и без неё `_compare` печатал бы «H2 ПОДТВЕРЖДЕНА» на двух одинаковых снимках — то есть потребовал бы поверить подтверждению, которого не было
- 2026-09-13 · выбрал заводить счётчик отдельной гипотезой H8 в реестре вместо строки в разборе поставки — потому что цена её ошибки — весь `collect/quota.py`: `LiveQuota.units_left` считает остаток как `limit - used`, и при неподвижном `used` он всегда равен лимиту — 1 469 188 units, то есть `preflight` пропустит любую смету. Такое обязано жить в реестре гипотез, а не в разборе одной поставки
- 2026-09-13 · выбрал вердикт по **сдвигу** счётчика между снимками вместо сравнения абсолютного значения с журналом — потому что живой запуск напечатал «✓ счётчик обновился» на числе 532 476, которое у ключа заказчика такое независимо от наших запросов: прежняя ветка `used >= spent` зелёная всегда, кроме буквального нуля
- 2026-09-13 · выбрал не записывать снимок в режиме `--quota-only` вместо записи без месяцев — потому что имя файла содержит дату, и бесплатная проверка затёрла бы полный снимок того же дня: завтрашнее сравнение по H2 осталось бы без вчерашних чисел, то есть дешёвая проверка сломала бы дорогую
- 2026-09-13 · выбрал шов `_refuse_reason()` вместо двух `if` внутри `_main` — потому что оракул на запись снимка иначе упирается в отказ фикстуры и проходит зелёным, ничего не проверив: первая редакция теста `test_quota_only_does_not_overwrite_the_day_snapshot` делала именно это
