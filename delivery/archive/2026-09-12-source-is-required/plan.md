# Plan

Класс M. Снять умолчание, назвать источник у вызывающих, поставить оракул.

## Уроки по затронутым путям (`archive/INDEX.md`)

- **L53** — источник рядов не берётся умолчанием обработчика; это третье
  повторение, и потому чинится сигнатура, а не вызов.
- **L23** — молчаливый ответ хуже отказа: «данных нет» при целой базе.
- **L48** — оракул стоит на свойстве (нет умолчания), а не на списке вызовов.

Не применимы: **L88**, **L95** — файлов и числовых порогов поставка не трогает.

## Шаги

1. `classify/verdicts.py`, `preview.py`, `recalc.py`, `diagnose.py`,
   `cases/builder.py` — убрать `= MetricSource.FIXTURE`.
2. `scripts/run_collect.py` — `config_source()` в `classify` и `explain`.
3. `api/routers/rulesets.py` — `configured_source()` в `preview` и `recalc`.
4. `tests/test_source_is_required.py` — оракул на сигнатуры.
5. Тесты: назвать источник явно.

## Rejected alternatives

- **Поправить два вызова и оставить умолчание** — отклонено, потому что это
  третий случай подряд: умолчание и есть дефект, а не его проявление.
- **Сделать умолчанием `LIVE`** — отклонено, потому что тогда ошибка сменит
  знак: тесты и разработка пошли бы в живой Ahrefs за деньги заказчика.
- **Читать конфиг внутри функций классификации** — отклонено, потому что ядро
  не знает про конфиг приложения по контракту слоёв (`delivery/CONSTITUTION.md`),
  и это сломало бы тестируемость без сети.
