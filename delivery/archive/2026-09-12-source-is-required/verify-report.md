# Verify report

**Date:** 2026-09-12
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (by=human:anthony at=2026-09-12) — проверено тем же
живым прогоном: до правки пять собранных доменов давали «данных не хватает»,
после — `medium` у одного и `poor` у четырёх, с числами по каждому условию
**CI run:** пуш после приёмки
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — ruff, ruff format, mypy чисты
- [x] PASS — предохранители: диф в пределах 800

## Behavior oracles
- [x] PASS — pytest 468 passed, 1 skipped
- [x] PASS — новый оракул держит девять сигнатур; E1–E4 закрыты

## Product oracles (живой ключ Ahrefs)
- [x] PASS — `classify` в живом режиме: good 0, medium 1, poor 4,
      insufficient_data 11 (последние — фикстурные проекты без живых рядов)
- [x] PASS — `explain ahrefs.com` показал разбор по условиям на настоящих
      числах: трафик +341 % (955 014 → 4 215 432 визитов США), ссылающиеся
      домены +47,6 % при пороге 50 — и именно это не пустило проект в «хорошие»
- [x] PASS — ступень кейса докупила данные (200 units по смете) и `render`
      собрал **настоящий PDF из живых данных**: 62 591 байт, обе кривые,
      таблица А → Б, сноска и атрибуция на месте

## Найдено этим прогоном сверх спеки

**Подпись последнего значения на кривой трафика обрезана** («3 596 46…»): она
упирается в границу `viewBox`. Тот же класс, что урок L84, где срезалась
легенда. Артефакт уходит клиенту, поэтому дефект не косметический — записан
в задачи следующей поставки.

**Живой `collect` идёт по всей базе** — фильтра по списку доменов нет.
На стенде это означало поход в Ahrefs за фикстурными доменами; часть из них
оказалась настоящими сайтами. Разделение по источнику выдержало.

## Ревью рисковых мест

**Безопасность.** Правка меняет сигнатуры и вызовы; прав и доступов не трогает.

**Деньги.** Риска нет: обязательный аргумент не добавляет обращений к Ahrefs.
Наоборот, снимает худший сценарий — «классификация молчит, и прогон запускают
второй раз, полагая, что данных не хватило».

**Транзакция БД.** Записи те же: вердикты пишутся как прежде, изменился только
источник рядов, по которым они считаются.

**Интеграция.** Источник теперь называют три вызывающих: CLI (`config_source`),
API (`configured_source` — спрашивает фабрику провайдера) и тесты (явно
`FIXTURE`). Ядро по-прежнему не знает про конфиг приложения — контракт слоёв
соблюдён.

## Spec coverage gaps
- Тот же класс умолчаний мог остаться в экспорте и графиках; оракул растёт
  вместе со списком функций, читающих ряды.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD~1 -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 15 code (+10 process docs) / +144/-91 (net +53) |
| commits | 1 |
| time_to_accepted_spec | 0.0h |
| rework_after_done | 0 commit(s) after first phase: handoff |
| harness_hardened | yes — tests/test_source_is_required.py (новый оракул) |
| implement_retries | 3 — 28 красных тестов без источника, забытый импорт `MetricSource`, не-ASCII имена в оракуле |
| verify_fails_before_green | 1 — дефект найден живым прогоном, тестами невидим по построению |
| est_token_or_cost | ~400 units по заголовкам (ступень кейса и пересборка); списано 0 |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
