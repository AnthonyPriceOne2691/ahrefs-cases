# Verify report: expired-session-sends-you-to-the-door

## Чем проверено

| Что | Чем | Результат |
|---|---|---|
| Поведение при протухшей сессии | `vitest run src/auth/__tests__/session-expiry.test.tsx` | 5 passed |
| Проверки падали до правки | те же тесты на исходниках из HEAD | 4 failed из 5 |
| Пятая проверка — сторож | «на входе 401 означает неверный пароль» | проходила и до правки, как и должна: это защита от перегиба, а не воспроизведение |
| Фронт целиком | `vitest run` | 140 passed |
| Бэкенд целиком | `pytest -q` | 580 passed, 1 skipped |
| Типы | `tsc --noEmit` в `web/` | чисто |

## Чего проверка НЕ доказывает

Проверки гоняют **контракт**, а не ночь: токен не протухал по-настоящему, ему
отказывал подменённый сервер. Настоящее протухание проверяется одним способом —
оставить вкладку открытой и вернуться утром; это и записано в `observe_signal`
со сроком до 29.09.

## Найдено попутно, не чинилось

`vitest` печатает «140 passed» и рядом «Errors 3 errors» (`TypeError: URL is not
a constructor` из `react-router`, вызванного `app/screenState.ts:57`), а код
возврата при этом нулевой. Ошибки существуют и без тестов этой поставки —
проверено прогоном с исключением файла. Записано как **Z24**: сам vitest
предупреждает, что это может давать ложно проходящие тесты.
