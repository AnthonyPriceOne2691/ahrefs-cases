# Tasks: вход в сервис

Уроки архива, применимые к диффу: L23, L1, L33, L21, L8, L50 — см. plan.md.

## Пароли и токены
- [x] bcrypt: хеш и проверка
- [x] JWT: выпуск с TTL и разбор с причиной в логе
- [x] пустой секрет — токен не выпускается

## Зависимости
- [x] `current_user` из заголовка `Authorization`
- [x] группа берётся из пользователя, а не из заглушки
- [x] `require_right` не переписан — подставлен источник

## Роутеры
- [x] `POST /api/auth/login`
- [x] `GET /api/auth/me` с группой и правами

## Чистка
- [x] свойства `can_*` у модели убраны: правило прав одно

## Команда
- [x] `useradd <почта> <группа>` с запросом пароля

## Проверки
- [x] E1–E10
- [x] пароль не появляется ни в логе, ни в ответе
