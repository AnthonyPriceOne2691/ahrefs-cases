# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.
- 2026-09-11 · выбрал одинаковый ответ и одинаковое время ответа на «нет такой почты» и «неверный пароль» вместо точной причины — потому что разница превращает форму входа в перечислитель учётных записей; в `routers/auth.login` для этого проверяется `_DUMMY_HASH` даже у ненайденного пользователя, иначе ответ возвращался бы на ~0,25 с быстрее (столько стоит bcrypt) и рассказывал то же самое
- 2026-09-11 · выбрал класть группу в токен вместо запроса в базу на каждый вызов — потому что проверка прав стоит на каждом роутере, а смена группы редка: цена решения — до 12 часов задержки (TTL токена), и она названа в спеке
- 2026-09-11 · выбрал удалить свойства `can_*` у модели пользователя вместо того, чтобы оставить их рядом с таблицей прав — потому что ими не пользовался никто: это копия правила, которая не расходится ровно до первой ссылки на неё (урок L33)
- 2026-09-11 · выбрал перечитывать пользователя из базы на каждом защищённом вызове вместо доверия группе из токена — потому что при TTL 12 часов выключенный сотрудник продолжал бы работать до истечения токена; цена — один `SELECT` на вызов, и она названа
- 2026-09-11 · выбрал `str` вместо `EmailStr` в форме входа — потому что почта здесь ключ поиска, а `EmailStr` потребовал бы зависимости `email-validator` ради проверки формата поля, по которому мы делаем `SELECT`
