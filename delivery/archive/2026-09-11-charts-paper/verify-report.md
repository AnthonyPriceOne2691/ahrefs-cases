# Verify report

**Date:** 2026-09-11
**Verifier:** human:anthony
**asserts_reviewed_by:** n/a (дайджест ниже, непривязанных 0)
**CI run:** https://github.com/AnthonyPriceOne2691/ahrefs-cases/actions/runs/34624417189
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — pre-commit, 29 хуков, упавших 0
- [x] PASS — класс S: один модуль рисования, его тесты и упаковка образа

## Behavior oracles
- [x] PASS — pytest 432 passed, 1 skipped (+2); vitest 44
- [x] PASS — E1–E3 закрыты тестами

## Artifact oracle (лист и экран — глазами)
- [x] PASS — E4: PDF пересобран **в контейнере**, лист сверен: рисунок встал
      плиткой в один ряд с метриками, ничего не разъехалось
- [x] PASS — E5: тёмная тема веб-карточки — подписи значений и осей читаются
- [x] PASS — E6: светлая тема — вид прежний

## Ревью рисковых мест

**Подложка внутри рисунка, а не плашкой на фронте.** Цвет полотна объявлен в
рисовальщике (в него затухает градиент), и вторая копия этого цвета в CSS
разошлась бы с первой при первой правке. Заодно правильным стал скачанный файл
и любой будущий потребитель SVG.

**Один рисунок остаётся одним.** Тёмный вариант по параметру эндпоинта
отклонён: это два рисунка вместо одного, а они расходятся молча (L75).

**Легенда срезалась всегда.** Её базовая линия стояла ровно на границе
`viewBox`: выносные элементы букв уходили за холст. Без рамки это было не
видно, с бумагой стало заметно — и починено. Правка касается и PDF, и там она
тоже к лучшему.

**Печатный лист проверен пересборкой, а не рассуждением.** Кейс собран в
контейнере, HTML того же кейса снят и просмотрен: вид листа не изменился сверх
того, что рисунок обзавёлся рамкой, как у плиток.

**Найдено и починено по дороге, и это важнее самой поставки.** В образе не было
каталога `templates/`: `render` внутри контейнера падал `TemplateNotFound`. То
есть боевой образ не умел собирать кейсы — то, ради чего сервис существует.
Шаблон теперь копируется в обе питоновские стадии и монтируется в дев-компоуз.
Тот же класс, что L69: в контейнере эту дорогу никто ни разу не проходил, а
локально она работала, потому что `templates/` лежит рядом.

**Деньги и безопасность.** Рисование ничего не покупает; прав и путей поставка
не трогает.

**Транзакция БД.** Записей нет.

## Что осталось за границей

Тёмный вариант рисунка не делался (см. выше). Форма подписи оси при свёртке
осталась месяцем начала периода.
## Assertion digest (ревью ожиданий, не кода)

База: `HEAD~1` · сгенерировано `assert_digest.sh`

Новых/изменённых утверждений: **5**, из них без ссылки на пример спеки:
**0**. Вопрос к каждому непривязанному один: **откуда взято ожидаемое
значение — из спеки или придумано под реализацию?**

```
L82	assert body.startswith("<rect"), "подложка обязана идти под всем остальным"
L82	assert f'fill="{SURFACE}"' in body
L82	assert 'width="419"' in body
L82	assert 'height="209"' in body
E3	assert curves_svg([], period_start=date(2025, 1, 1)) == ""
```

✅ **Каждое утверждение ведёт к примеру спеки** (E3 L82), а примеры человек
подписал до кода (`human_ok_spec`). Подпись под дайджестом здесь
**не требуется**: она уже стоит, заранее и на числах. Пиши в verify-report
`asserts_reviewed_by: n/a (все утверждения ведут к одобренным примерам)`.

asserts_without_example: 0

## Spec coverage gaps
- Ничего из объявленного не осталось несделанным. Сверх объявленного починены
  легенда (срезалась границей холста) и отсутствие шаблонов в образе.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD~1 -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 4 code (+9 process docs) / +77/-3 (net +74) |
| commits | 1 |
| time_to_accepted_spec | 0.0h |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | no |
| implement_retries | 1 — `TemplateNotFound` в контейнере: проверку PDF пришлось разблокировать починкой образа |
| verify_fails_before_green | 0 |
| est_token_or_cost | n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
