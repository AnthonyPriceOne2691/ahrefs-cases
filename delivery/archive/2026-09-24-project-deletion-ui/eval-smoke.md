# Eval smoke — this shipment

Путь человека кнопкой: боевая сборка ветки (`vite build`, `vite preview` с прокси
на API ветки), копия дев-базы `cases_delui_check` (75 проектов, 29 прогонов),
копия каталога выгрузки (468 файлов), headless Chrome 154 по CDP, светлая и
тёмная темы — переключателем приложения; 24.09.2026. Копии удалены.

- [x] K1 — сотрудник (`clerk@delete.local`, группа user): у `lonelyplanet.com`
      кнопки нет, карточка кончается сноской
- [x] K2 — инженер: подтверждение «Уйдут: точек рядов: 104 … вердиктов: 1;
      кейсов: 10; файлов PDF: 3. Останутся: строк журнала прогонов: 4 …»; у
      второй кампании `nordvpn.com` — «другие кампании этого сайта: 1»; у обоих
      строка про пачку
- [x] K3 — «Отмена» закрыла подтверждение, проект на месте
- [x] K4 — «Да, удалить»: итог «Проект lonelyplanet.com удалён» с числами
      ответа, ссылка ведёт на `/projects`; в копии базы строк проекта 0,
      `units_ledger` 590 строк и 27 366 units — как до удаления
- [x] K5 — прогон 1089 переведён в `running`: у `machinemfg.com` отказ
      «прогон 1089 ещё идёт (running): он пишет строки проектов — удалите
      проект, когда прогон закончится», проект на месте; статус возвращён
- [x] K6 — экран «Пользователи»: «удалять проекты» у инженеров и админов,
      ключа `delete_projects` на экране нет
- [x] K7 — прогон 1088: «lonelyplanet.com (проект удалён)» в строке «пропущен:
      нет данных»; «без замечаний собрано: 57, из них проектов удалено: 1»
- [x] K8 — «Кейсы»: у пачки предупреждение словами сервера, «Скачать ZIP»
      недоступна, «Пересобрать кейсы» доступна
