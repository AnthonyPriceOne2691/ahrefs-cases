# Tasks: Ф3а — вердикт классификации

Снизу вверх: чистые функции, потом слой базы, потом команда. Уроки архива,
применимые к диффу: L9, L8, L12 — см. plan.md.

## Пороги как типы
- [x] `classify/thresholds.py`: модель порогов (окна, eligibility, группы, score)
- [x] разбор `payload` из `Ruleset` с внятной ошибкой на кривой структуре
- [x] сид из `config/thresholds.example.yml`, идемпотентный по версии
- [x] тест: пороги читаются из БД, правка файла на живом сервисе ничего не меняет
- [x] тест: конструирование по имени поля (урок L4), дефолты не подменяют значения

## Точки и дельты
- [x] `classify/series.py`: `MetricSeries` из `MetricPoint` (чтение, без решений)
- [x] `classify/points.py`: среднее по окну вокруг границы периода
- [x] тест: выброс на границе не меняет группу (D1)
- [x] `classify/deltas.py`: абсолютная и относительная дельта по метрикам
- [x] тест: дельта от нулевой базы не даёт бесконечности и не роняет расчёт

## Правила
- [x] `classify/rules.py`: eligibility (месяцы, дыра) → `insufficient_data` (D6, D9)
- [x] правила групп таблицей условий; каждое условие пишет запись `reasons`
- [x] «главная И минимум одна подтверждающая» (D7, D8)
- [x] порог месяцев понижает группу, а не отсекает проект (D5)
- [x] `score` и сортировка по абсолютному приросту (D11)
- [x] тест: `reasons` содержит значение, порог и вердикт по каждой метрике (D10)

## Golden-таблица
- [x] `tests/test_classify_golden.py`: семь сценариев Ф2а → группа (D13)
- [x] таблица считается без базы и без сети
- [x] канарейка: подмена порога меняет группу — таблица действительно судит

## Слой базы и команда
- [x] `classify/verdicts.py`: запись `Verdict` с `ruleset_id`, `reasons`, точками
- [x] тест: вердикт хранит версию порогов; другая версия — другой вердикт (D12)
- [x] `scripts/classify_projects.py` или подкоманда: распределение по группам
- [x] тест: классификация не делает ни одного HTTP-запроса
- [x] `eval-smoke.md`: прогон на данных Ф2 и объяснение по одному проекту

## Закрытие
- [x] покрытие ядра ≥ 80 % (не ниже 94 %, достигнутых в Ф2)
- [x] `decisions.md` по ходу, не перед handoff
- [x] `assert_digest` → 0 непривязанных; новые ожидания дописываются в спеку
