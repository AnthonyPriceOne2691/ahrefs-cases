# Decisions

- 2026-09-11 · выбрал убрать `org_cost` из шага 1 вместо оставить «пусть будет» — потому что второе биллингуемое поле удваивает цену строки (21 против 11), а читателя у него нет ни в правилах Ф3, ни в блоках кейса по ТЗ
- 2026-09-11 · выбрал брать месяцы до старта работ по `pre_start_baseline_months` вместо безусловных трёх — потому что точка А считается окном вперёд от `period_start`, потребителя у запаса сейчас нет, и он стоит 33 units на домен
- 2026-09-11 · выбрал держать «сколько строк влезает под минимум» в модели стоимости вместо константы 4 в планировщике — потому что это следствие замеренной цены строки, а не свойство мира: при двух полях влезает две, и константа разошлась бы с моделью молча
