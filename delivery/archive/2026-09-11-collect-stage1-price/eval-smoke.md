# Eval smoke — this shipment

Derived from spec acceptance. Run during verify.

- [x] E9 — `select` уходит как `date,org_traffic`, цена строки 11
- [x] E8 — смета прогона на сотню доменов 19 800 вместо 44 100
- [x] E8a — при цене строки 21 под минимум влезает две строки
- [x] E9a — `AHREFS_COLLECT_SCHEME` читается из окружения, неверное значение падает с именем переменной
- [x] Golden-таблица классификации зелёная без правок
