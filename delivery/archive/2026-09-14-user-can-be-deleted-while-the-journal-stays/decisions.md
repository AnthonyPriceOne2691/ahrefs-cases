- 2026-09-14 · выбрал два пути удаления при одном видимом поведении (`delete_user`: пометка `deleted_at` при наличии прогонов, `session.delete` при их отсутствии) вместо одного — потому что `runs.started_by` стоит с `ON DELETE RESTRICT`, и journal обязан пережить увольнение; но учётка с опечаткой в почте прогонов не имеет (0 строк в `runs`), и оставлять её значит держать почту занятой навсегда — а это самый частый повод удалять
- 2026-09-14 · выбрал прятать удалённых из `list_users` и из `_get_or_404` вместо фильтра только в списке — потому что иначе правка добиралась бы до того, кого в списке нет: включить вход, сменить группу, перевыпустить пароль; строка живёт ради журнала, а не ради управления
- 2026-09-14 · выбрал показывать автора прогона именем и пометкой (`started_by_name`, `started_by_deleted` в `RunRow`) вместо одного `started_by: int` — потому что журнал существует ради вопроса «кто это запускал», а отвечал на него номером строки: в `RunsTable` не было такой колонки вовсе, 0 из 6 столбцов
- 2026-09-14 · выбрал забирать авторов одним запросом на страницу (`_authors`) вместо запроса на строку — потому что 20 прогонов давали бы 20 походов в базу за тем же десятком людей
- 2026-09-14 · выбрал инлайн-подтверждение удаления вместо модального окна — потому что попапы Mantine в jsdom рендерятся секундами и тест файла уходил в таймаут (урок L76), а подтверждение обязано быть проверено: удаление необратимо
- 2026-09-14 · выбрал право `manage_users` для удаления вместо проверки группы — потому что таблица прав в проекте одна (`api/deps._GROUP_RIGHTS`), и вторая проверка «только админ и инженер» разошлась бы с ней при первом же личном праве; по таблице доступа ТЗ `manage_users` — это ровно 2 группы из 3
