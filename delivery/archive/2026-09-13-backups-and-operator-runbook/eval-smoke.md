# Eval smoke — this shipment

- [x] E1 — новый том в компоузе краснит оракул (проверено добавлением `fakedata`)
- [x] E2 — без запущенной базы бэкап отказывается и возвращает 1
- [x] E3 — восстановление без `--yes` только рассказывает, что сделает
- [x] E4 — ротация оставляет `KEEP` свежих копий
