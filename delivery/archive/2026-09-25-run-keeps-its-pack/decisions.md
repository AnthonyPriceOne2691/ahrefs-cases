# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-25 · выбрал копию архива в `runs/<номер>/` и путь в снимке прогона вместо колонки `run_id` у кейсов и артефактов — потому что колонка требует миграции и всё равно не отдала бы «то, что собрал прогон»: PDF кейса переживают пересборку, а пачка дня переписывается, и собрать архив заново по строкам значило бы получить другой файл, чем ушёл в прогоне
- 2026-09-25 · выбрал отпечаток «путь и время» свежего архива до и после сборки вместо сверки времени файла с `started_at` прогона — потому что время прогона пишет база, а время файла — том воркера, и сравнение двух часов ломается от их расхождения; отпечаток сравнивает файл с самим собой
- 2026-09-25 · выбрал «копия не отложилась — сборка всё равно готова» вместо падения прогона — потому что пачка дня на «Кейсах» уже собрана и цела, а упавший прогон при собранной пачке соврал бы в журнале; причина пишется в лог с номером прогона (`run_pack_not_kept`)
- 2026-09-25 · выбрал `build_cases` решением сервера вместо вычисления на экране — потому что «последний „данные под кейс“ без сборки после» считается по всем прогонам, а экран видит одну страницу журнала и на второй странице ошибся бы
