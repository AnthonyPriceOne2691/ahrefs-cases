# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-24 · выбрал отбирать судьбы на экране (`RunFates.tsx::Fates`, `COLLECTED`) вместо фильтра в `GET /api/runs/{id}` — потому что судьбы читают и другие потребители (CLI, пометка «(проект удалён)» из параллельной поставки удаления), а владелец просил про раскрытие, не про ответ API; на №1088 это 18 строк из 75
- 2026-09-24 · выбрал показывать число собранных строкой «без замечаний собрано: N» вместо того, чтобы молча убрать их — потому что без числа раскрытие «18 строк» при шапке «57 из 75» читается как потеря 57 проектов; строка одна и число проверяемо по шапке (57 = 57)
- 2026-09-24 · выбрал ключ строки `домен#номер` (`TroubleTable`) вместо домена — потому что у двух кампаний одного сайта домен один (на №1088 `zeta-clinic.example` дважды), и React с одинаковым ключом вправе потерять строку при обновлении; тест J2 ловит предупреждение «same key»
