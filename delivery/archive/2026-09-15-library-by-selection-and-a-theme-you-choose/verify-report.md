# Verify report: library-by-selection-and-a-theme-you-choose

## Чем проверено

| Что | Чем | Результат |
|---|---|---|
| Отметки и выдача выборки | `vitest run src/pages/__tests__/cases.test.tsx` | 16 passed |
| Проверки падали до правки | те же тесты на исходниках HEAD | 3 failed из 4 новых |
| Четвёртая — сторож | «пока ничего не выбрано, панели выбора нет» | проходила и до: это защита от шума, а не воспроизведение |
| Значок темы | `vitest run src/app/__tests__/theme-toggle.test.tsx` | 3 passed; до правки `Shell.tsx` — 3 failed |
| Фронт целиком | `vitest run` | 140 passed |
| Бэкенд целиком | `pytest -q` | 580 passed, 1 skipped |
| Типы и стиль | `tsc --noEmit`, `eslint src`, `prettier --check` | чисто; 0 предупреждений при планке 0 |
| Светлая тема глазами | девять экранов на поднятом стенде, снимки по видимой области | найдено четыре места, три починены |

## Светлая тема: что нашли глазами и что оставили

Починено: половинчатое переключение (главное — см. decisions); контраст светлых
вариантов на «Расходе units», в журнале прогонов, у пилюль статусов и подписей —
с 2.2–3.5 : 1 до 5.8–8.3 : 1; `--ink-faint` с 3.8 : 1 до 4.8 : 1 (этими чернилами
написана группа «данных не хватает» — четвёртая группа проекта, а не украшение);
заведён `--hairline`, отдельный от `--glass-edge`: кромка стекла светлая, она
рисует блик, а разделитель обязан быть темнее фона.

Оставлено с причиной: выключенные кнопки (одинаково в обеих темах — поведение
Mantine `disabled`); общий сиреневый фон пилюль групп (цвет живёт в чернилах,
так задумано в `GroupBadge.tsx`); светлая бумага SVG-графиков (`#fcfcfb` из
`export/charts.py`) — на светлой теме это попадание, рисунок читается как лист
PDF.

## Чего проверка НЕ доказывает

Тесты держат договор: кнопка есть, схема меняется, выбор переживает
пересборку дерева. Что светлая тема **читается** — вывод глаз и замеров
контраста, а не оракула; оракула на «красиво» не существует, и это записано
здесь честно, а не закрыто зелёной галочкой.

asserts_reviewed_by: human:anthony at=2026-09-15
