# Tasks: приём по HTTP и смета

Уроки архива, применимые к диффу: L53, L23, L10, L50, L32/L34, L57, L51 — см. plan.md.

## Приём
- [x] байты → таблица: XLSX из потока, CSV через уже существующий разбор текста
- [x] `POST /api/intake/file`: имя параметром, предел размера, отказ по формату
- [x] `POST /api/intake/link`: Google Sheet, причина закрытой таблицы доносится
- [x] ответ: принято / создано / обновлено / построчные отказы с причиной

## Смета
- [x] `GET /api/runs/estimate`: units, запросы, кэш, разбивка по схеме
- [x] вердикт preflight тремя состояниями, остаток и резерв — числами
- [x] маршрут объявлен до `/{run_id}`

## Окна точек
- [x] один источник окон для CLI, задачи и сметы
- [x] тест: прогон из очереди и смета берут одинаковые окна

## Проверки
- [x] E1–E12
- [x] карта репозитория обновлена вместе с кодом
