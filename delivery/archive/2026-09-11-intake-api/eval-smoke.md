# Eval smoke — this shipment

Derived from spec acceptance. Run during verify.

- [x] E1 — XLSX на десять строк: принято 10
- [x] E2 — битая строка отклонена с причиной, годные приняты
- [x] E3 — повторная загрузка обновляет, не удваивает
- [x] E4 — неизвестный формат: `400`, не `500`
- [x] E5 — превышен предел: `413` с названным пределом
- [x] E6 — закрытая Google Sheet: `400` про страницу входа
- [x] E7 — без права `run`: `403` с названием права
- [x] E8 — смета совпадает с расчётом прогона
- [x] E9 — не хватает квоты: `may_start=false`, числа в причине
- [x] E10 — остаток неизвестен: вердикт `unknown`
- [x] E11 — `/api/runs/estimate` не перехвачен `/{run_id}`
- [x] E12 — окна точек у сметы и у задачи одни
