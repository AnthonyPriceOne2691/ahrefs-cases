# Tasks: очередь и запуск прогона

Уроки архива, применимые к диффу: L53, L11, L24, L2, L13, L54 — см. plan.md.

## Очередь
- [x] `JobQueue` с двумя реализациями
- [x] выбор по конфигу, одно место
- [x] аргументы задач — простые значения

## Задачи
- [x] прогон сбора со своей сессией
- [x] сборка пачки кейсов
- [x] упавшая задача оставляет прогон `failed` с причиной

## Роутер
- [x] `POST /api/runs` за правом `run`
- [x] `GET /api/runs` и `GET /api/runs/{id}`
- [x] замок: второй прогон — 409 с номером активного

## Проверки
- [x] E1–E10
