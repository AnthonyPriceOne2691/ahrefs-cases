# Verify report: sqlalchemy-below-2-1

## Чем проверено

| Что | Чем | Результат |
|---|---|---|
| H1: CI зелёный, установлен 2.0.x | GitHub Actions, PR #28: https://github.com/AnthonyPriceOne2691/ahrefs-cases/actions/runs/36055805685 | delivery, gates, tests — pass; в логе установки `sqlalchemy-2.0.54` |
| Падение до правки | run 36054208631 (PR #26, код тот же, установился 2.1.0) | 47 failed на `SADeprecationWarning: Passing expression to distinct … DISTINCT ON … deprecated` |
| Прод не задет | `sqlalchemy.__version__` в контейнере api | 2.0.54 |
| Канон | `scripts/okf_validate.py` | 0 ошибок; карта репозитория говорит о границе |

## Ревью рисковых мест

**Граница `<2.1` — временная мера, а не решение.** Остальные зависимости по-прежнему только с
нижней границей (`fastapi`, `pydantic`, `alembic`, `weasyprint` и прочие в `pyproject.toml`), и
следующий выпуск любой из них может так же уронить CI или прод. Записано как Z48; лечение —
зафиксированные версии для CI и образа.

**Снимать границу нельзя без правки `collect/cache.py`**: `.distinct(expr)` там — DISTINCT ON;
переход на `postgresql.distinct_on` требует 2.1 и в локальном окружении, и в образе прода.

## Чего проверка НЕ доказывает

Что 2.0.x и дальше будет выходить без ломающих изменений: граница держит мажор-минор, патчи
2.0.* ставятся как раньше.

## Verdict
- [ ] READY FOR HANDOFF — оракулы зелёные; ждёт подписи human:anthony (verifier)
- [ ] NEED CONVERGE (new tasks)
- [ ] BLOCKED

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base origin/main -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 2 code (+4 process docs) / +5/-1 (net +4) |
| commits | 1 |
| time_to_accepted_spec | n/a (no spec.md in history — class S?) |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | no |
| implement_retries | 1 — первый коммит отбил гейт канона: `pyproject.toml` в `implementation:` карты репозитория, концепт дописан |
| verify_fails_before_green | 0 |
| est_token_or_cost | n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
