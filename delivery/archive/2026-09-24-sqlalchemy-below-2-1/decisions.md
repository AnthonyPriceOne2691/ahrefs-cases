# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-24 · выбрал границу `sqlalchemy[asyncio]>=2.0,<2.1` в `pyproject.toml` вместо перехода на `postgresql.distinct_on` в `collect/cache.py` — потому что переход требует 2.1 везде (локально стоит 2.0.52, на проде 2.0.54), а CI был красным у всех PR: 47 упавших тестов на одном устаревшем вызове
