# Eval smoke — this shipment

Derived from spec acceptance. Run during verify.

- [x] E1 — боевые образы собираются
- [x] E2 — шесть сервисов поднимаются, migrate до api
- [x] E3 — `/` отдаёт приложение через nginx
- [x] E4 — `/api/health` проходит через прокси
- [x] E5 — вход через nginx возвращает токен
- [x] E6 — маршрут SPA напрямую не даёт 404
- [x] E7 — без `JWT_SECRET` сервис не поднимается молча
- [x] E8 — в образе нет dev-зависимостей и исходников фронта
- [x] E9 — убитый контейнер поднимается сам
