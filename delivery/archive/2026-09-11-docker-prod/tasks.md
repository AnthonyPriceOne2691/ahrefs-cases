# Tasks: боевой образ и компоуз

Уроки архива, применимые к диффу: L69, L70, L7, L23/L64 — см. plan.md.

## Образ
- [x] стадия сборки фронта и стадия nginx со статикой
- [x] в боевом образе нет dev-зависимостей и исходников фронта

## nginx
- [x] статика отдаётся, `/api` уходит в бэкенд
- [x] маршрут SPA, открытый напрямую, отдаёт `index.html`

## Компоуз
- [x] без монтирования исходников и без `--reload`
- [x] секреты только из окружения, в файле — имена
- [x] `restart: unless-stopped`, migrate до api

## Проверка исполнением
- [x] E1–E9
- [x] README: боевой запуск
