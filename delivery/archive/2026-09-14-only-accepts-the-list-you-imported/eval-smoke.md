# Eval smoke — this shipment

- [x] E1 — CSV приёма в `--only` даёт список доменов из колонки `domain`
- [x] E2 — файл со строками-доменами работает как раньше
- [x] E3 — перечисление через запятую работает как раньше
- [x] E4 — файл без колонки `domain` и без доменов отказывает с внятной причиной
