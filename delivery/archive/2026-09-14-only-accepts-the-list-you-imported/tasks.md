# Tasks: only-accepts-the-list-you-imported

Уроки по затронутым путям (`archive/INDEX.md`): **L124** (домен снаружи —
через тот же нормализатор, что приём), **L111** (у платящей команды есть
область действия), **L34** (сообщение обязано называть следующий шаг).

- [x] Файл с колонкой `domain` читается как список доменов.
- [x] Файл со строками-доменами читается как раньше.
- [x] Перечисление через запятую работает как раньше.
- [x] Строка заголовков не превращается в «не домен».
- [x] pytest, ruff, mypy, гейты зелёные.
