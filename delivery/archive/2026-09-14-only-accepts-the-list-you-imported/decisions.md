# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-14 · выбрал читать колонку `domain` из файла приёма в `--only` (`_names_in`) вместо того, чтобы оставить только построчный список — потому что файл приёма и есть боевой ввод: первая же команда после `intake` на наборе из 28 доменов отказала строкой «`domain,period_start,…`: не домен», то есть показала человеку его собственный заголовок и ничего больше
- 2026-09-14 · выбрал искать разделитель через `csv.Sniffer` в `_names_in` вместо того, чтобы предполагать запятую — потому что агентство выгружает списки из Excel, где разделитель `;`, и предположение отказывало бы ровно на том файле, который принесут
- 2026-09-14 · выбрал признак «в первой строке есть `domain` и один из `,;\t`» (`_names_in`) вместо расширения файла — потому что `.csv` бывает у списка без заголовков, а `.txt` — у выгрузки с ними: расширение говорит о названии файла, а не о его содержимом
