# Verify report

**Date:** 2026-09-11
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (by=human:anthony at=2026-09-11) — утверждение
одно и проверено исполнением: в отчёте прогона 34635574694 раздела ANNOTATIONS
нет вовсе, до правок их было два
**CI run:** https://github.com/AnthonyPriceOne2691/ahrefs-cases/actions/runs/34635574694
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — pre-commit, 29 хуков, упавших 0
- [x] PASS — YAML разбирается, шаги всех трёх job'ов на месте
- [x] PASS — предохранители: класс S, 2 файла, правка в пределах S

## Behavior oracles
- [x] PASS — ветвление проверено на обеих раскладках локально (bash, без pip):
      «пакет в корне» → `-e .`, «есть `backend/pyproject.toml`» → `-e backend`
- [x] PASS — прогон CI зелёный: gates 2m5s, delivery 7s, tests 2m51s

## Product oracles (исполнение)
- [x] PASS — прогон 34635225808 (первая правка): предупреждения про
      editable-установку нет, в логе видно `Preparing editable metadata
      (pyproject.toml)` — проект ставится
- [x] PASS — прогон 34635574694 (вторая правка): **ANNOTATIONS в отчёте
      отсутствует целиком**

## Найдено этим же прогоном

**Второе предупреждение того же класса.** Убрав первое, увидел в отчёте
«Node.js 20 is deprecated»: в job `tests` стояли `checkout@v4` и
`setup-python@v5`, тогда как в соседних job'ах того же файла — v7. Тот же
механизм: предупреждение читают, погасить не могут, перестают читать. Поднято
до v7 в этой же поставке.

## Ревью рисковых мест

**Безопасность.** Риска нет: правка касается версий действий и условия установки;
секретов, прав и доступов не трогает.

**Деньги.** Риска нет: Ahrefs не участвует, прогон CI бесплатен для проекта.

**Транзакция БД.** Не применимо: изменений в коде и схеме нет.

**Интеграция (сам CI).** Единственный риск — сломать шаг установки и получить
джобу, которая «зелена пропусками». Поэтому ветка «корень» ставит проект, а не
пропускает установку; а шаг «Инструменты гейтов на месте» ниже переводит
пропуск в красное — он и проверил бы ошибку, но прогон зелёный и без него.

## Spec coverage gaps
- Нет: поставка закрывает оба предупреждения целиком.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD~2 -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 2 code (+6 process docs) / +21/-6 (net +15) |
| commits | 2 |
| time_to_accepted_spec | n/a (no spec.md in history — class S?) |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | yes — .github/workflows/quality.yml |
| implement_retries | 1 — первый коммит не прошёл гейт секретов: detect-secrets обновил baseline |
| verify_fails_before_green | 1 — первый прогон погасил одно предупреждение и открыл второе |
| est_token_or_cost | n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
