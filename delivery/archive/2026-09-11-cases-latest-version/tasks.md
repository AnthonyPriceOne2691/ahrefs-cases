# Tasks: cases-latest-version

- [x] `list_cases` отдаёт по одному свежему кейсу на проект; `all_versions=true`
      возвращает историю целиком.
- [x] Свежесть — по наибольшему `Case.id`, а не по времени: две сборки одной
      секунды дали бы два «свежих» кейса одного проекта.
- [x] Оракул `test_library_shows_one_case_per_project`: заводит вторую версию и
      требует одну строку по умолчанию и две с историей.
- [x] Тумблер «показать все версии» на экране — запросом к серверу, а не
      фильтрацией полученной страницы.
- [x] Тест E9: тумблер уходит в запрос.
- [x] pytest, vitest, eslint и tsc зелёные.
- [x] Живьём: 6 строк по умолчанию, 50 с историей, тумблер уходит запросом.
