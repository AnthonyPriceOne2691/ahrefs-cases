# Decisions: journal-small-fixes

- 2026-09-24 · выбрал переводить домен судьбы на выходе API (`api/routers/runs.run_status`, `to_unicode`) вместо правки `collect/run_journal.fates` — потому что журнал хранит канон, отправленный в Ahrefs, и по нему ключует свёртку судеб, а в `run_journal.py` идёт работа другого агента; вид для человека — забота выдачи, одна строка в роутере
- 2026-09-24 · выбрал `budget.live_runs` (ответ «какие из этих прогонов живые» тем же `_live_run`) вместо проверки `mode == 'live'` на экране или в `_row` — потому что правило «условные units — не расход» живёт одним предикатом (L208), а вторая копия разошлась бы с «потрачено» на первом новом режиме; W6 держит совпадение с `spend_summary` по приращению
- 2026-09-24 · выбрал два поля `live` и `mode` вместо одного — потому что `live` решает сервер правилом, а `mode` — записанное слово для человека: у прогона без режима (на стенде 1 из 29, №132) пометка другая («режим не записан»), чем у fixture (20 из 29)
- 2026-09-24 · выбрал не переключать вид домена на списке проектов, карточке и библиотеке кейсов вместо «всё разом» — потому что поиск списка сравнивает канон (`ilike` по `projects.domain`): показав `müller-shop.de`, экран не нашёл бы его по «müller»; записано Z47 как отдельная поставка
- 2026-09-24 · выбрал брать авторов и режим страницы журнала по одному разу (`authors`, `live` до цикла) вместо прежнего `_authors` внутри генератора — потому что он звался на каждой строке: 20 одинаковых запросов на страницу вместо одного
