# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-15 · выбрал вынести раскрытие в общий хук `app/foldedChoice.ts` и перевести на него `UsersBody` вместо копии поведения в «Порогах» — потому что три случая нажатия различаются неочевидно (тот же — свернуть, никого — открыть сразу, другой — свернуть и только потом открыть по таймеру `FOLD_MS`), и второй экземпляр разошёлся бы именно на третьем: он и делает раскрытие спокойным
- 2026-09-15 · выбрал считать предпросмотр раскрытием версии вместо кнопки «Что изменится» — потому что кнопка спрашивала то же самое вторым нажатием: человек уже указал версию, о которой спрашивает, и повторять указание нечем
- 2026-09-15 · выбрал зависимость эффекта только от раскрытой версии (`[chosen]`) вместо полного списка зависимостей — потому что список версий приезжает тем же запросом и обновляется вместе с ним: предпросмотр пересчитывался бы на каждом фоновом перезапросе, а он проходит по всем проектам базы
- 2026-09-15 · выбрал держать признак открытого окна в адресе И в памяти браузера вместо одного места — потому что у них разная работа: адрес отвечает за F5, «назад» и ссылку, но переход в соседний раздел и возврат по меню приводят на чистый адрес, и окно закрылось бы само; расхождение решается в одну сторону — при монтировании адрес дополняется из памяти, дальше ведёт адрес
- 2026-09-15 · выбрал запоминать и закрытие тоже (`remember(name, false)` в `stickyFlag`) вместо запоминания только открытия — потому что иначе человек, закрывший окно и ушедший, возвращался бы к снова открытому: память говорила бы «было открыто», а он уже ответил, что не надо
- 2026-09-15 · выбрал не сохранять набранное в полях между перезагрузками вместо черновика в памяти браузера — потому что поля заполняются значениями версии-основы, и черновик, переживший F5, показывал бы числа, которых нет ни в одной версии; обещать сохранность того, что нигде не хранится, хуже, чем честно перезаполнить
- 2026-09-15 · выбрал закрывать окно в тесте клавишей `Escape` вместо клика по кнопке закрытия — потому что у кнопки Mantine своё служебное имя (`getByRole('button', { name: /Close/ })` его не нашёл), и тест, приколоченный к нему, ломался бы от смены версии библиотеки
- 2026-09-15 · выбрал вынести `EditWindow` и `useFormOfOpenWindow` отдельными сущностями вместо того, чтобы оставить их в `EditSection` — потому что с окном внутри функция выросла до 97 строк при планке 80, а ратчет предупреждений ESLint стоит на нуле: планка тут не вкус, а действующий гейт
