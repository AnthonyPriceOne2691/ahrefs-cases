# Plan: thresholds-open-by-version-and-edit-in-a-window

Уроки, прочитанные перед работой (`archive/INDEX.md`): **L161–L162** (состояние
экрана живёт в адресе), **L94** (право раздела берётся из таблицы доступа ТЗ),
**L91** (длинный компонент режется), грабли Mantine в jsdom (попапы
рендерятся не в том же кадре — ждать `findByRole('dialog')`).

1. **Вынести раскрытие в `app/foldedChoice.ts`** и перевести на него экран
   людей: два экземпляра поведения разошлись бы на третьем случае нажатия, а
   именно он делает раскрытие спокойным.
2. **`thresholds/VersionsSection.tsx`** — таблица плюс `Collapse` с порогами
   версии и предпросмотром; предпросмотр считается раскрытием, кнопка убрана.
3. **`app/stickyFlag.ts`** — признак экрана в адресе плюс память браузера:
   адрес не переживает уход на соседний экран, память не переживает ссылку.
4. **`thresholds/EditSection.tsx`** — кнопка на странице, поля в `Modal`.
5. **Тесты**: правка существующих (они держали прежнее устройство) плюс новые
   на раскрытие и на живучесть окна.
