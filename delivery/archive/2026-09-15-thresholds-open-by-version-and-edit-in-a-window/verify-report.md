# Verify report: thresholds-open-by-version-and-edit-in-a-window

## Чем проверено

| Что | Чем | Результат |
|---|---|---|
| Раскрытие версий | `vitest run src/pages/__tests__/thresholds-preview.test.tsx` | 9 passed (3 новых) |
| Правка в окне | `vitest run src/pages/__tests__/thresholds-edit.test.tsx` | 12 passed (3 новых) |
| Экран людей после переезда на хук | `vitest run src/pages/__tests__/users*.test.tsx` | 21 passed |
| Фронт целиком | `vitest run` | 147 passed |
| Типы и стиль | `tsc --noEmit`, `eslint src` | чисто; 0 предупреждений при планке 0 |

## Что пришлось поправить в существующих тестах

Четыре проверки предпросмотра нажимали кнопку «Что изменится», которой больше
нет: теперь они раскрывают версию. Одна проверка правки искала поля сразу после
нажатия — окно появляется не в том же кадре, добавлено ожидание
`findByRole('dialog')`. Это не подгонка под реализацию: прежние проверки держали
прежнее устройство экрана и обязаны были покраснеть.

## Чего проверка НЕ доказывает

**Плавность.** Тесты держат договор — что раскрывается, что сворачивается, что
остаётся в адресе. Что анимация выглядит спокойно, показывает только экран:
`Collapse` в jsdom не анимируется, высоты нулевые. Это записано в
`observe_signal`.

**Возврат на экран проверен пересборкой дерева**, а не настоящим переходом по
меню: в тесте дерево размонтировано и собрано заново с чистым адресом — это та
же ситуация для компонента, но не тот же путь для человека.
## Assertion digest (ревью ожиданий, не кода)

База: `origin/main` · сгенерировано `assert_digest.sh`


⚠ **Внимание: 2 незакоммиченных тест-файл(ов).** Дайджест считает
ЗАКОММИЧЕННЫЙ дифф, поэтому их утверждений здесь нет. Закоммить и повторить
до того, как ставить `asserts_reviewed_by: n/a` — иначе n/a подписывает пустоту.

**Новых утверждений нет.**

Это либо тесты не добавлялись (тогда чем закрыт DoD?), либо изменения
только в реализации. Второе законно; первое — повод спросить.

asserts_without_example: 0

asserts_reviewed_by: human:anthony at=2026-09-15
