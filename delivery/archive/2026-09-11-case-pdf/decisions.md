# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.
- 2026-09-11 · выбрал свой макет вместо ожидания референсных кейсов заказчика — потому что ноль из восьми материалов «Подготовить до старта» готовы третью неделю, а блоки ТЗ уже описаны: шаблон `templates/case.html.j2` строится из `CaseData` и при появлении референсов меняет CSS, а не структуру (решение владельца «делай пока сам»)
- 2026-09-11 · выбрал палитру и правила плиток из руководства по визуализации вместо подбора цветов на глаз — потому что цвет метрики в плитке обязан совпасть с её цветом на графиках поставки 4: цвет следует за сущностью, а не за местом
- 2026-09-11 · выбрал системный шрифт вместо веб-шрифта — потому что рендер запрещает сеть загрузчиком `_DenyNetwork`: веб-шрифт с fonts.googleapis.com обрывал бы сборку кейса на сервере без выхода наружу, а не подставлял запасной
- 2026-09-11 · выбрал fail-closed в стоп-листе вместо пропуска при ошибке разбора — потому что «проверка не отработала» и «проверка пройдена» различаются ценой публикации запрещённого упоминания (тот же класс, что урок L1)
- 2026-09-11 · выбрал класс-загрузчик с `_fail_on_errors` вместо простой функции — потому что WeasyPrint без этого атрибута превращает исключение загрузчика в предупреждение и продолжает рендер: запрет сети, написанный очевидным способом, дал бы «кейс собрался, только без картинки»
- 2026-09-11 · выбрал поставить `libharfbuzz-subset0` в CI вместо глушения DeprecationWarning — потому что предупреждение говорит правду: будущие версии WeasyPrint потребуют библиотеку, и сервер агентства тоже; глушение перенесло бы отказ на прод
- 2026-09-11 · выбрал вывести `src/ahrefs_cases.egg-info/` из индекса вместо того, чтобы коммитить его изменения — потому что это генерируемый установкой файл: он менялся при каждом `pip install -e` и шумел в каждом диффе
