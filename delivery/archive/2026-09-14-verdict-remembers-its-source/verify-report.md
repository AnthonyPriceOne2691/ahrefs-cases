# Verify report

**Date:** 2026-09-14
**Verifier:** agent:claude (оракулы и прогон по стенду), human:anthony (приёмка)
**asserts_reviewed_by:** yes (at=2026-09-14) — проверено на живых и фикстурных
данных стенда: по
версии порогов `0.1.0-проверка`, чьи вердикты вынесены до появления поля, кейсы
отказались собираться поимённо — 14 доменов, у каждого названа причина и
следующий шаг; по действующей версии, где источник записан, собралось 34 кейса
из 40 проектов, и один из них отрисован в PDF (`ahrefs.com`, 1 стр.)

## Shape oracles
- [x] PASS — ruff, ruff format, mypy strict (110 файлов)
- [x] PASS — import-linter / dependency-cruiser: направление зависимостей не тронуто
- [x] PASS — `delivery_check` (0/0), `okf_sync_gate` (9 концептов)
- [x] PASS — ESLint, Prettier, tsc --noEmit

## Behavior oracles
- [x] PASS — pytest: 538 passed, 1 skipped (было 534 до поставки, 529 до её тестов)
- [x] PASS — vitest: 106 passed (было 104)
- [x] PASS — E1–E8 из `eval-smoke.md`

## Ревью рисковых мест

**Главный риск поставки — не пропустить дефект, а начать отказывать здоровым
кейсам.** Сверка «числа вердикта воспроизводятся по рядам» в наивном виде
(«вердикт равен рядам») отказала бы **каждому** кейсу в нормальном порядке
прогона: ступень кейса докупает DR, стоимость трафика и кривую позиций
**после** классификации. Поэтому сверяются только метрики, которые в вердикте
есть, а докупленные по-прежнему идут отдельным каналом (`stale_subjects`,
L41). Держит это не рассуждение, а оракул
`test_normal_run_order_still_produces_cases`: он проходит путь целиком —
`collect` → `classify` → `stage2` → `case-data` → `cases` — и требует
собранного кейса **без** повторной классификации.

**Окна берутся из версии порогов вердикта, а не действующей.** Иначе первая же
правка окон обнулила бы все прошлые кейсы: они объясняются своими порогами
(инвариант 6 канона кейса). Следствие, замеченное при реализации: `build_cases`
теперь разбирает пороги версии и падает `ThresholdsError` на версии с
непригодным `payload`. Это верно по существу — без окон сверить числа нечем, —
и сообщение называет поле; CLI такой отказ уже ловит.

**Миграция ничего не выдумывает и ничего не удаляет.** Поле допускает `NULL`, и
это объявленное состояние «источник неизвестен», а не забывчивость: на стенде
часть вердиктов посчитана по живым рядам, часть по фикстурным. Удалить старые
вердикты нельзя — `cases.verdict_id` стоит с `ON DELETE RESTRICT`, и миграция
упала бы на стенде с собранными кейсами, а запись о выданном клиенту артефакте
важнее стройности схемы.

**Отказ, а не тихая подмена.** Читать ряды по источнику вердикта было бы проще
и незаметнее: оператор, попросивший кейсы по живым данным, молча получил бы
фикстурные. Отказ громкий, причина названа, действие бесплатно.

**Безопасность.** Новых эндпоинтов и параметров нет: `source` у карточки
(`api/routers/projects.py::project_card`) существовал и раньше, добавились два
поля ответа — `series_source` и `source_mismatch`. Оба называют режим чтения
(`live` / `fixture`), а не данные и не ключ: это имена режимов, и видит их тот,
у кого уже есть право читать проекты. Прав, ролей и проверки токена поставка не
касается. Живой ключ в ней не участвует нигде: сверка считает по базе.

**Производительность.** Сверка не добавляет ни одного запроса к базе: серия уже
прочитана — она нужна кривым, — а пересчёт точек идёт по ней в памяти, два
`points.point_a/point_b` на проект, то есть порядка «метрики × месяцы окна»
операций. На сотне доменов это доли секунды против сетевого сбора. Один новый
запрос появляется на **прогон**, а не на проект: `thresholds_of(ruleset)` в
`build_cases` разбирает пороги версии один раз для всех.

**Правило одно на двух потребителей.** `source_mismatch` живёт у вердикта, а не
в кейсе: карточка проекта кладёт рядом ту же пару — числа вердикта и кривые
рядов, — и второй экземпляр правила разошёлся бы с первым на случае «источник
не записан вовсе» (класс урока L127).

## Что показала проверка на стенде

```
$ run_collect.py cases --version 0.1.0-проверка
вердикта этой версии нет: 20
вердикт не про эти данные: 14
ahrefs.com: вердикт вынесен до того, как источник рядов стали записывать,
  и сверить его числа не с чем — перезапустите `classify` по этому источнику
...
$ run_collect.py cases          # действующая версия, источник записан
кейс собран: 34
вердикт не про эти данные: 0
$ run_collect.py render ahrefs.com
ahrefs.com → data/out/сайт в нише seo-инструменты — Кейс.pdf (1 стр.)
```

## Живая проверка на двух источниках (14.09.2026, разрешена владельцем)

Стенд — тот самый случай Z10: по калибровочным доменам лежат и живые, и
фикстурные ряды. Вердикты были посчитаны по фикстурным.

```
$ run_collect.py cases --source live        # до пересчёта
кейс собран: 0
вердикт не про эти данные: 34
ahrefs.com: вердикт вынесен по рядам «fixture», а показаны «live» — ...
```

**Это и есть Z10 в лицах:** тридцать четыре кейса, которые до поставки
собрались бы с таблицей из фикстур и кривой из живых данных.

```
$ AHREFS_PROVIDER=live run_collect.py classify   # бесплатно, в сеть не ходит
good: 5, medium: 5, poor: 14, insufficient_data: 16
$ run_collect.py cases --source live
кейс собран: 10
вердикт не про эти данные: 0
$ run_collect.py render cazoo.co.uk --source live
cazoo.co.uk → data/out/сайт в нише авто — Кейс.pdf (1 стр.), sha256 a9f5bac1faa9
```

Сверка числа таблицы руками, а не оракулом: период работ кончается 2025-12,
окно Б — два месяца, в живой серии это 480 413 (ноябрь) и 262 170 (декабрь),
среднее 371 292. В таблице кейса стоит **371 292**, и кривую рисует та же
серия. Числа в кейсе на живых данных сходятся с данными программно — требование
конституции выполнено не декларацией, а замером.

Расход units за проверку: **ноль**. Ни одна из команд не делает запросов:
`classify` считает по купленному, чтение рядов ключом не пользуется.

## Spec coverage gaps

- Два вердикта одного проекта под разные источники по-прежнему невозможны
  (ключ `(project_id, ruleset_id)`): пересчёт по другому источнику
  перезаписывает вердикт вместе с источником. Объявлено в `spec.md`.
- **Живая проверка вскрыла Z11** (записан в `docs/FINDINGS.md`): у `classify`,
  `explain`, `preview`, `recalc` и `diagnose` источник берётся из режима
  провайдера, поэтому пересчёт по уже купленным живым рядам заставляет поднять
  живой режим — ради операции, которая в Ahrefs не ходит. Это тот же класс, что
  L138, применённый наполовину. Чинится следующей поставкой, в область этой не
  добавлено намеренно (урок L15: пришедшее требование не дописывают в открытую
  поставку).

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD~1 -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 16 code (+10 process docs) / +678/-34 (net +644) |
| commits | 1 |
| time_to_accepted_spec | 0.0h |
| rework_after_done | 0 |
| harness_hardened | yes — `test_normal_run_order_still_produces_cases`: первый оракул, проходящий **настоящий порядок прогона** целиком, а не отдельную функцию. Заведён не после ошибки в бою, а против той, которую эта же поставка могла внести: сверка «вердикт равен рядам» отказала бы каждому кейсу |
| implement_retries | 2 — оба от новой сверки и оба по делу: тестовые версии порогов с пустым `payload` перестали разбираться (окна берутся из версии), а проекты без рядов в базе стали законно отказывать |
| verify_fails_before_green | 2 — `delivery_check` краснел на формате журналов (§12.2a: «отклонено, потому что», цена решения), не на поведении кода. Третий красный — снятый владельцем запрет `AHREFS_PROVIDER=live`, ожидаемый и не связанный с поставкой |
| est_token_or_cost | units Ahrefs: **0** (сверка считает по купленному; живая проверка запросов не делала) |
