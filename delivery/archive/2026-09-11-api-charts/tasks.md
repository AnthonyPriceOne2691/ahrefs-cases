# Tasks: графики проекта по HTTP

Уроки архива, применимые к диффу: L53, L41, L45/L46, L50 — см. plan.md.

## Рисунок
- [x] компоновка «два графика» в одном месте, зовут оба
- [x] ряды под кривые доступны API, а не только сборке кейса

## Эндпоинт
- [x] `GET /api/projects/{id}/charts` под правом `read`
- [x] полосы окон из записанного вердикта; нет вердикта — нет полос
- [x] нет рядов — пустой список, а не пустые оси

## Источник
- [x] ряды читаются источником из конфига, а не умолчанием обработчика
- [x] то же для карточки

## Проверки
- [x] E1–E8
