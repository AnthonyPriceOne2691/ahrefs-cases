# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-14 · выбрал считать кандидатов классификацией (`classify/candidates.py`) вместо префильтра в `collect/funnel.py` — потому что две меры одного роста расходятся на сезонных рядах вдвое: `wineverygame.com` 0,93× против 1,86×, и проект оставался без подтверждающих метрик, то есть без шанса на `good` при любых порогах
- 2026-09-14 · выбрал удалить `collect/funnel.py` вместо того, чтобы оставить его «грубой защитой» — потому что классификация на данных шага 1 не делает ни одного запроса: грубая мера ничего не экономит, а ошибается, и оставленная точка входа потребовала бы поддерживать два ответа на один вопрос
- 2026-09-14 · выбрал разместить правило в `classify` вместо того, чтобы учить `funnel` считать по окнам — потому что окна приходят из версии порогов, и `funnel._traffic_growth` потребовал бы аргумента `Windows` от вызывающего — то есть `collect` начал бы зависеть от `classify`, что запрещено контрактом `layers` в `.importlinter`
- 2026-09-14 · выбрал оракул на нарочно сезонном ряду (всплеск 60 000 в первом месяце, 50 000 в последнем) вместо теста на живых данных — потому что живые ряды меняются у Ahrefs, а форма, ловящая дефект, описывается двенадцатью числами и не зависит от сети
