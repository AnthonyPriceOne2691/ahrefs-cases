# Tasks: объём работ — замечание

Уроки архива, применимые к диффу: **L23** (не сливать разные причины в одну),
**L32/L34** (состояний больше двух: принято, принято с замечанием, отклонено),
**L64** (на пути отказа человек обязан увидеть, что именно чинить). Не
применимы: **L53**, **L61** — выбора источника и прав здесь нет; **L57** —
новых кодов ответа поставка не вводит, `notices` едут в теле уже
существующего `200`.

## Приём
- [x] тип замечания отдельно от отказа
- [x] текстовый объём работ не роняет строку
- [x] отказ сильнее замечания: строка без домена отклоняется

## Показ
- [x] CLI печатает замечания своим разделом
- [x] API отдаёт `notices`; `rejected_rows` от них не растёт
- [x] экран показывает замечания отдельным списком

## Проверки
- [x] E1–E10
