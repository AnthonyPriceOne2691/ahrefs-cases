# Eval smoke — this shipment

Экономия предъявляется двумя запусками подряд, а не рассказом о ней.

```
$ python3 scripts/run_collect.py all config/projects.example.csv
$ python3 scripts/run_collect.py collect
$ python3 scripts/run_collect.py stage2
```

- [x] первый прогон: `запросов: сделано 3, сэкономлено кэшем 0`, `units: смета 150, потрачено 150`
- [x] второй прогон: `сделано 0, сэкономлено кэшем 3`, `потрачено 0` — главный DoD всей Ф2
- [x] шаг 2: `кандидатов: N из 3`, задач вчетверо больше числа проектов, отчёт не путает одно с другим
- [x] `collect --refresh`: запросы делаются снова, число точек в базе не растёт
- [x] прогон при исчерпанной квоте не начинается: причина названа числами, `started_at` пуст
- [x] `run_collect.py intake нет.csv` → строка и код 2, без трассировки

Прогнано 2026-09-10 на чистой дев-базе. Наблюдалось:

```
1) сделано 3, сэкономлено 0 · смета 150, потрачено 150 · точек 100
2) сделано 0, сэкономлено 3 · смета 0,   потрачено 0   · точек 0     ← DoD Ф2
3) stage2: кандидатов 3 из 3 · задач 12 · смета 465, потрачено 465
4) --refresh: сделано 3 · точек 100, в базе по-прежнему 500 (не удвоились)
5) AHREFS_UNITS_MIN_LEFT=999999 → «не хватает units: остаток 10000 … запас 999999»,
   код возврата 1, ни одного запроса
6) intake нет.csv → «файл не найден», код возврата 2, без трассировки
```
