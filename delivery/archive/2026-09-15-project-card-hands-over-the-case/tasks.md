# Tasks: project-card-hands-over-the-case

Уроки по затронутым путям (`archive/INDEX.md`): **L41** (числа экрана берутся из
записанного вердикта — кнопка отдаёт тот же файл, что собран по нему),
**L161–L162** (состояние экрана в адресе: шаг кривой сознательно оставлен в
памяти компонента, он не переживает F5 и не должен — это не состояние экрана, а
поза для разглядывания), **L170** (признак, который никто не спрашивает, —
надпись: поэтому у кнопки есть выключенное состояние с названной причиной, а не
молчаливая серость).

- [x] Фильтр `project_id` у `GET /api/cases`; пустой ответ на чужой проект.
- [x] Кнопка «Скачать PDF» на карточке, рядом — причина, когда качать нечего.
- [x] Две причины различаются: кейса нет и кейс без файла — это разное.
- [x] Смена шага не снимает блок: прежние кривые ждут новые на месте.
- [x] Переключатель не пропадает под пальцем.
- [x] Подмена рисунка показана приглушением, а не молча.
- [x] Ручка выдачи выборки одним архивом + отказ называет номера.
- [x] `selection` не уходит в номер кейса — маршрут объявлен раньше.
- [x] 7 проверок vitest и 5 pytest; падали до правки те, что должны были.
- [x] vitest (140), pytest (580), tsc, eslint (0 предупреждений при планке 0).
