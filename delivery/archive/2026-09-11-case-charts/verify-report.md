# Verify report

**Date:** 2026-09-11
**Verifier:** human:anthony
**asserts_reviewed_by:** n/a (все утверждения ведут к одобренным примерам — см. дайджест ниже)
**CI run:** https://github.com/AnthonyPriceOne2691/ahrefs-cases/actions/runs/34591447428
**Commit:** 87d6f58

## Shape oracles
- [x] PASS — pre-commit, 29 хуков, упавших 0
- [x] PASS — предохранители: net_loc 576 при пороге 800
- [x] PASS — контракты слоёв целы; дублей не прибавилось (цвета метрик теперь
      один словарь на шаблон и на график, а не два списка)

## Behavior oracles
- [x] PASS — pytest 318 passed, 1 skipped
- [x] PASS — девять тестов кривых по примерам E1–E10

## Product oracles
- [x] PASS — три кейса на фикстурах собираются в **одну** страницу с двумя графиками
- [x] PASS — **смотрел глазами**: кривая трафика с заливкой и ореолом, кривая
      позиций двумя рядами с легендой, отметка старта, затенённые окна А и Б,
      круглые подписи шкалы
- [x] PASS — units не тратятся: ряды уже куплены ступенью кейса

## Ревью рисковых мест

**Честность картинки.** Главный риск поставки не в вёрстке: график может
соврать про величину, и заметят это не сразу. Три предохранителя — сглаживания
нет нигде (проверяется отсутствием кривых Безье в пути SVG), пропущенный месяц
остаётся пропуском, а окна точек А и Б показаны, потому что последний
измеренный месяц (116 190 у примера) и точка Б (113 312) различаются по
построению. Изометрия отклонена по той же причине: её верхняя грань прибавляла
каждому столбцу около тысячи визитов на шкале примера.

**Безопасность.** Новых входов нет. SVG собирается нами из чисел кейса и
вставляется в шаблон через `| safe` — это единственное исключение из
автоэкранирования, и оно названо в шаблоне комментарием. Данные заказчика
(ниша, услуга, клиент) в SVG не попадают вовсе.

**Транзакция БД.** Записи нет: рисование идёт по уже прочитанной структуре
кейса, `charts.py` о базе не знает и не импортирует ничего из `storage`, кроме
перечисления метрик.

**Производительность.** Два SVG на кейс — около сотни строк текста каждый;
рендер листа остался в тех же ~0,2 с. Библиотеку рисования не тащили.

**Интеграция.** Тронут `classify/points.py`: `window_from` стала публичной, а
`_window_forward/_window_backward` теперь зовут её. Поведение не изменилось —
держит golden-таблица классификации (11 тестов). `CaseData` получила три поля с
умолчаниями, поэтому старые тесты структуры не переписывались.

**Вёрстка.** «Должно поместиться» проверено числом, а не глазом: два графика в
столбик давали 356 мм полосы при печатном поле 267 мм. Решение поставить их
рядом и подрезать интерлиньяж таблицы пришло из замера; одностраничность держит
тест на готовом PDF.

**Канон.** Концепт кейса пополнился тремя инвариантами (кривая по измеренным
месяцам, окна А и Б показаны, цвет закреплён за метрикой), концепт точек А и Б —
шестым: окно перестало быть внутренней деталью расчёта.

## Assertion digest (ревью ожиданий, не кода)

База: `c8f04fd` · сгенерировано `assert_digest.sh`



asserts_without_example: 0

## Spec coverage gaps
- **Кривая ссылающихся доменов не рисуется.** ТЗ называет две динамики; ряд
  собирается, но третий график съел бы место, оставленное под текст.
- **Тумблер «месяц/квартал/год»** — экран Ф6; кейс отдаёт месяцы.
- **Текст абзацами, сверка чисел и запись `Case`** — следующая поставка.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base c8f04fd -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 8 code (+11 process docs) / +631/-39 (net +592) |
| commits | 1 |
| time_to_accepted_spec | 0.0h |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | yes — tests/test_cases_charts.py (новый оракул) |
| implement_retries | MANUAL — fills from session log |
| verify_fails_before_green | MANUAL — count red verify runs (CI run list) |
| est_token_or_cost | MANUAL / n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
