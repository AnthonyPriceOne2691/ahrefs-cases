# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.
- 2026-09-11 · выбрал вариант А (плоские данные, объём оформлением) вместо изометрии — потому что на собранном прототипе верхняя грань столбца прибавляла ≈4,5 px, то есть около 1 000 визитов на шкале примера, и форма роста по столбцам читается хуже; решение владельца после сравнения двух листов
- 2026-09-11 · выбрал показать окна точек А и Б прямо на кривой вместо того, чтобы объяснять расхождение сноской — потому что последний месяц кривой (33 373) и точка Б (33 725) различаются по построению, и клиент сверит их раньше, чем прочитает сноску
- 2026-09-11 · выбрал собирать SVG строками вместо библиотеки графиков — потому что это около сотни строк геометрии, а зависимость пришлось бы ставить на сервер агентства и отдельно проверять её поведение в WeasyPrint (где уже нашлись две ловушки: `stop-opacity` и шрифт в `<text>`)
- 2026-09-11 · выбрал поставить два графика рядом вместо столбика — потому что в столбик лист даёт 356 мм полосы при печатном поле 267 мм (замерено на готовом документе), а кейс обязан оставаться одностраничным
- 2026-09-11 · выбрал сделать `points.window_from` публичной вместо арифметики месяцев в рисовальщике — потому что второй экземпляр правила окна разошёлся бы с первым при первой правке (урок L29), а окно теперь показывают, а не только считают
- 2026-09-11 · выбрал круглую верхнюю подпись шкалы вместо максимума с запасом — потому что запас нужен рисунку (метка последнего значения не упирается в край), а подпись «130 133» показывает читателю арифметику отступа вместо данных
