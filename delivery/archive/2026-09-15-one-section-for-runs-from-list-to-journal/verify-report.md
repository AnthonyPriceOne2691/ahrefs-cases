# Verify report: one-section-for-runs-from-list-to-journal

## Чем проверено

| Что | Чем | Результат |
|---|---|---|
| Журнал, отборы, страницы, раскрытие | `vitest run src/pages/__tests__/ops.test.tsx` | 23 passed (7 новых) |
| Приём списка и смета в окне | `vitest run src/pages/__tests__/intake.test.tsx` | 13 passed (2 новых) |
| Меню | `vitest run src/app/__tests__/nav.test.ts` | passed, «Загрузки» больше нет |
| Сервер: страницы, отборы, авторы | `pytest tests/test_api_runs.py -q` | 15 passed (2 новых) |
| Бэкенд целиком | `pytest -q` | 587 passed, 1 skipped |
| Фронт целиком | `vitest run` | 156 passed |
| Типы и стиль | `mypy`, `ruff`, `tsc`, `eslint`, `prettier` | чисто; 0 предупреждений при планке 0 |

## Что вскрылось по дороге

**Помощник тестов путал журнал и запуск.** После слияния оба живут по адресу
`/api/runs` и различаются методом, а помощник сличал только путь — и отдавал
журналу ответ запуска. Падало это не на сравнении, а глубже: `(rows ?? []).some
is not a function` в коде экрана, который в этом не виноват. Помощник научен
методу; при этом первая правка сделала поиск жадным (`GET /api/runs` перехватил
`/api/runs/estimate`), а вторая — сортировала по длине ключа вместе с методом
(`GET /api/runs` обошёл `/api/runs/7`). Теперь побеждает самое длинное
совпадение **пути**.

## Чего проверка НЕ доказывает

**Что колонки не прыгают и фон чередуется правильно.** Оракулы держат
устройство: раскрытие — отдельная строка на всю ширину, полоса считается по
номеру прогона. Но ширины и цвета в jsdom не вычисляются, и «таблица не
дёргается» — вывод глаз. Записано в `observe_signal`.

**Что окно сметы переживает настоящий переход по меню.** В тесте дерево
пересобрано с чистым адресом — для компонента это та же ситуация, для человека
другой путь.
## Assertion digest (ревью ожиданий, не кода)

База: `origin/main` · сгенерировано `assert_digest.sh`


⚠ **Внимание: 4 незакоммиченных тест-файл(ов).** Дайджест считает
ЗАКОММИЧЕННЫЙ дифф, поэтому их утверждений здесь нет. Закоммить и повторить
до того, как ставить `asserts_reviewed_by: n/a` — иначе n/a подписывает пустоту.

**Новых утверждений нет.**

Это либо тесты не добавлялись (тогда чем закрыт DoD?), либо изменения
только в реализации. Второе законно; первое — повод спросить.

asserts_without_example: 0

asserts_reviewed_by: human:anthony at=2026-09-15
