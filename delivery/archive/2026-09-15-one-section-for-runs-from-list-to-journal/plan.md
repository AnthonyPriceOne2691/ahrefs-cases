# Plan: one-section-for-runs-from-list-to-journal

Уроки, прочитанные перед работой (`archive/INDEX.md`): **L161–L162** (состояние
экрана в адресе), **L173** (путь-слово объявляется раньше пути-номера),
**L181** (поведение выносят, а не копируют), **L182** (адрес и память
браузера — у каждого своя работа), **L91** (длинный компонент режется),
грабли Mantine в jsdom (всплывающие списки и календари — секунды на кадр).

1. **`SourceForm`**: `FileButton` вместо `FileInput`; поле только показывает имя.
2. **`RunsPage`** собирает приём, смету-окно и журнал; `IntakePage` удаляется.
3. **`nav.ts`**: один раздел `/runs` с правом `run` — раздел не только
   показывает журнал, но и тратит деньги.
4. **Сервер**: `offset`, `started_by`, `since`, `until` у журнала и отдельный
   путь `/api/runs/authors` — **до** `/{run_id}`.
5. **Журнал на экране**: отборы родными полями, листалка общая (`components/Pager`).
6. **Судьбы**: жёлтый знак вопроса в ячейке, раскрытие — отдельной строкой во
   всю ширину; чередование фона красится по номеру прогона, а не по позиции
   строки, иначе вставленная строка сбивает чётность.
