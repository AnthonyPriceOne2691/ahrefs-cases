# Verify report

**Date:** 2026-09-11
**Verifier:** human:anthony
**asserts_reviewed_by:** n/a (утверждений в коде не добавлено — меняется поведение самого прогона; дайджест ниже)
**CI run:** https://github.com/AnthonyPriceOne2691/ahrefs-cases/actions/runs/34609892434
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — pre-commit, 29 хуков, упавших 0
- [x] PASS — предохранители: класс S, один файл кода

## Behavior oracles
- [x] PASS — pytest 406 passed, 1 skipped при поднятой базе

## Product oracles (проверено с погашенной базой)
- [x] PASS — E1: `pytest` останавливается, печатает команду подъёма и
      `AHREFS_TESTS_ALLOW_NO_DB=1`, код возврата 1
- [x] PASS — E2: выбранные тесты без базы прошли (49 passed, код 0)
- [x] PASS — E3: с поднятой базой прогон прежний — 406 passed
- [x] PASS — E4: с переменной — прежние 253 passed, 154 skipped, код 0

## Ревью рисковых мест

**Остановка одна, а не сотня.** Причина одна — сто одинаковых красных спрятали
бы её среди самих себя. Отсюда `pytest.exit` на этапе сбора, а не `fail` в
фикстуре.

**Останавливаемся только там, где база нужна.** Выбор тестов проверяется по их
фикстурам (`needs_db`, `migrated_db`, `db_session`): прогон чистых модульных
тестов базы не требует и докера требовать не должен. Проверено E2.

**Выход оставлен и сделан видимым.** Пропуск сам по себе не зло — злом было
умолчание. Набранная руками переменная переносит выбор на человека и остаётся
в его истории команд.

**CI не задет.** Там база есть, путь остановки не достигается; прогон CI это и
подтвердил.

**Безопасность и деньги.** Поставка касается только запуска тестов: ни прав, ни
API, ни Ahrefs не трогает. Ходить в сеть тесты по-прежнему не могут.

**Производительность.** Проба базы кэширована на прогон (`lru_cache`), а не
повторяется на каждый тест; раньше она и так была разовой (сессионная фикстура),
и это свойство сохранено.

## Что осталось за границей

Поднимать базу из тестов самим не делалось намеренно: у прогона нет права
запускать докер на чужой машине.
## Assertion digest (ревью ожиданий, не кода)

База: `HEAD~1` · сгенерировано `assert_digest.sh`

**Новых утверждений нет.**

Это либо тесты не добавлялись (тогда чем закрыт DoD?), либо изменения
только в реализации. Второе законно; первое — повод спросить.

asserts_without_example: 0

## Spec coverage gaps
- Ничего из объявленного не осталось несделанным.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD~1 -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 1 code (+7 process docs) / +54/-8 (net +46) |
| commits | 1 |
| time_to_accepted_spec | 0.0h |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | no |
| implement_retries | 1 — глобальная переменная вместо `lru_cache` (гейт PLW0603) |
| verify_fails_before_green | 0 |
| est_token_or_cost | n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
