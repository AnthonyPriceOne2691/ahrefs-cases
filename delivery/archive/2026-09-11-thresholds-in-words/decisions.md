# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-11 · выбрал переводить пороги в предложения (`groupSentences` в `pages/thresholds/rules.ts`) вместо показа `payload` как JSON — потому что утверждают пороги Head of Link Building и Owner: `groups.good.org_traffic.growth_pct_min: 100` утверждается вслепую, «рост трафика от 100 %, и при этом прирост не меньше 2 000 визитов» — осознанно
- 2026-09-11 · выбрал показывать окна точек и нормализацию, пометив «здесь не правятся», вместо того чтобы их скрыть, — потому что скрытое поле читается как отсутствующее, а `windows.point_a_months` меняет сами числа А и Б, по которым потом спорят о пороге
- 2026-09-11 · выбрал оставить раздел под правом `edit_thresholds` вместо `read` — потому что таблица доступа ТЗ говорит «Пользователь — пороги: нет»; попытку открыть раздел поймал оракул `app/__tests__/nav.test.ts`, и право раздела остаётся договорённостью с заказчиком, а не следствием прав эндпоинта
- 2026-09-11 · выбрал показывать ноль в `guards.end_vs_peak_min_pct` и `eligibility.min_traffic_point_b` как «не задано» вместо «0 %» — потому что ноль здесь означает выключенное условие, а «конец периода не ниже пика на 0 %» читается как работающее правило
- 2026-09-11 · выбрал переводить `baseline` словами (`baselineWords`), незнакомое значение оставляя как есть, вместо показа `period` — потому что прогон живого экрана показал латинское `period` там, где остальной экран по-русски, а подмена незнакомого значения знакомым словом соврала бы про версию, заведённую руками
