# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-24 · выбрал передавать в `pack` номер проекта (`ToPack.project_id`) и получать его в каждом исходе вместо сопоставления исходов с попытками по позиции во входе — потому что ключ по домену разошёлся ровно в трёх местах `pack_cases` (`numbered`, `blocked`, поиск пути), и все три выросли из того, что `pack` возвращал исход с доменом: позиционная связь осталась бы неявной, а `packed[item.project_id]` читается одной строкой
- 2026-09-24 · выбрал писать строку `cases` кейсом из `PackedCase.case` вместо кейса попытки — потому что в файл лёг именно он (с номером сборки), и прежний `numbered.get(item.domain, item.case)` дал обеим кампаниям `nordvpn.com` подсветку одной; данные строки и содержимое файла теперь берутся из одного объекта
- 2026-09-24 · выбрал сортировать кампании одного сайта в `build_cases` по началу периода и номеру строки вместо сортировки только в `pack_cases` — потому что порядок нужен всем трём путям (`cases`, `render`, `pack`), а обещание «два запуска подряд дают одинаковый список» записано в докстроке `build_cases`; на копии базы «выживала» то кампания 13039, то 13040 (сборки 1, 3–5, 7, 9 против 2, 6, 8, 10)
- 2026-09-24 · выбрал тестировать через настоящий `pack_cases` со своими строками в общей базе вместо сессионной обёртки в откатываемой транзакции — потому что обёртки у `pack_cases` нет (он открывает свою сессию), а красный тест обязан падать по причине дефекта на `origin/main`, а не на отсутствии новой функции (§3.1c); цена — уборка своих строк и своя версия порогов, как в `test_api_runs.py`
- 2026-09-24 · выбрал верхнюю границу `sqlalchemy[asyncio]>=2.0,<2.1` в `pyproject.toml` вместо переписывания DISTINCT ON в `cache.share_twin_points` на `postgresql.distinct_on` — потому что CI PR #27 упал 47 тестами не по диффу: между прогоном соседней ветки (2.0.54, зелёный) и нашим через четыре минуты вышел SQLAlchemy 2.1.0, и `filterwarnings = error` сделал его предупреждение падением; `distinct_on` в 2.0 нет (локальный venv — 2.0.52), а `share_twin_points` — путь денег, и переписывать его запрос ради совместимости — отдельная поставка со своими оракулами
