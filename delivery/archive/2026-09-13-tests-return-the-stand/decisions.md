# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-13 · выбрал частичный уникальный индекс `uq_ruleset_single_active` вместо дисциплины вызывающих — потому что дисциплина уже дала сбой дважды за день (тесты ставили флаг, не гася чужой; на стенде оказались активны `0.0.0-default` и `0.1.0-проверка`), а `active_ruleset` при двух активных молча берёт новейшую по id: вердикты считались бы порогами, которых никто не утверждал
- 2026-09-13 · выбрал явный порядок в `recalc.activate` (`UPDATE ... SET is_active=false` + flush, затем зажечь одну) вместо цикла по объектам — потому что индекс проверяется на каждом statement'е, а цикл оставлял порядок записи на усмотрение ORM: переключение падало бы нарушением ограничения через раз и только на базе, где активная версия уже есть: первый прогон после индекса дал 32 ошибки в `test_api_thresholds` и соседях
- 2026-09-13 · выбрал общие помощники в `tests/owned_rows.py` вместо копий в трёх файлах тестов — потому что копии уже разошлись: `test_api_read` ставил флаг с комментарием «выключать чужую активность нельзя», `test_api_thresholds` — без, и оба были неправы по-своему: три копии потребовали бы трёх одинаковых правок при следующем таком случае
- 2026-09-13 · выбрал savepoint (`begin_nested`) в оракуле нарушения вместо голого flush — потому что нарушение обрывает транзакцию целиком, а тестовая транзакция здесь внешняя и на ней держится изоляция от стенда: первый прогон дал 33 ошибки в соседних тестах
- 2026-09-13 · выбрал гасить лишние активные версии в самой миграции вместо падения на грязных данных — потому что мигрировать предстоит ту самую базу, где это состояние и возникло: миграция, падающая на данных, которые сама должна привести в порядок, бесполезна
