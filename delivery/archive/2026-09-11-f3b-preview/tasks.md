# Tasks: Ф3б-2 — предпросмотр

Уроки архива, применимые к диффу: L30, L13, L21, L19, L8 — см. plan.md.

## Общий проход
- [x] `verdicts.evaluate`: расчёт + проверка покрытия по списку проектов, без записи
- [x] `recalc` переписан на него — поведение Ф3б-1 не изменилось (тесты зелёные)

## Предпросмотр
- [x] `classify/preview.py`: «было → стало», сводка по направлениям
- [x] «было» берётся по активной версии; нет вердикта — так и сказано
- [x] нехватка покрытия показывается отдельно, а не как «не изменится»
- [x] «изменений нет» говорится словами

## Проверки
- [x] тест E2: ни одной новой строки в `verdicts`, статусы проектов не тронуты
- [x] тест E3: предпросмотр и пересчёт дают одинаковые группы
- [x] тест E8, E8a, E9b
- [x] подкоманда `preview <версия>`
