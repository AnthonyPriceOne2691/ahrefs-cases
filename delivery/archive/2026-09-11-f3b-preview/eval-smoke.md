# Eval smoke — this shipment

Derived from spec acceptance. Run during verify.

- [x] E2 — список «было → стало» есть; строк в `verdicts` до и после поровну, статус проекта не изменился
- [x] E3 — предпросмотр и пересчёт по одной версии дают одинаковые группы по одним проектам
- [x] E8 — «изменений нет: эта версия порогов оставит все группы прежними» — словами
- [x] E8a — проект без вердикта показан как «нет вердикта → группа», а не спрятан
- [x] E9b — проект с нехваткой показан отдельной строкой с месяцами; в «без изменений» не попал
- [x] Сводка направлений считает проекты и не прыгает между двумя запусками
- [x] После применения и активации версии предпросмотр той же версии говорит «изменений нет»
