# Verify report

**Date:** 2026-09-13
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (agent:claude at=2026-09-13) — утверждения проверены
на числах живого замера: 530 812 → 532 476 за сутки при 2 262 в журнале, и нулевой
сдвиг сразу после платного запроса
**CI run:** пуш после приёмки
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — ruff, ruff format, mypy strict (109 файлов) чисты
- [x] PASS — tsc, eslint (0 ошибок, 0 предупреждений — сложность экрана снята выносом)
- [x] PASS — `delivery_check`: 0 ошибок, 0 предупреждений
- [x] PASS — `okf_validate`: канон `ops/first-live-run.md` тронут вместе с кодом

## Behavior oracles
- [x] PASS — pytest: 491 passed, 1 skipped (было 486)
- [x] PASS — vitest: 97 passed (было 95)
- [x] PASS — E1–E5

## Ревью рисковых мест

**Деньги.** Поставка делает предохранитель строже, а не мягче: остаток
уменьшается на величину, которой раньше не было. Лишних запросов к Ahrefs не
появляется — вычет считается по своей базе одним агрегатом.

**Односторонняя ошибка.** Если счётчик часть расхода уже учёл, мы вычтем её
дважды и откажем лишний раз. Это выбрано сознательно: цена обратной ошибки —
прогон, оборванный на середине, с половиной собранных проектов и потраченными
units. Окно вычета (24 ч) — верхняя граница замера, и она настраивается.

**Транзакция БД.** Записей поставка не добавляет: `live_spend_since` и
`uncounted_spend` только читают. Сессия та же, что у остальных проверок
прогона, — отдельного `commit` не появляется.

**Производительность.** Класс подняли `.all()`-соседи в диффе роутера расхода и
`json`-снимок `params_snapshot`. Риска нет: `live_spend_since` — один агрегат
`sum()` с `join` на `runs` и фильтром по `created_at`, строки наружу не
отдаются; на экране расхода он добавляет один запрос к уже существующим трём.

**Совместимость экрана.** `uncounted` в `UsageView` со значением по умолчанию 0:
старый клиент, не знающий поля, продолжает работать, а новый при нуле не
показывает ничего лишнего (проверено примером E12).

## Spec coverage gaps
- Настоящая величина отставания счётчика неизвестна: замерено «больше минут,
  не больше суток». Уточняется вторым снимком пробника.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 0 code (+0 process docs) / +0/-0 (net +0) |
| commits | 0 |
| time_to_accepted_spec | n/a (no spec.md in history — class S?) |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | no |
| implement_retries | MANUAL — fills from session log |
| verify_fails_before_green | MANUAL — count red verify runs (CI run list) |
| est_token_or_cost | MANUAL / n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
