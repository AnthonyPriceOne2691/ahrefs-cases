# Verify report

**Date:** 2026-09-13
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (agent:claude at=2026-09-13) — числа сверены с замером
12.09.2026 (цены полей) и пересчитаны моделью: 28 970 units, 290 на домен
**CI run:** пуш после приёмки
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — ruff, ruff format, mypy strict чисты
- [x] PASS — `okf_validate`: 17 файлов канона, 0 ошибок
- [x] PASS — `delivery_check`: 0 ошибок, 0 предупреждений

## Behavior oracles
- [x] PASS — pytest: 496 passed, 1 skipped (было 491)
- [x] PASS — E1–E4 плюс оракул на цену второго маршрута

## Ревью рисковых мест

**Деньги.** Поставка ничего не тратит и ничего не разрешает: расчёт чистый, к
Ahrefs не ходит. Но она меняет **число, которое называют заказчику**, — с
«27 330 / лимит от 30 000» на «28 970 / лимит от 35 000». Разговор придётся
повторить, и это записано в `docs/FINDINGS.md` (Z7), а не оставлено в архиве.

**Производительность.** Класс подняли `re.search` / `re.compile` в `write` и
`table_in`. Риска нет: оба зовутся по разу на запуск скрипта или теста, документ
в двадцать килобайт, в рантайме сервиса этот модуль не участвует вовсе.

**Новый модуль.** `units_profile` — чистые функции над спеками: ни сессии, ни
сети, ни состояния. Единственный побочный эффект — запись документа, и он за
флагом `--write`.

**Канон.** Числа поправлены в трёх местах разом (`ops/first-live-run.md`,
`engineering/collection/collection-scheme.md`, `log.md`): расхождение канона с
документом — это следующая редакция «27 330 против 33 560».

## Spec coverage gaps
- Цена `keywords-history` замерена узким окном; при длинной истории у Ahrefs
  может включиться другой тариф. Слышно будет сразу: расхождение сметы с фактом
  громче 20 % пишется как `ahrefs_estimate_missed`.
- Смета боевого списка считается сервисом на экране загрузки — здесь профиль,
  а не список.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 0 code (+0 process docs) / +0/-0 (net +0) |
| commits | 0 |
| time_to_accepted_spec | n/a (no spec.md in history — class S?) |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | no |
| implement_retries | MANUAL — fills from session log |
| verify_fails_before_green | MANUAL — count red verify runs (CI run list) |
| est_token_or_cost | MANUAL / n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
