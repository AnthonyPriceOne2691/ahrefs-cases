# Verify report

**Date:** 2026-09-13
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (agent:claude at=2026-09-13) — оракул шапки проверен на
обратимость: без `visibleFrom` падает, с ним проходит
**CI run:** пуш после приёмки
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — tsc, eslint (0 ошибок, 0 предупреждений), prettier
- [x] PASS — `delivery_check`: 0 ошибок, 0 предупреждений

## Behavior oracles
- [x] PASS — vitest: 102 passed (было 97), новый файл `app/__tests__/shell.test.tsx`
- [x] PASS — E1–E4

## Ревью рисковых мест

**Новый модуль.** `status.ts` — чистый словарь и две функции над ним, без
состояния и без сети. Устроен как `groups.ts`, рядом с ним и по той же причине.

**Совместимость.** Сырое значение статуса осталось в разметке атрибутом
`data-status`: по нему ищут существующие тесты и им же удобно отлаживать. Видит
человек слово, ищет машина — значение.

**Риска производительности нет:** словарь статический, поиск по ключу; ни
запросов, ни вычислений на рендере не добавилось.

**Чего тест не проверяет.** Ширину экрана jsdom не считает: оракул проверяет
механизм (класс «видно от sm»), а не картинку. Настоящая проверка — глазами на
400 px, она за человеком и записана в `runtime_paths`.

## Spec coverage gaps
- Статусы проектов (`ProjectStatus`) на экранах не показываются вовсе — там
  группа и вердикт. Если появятся, словарь ждёт их в том же файле.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 0 code (+0 process docs) / +0/-0 (net +0) |
| commits | 0 |
| time_to_accepted_spec | n/a (no spec.md in history — class S?) |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | no |
| implement_retries | MANUAL — fills from session log |
| verify_fails_before_green | MANUAL — count red verify runs (CI run list) |
| est_token_or_cost | MANUAL / n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
