# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-13 · выбрал скрывать на узком экране **почту** (`visibleFrom="sm"` в `Shell.tsx`) вместо уменьшения шрифта или переноса строки — потому что высота `AppShell.Header` задана жёстко (56 px) и перенос наезжает на содержимое: из четырёх элементов почта самая длинная и единственная, которую человек про себя и так знает
- 2026-09-13 · выбрал один словарь статусов (`pages/status.ts`) вместо подписи в каждой таблице — потому что словарь групп уже разъезжался в двух местах («good → poor» посреди русского предпросмотра порогов), а статус печатают три места — `cases/CasesTable.tsx`, `runs/RunsTable.tsx` и `intake/RunLine.tsx`: три копии словаря потребовали бы править слово трижды и разошлись бы на первом же новом статусе
- 2026-09-13 · выбрал показывать незнакомый статус сырым вместо «неизвестно» — потому что так уже сделано у групп (`groupWord`), и сырое значение говорит человеку, что сервис знает больше экрана: пример `E14` держит это на статусе `reaped`, которого в перечислении нет
- 2026-09-13 · выбрал проверять класс `visible-from-sm` вместо ширины экрана — потому что jsdom ширину не считает вовсе, и тест на «влезло в строку» потребовал бы браузера в CI; проверено на обратимость — снял `visibleFrom` в `Shell.tsx`, пример `E1` упал, вернул — прошёл
- 2026-09-13 · выбрал вынести новый пример кейсов в отдельный `describe` вместо дописывания в существующий — потому что eslint считает строки стрелочной функции `describe` (порог 80), и ratchet предупреждений запрещает рост: 87 строк дали бы первое предупреждение в проекте
