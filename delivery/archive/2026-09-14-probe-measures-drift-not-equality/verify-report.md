# Verify report

**Date:** 2026-09-14
**Verifier:** agent:claude (оракулы), human:anthony (приёмка)
**asserts_reviewed_by:** yes (at=2026-09-14) — оракулы построены на настоящих
числах замера: 4 550 157 → 4 550 154 в закрытом месяце больше не опровергает
правило, 100 000 → 101 000 опровергает по-прежнему, 0 → 12 значим всегда

## Shape oracles
- [x] PASS — ruff, ruff format, mypy strict
- [x] PASS — `delivery_check`, `okf_sync_gate`, все гейты `pre-commit`

## Behavior oracles
- [x] PASS — pytest: 553 passed, 1 skipped
- [x] PASS — E1–E4 из `eval-smoke.md`

## Ревью рисковых мест

**Допуск не должен превратиться в глухоту.** Оракул, стерегущий только «шум не
пугает», зеленел бы и у пробника, который молчит всегда, — поэтому граница
проверена с обеих сторон: 1 % в закрытом месяце по-прежнему даёт
«ОПРОВЕРГНУТА» со ссылкой на `closed_through`.

**Откуда взялось число.** 0,01 % — на три порядка выше замеренного дрейфа
(0,00007 %) и на шесть порядков ниже порогов, решающих группу (десятки
процентов). Это не подгонка под замер: между «шум оценки» и «пересчитанный
месяц» лежит четыре порядка пустоты, и граница поставлена внутри неё.

**Молчание о шуме — второй дефект.** Приравнять дрейф к «ничего не изменилось»
значило бы, что следующий замер сравнивает с неверной картиной. Поэтому у шума
свой исход и своя печать максимума.

**Интеграция и деньги.** Разведочный скрипт, вне рантайма сервиса: запросов к
Ahrefs не добавляется, поведение сбора не меняется. Риска безопасности нет —
ни ключа, ни сети, ни ввода извне: сравниваются два своих снимка.

## Spec coverage gaps

- Допуск один на все метрики. `refdomains` и ключи — счётные величины, а не
  оценки, и для них шум в 0,01 % неестественен. Пробник смотрит только
  `org_traffic`, поэтому вопрос не встаёт; встанет — понадобится допуск на
  метрику, а не на пробник.

## Verdict
- [x] READY FOR HANDOFF
