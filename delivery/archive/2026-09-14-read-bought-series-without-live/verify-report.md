# Verify report

**Date:** 2026-09-14
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (agent:claude at=2026-09-14) — собрано десять кейсов по
живым рядам при `provider=fixture`; запрет на живой Ahrefs при этом ни разу не снимался

## Shape oracles
- [x] PASS — ruff, ruff format, mypy strict (110 файлов)
- [x] PASS — `delivery_check`

## Behavior oracles
- [x] PASS — pytest: прежний набор зелёный
- [x] PASS — E1–E3

## Ревью рисковых мест

**Деньги.** Флаг не может ничего купить: `show_cases`, `render_case` и
`pack_cases` только читают базу. Провайдер остаётся единственным, кто решает,
откуда **покупать**, и запрет на `live` не тронут.

**Путаница источников.** Обратная сторона флага: теперь можно прочитать живые
ряды при фикстурном провайдере — и легко забыть, что именно читаешь. Поэтому
режим печатается первой строкой вывода («провайдер: fixture»), а сам источник
назван в аргументе.

**Найденное по дороге.** Сборка кейсов вскрыла дефект старше этой поставки:
вердикт не хранит источник, по которому посчитан, и кейс может показать таблицу
из одного источника, а график из другого (Z10 в `docs/FINDINGS.md`, замерено на
`wickes.co.uk`: 35 394 против 1 058 129 на графике). Чинится следующей
поставкой — здесь только зафиксировано.

**Длина файла.** Поставка перевела `scripts/run_collect.py` за лимит 500 строк,
и гейт это поймал. Разделено по шву «разбор аргументов / операции»:
`cli/collect_commands.py` (354 строки) и скрипт (218). Порог не поднимался.

## Spec coverage gaps
- Флаг не добавлен командам, которые считают вердикты (`classify`, `recalc`,
  `preview`): там источник берётся из конфига, и менять это до починки Z10
  значит множить способы получить смешанные данные.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 0 code (+0 process docs) / +0/-0 (net +0) |
| commits | 0 |
| time_to_accepted_spec | n/a (no spec.md in history — class S?) |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | no |
| implement_retries | MANUAL — fills from session log |
| verify_fails_before_green | MANUAL — count red verify runs (CI run list) |
| est_token_or_cost | MANUAL / n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
