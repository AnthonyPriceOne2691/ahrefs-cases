# Eval smoke — this shipment

- [x] E1 — `classify --source live` при провайдере `fixture` пишет вердикт с источником `live`
- [x] E2 — без флага источник берётся из режима провайдера
- [x] E3 — у каждой читающей и считающей команды есть `--source`
- [x] E4 — у платящих команд флага нет
- [x] E5 — `repro_test` каждой поставки указывает на существующий тест
