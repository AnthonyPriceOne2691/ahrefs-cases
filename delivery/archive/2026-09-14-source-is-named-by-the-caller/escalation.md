# Escalation

**Что блокирует.** Запрет `Bash(AHREFS_PROVIDER=live:*)` снят владельцем
14.09.2026 для живой проверки Z10. Пока он снят, `delivery_check` красный
(§4.5: правило объявлено в `delivery/CONSTITUTION.md`, но отсутствует в
`.claude/settings.json`), а коммит на красном гейте запрещён конституцией.
Сама работа при этом зелёная: 543 теста, ruff, mypy, окф-гейт, живая проверка
на стенде.

**Что пробовал.** Проверил, что блокировка не мнимая: гейт сверяет два списка в
обе стороны, и «правило в конституции, но не в настройках» — это граница,
которую сдвинули без ревью. Настройки не правил намеренно: снятие сделал
владелец, возврат — тоже его действие, и тихо вернуть его агентом значило бы
отменить решение человека, не спросив. Правку `.claude/settings.json` в индекс
не добавлял: временно снятому запрету не место в истории.

**Какой нужен ответ.** Возвращать ли запрет сейчас.

**Вариант А — вернуть запрет.** Цена: живые хвосты Ф7 (H2, поведение под 429,
замер `batch-analysis` H6) потребуют снять его снова — одно действие владельца.
Выгода: `delivery_check` зелёный, две готовые поставки уходят в коммит сразу.

**Вариант Б — оставить снятым до конца живых работ Ф7.** Цена: обе поставки
лежат в индексе некоммиченными, и с каждым часом растёт шанс смешать их с
третьей; CI по ним не проходил. Выгода: не дёргать запрет дважды за вечер.

**Что нужно от этой поставки в любом случае:** ничего. Она закончена и не
зависит от ответа — блокирован только коммит.
