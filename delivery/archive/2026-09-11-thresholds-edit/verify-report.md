# Verify report

**Date:** 2026-09-11
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (by=human:anthony at=2026-09-11) — утверждения
проверены на стенде числами: правка «хорошего» 100 → 60 % сохранилась новой
версией, предпросмотр посчитался сам, применение переставило отметку
действующей, пересчёт отчитался «проектов: 11 (пересчитано 5, пропущено 6)»
**CI run:** пуш после приёмки
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — pre-commit, 29 хуков, упавших 0
- [x] PASS — предохранители: net_loc 798 при пороге 800, файлов 12 из 25
- [x] PASS — eslint без предупреждений (сложность `EditSection` и `valuesFrom`
      разрезана), tsc чист

## Behavior oracles
- [x] PASS — vitest 83 (+9), pytest не затронут (фронтовая поставка)
- [x] PASS — E1–E9 закрыты тестами

## Product oracles (живой стек, Chrome по CDP)
- [x] PASS — форма заполнилась значениями действующей версии; правка
      «хорошего» 100 → 60 % сохранилась версией `2026-09-прогон-3`
- [x] PASS — предпросмотр посчитался **сам**, без второго нажатия
- [x] PASS — применение в два нажатия: первое спросило, второе применило;
      карточка сверху переехала на новую версию
- [x] PASS — пересчёт отчитался строками сервера, включая шесть пропущенных
      проектов с названными месяцами точки А
- [x] PASS — стенд возвращён: действующей снова `0.0.0-default`, вердикты
      пересчитаны по ней, три прогонные версии и их вердикты удалены

## Найдено прогоном живого экрана

**Очистка поля возвращала бы `422`.** Форма отправляла `null` за любое пустое
поле, но модель порогов принимает отсутствие только у двух (`growth_abs_min`,
`growth_pct_max`). У «конца периода не ниже пика» и «минимального трафика точки
Б» ноль **и есть** «выключено», а `None` — ошибка разбора. Теперь пустое поле
уходит нулём там, где ноль законен, а обязательные поля названы до сохранения и
блокируют кнопку.

**Плашка не знала, что версию применили.** После активации она продолжала
говорить «сохранена и пока не действует» — то есть сообщала прямо обратное
состоянию, которое сама же и вызвала.

**Звёздочки Markdown в тексте.** `**не действует**` показывалось как есть: JSX
их не разбирает.

## Ревью рисковых мест

**Безопасность.** Форма показывается по праву `edit_thresholds` из
`/api/auth/me`, и это удобство: сохранение, активация и пересчёт закрыты тем же
правом на сервере, а тест E1 проверяет, что без права формы нет вовсе.

**Деньги.** Риска нет: ни сохранение, ни активация, ни пересчёт к Ahrefs не
обращаются — пересчёт идёт по уже купленным рядам. Единственный платный ресурс
тратит только сбор, и его в этой поставке нет.

**Транзакция БД.** Экран пишет три раза, и все три — через существующие
эндпоинты: создание версии, активация, пересчёт вердиктов. Версия неизменяема,
правка создаёт новую — прошлые вердикты остаются объяснимыми.

**Интеграция.** Структуру проверяет сервер (`thresholds.parse`, `extra=forbid`),
и его текст показывается как есть: вторая проверка на фронте начала бы отклонять
законные версии. Имя версии уходит через `encodeURIComponent` — версии бывают
кириллическими.

## Spec coverage gaps
- Пересчёт идёт синхронно: на сотне проектов это заметное время. Станет
  долгим — ему место в очереди прогонов, но это вопрос к API.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD~1 -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 11 code (+9 process docs) / +1017/-219 (net +798) |
| commits | 1 |
| time_to_accepted_spec | 0.0h |
| rework_after_done | 0 commit(s) after first phase: handoff |
| harness_hardened | yes — web/src/pages/__tests__/thresholds-edit.test.tsx (новый оракул), web/src/test/thresholds.tsx (новый оракул) |
| implement_retries | 4 — сложность `EditSection` и `valuesFrom`, объём поставки (сведён стенд тестов), длина describe, неоднозначный матчер в тесте |
| verify_fails_before_green | 3 — очистка поля дала бы `422`; плашка врала после применения; звёздочки Markdown |
| est_token_or_cost | n/a (Ahrefs не вызывался) |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
