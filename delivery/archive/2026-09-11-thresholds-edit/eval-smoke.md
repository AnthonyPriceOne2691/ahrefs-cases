# Eval smoke — this shipment

- [x] E1 — без права `edit_thresholds` формы нет
- [x] E2 — поля заполнены значениями версии-основы
- [x] E3 — версия создаётся неактивной
- [x] E4 — предпросмотр считается сам после сохранения
- [x] E5 — занятое имя: `409` словами сервера
- [x] E6 — кривая структура: `422` с именем поля
- [x] E7 — первое нажатие не применяет, а спрашивает
- [x] E8 — подтверждение применяет, плашка знает об этом
- [x] E9 — пересчёт отчитывается строками сервера
- [x] пустое обязательное поле не уезжает на сервер; выключаемое уходит нулём
