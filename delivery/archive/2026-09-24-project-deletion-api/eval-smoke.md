# Eval smoke — this shipment

Исполнение на копии дев-базы (`cases_delete_check`, 75 проектов, 1808 кейсов) и
копии каталога выгрузки (468 файлов), API из worktree на :8765, 24.09.2026.
Копия базы и каталог после проверки удалены.

- [x] D1 — `legal-partners.pl` (915): ответ `260 точек, 2 вердикта, 92 кейса,
      21 файл, 24 строки журнала`; по таблицам ровно −1/−260/−2/−92/−92, на
      диске −21 файл; карточка и повторное удаление — `404`
- [x] D2 — `run_items` 578 → 578 (без ссылки +24), `runs` 29 → 29,
      `units_ledger` 590 строк и 27 366 units до и после каждого удаления
- [x] D3 — `nordvpn.com`: после удаления кампании 13039 у 13040 те же 116 точек,
      1 вердикт, 11 кейсов, 11 артефактов, 4 строки журнала
- [x] D4 — все 5 PDF `nordvpn.com` общие у двух кампаний (Z39): первое удаление
      стёрло 0, второе — 5
- [x] D5 — тест: файл вне каталога выгрузки не тронут
- [x] D6 — у проверяющего инженера `/me` содержит `delete_projects`; матрица
      групп и личных решений — тест
- [x] D7 — повторное удаление `legal-partners.pl` — `404`
- [x] D8 — последний прогон переведён в `running`: `409` «прогон 1089 ещё идёт
      (running)…», у проекта те же 260 точек; статус возвращён
- [x] D9 — настоящий воркер RQ, прогон 1814, удаление каждые 50 мс:
      `queued` → 409, `running` → 409, `done` и хвост задачи → 409 «сервис
      дописывает…», после задачи → 200; прогон остался `done`, ошибки нет
- [x] D10 — прогон 1088: до удаления две судьбы `nordvpn.com` по 132 units,
      после удаления обеих кампаний — одна «ok, 264, удалён» (граница Z41);
      разные домены — тест
- [x] D11 — пачка после удаления второй кампании `nordvpn.com`:
      `outdated: project_deleted`, скачивание `409` теми же словами
- [x] D12 — пачка до удалений: `outdated: null`
- [x] D13 — предпросмотр совпал с ответом удаления у всех трёх проектов
      (`files 0 / 5 / 21`, `pack_blocked false / true / true`)
- [x] D14 — тест: `_run_guarded` держит замок работы, после задачи он свободен
