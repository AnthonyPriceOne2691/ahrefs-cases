# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-11 · выбрал опрашивать журнал только пока есть незакрытый прогон вместо постоянного опроса — потому что прогон идёт часами, а экран открыт минутами: `refetchInterval` без условия греет базу ради секунды точности
- 2026-09-11 · выбрал искать активный прогон по всем загруженным строкам (`hasOpenRun` в `RunsPage.tsx`) вместо первой — потому что журнал отсортирован по номеру (`Run.id.desc()` в `runs.list_runs`), а закончиться прогон может раньше соседа: у пары «186 failed, 187 running» первая строка не активна
- 2026-09-11 · выбрал показывать резерв отдельным числом вместо одного «потрачено» — потому что резерв удерживает units идущего прогона, и без него остаток выглядит больше, чем есть на деле
- 2026-09-11 · выбрал четыре степени алертов вместо единого списка — потому что «готовая пачка» и «мало units» одним цветом читаются как одно и то же, а действия по ним противоположные
