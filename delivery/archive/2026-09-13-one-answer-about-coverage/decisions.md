# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-13 · выбрал считать покрытие внутри `compute_verdict` вместо обёртки `evaluate` — потому что `compute_verdict` зовут три модуля, и `classify_all` звал её напрямую: правило в обёртке видно только двум вызывающим из трёх, а четвёртый вызывающий снова его не увидит: `evaluate` потребовала бы дублировать проверку в каждом новом пути
- 2026-09-13 · выбрал границей «не покупали» период работ вместо краёв собранной серии — потому что шаг 1 покупает период целиком: месяца внутри периода нет не потому, что его не купили, а потому что его нет у Ahrefs. Живая проверка показала цену прежней границы: девять проектов из девятнадцати остались без вердикта после активации версии
- 2026-09-13 · выбрал записывать вердикт `insufficient_data` вместо пропуска без записи в `recalc` — потому что после активации версии у пропущенного проекта оставался вердикт **предыдущей** версии: карточка показывала группу, посчитанную не теми порогами, и `Verdict.ruleset_id` этого не спасал: он в базе есть, но на карточке не показан, а `report.skipped` потребовал бы от человека держать в голове список из девяти доменов
- 2026-09-13 · выбрал справочную запись `point_a_months_used` вместо молчания — потому что `_truncated_history` молчит при отставании в один месяц (`months_late <= 1`), а окно точки как раз два: сняв запрет на вердикт, мы остались бы без единственного места, где сказано «посчитано по половине окна»
