# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.

- 2026-09-11 · выбрал решение на пару «проект + endpoint» вместо решения на проект — потому что на одном периоде выгодные схемы противоположны: `keywords-history` (строка 51) дешевле точками 204 против 918, `refdomains-history` (строка 5) дешевле историей 90 против 100
- 2026-09-11 · выбрал оставить шаг 2 без флага вместо второго переключателя — потому что флаг шага 1 существует из-за Z6, а правило дыры считает `max_gap_months` только по `org_traffic` (`classify/verdicts.py`): на шаге 2 запрещать нечего, а лишний флаг — это знание, которого нет у того, кто его переключает
