# Eval smoke — this shipment

Derived from spec acceptance. Run during verify.

- [x] E1/E2 — короткий период собирается историей (77 против 100), длинный точками (100 против 209); обе цены и причина в решении — `tests/test_collect_scheme.py::test_scheme_is_chosen_by_price_and_counted_in_rows`
- [x] E3 — граница между 8 и 9 месяцами, считается по строкам (тот же тест, случаи 9 и 10 строк)
- [x] E4 — при равной цене (110 = 110 при окне 5) выбрана история
- [x] E5 — пороги просят 2 месяца, запрос берёт 4, цена та же 50
- [x] E6 — окно 6 из порогов даёт 132 и переворачивает выбор на историю
- [x] E7 — перекрытие окон названо в объяснении; отдельного правила нет и доказано, что не нужно
- [x] E8 — при `pre_start_baseline_months: 0` месяцы до старта не запрашиваются; с 3 цена растёт ровно на 33
- [x] E9 — `org_cost` ушёл из шага 1, golden-таблица классификации зелёная без правок (240 тестов)
- [x] E10 — смета совпадает с числом точек провайдера в обеих схемах
- [x] E11 — прогон на сотне доменов: 10 000 units при `auto` против 19 800 историей, разбивка и «стоимость запуска на 100 URL» в отчёте
- [x] E12 — купленное окно не покупается второй раз; строка `cached` с причиной на месте
- [x] E13 — упор в глубину истории сдвигает точку А, окно не переворачивается (нашёл дефект)
- [x] E14 — зафиксировано словами в спеке и в докстрингах: у проекта, собранного точками, графика нет, история под него докупается в Ф4
