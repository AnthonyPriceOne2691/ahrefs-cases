# Verify report

**Date:** 2026-09-12
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (by=human:anthony at=2026-09-12) — показания сняты
живым ключом при владельце: девять запросов узкими окнами, заголовки
`x-api-units-cost-row` и тела ответов перед глазами
**CI run:** пуш после приёмки
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — ruff, ruff format, mypy по тронутым файлам чисты
- [x] PASS — предохранители: диф в пределах 800

## Behavior oracles
- [x] PASS — pytest 459 passed, 1 skipped
- [x] PASS — E1–E8 закрыты тестами; новый оракул держит таблицу замеров

## Product oracles (живой ключ Ahrefs)
- [x] PASS — квота: имена полей совпали с `collect/quota.py`
      (`limits_and_usage.units_limit_api_key` / `units_usage_api_key`)
- [x] PASS — глубина истории: январь 2024 отдан, два года назад доступны (H3)
- [x] PASS — сетка помесячная, все точки — первые числа (H4)
- [x] PASS — формула цены сошлась на семи замерах, включая крайние случаи
      (пять бакетов — 6, два дорогих поля — 21)
- [x] PASS — сбор пяти живых доменов прошёл: 18, 16, 13, 9 и 3 точки по
      периодам, формы разобрались без ошибок

## Найдено живым прогоном сверх спеки

**Живой сбор идёт по всей базе.** На стенде лежали одиннадцать фикстурных
проектов, и `collect` пошёл за ними в Ahrefs — часть «синтетических» доменов
оказалась настоящими сайтами, и по ним пришли живые данные. Разделение по
источнику выдержало (точки легли отдельной строкой `source=live`), стенд
почищен вручную. Это не дефект кода, но дыра в операционной процедуре: **у
живого прогона нет фильтра по списку**. Занесено в задачи Ф7.

**Источник рядов по умолчанию — `FIXTURE`** в `classify_all`, `recalc`,
`preview` и `diagnose`, и в живом режиме классификация объявила «данных нет» по
всем пяти собранным доменам. Третье повторение урока L53. Чинится следующей
поставкой: аргумент станет обязательным, а не умолчанием.

## Ревью рисковых мест

**Деньги.** Заголовки насчитали ~2 500 units за всю сессию (девять пробных
запросов плюс сбор), счётчик `units_usage_api_key` не сдвинулся ни разу:
`x-api-units-cost-total-actual` приходит нулём. Отличить «не тарифицируется» от
«счётчик обновляется раз в сутки» сегодня нельзя — проверяется завтра одним
бесплатным запросом. До тех пор ничего в поведении сервиса на этом основании не
меняем: preflight по-прежнему считает по смете.

**Транзакция БД.** Поставка меняет спеки и модель цены; записей не добавляет.
Якорь серии — новое поле запроса, живой провайдер его игнорирует.

**Безопасность.** Ключ нигде не печатается: пробник читает его конфигом, в
выводе только имена полей, формы дат и числа.

**Интеграция.** Модель цены общая у fixture и live: фикстура считает units по
той же формуле, поэтому смета проверяется на тех же числах, что применит бой.

## Spec coverage gaps
- H2 (закрыт ли текущий месяц) — требует повтора через сутки.
- Пересчёт документов экономики — после боевого прогона на сотне доменов.

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD~1 -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 8 code (+11 process docs) / +269/-102 (net +167) |
| commits | 1 |
| time_to_accepted_spec | 0.0h |
| rework_after_done | 0 commit(s) after first phase: handoff |
| harness_hardened | yes — tests/test_collect_endpoints_live.py (новый оракул) |
| implement_retries | 5 — цена refdomains через `flat_cost`, 16 красных тестов схемы, якорь фикстуры, не-ASCII имена переменных, гейт канона |
| verify_fails_before_green | 3 — два неверных ключа списка и семикратная ошибка цены найдены живым ключом |
| est_token_or_cost | ~2 500 units по заголовкам; фактически списано 0 (см. «Чего замер не покрыл») |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
