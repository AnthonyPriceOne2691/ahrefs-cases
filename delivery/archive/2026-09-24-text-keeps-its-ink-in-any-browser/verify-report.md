# Verify report: text-keeps-its-ink-in-any-browser

## Чем проверено

| Что | Чем | Результат |
|---|---|---|
| T1: `Text` без цвета ставит себе `--text-color: currentColor` | `vitest -t 'текст без цвета не берёт чужую переменную'` | passed; **падал до правки** (`expected '' to be 'currentColor'`) |
| T2: цвет из пропса щит не перекрывает | `vitest -t 'цвет из пропса щит не перекрывает'` | passed (страж от перегиба) |
| T3: приглушённый остаётся приглушённым | `vitest -t 'приглушённый текст остаётся приглушённым'` | passed (страж от перегиба) |
| Фронт целиком | `vitest run` | 178 passed |
| Типы и стиль | `tsc --noEmit`, `eslint --max-warnings 0`, `prettier --check` | чисто |

## Исполнение рисковых путей

`web/src/theme.ts` — цвет текста решает каскад браузера, jsdom его не считает.
Исполнено боевой сборкой (`vite build`) в headless Chrome 154 по CDP, at=2026-09-24.

Сборка та же, что на проде: прод отдаёт `index-BrpY5uCT.js` и
`index-nsjzGzL1.css`, локальная сборка с `a6c1122` дала ровно эти имена.
После правки CSS не изменился (`index-nsjzGzL1.css`), изменился только JS.

Замер — скрипт по всем видимым текстам: цвет текста против темы (в тёмной
находка — текст темнее яркости 0.2 не на светлой плотной подложке). Утечка
подложена так, как её ставит расширение: `<style>` с `:root { --text-color:
#000 }` до скриптов страницы.

16 состояний на копии дев-базы (75 проектов, 29 прогонов): прогоны, раскрытый
прогон, смета, шаг 2, проекты, карточки четырёх групп, кейсы, расход, пороги,
раскрытая версия, окно правки, люди, раскрытый человек. Отдельно — подсказка к
Google Sheet (наведение мышью) и пустая база, как на проде.

| Прогон | Результат |
|---|---|
| Старая сборка, тёмная, утечка | нечитаемых текстов на каждом состоянии: 25, 49, 26, 26, 23, 8, 14, 14, 4, 3, 3, 3, 13, 14, 4, 11; с подсказкой к Google Sheet — 48. Совпадает со снимками владельца: `Text` чёрный `(0, 0, 0)`, подписи полей светлые |
| **Новая сборка, тёмная, утечка** | **0** нечитаемых текстов; остаются только выключенные кнопки («Назад» на первой странице, «Скачать PDF» без файла, «Загрузить по ссылке» без ссылки, «Сохранить» у нетронутой формы) — тусклые по замыслу Mantine |
| Новая сборка, светлая, утечка | 0 на всех 16 состояниях |
| Новая сборка, пустая база, тёмная, утечка | 0 (кроме выключенной «Загрузить по ссылке») |
| T5: ссылка в подсказке | `rgb(116, 143, 252)` в старой и новой сборке — цвет `Anchor` не тронут: его правило в CSS Mantine стоит позже правила `Text` |
| **Без утечки: старая против новой** | **32 из 32 снимков совпали до пикселя** (16 состояний × 2 темы) — в чистом браузере правка не меняет ничего |

Колонка примеров в подсказке после правки выглядела тусклее — замер
пикселей: `(128, 128, 128)` до и после. Иллюзия соседства: рядом с чёрным
серое казалось светлее.

## Что осталось за рамками

Выключенные кнопки в тёмной теме — `rgb(105, 105, 105)` на тёмной заливке.
Это норма состояния Mantine, а не дефект контраста; владелец их не отмечал.

Кто именно определяет `--text-color` в браузере владельца, не установлено:
профиль Chrome закрыт защитой macOS. Проверка на стороне владельца — окно
инкогнито (расширения там по умолчанию выключены) до выкладки правки.

## Ревью рисковых мест

**Щит действует на каждый `Text` и на всё, что Mantine строит на нём.**
`Text` берёт стили темы по имени `["Text", __staticSelector]`, поэтому
`Text.extend({ styles })` в `web/src/theme.ts` получают и производные — `Anchor`
в первую очередь. Ссылка цвет не теряет: её правило `.m_849cf0da` стоит в CSS
Mantine позже правила `Text` (`.m_b6d8b162`) и перебивает `currentColor` при
равном весе — замерено в подсказке к Google Sheet, `rgb(116, 143, 252)` до и
после. Если обновление Mantine переставит порядок, ссылки станут наследовать
цвет — но ровно так же они наследовали бы его и без щита (неопределённая
переменная давала то же наследование), то есть новой уязвимости щит не вносит.

**Вложенный `Text` внутри цветного.** Раньше вложенный наследовал саму
переменную родителя (`--text-color: red`), теперь ставит себе `currentColor` и
наследует цвет родителя — тот же красный. Результат совпадает.

**Цвет из пропса.** Щит ставится только без `props.color`, а `vars` Mantine
ложатся поверх `styles` темы (`getStyle`, Mantine 8.3.18) — цвет пропса
выигрывает дважды. Страж T2 держит это тестом.

**Чего щит не закрывает.** Наши собственные токены на `:root` (`--ink`,
`--canvas`, `--glass-fill`) чужое определение того же веса перекрыло бы по
порядку таблиц. Сегодня таких жалоб нет, и проход с подменой их не трогал;
если появятся — лечится тем же приёмом (имя с префиксом или определение на
элементе), а не новым разбором.

## Чего проверка НЕ доказывает

**Что у владельца дело именно в `--text-color`.** Механизм доказан подменой:
одна строка даёт ровно ту же картину, вплоть до границы «`Text` чёрный,
`label` светлый» и цвета `#000`. Но какое расширение (или что-то другое)
определяет переменную в его браузере, не видно — профиль закрыт. Если после
выкладки абзацы останутся чёрными, гипотеза неверна, и разбор продолжается
с этой точки — записано в `observe_signal`.

**Что других таких переменных нет в чужих браузерах.** Скан CSS Mantine нашёл
три правила цвета на неопределённой переменной; два компонента в сервисе не
используются. Наши собственные токены (`--ink`, `--canvas`) определены на
`:root` и чужим определением с тем же весом перекрываются по порядку — эту
поверхность проход не трогал.

## Verdict
- [ ] READY FOR HANDOFF — оракулы зелёные; ждёт подписи human:anthony (verifier)
- [ ] NEED CONVERGE (new tasks)
- [ ] BLOCKED

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base origin/main -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 3 code (+6 process docs) / +70/-1 (net +69) |
| commits | 1 |
| time_to_accepted_spec | n/a (no spec.md in history — class S?) |
| rework_after_done | 0 commit(s) after first phase: handoff |
| harness_hardened | yes — web/src/app/__tests__/text-ink.test.tsx (новый оракул) |
| implement_retries | 2 — `tsc`: TS7006 (тема объектом без типов), TS2322 (`vars` без `--text-color` в типе Mantine); оба до первого зелёного прогона |
| verify_fails_before_green | 0 по продуктовым оракулам (тест и проход в браузере зелёные с первого раза после правки); `delivery_check` дважды красный по форме — строка решений без «вместо», нет вердикта и метрик на handoff |
| est_token_or_cost | n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
