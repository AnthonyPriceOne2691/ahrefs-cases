# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-24 · выбрал щит в теме (`components.Text` в `web/src/theme.ts`) вместо правки 47 файлов с `Text` или перечисления цвета в каждом — потому что уязвимость одна и живёт в компоненте: `color: var(--text-color)` без запасного значения; урок L83 — лечить внутри, а не у каждого потребителя
- 2026-09-24 · выбрал `--text-color: currentColor` на самом элементе (`styles.root` в `Text.extend`, `web/src/theme.ts`) вместо сброса переменной на `body` (`--text-color: initial` в `glass.css`) — потому что сброс на `body` проигрывает определению с селектором `*` или на самом `body`, а свойство на элементе перекрывает любое унаследованное; `currentColor` в свойстве `color` значит «как у родителя» — ровно то наследование, которое Mantine задумывал, и 32 из 32 снимков без утечки совпали до пикселя
- 2026-09-24 · выбрал ставить щит только без пропса `color` (`props.color ? {} : …` в `web/src/theme.ts`) вместо того, чтобы ставить его всегда, — потому что порядок слияния в `getStyle` (`vars` ложатся поверх `styles` темы) и так отдал бы цвет пропсу, но это внутренность Mantine 8.3.18, а не обещание API: при обновлении, поменявшем порядок, щит молча перекрыл бы все цветные `Text`; страж T2 в `text-ink.test.tsx` держит именно это
- 2026-09-24 · выбрал `styles` в `Text.extend` вместо `vars` — потому что `--text-color` Mantine ставит, но в тип переменных `Text` не внёс (`TextCssVariables` без неё), и `vars` не пропускает её без приведения типа (`tsc` дал TS2322); `styles` принимает любые custom properties
- 2026-09-24 · выбрал проверять результат подменой в боевой сборке (Chrome по CDP) вместо одного теста в jsdom — потому что jsdom каскад не считает: тест видит, что щит стоит, но не что текст виден; урок L176 — правка, проигравшая каскаду, молчит
