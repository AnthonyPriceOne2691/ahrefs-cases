# Verify report

**Date:** 2026-09-14
**Verifier:** agent:claude (оракулы и проверка через API), human:anthony (смотрит глазами)
**asserts_reviewed_by:** yes (at=2026-09-14) — листание кейсов проверено на
работающем сервисе, а не только в jsdom: `/api/cases?limit=20&offset=0` отдаёт
`zooplus.de … petalrepublic.com`, `offset=20` — `patient.info … kiwi.com`, то
есть страницы разные и приходят от сервера

## Shape oracles
- [x] PASS — eslint (0 предупреждений), prettier, `tsc --noEmit`
- [x] PASS — `delivery_check`, гейты `pre-commit`

## Behavior oracles
- [x] PASS — vitest: 107 passed (было 106)
- [x] PASS — E1–E4 из `eval-smoke.md`

## Ревью рисковых мест

**Светлая полоса за шапкой — не вкусовая правка.** Причина найдена в рецепте
стекла: заливка панели 0.08 пропускает яркое пятно `--canvas`, а фон страницы
закреплён (`background-attachment: fixed`), поэтому пятно стоит, панели по нему
едут — и полоса появлялась то у одной панели, то у другой. Правка поднимает
заливку до 0.12: панель читается как одна поверхность, стекло остаётся стеклом.
**Проверить это может только человек в браузере:** в jsdom фона нет вовсе, и
оракула на такое не существует.

**Листание кейсов было не «неудобно», а неверно.** Сервер отдавал первые
пятьдесят строк, фронт показывал их как всю библиотеку — и на стенде с 67
кейсами шестнадцать не существовали для пользователя. Нарезка уже полученного
повторила бы ту же ложь, поэтому взят `offset`, который у эндпоинта был всегда.

**Двадцать строк — не круглое число из головы.** Пятьдесят не помещаются на
экран, и листалка оказывается за краем: человек узнаёт о ней, домотав до конца.
То же число стоит в тесте листания, и разойдясь с экраном, тест перестанет
проверять листание — поэтому в тесте оно названо и объяснено.

**Смена тумблера версий возвращает на первую страницу.** Список становится
другой длины, и третьей страницы прежнего в новом может не быть вовсе. Тот же
приём уже стоял у фильтров проектов — теперь он и у кейсов, и оракул это держит.

**Выравнивание.** Домен слева, остальное по центру: домен читают сверху вниз
как имя строки. Центрировать имена разной длины значит получить рваный край, по
которому глаз не ведёт.

**Деньги и безопасность.** Не затронуты: правки в вёрстке и в параметрах
запроса списка. Ни одного нового обращения к Ahrefs.

**Экран людей.** Выравнивание то же, что у проектов и кейсов: почта слева,
остальное по центру. Панель человека раскрывается движением и сворачивается им
же; при переходе к другому текущая сначала уезжает, и только потом приезжает
новая — подмена содержимого под открытой панелью читается как подмена, а не как
переход. Держится это двумя состояниями: `chosen` (кого показываем) и `shown`
(что ещё на экране, пока едет вниз).

**Третьей кнопки у права больше нет, и это смысловая правка, а не подпись.**
«Как в группе» называла не право, а способ хранения. Теперь показывается итог —
«да» или «нет», — и он встаёт по группе сам: перевёл человека в инженеры, и
«править пороги» стало «да». Хранение осталось трёхзначным нарочно: решение,
совпавшее с группой, **не записывается**, поэтому право продолжает ехать за
группой; записывается только отличие, и оно переживает перевод. Нажав «да»
инженеру и записав его, мы приколотили бы право намертво — а руководитель
нажимал «да», а не «навсегда». Оба случая держат оракулы.

**Чего оракулы не покрывают.** Само движение: в jsdom нет разметки и переходов,
а `Collapse` держит содержимое в DOM даже свёрнутым. Проверяется решение,
которое движением показывают (`data-selected` у строки), — плавность смотрит
человек.

## Spec coverage gaps

- «Вперёд» гаснет по признаку «страница неполная», а не по общему числу строк:
  сервер количества не отдаёт. Последняя ровно полная страница поэтому даёт
  ещё один клик в пустоту — честно, но небезупречно; чинится счётчиком в API.

## Verdict
- [x] READY FOR HANDOFF
