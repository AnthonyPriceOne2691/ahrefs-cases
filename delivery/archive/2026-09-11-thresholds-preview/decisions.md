# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-11 · выбрал считать предпросмотр запросом `previewRuleset` вместо расчёта на экране — потому что тот же проход `verdicts.evaluate` считает и пересчёт, а вторая реализация разошлась бы с первой ровно на калибровке, где по предпросмотру утверждают порог
- 2026-09-11 · выбрал показывать четыре исхода отдельными блоками в `PreviewPanel.tsx` вместо одного числа «изменится N» — потому что за ним прячутся разные действия человека: смена группы обсуждается, «данных не хватает» требует докупить месяцы, а «впервые» означает, что проект вообще не был классифицирован
- 2026-09-11 · выбрал зелёную плашку только при `allCounted` (`missing_data.length === 0`) вместо всегда зелёной — потому что на живом стенде «никто не сменит группу» стояло над «данных не хватает: 6» из 11 проектов, то есть успокаивало, не посчитав половину базы
- 2026-09-11 · выбрал вынести словарь названий групп в `pages/groups.ts` вместо второй копии в `PreviewPanel` — потому что прогон показал «good → poor» посреди русского экрана, а `GroupBadge` те же значения уже переводил: одно имя в двух местах расходится молча
- 2026-09-11 · выбрал останавливать всплытие на кнопке предпросмотра (`event.stopPropagation()` в `VersionsTable.tsx`) вместо общего обработчика строки — потому что клик по строке выбирает версию, а клик по кнопке считает: без остановки одно нажатие делало бы оба
