# Tasks: читающие роутеры

Уроки архива, применимые к диффу: L41, L32/L34, L1, L13, L51, L8 — см. plan.md.

## Схемы
- [x] проект в списке, карточка, вердикт с объяснением
- [x] кейс в библиотеке, расход units

## Роутеры
- [x] `GET /api/projects` с фильтрами и границей выдачи
- [x] `GET /api/projects/{id}` — вердикт, точки, ряды
- [x] `GET /api/cases` и `GET /api/cases/{id}/download`
- [x] `GET /api/usage`
- [x] все за правом `read`

## Границы
- [x] лимит с потолком, а не «сколько попросят»
- [x] нет проекта — 404; нет файла — 404

## Проверки
- [x] E1–E10
