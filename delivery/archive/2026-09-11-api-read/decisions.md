# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.
- 2026-09-11 · выбрал лимит с потолком (100) и смещение вместо курсорной пагинации — потому что объём известен: сотня проектов на прогон, и в отладке `?limit=20&offset=40` читается глазами, а курсор нет
- 2026-09-11 · выбрал отдавать записанный вердикт вместо пересчёта на лету — потому что карточка обязана показывать то, по чему принято решение (урок L41), а пересчёт живёт командой `recalc` со своей версией порогов
- 2026-09-11 · выбрал 404 при отсутствии файла на диске вместо пустого ответа — потому что путь в базе и файл на диске расходятся при переносе и ручной чистке, и «скачалось ничего» неотличимо от «кейса нет»
- 2026-09-11 · выбрал фабрику `build_quota()` вместо умолчания `options.quota or FixtureQuota()` в раннере — потому что умолчание вызывающего делало preflight в живом режиме бессмысленным: смета сверялась с фикстурными 10 000, а не с остатком Ahrefs
- 2026-09-11 · выбрал строить движок в тестах с `NullPool` вместо подмены `get_engine` — потому что функцию импортируют по имени в нескольких местах, и подмена оставила бы часть кода на пулированном движке: два движка вместо одного
