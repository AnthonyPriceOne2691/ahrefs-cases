# Observed in production

**Поставка:** 2026-09-10-f1-skeleton
**Наблюдали:** 2026-09-24 (первая выкладка на общий сервер, `main` 8b80c53) → 2026-09-24
**Сигнал из STATUS:** A1–A5 из spec подтверждены прогоном; `alembic upgrade head` на чистой базе проходит в чистом клоне

До 24.09.2026 прода не было, поэтому окно наблюдения открылось только с
выкладкой. Установка с нуля — ровно тот случай, про который сигнал: чистый
клон в `/srv/ahrefs-cases`, пустой том базы, первый `docker compose up -d`.

## Примеры спеки против прода

| id | Обещано в спеке | Что в проде | Вердикт |
|---|---|---|---|
| A1 | `alembic upgrade head` на пустой базе без ошибок; `downgrade base` тоже | сервис `migrate` — `Exited (0)`, в журнале 6 × `Running upgrade`, клон на `8b80c53`. `downgrade base` в проде не запускали: он сносит таблицы боевой базы | upgrade ПОДТВЕРЖДЁН; downgrade в проде не проверяется по построению |
| A2 | `GET /api/health` при поднятой базе → `200`, `{"status": "ok", "migration": "<revision>"}` | код 200, `status: ok`, `migration` совпадает с `alembic heads` на сервере, `provider: fixture` | ПОДТВЕРЖДЁН (поле `provider` — добавка более поздней поставки) |
| A3 | `GET /api/health` при остановленном postgres → `503` с причиной | в проде не проверяли: останавливать боевую базу ради наблюдения нечестно по отношению к тем, кто в ней работает | НЕ НАБЛЮДАЛСЯ — остаётся на тестах |
| A4 | импорт конфига без `.env` → дефолты, `AHREFS_PROVIDER == "fixture"` | боевой образ, запущенный без окружения: `provider=fixture key_set=False` | ПОДТВЕРЖДЁН |
| A5 | `AHREFS_PROVIDER=live` без ключа → внятная ошибка на старте | в проде не проверяли: живой режим на сервере включает только владелец (`AGENTS.md`), даже без ключа | НЕ НАБЛЮДАЛСЯ — остаётся на тестах |

## Метрика: стало ли хуже

Не применимо: поставка — скелет, метрики «до» у неё нет.

## Что сделано по результату

- [x] Расхождений нет — наблюдение закрыто. Два примера (A3, A5) в проде не
      наблюдаются по построению и названы здесь, а не пропущены молча.
