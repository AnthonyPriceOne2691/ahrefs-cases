# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.

- 2026-09-10 · выбрал не заводить пустые роутеры вместо каркасов всех семи роутеров сразу — потому что роутер без реализации попадает в схему OpenAPI и обещает фронту то, чего нет; каркас экономит минуты, ложное обещание стоит отладки на Ф6
- 2026-09-10 · выбрал в `/api/health` собственную короткую пробу базы с таймаутом вместо сессии из зависимости FastAPI — потому что при мёртвой базе ошибка возникала до входа в `try` обработчика и наружу шла 500 без причины вместо 503 с причиной (A3)
- 2026-09-10 · выбрал писать `DROP TYPE` в downgrade руками вместо доверия alembic autogenerate — потому что ENUM-типы создаются неявно и не удаляются: цикл upgrade → downgrade → upgrade падал на `DuplicateObject`, то есть миграция была одноразовой
- 2026-09-10 · выбрал переписать правило гейта `service-no-web` под раскладку проекта (`scripts/lint/adapted.json`) вместо waiver на канонное правило — потому что канонный фильтр искал `services/`, которого здесь нет, и гейт был зелёным на нуле просмотренных файлов; waiver зафиксировал бы это как «нарушений нет»
- 2026-09-10 · выбрал обновлять `pip` в venv гейтов (`python -m pip install -U pip` в job `gates`, `.github/workflows/quality.yml`) вместо waiver на PYSEC самого pip — потому что waiver объявлял бы проблему вместо решения: 7 записей PYSEC на `pip` 25.0.1 из образа раннера, ценой одной строки в workflow против 7 постоянных исключений в `deps_audit_baseline.txt`
- 2026-09-10 · выбрал `ci-oracles: tooling` (required status checks выключены, защита main держится на ruleset id 22794937 + `.github/workflows/main-guard.yml` + `scripts/merge_guard.sh`) вместо `deployed` — потому что required checks закрывают прямой push в main, а весь поток Ф1 это 7 прямых коммитов одного исполнителя: цена включения сегодня — PR на каждый коммит при нуле ревьюеров
- 2026-09-10 · выбрал `populate_by_name=True` в `model_config` (`src/ahrefs_cases/config/_base.py`) вместо приёма только алиасов переменных окружения — потому что вариант с алиасами **не поддерживает** конструирование по имени поля: `AhrefsSettings(provider="live")` уходил в `extra="ignore"`, и 1 из 5 примеров спеки (A5, fail-fast без ключа) был бы зелёным на дефолтном `fixture`, не проверив ничего
