# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-24 · выбрал держать правило одним предикатом прогона (`_live_run` в `collect/budget.py`) и звать его из всех сумм вместо фильтра по режиму в роутере `usage.py` — потому что фильтр в роутере был бы второй копией правила, а величина разошлась ровно так: правило L116 стояло в `live_spend_since`, роутер написал свою сумму по тому же журналу, и на дев-базе «потрачено» стало 27 366 вместо 11 952
- 2026-09-24 · выбрал показывать условные units отдельной строкой («Условные units: N — столько стоили бы прогоны без живого ключа…», поле `conditional`) вместо того, чтобы спрятать их совсем, — потому что без неё экран говорил бы «потрачено 0» рядом с журналом прогонов, где у каждого fixture-прогона «факт 1 936»: новое расхождение вместо старого и без слов; докстрока `budget.py` прямо говорит, что фикстуры считают units, чтобы экран не разрабатывался на нулях
- 2026-09-24 · выбрал строку текста вместо пятого бейджа рядом с «потрачено» — потому что бейджи ряда — числа одного счёта (потрачено, резерв, остаток, вычет), и условные units среди них читались бы как часть расхода; урок L191 — гипотетическая величина называет свою гипотезу, а в бейдж она не помещается
- 2026-09-24 · выбрал считать условным всё, что не живое (`everything - live` в `spend_summary`), вместо отбора `provider == 'fixture'` — потому что тогда живой и условный расход делят журнал без остатка и сумма сверяется одним SQL; на дев-базе это 1 unit прогона без режима (след старого теста), который при отборе по `fixture` не попал бы никуда
- 2026-09-24 · выбрал знаменателем стоимости на сто домены, оплаченные живьём (`count(distinct target)` по тем же строкам, что `live`), вместо проектов из `run_items` живых прогонов (первая редакция) — потому что удаление проектов, которое готовится параллельно каскадом, обнуляет `project_id` у судеб прогона (`ondelete=SET NULL`), и знаменатель по проектам уменьшался бы без единого запроса (R10: 1 вместо 2), а журнал расхода удаление не трогает; цена — домен из кэша или прерванный предохранителем в знаменатель не входит, и экран подписан «оплачено доменов»; предложено координатором по итогам разведки удаления
- 2026-09-24 · выбрал знаменатель из журнала вместо прежнего `count(Project)` — потому что все проекты базы разбавляют стоимость проектами, за которые живьём не платили: на дев-базе 75 проектов против 62 оплаченных доменов, 15 936 вместо 19 277 на сто; на проде, по данным координатора, 53 против 51 — 19 736 вместо 20 510
- 2026-09-24 · выбрал показать знаменатель рядом со стоимостью на сто (поле `live_domains`, «потрачено 11 952 units, оплачено доменов — 62») вместо одного итогового числа — потому что знаменатель этой поставкой и меняется, а метрику заказчик сверяет; число, выведенное из невидимого делителя, проверить нельзя (L87)
- 2026-09-24 · выбрал оставить цену отдельного прогона (`run_spend`, `Run.units_actual`, «смета → факт» в журнале и «потрачено units N» в строке прогона) как есть вместо перевода на правило живых units — потому что у прогона режим один и смешения в его числе нет; что журнал не называет режим прогона — вопрос экрана прогонов, это четвёртый и пятый модуль (`api/routers/runs.py`, `web/src/pages/runs/`), а поставка по правилу проекта — два-три модуля
- 2026-09-24 · выбрал не трогать `purchases.bought_metrics` и `reserved_units` вместо того, чтобы подвести их под тот же предикат, — потому что первый отвечает «покупали ли», а не «сколько», и его смешение режимов — другой экран (карточка проекта), вынесенный в открытые вопросы; второй — резерв идущего прогона, а не расход, и режимы в нём встречаются, только если API и воркер запущены в разных режимах (L197)
- 2026-09-24 · выбрал мерить экран в тесте API приращением от своих прогонов (`test_api_usage.py`) вместо абсолютных чисел — потому что база общая: на стенде 27 366 units чужих прогонов, приложение ходит своими соединениями, и откат транзакции его не изолирует (L68); точные значения проверяются на пустой базе в `test_budget_spend.py`
- 2026-09-24 · выбрал разнести новые тесты экрана в отдельный `describe` и вынести общий `USAGE` на уровень модуля вместо дописывания в `describe('расход units')` — потому что eslint `max-lines-per-function` насчитал блоку 105 строк при пороге 80, а ратчет предупреждений стоит на нуле
- 2026-09-24 · выбрал снять из E7 проверку `/прогонов не было/` вместо её правки на месте — потому что это ровно тот текст, который поставка меняет, и его новую редакцию держит R6 (`queryByText(/прогонов не было/)` там отрицательный); E7 остаётся про неизвестный остаток
