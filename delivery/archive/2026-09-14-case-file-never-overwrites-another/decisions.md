# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-14 · выбрал общее правило `pdf_renderer.unique_name` для архива и записи на диск вместо копии в каждом пути — потому что правило уже было написано (`archive._unique`), но действовало только на ZIP: запись файла затирала молча, а это второй из двух путей выдачи — ровно класс урока L142
- 2026-09-14 · выбрал сравнивать содержимое перед разведением имени вместо того, чтобы всегда добавлять «(2)» — потому что рендер детерминирован (`test_render_is_deterministic`), и повторный `render` того же кейса иначе плодил бы близнецов: на стенде за две пересборки `ahrefs.com` появилось два файла с одинаковым sha256 `46eeca70a161`, и контрольная сумма артефакта перестала бы отличать «пересобрали» от «переименовали»
- 2026-09-14 · выбрал сравнивать только кандидатов этого имени (`X — Кейс.pdf` и `X — Кейс (N).pdf`) вместо всех файлов каталога — потому что чужое имя с тем же содержимым принадлежит другому кейсу, и трогать его нельзя; в `data/out/` на стенде лежит 80 файлов, и полное сравнение читало бы их все на каждый рендер
