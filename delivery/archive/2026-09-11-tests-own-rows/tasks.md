# Tasks: тесты удаляют только своё

Уроки архива, применимые к диффу: L79, L8, L68, L58 — см. plan.md.

## Уборка
- [x] общий помощник: удаление по владению
- [x] четыре модуля переведены на него
- [x] прогоны удаляются по своему пользователю, чужие остаются

## Оракул
- [x] `delete(Model)` без условия в `tests/` краснеет
- [x] выход пометкой с причиной

## Проверка
- [x] E1–E6, из них E4 и E5 — исполнением на дев-базе
