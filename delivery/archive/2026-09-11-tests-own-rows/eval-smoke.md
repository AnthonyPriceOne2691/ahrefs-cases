# Eval smoke — this shipment

- [x] E1 — `delete(Model)` без условия краснеет с файлом и строкой
- [x] E2 — `delete(Model).where(...)` проходит
- [x] E3 — помеченный выход проходит
- [x] E4 — контрольный проект с точками и вердиктом пережил прогон
- [x] E5 — контрольный прогон и расход units пережили прогон
- [x] E6 — все тесты зелёные
