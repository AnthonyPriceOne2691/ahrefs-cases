# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-13 · выбрал нормализовать `--only` в CLI (`_canonical` в `scripts/run_collect.py`) вместо нормализации внутри `collect_all` — потому что контракт `layers` в `.importlinter` держит `collect` и `intake` на одном уровне: импорт между ними запрещён, и обход контракта ради удобства дороже вызова у того, кто и так знает оба модуля
- 2026-09-13 · выбрал отказ исключением (`OnlyNotADomainError`, код возврата 2) вместо пропуска плохого имени с пометкой, как это делает приём, — потому что в `--only` человек называет ровно то, что хочет собрать: пропустить названное молча значит собрать не то, о чём просили, а в приёме на сотню строк одна плохая — рядовой случай
- 2026-09-13 · выбрал печатать превращение имени («домен приведён к канону: X → Y») в `scripts/run_collect.py` вместо тихой нормализации — потому что `collect_all` печатает отчёт по каноническому имени, и в журнале прогона оно другое, чем набрал человек, и без этой строки расхождение выглядит как чужой проект: `_canonical` печатает «X → Y» ровно тогда, когда канон не совпал с вводом
- 2026-09-13 · выбрал называть в журнале обе ошибки цепочки (`failure_reason` в `collect/run_journal.py`) вместо последней — потому что живой прогон записал `MissingGreenlet` там, где причиной был `AttributeError` на поле конфига: ошибка, случившаяся при обработке другой, почти всегда следствие, и чинили бы событийный цикл вместо перезапуска воркера: `failure_reason` обходит `__cause__`/`__context__` и называет первопричину, а `runner._fail_run` и `workers._run_guarded` пишут одним вызовом: без него каждый второй разбор падения потребовал бы логов контейнера, которых у оператора нет
- 2026-09-13 · выбрал обрезать причину до 400 символов вместо полной трассировки — потому что `Run.error` показывается строкой в таблице прогонов: трассировка там нечитаема, а оператору нужна причина, а не кадры стека
