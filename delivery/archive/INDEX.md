# Archive index

Что это: адресный указатель по `archive/`. Перед implement найди строки, чьи пути
пересекаются с твоим диффом, прочитай их уроки и сошлись на id в `plan.md`
(M/L) или `tasks.md` (S). Не применимо — напиши одной строкой почему.

Правила ведения:
- Строка добавляется на handoff, вместе с переносом поставки в `archive/`.
- Пути — префиксы от repo-root, в backticks. Только те, где жил урок.
- Урок — что мы **узнали о системе**, а не что сделали. «Добавил кэш» — не урок.
  «Пустой robots.txt и недоступный robots.txt — разные случаи, мы их слили» — урок.
- Урок без id не считается: id — то, чем на него ссылаются.
- Поставка без урока — строка с `—` в колонке. Это законно и означает «ничего
  нового не узнали»; частое `—` подряд означает, что разборы стали формальными.

| Дата | Поставка (каталог) | Пути | Урок |
|---|---|---|---|
| 2026-07-20 | `2026-07-20-robots-cache` | `backend/features/robots/` | **L1:** пустой robots.txt и недоступный (500) — разные случаи; слитые в один дают «запретить всё» на аварии апстрима. Тест A3 в той поставке |
| 2026-07-24 | `2026-07-24-billing-retry` | `backend/features/billing/`, `frontend/src/billing/` | **L2:** ретрай по таймауту без идемпотентного ключа удваивает списание; ключ обязан приходить от вызывающего, а не генерироваться внутри |
| 2026-07-26 | `2026-07-26-typing-pass` | `backend/features/` | — |
| 2026-09-10 | `2026-09-10-contour-wave-0` | `delivery/`, `scripts/`, `.git/info/exclude` | **L3:** `.git/info/exclude` действует только на НЕотслеживаемые пути — после `git rm --cached` любой `git add -A` возвращает файл в индекс. Правильная команда в неправильном порядке выглядит как выполненная; различитель — `git ls-files`, а не отсутствие файла в `git status` |
| 2026-09-10 | `2026-09-10-f1-skeleton` | `src/ahrefs_cases/config/`, `tests/test_config.py` | **L4:** в `pydantic-settings` `populate_by_name` выключен по умолчанию — конструктор принимает только алиас переменной окружения, а поле по имени молча уходит в `extra="ignore"`. Тест, который конструирует `Settings(field=…)`, зелёный и не проверяет ничего: объект собран с дефолтом. Различитель — подать заведомо неверное значение и убедиться, что тест краснеет |
| 2026-09-10 | `2026-09-10-f1-skeleton` | `migrations/`, `tests/test_migrations.py` | **L5:** alembic autogenerate создаёт ENUM-типы неявно и не пишет `DROP TYPE` в downgrade. Миграция работает ровно один раз: upgrade → downgrade → upgrade падает на `DuplicateObject`. Одиночный `upgrade head` этого не показывает — нужен цикл, и проверка типов **после** downgrade, а не только таблиц |
| 2026-09-10 | `2026-09-10-f1-skeleton` | `.github/workflows/quality.yml`, `scripts/lint/` | **L6:** отдельный venv для job'а гейтов без установки самого проекта — гейт зелёный, потому что смотрит не туда: mypy не находит плагины (`pydantic.mypy`), `pip-audit` аудитит собственную оснастку контура вместо зависимостей продукта. Тот же класс, что фильтр гейта под чужую раскладку: **число просмотренных файлов важнее цвета** |
| 2026-09-10 | `2026-09-10-f2a-intake-fixtures` | `delivery/active/STATUS.md`, `.github/workflows/quality.yml` | **L7:** объявление, живущее прозой, теряется при первом переносе поставки. `stack-selftest: external` стояло абзацем в разделе STATUS Ф1, CI ищет его `grep 'stack-selftest:'` — при открытии следующей поставки оно не переехало, и шаг «Canon payload selftest» упал. Различитель: то, что читает машина, обязано быть полем, а не текстом рядом |
| 2026-09-10 | `2026-09-10-f2a-intake-fixtures` | `tests/conftest.py` | **L8:** дев-база живёт между прогонами и копит следы ручных запусков CLI. Тест, читающий «все проекты», начинает зависеть от того, гонял ли кто-то скрипт полчаса назад — тот же класс, что тест health'а в Ф1 (L-случай «зелёный по причине окружения»). Лечение: `TRUNCATE` **внутри** тестовой транзакции — чужие данные возвращает откат |
| 2026-09-10 | `2026-09-10-f2a-intake-fixtures` | `src/ahrefs_cases/collect/fixtures/generator.py` | **L9:** `hash()` строки в Python рандомизируется между процессами (PYTHONHASHSEED). Генератор, «детерминированный» по `hash(домен)`, даёт разные данные в CI и локально: golden-таблицы краснеют через раз без единой правки кода. Зерно обязано быть стабильным — `sha256`, а не встроенный хеш |
| 2026-09-10 | `2026-09-10-f2a-intake-fixtures` | `src/ahrefs_cases/intake/gsheet_source.py` | **L10:** закрытая Google Sheet отвечает на CSV-экспорт **страницей входа со статусом 200**. Разбор такого ответа как CSV даёт «принято 0, отклонено 0» — ложь успехом вместо ошибки доступа. Различитель — HTML-маркер в начале тела, а не код ответа |
| 2026-09-10 | `2026-09-10-f2b-units-economy` | `src/ahrefs_cases/collect/runner.py`, `src/ahrefs_cases/collect/breaker.py` | **L11:** `asyncio.as_completed` создаёт задачи для ВСЕХ корутин сразу, поэтому проверка «пора ли останавливаться» снаружи семафора успевает пройти до первой неудачи: предохранитель не срабатывал вовсе (10 запросов вместо 3). Решение о продолжении принимается там же, где берётся ресурс, — под семафором |
| 2026-09-10 | `2026-09-10-f2b-units-economy` | `migrations/versions/`, `src/ahrefs_cases/storage/_enums.py` | **L12:** `sa.Enum(SomeStrEnum)` хранит в postgres **имена** членов (`SKIPPED_ABORTED`), а не значения (`skipped_aborted`). `ALTER TYPE ... ADD VALUE` с значением проходит зелёным, а падает первая вставка. Проверять надо `enum_range`, а не успех миграции |
| 2026-09-10 | `2026-09-10-f2b-units-economy` | `src/ahrefs_cases/collect/run_journal.py`, `src/ahrefs_cases/collect/runner.py` | **L13:** прогон, где у проекта несколько задач (шаг 2 воронки — четыре endpoint'а), нельзя описывать счётчиками задач: отчёт печатал «проектов 3, собрано 12». Исход сворачивается по проекту, число задач показывается отдельно. Нашлось сквозным прогоном, ни один тест этого не ловил |
| 2026-09-10 | `2026-09-10-f2b-units-economy` | `delivery/`, `.secrets.baseline` | **L14:** `detect-secrets` считает секретом hex-идентификаторы ревизий alembic. Коммит отклоняется, а вывод `git commit` легко принять за успешный — я дважды едва не отчитался о несделанном. Различитель — `git log`, а не отсутствие ошибки в выводе; лечение — `pragma: allowlist secret` на строке, не расширение baseline |
| 2026-09-10 | `2026-09-10-f2b-units-economy` | `delivery/archive/2026-09-10-f2b-units-economy/STATUS.md` | **L15:** требование, пришедшее в середине поставки, — повод пересмотреть её границы, а не дописать в неё ещё один блок. Ф2б открывалась на 10 примеров, закрылась на 18 и 2521 строку при пороге 800: класс L под waiver вместо двух проверяемых по отдельности поставок |
| 2026-09-10 | `2026-09-10-f2b-units-economy` | `.github/workflows/quality.yml`, `delivery/STACK-ACCEPTANCE.md` | **L16:** продолжение L7. Шаг CI искал объявление варианта D только в `delivery/active/STATUS.md`, а этот файл по §2.3a **не существует между поставками** — значит CI краснел каждый раз, когда поставка уходила в архив, без ошибки исполнителя и без способа починить, не открыв новую поставку. Свойство проекта не должно жить в файле, который по построению исчезает; объявление переехало в `delivery/STACK-ACCEPTANCE.md`, адаптация записана в `scripts/lint/adapted.json` |
| 2026-09-10 | `2026-09-10-f3a-verdict` | `src/ahrefs_cases/classify/points.py`, `config/thresholds.example.yml` | **L17:** усреднение по окну у границы периода **не защищает** от одиночного выброса, а лишь делит его: месяц с удвоением на плоской серии даёт +50 % при окне 2, +25 % при 4, +16,7 % при 6 — выше порога «средних» (+10 %) при любом практичном окне. Защита от выбросов — медиана, а не окно; окно решает другую задачу (сгладить нормальный разброс) |
| 2026-09-10 | `2026-09-10-f3a-verdict` | `src/ahrefs_cases/classify/rules.py` | **L18:** в правилах, которые сами себя объясняют, нельзя судить решение по всем записям объяснения. Информационные факты («подтверждающая метрика не сработала» при `supporting_required: 0`) валили группу, хотя ничего не требовали. Решающее условие и объяснительный факт — разные вещи, и различие должно быть в типе, а не в договорённости |
| 2026-09-10 | `2026-09-10-f3a-verdict` | `tests/test_classify_golden.py` | **L19:** golden-таблицу нельзя строить на форме, стоящей у порога: `weak_growth` (×1.12) при пороге +10 % и шуме ±3 % даёт `poor` на шести доменах из двенадцати и `medium` на остальных. Такое соответствие зелено по случайности выбранного домена; честный выход — убрать сценарий из таблицы и поставить тест, стерегущий сам разброс |
| 2026-09-10 | `2026-09-10-f3a-verdict` | `AGENTS.md`, `delivery/archive/2026-09-10-f3a-verdict/STATUS.md` | **L20:** продолжение L15. Резать фазу пополам недостаточно: Ф3а резалась заранее и всё равно дала 1584 строки при пороге 800 (второе превышение подряд). Единица поставки — два-три модуля, а не половина фазы; правило записано в `AGENTS.md`, потому что предохранитель, срабатывающий каждый раз, перестаёт быть сигналом |
| 2026-09-10 | `2026-09-10-f3a-verdict` | `config/thresholds.default.yml`, `src/ahrefs_cases/config/classify.py` | **L21:** тест, стоящий на файле, который по варианту D **вне репозитория** (пороги заказчика в `.git/info/exclude`), зелёный только на машине автора: в CI он падает на отсутствии файла, и никакая аккуратность в самом тесте этого не лечит. Проекту нужны нейтральные умолчания В репозитории, а клиентские данные подставляются развёртыванием; проверка на настоящих данных остаётся, но пропускается с явной причиной. Найдено наблюдением по `observe_signal`, а не ревью |
| 2026-09-10 | `2026-09-10-hotfix-live-mode` | `src/ahrefs_cases/collect/endpoints.py`, `scripts/probe_units.py` | **L22:** цену внешнего API нельзя выводить из документации — только замерять. Мы читали «минимум 50 units за запрос, сверху зависит от строк и полей» и построили модель «50 за запрос»; замер показал построчный биллинг `max(50, строки × (10×поле + 1))`, то есть занижение в девять раз. Один запрос за 100 units дал больше, чем весь разбор документации |
| 2026-09-10 | `2026-09-10-hotfix-live-mode` | `src/ahrefs_cases/collect/response_guard.py`, `src/ahrefs_cases/collect/quota.py` | **L23:** `.get(key, default)` на чужом API — не аккуратность, а стирание разницы между «формы не та» и «данных нет». У нас пустота имеет смысл («у домена нет истории»), поэтому одна опечатка в имени ключа дала бы сто доменов, помеченных пустыми, и успешный отчёт при нуле собранных данных. Разведка подтвердила: имена полей квоты действительно другие |
| 2026-09-10 | `2026-09-10-hotfix-live-mode` | `src/ahrefs_cases/collect/run_reaper.py`, `src/ahrefs_cases/config/ahrefs.py` | **L24:** перенося чужой паттерн, переноси и опору, на которой он держится. Реапер из CRM судит о смерти прогона по возрасту, и это верно только потому, что там есть жёсткий таймаут RQ-джобы. У нас очереди нет — паттерн приехал без опоры и добивал живые прогоны (120 минут против порога 60). Опору пришлось создать: лимит прогона плюс инвариант конфига |
| 2026-09-11 | `2026-09-11-collect-stage1-price` | `src/ahrefs_cases/collect/endpoints.py`, `src/ahrefs_cases/config/ahrefs.py` | **L25:** докстринг, который называет условие своей неверности, — рабочий инструмент, а не украшение. `history_lead_months` говорил: «цена запроса не зависит от числа строк, поэтому запас почти бесплатен — но если Ф7 покажет построчный биллинг, уменьшать надо будет здесь». Ф7 показал, и правка нашлась чтением, а не расследованием: 33 units на домен за данные, которых не читает никто. Различитель — писать в докстринге не только «почему так», но и «при каком открытии это перестанет быть верным» |
| 2026-09-11 | `2026-09-11-collect-scheme-planner` | `src/ahrefs_cases/collect/scheme.py` | **L26:** когда интервал упирается в границу, сдвигается одна его сторона, а вторая остаётся — и интервал переворачивается. У нас `max(1, месяцы+1)` превращал перевёрнутое окно в «запрос на одну строку»: 50 units за бессмысленный интервал, а ответ выглядел бы как «у домена нет истории». Защита — инвариант на конструкторе (построить такое окно нельзя вообще), а не проверка в вызывающем: вызывающих будет больше одного |
| 2026-09-11 | `2026-09-11-collect-scheme-planner` | `src/ahrefs_cases/collect/cache.py` | **L27:** watermark «собрано по такой-то месяц» описывает только **непрерывную** историю. Как только данные покупаются двумя разъединёнными окнами, граница от позднего окна объявляет купленным всё между ними, и раннее окно не покупается никогда. Кэш обязан спрашиваться по окну, а не по проекту; для окна решение бинарное, потому что запрос на один месяц и на четыре стоят одинаково |
| 2026-09-11 | `2026-09-11-collect-scheme-planner` | `src/ahrefs_cases/classify/verdicts.py`, `docs/UNITS_OPTIMIZATION.md` | **L28:** прежде чем обходить чужое правило, посмотри, на какие данные оно смотрит. Правило достоверности серии казалось несовместимым со схемой «две точки» (Z6) и стоило бы поставки на правку классификации — но `max_gap_months` считается только по `org_traffic`, то есть по шагу 1. Разреженность шага 2 его не задевает вовсе, а деньги именно в шаге 2: строка `keywords-history` стоит 51 unit против 11. Маршрут вместо правки |
| 2026-09-11 | `2026-09-11-collect-scheme-report` | `delivery/`, `AGENTS.md` | **L29:** гейт копипаста — индикатор границы поставки, а не только стиля. Дробление 990 строк «чистое правило отдельно от провода» дало два clone-пары: арифметика месяцев жила бы в двух местах, пока провод не приехал. Резать пришлось иначе — 154 / 784 / 94, — и это была подсказка гейта, а не догадка. Заодно измерено, почему порог 800 спорит с правилом «два-три модуля»: 43 % диффа в этом проекте — докстринги и комментарии |
| 2026-09-11 | `2026-09-11-f3b-recalc` | `src/ahrefs_cases/classify/coverage.py` | **L30:** «данных не покупали» и «данных нет» — разные состояния, и проверка покрытия сливает их по умолчанию. Написанная как «каких месяцев окна нет в серии», она превращает законный `insufficient_data` (домен без истории, дыра у Ahrefs) в пропуск и показывает заказчику ложную причину: первое чинится деньгами, второе — решением о проекте. Различитель — **собранный отрезок**: месяц внутри него дыра, вне — непокупленный |
| 2026-09-11 | `2026-09-11-f3b-recalc` | `src/ahrefs_cases/classify/coverage.py`, `src/ahrefs_cases/collect/scheme.py` | **L31:** экономия сбора сделала пороги не полностью переигрываемыми, и это свойство системы, а не дефект. Данные покупаются под ту версию порогов, что действует **в момент сбора** (окна точек, `pre_start_baseline_months`), поэтому пересчёт по другой версии может попросить месяцы, которых никто не покупал. Пока история бралась с запасом «на всякий случай», зависимости не было; с 11.09.2026 она есть, и пересчёт обязан её называть, а не усреднять то, что нашлось |
| 2026-09-11 | `2026-09-11-f3b-preview` | `src/ahrefs_cases/classify/preview.py` | **L32:** в любом отчёте «что изменится» состояний не два, а больше, и лишние сливаются в «не изменилось» сами собой. У предпросмотра их четыре: сменит группу, получит вердикт впервые, данных этой версией не хватает, действительно остался прежним. Первые два молчат как «всё в порядке», а различаются действиями: один требует классификации, другой — денег на догруз. Различитель — считать не «изменилось/нет», а перечислять состояния явно, каждое со своей строкой в отчёте и своим тестом |
| 2026-09-11 | `2026-09-11-f3v-diagnose` | `src/ahrefs_cases/classify/thresholds.py` | **L33:** поле, которое разбирается из чужого документа и не применяется нигде, **хуже отсутствующего**. `duration.normalize_after_months` жил в порогах с Ф3а: модель его принимала, код игнорировал, предупреждения не было. В документе порогов он выглядит рабочим, а в Ф6 станет полем ввода — поставив 12 на калибровке, заказчик увидел бы прежние группы и спорил бы о порогах вместо разговора о том, что формулы до сих пор нет. Отказ обязан стоять **на разборе**, а не в одном из путей: тогда его видят и классификация, и пересчёт, и предпросмотр |
| 2026-09-11 | `2026-09-11-f3v-diagnose` | `src/ahrefs_cases/classify/diagnose.py` | **L34:** повтор L32 на новом месте, за два дня второй раз. Там, где отчёт отвечает «что случилось», состояний всегда больше двух: у трафика их четыре — упал, не рос, не менялся, данных нет. Слить их в «нет роста» дёшево и почти незаметно, а стоит это неверного действия: у падения ищут поломку в конкретном месяце, у слабого роста спрашивают про состав работ, у плоской серии — жив ли домен вообще. Правило, выведенное из двух случаев: **перечисляй исходы явно и дай каждому свой тест**, иначе они схлопнутся сами |
| 2026-09-11 | `2026-09-11-contour-wave-b2` | `scripts/okf_sync_gate.py`, `.pre-commit-config.yaml`, `.github/workflows/quality.yml` | **L35:** гейт, который сравнивает две половины изменения, **зелёный по построению**, если обе половины лежат в одном индексе. Гейт синхронности канона с кодом был зелёным ровно до тех пор, пока концепты и правка кода стажировались вместе; правду он сказал только когда канон уже был закоммичен, а код изменён отдельно. Проверять такой гейт надо состоянием, которое он обязан ловить, а не прогоном на своей же поставке — иначе получаешь подпись «проверено» под непроверенным |
| 2026-09-11 | `2026-09-11-contour-wave-b2` | `.github/workflows/quality.yml`, `scripts/lint/adapted.json` | **L36:** канонный гейт несёт в себе предположение о потоке работы, и оно ломается тихо. `okf_sync_gate` берёт базу `origin/<base_ref>` — это PR-поток; при прямых коммитах в main база совпадает с HEAD, дифф пуст, и гейт краснеет на каждом пуше без единого нарушения. Лечится **выбором базы по потоку**, а не правкой канонного скрипта: на PR канонная база, на push — состояние до пуша. Отказ гейта на пустом диффе при этом правильный: зелёное там читалось бы как «проверено», а проверено ноль файлов |
| 2026-09-11 | `2026-09-11-collect-stage2-scheme` | `src/ahrefs_cases/collect/plan.py`, `src/ahrefs_cases/collect/scheme.py` | **L37:** правило, выведенное на одном endpoint'е, нельзя применять к набору: выгодная схема зависит от цены строки, а она у каждого своя. На одном и том же периоде `keywords-history` (строка 51, пять полей) дешевле собирать точками — 204 против 918, — а `refdomains-history` (строка 5) историей — 90 против 100, потому что минимум за запрос выше цены самого окна. Единица решения — пара «проект + endpoint», и захардкодить «этот точками, тот историей» нельзя: у `refdomains` граница тоже есть, просто дальше (с 21 строки) |
| 2026-09-11 | `2026-09-11-collect-stage2-scheme` | `knowledge/ops/first-live-run.md` | **L38:** гейт синхронности канона окупился в первый же рабочий день — и не там, где ожидалось. Он поймал не расхождение «код против описания», а **устаревшее число**: цена прогона в рантбуке живого прогона (27 330) изменилась этой же поставкой до 28 620. Числа в каноне стареют молча и опаснее прозы: их цитируют заказчику. Отсюда практика: число, которое спрашивают первым, держи в концепте, привязанном к коду, который его меняет |
| 2026-09-11 | `2026-09-11-collect-case-step` | `src/ahrefs_cases/collect/endpoints.py` | **L39:** у цены внешнего запроса два рычага, а не один. Схему (история против точек) мы крутили четыре поставки подряд и не трогали **состав полей**, хотя он в той же формуле: `10 × биллингуемых полей + 1`. Кривая позиций для кейса просит две корзины вместо пяти — строка 21 вместо 51, и та же информация обходится втрое дешевле. Различитель: спросить не «как дешевле купить эти данные», а «какие из этих полей кто-то читает» |
| 2026-09-11 | `2026-09-11-collect-case-step` | `src/ahrefs_cases/collect/plan.py`, `src/ahrefs_cases/collect/cache.py` | **L40:** данные под решение и данные под показ покупаются по разным правилам, и смешивать их дорого в обе стороны. Вердикту хватает двух точек, и там правильно выбирать дешёвое; кейс показывает кривую, и там цена не может решать — две точки дешевле серии вчетверо и бесполезны. Отсюда третья ступень воронки, которая покупает **после** классификации и только тем, у кого кейс будет: десять проектов из ста |
| 2026-09-11 | `2026-09-11-case-structure` | `src/ahrefs_cases/cases/builder.py`, `src/ahrefs_cases/cases/model.py` | **L41:** данные, купленные **после** решения, не появляются в том, что на этом решении построено. Ступень кейса докупает DR и стоимость трафика после классификации, а кейс берёт числа из вердикта: метрика в базе есть, в кейсе её нет, и выглядит это как «её не покупали». Различитель — сравнение состава серий с составом точек вердикта (`stale_subjects`), а не глаз; лечится бесплатным пересчётом, а не второй формулой границ внутри кейса |
| 2026-09-11 | `2026-09-11-case-structure` | `scripts/lint/check_jscpd_gate.sh` | **L42:** гейт копипаста, показавший дубль в **блоке импортов**, чаще всего говорит не про стиль, а про слишком широкую зависимость. Шесть одинаковых строк импорта с `classify/diagnose.py` исчезли не переснятием снимка, а сужением: `VerdictView.of` взяла версию порогов строкой вместо строки `Ruleset`, потому что имя версии — всё, что ей было нужно. Переснятый снимок закрыл бы сигнал вместе с причиной |
| 2026-09-11 | `2026-09-11-case-pdf` | `src/ahrefs_cases/export/pdf_renderer.py`, `templates/` | **L43:** у чужой точки расширения бывает скрытое поле-контракт, и без него запрет превращается в пожелание. Загрузчик ресурсов WeasyPrint, написанный самым очевидным способом (функция, которая бросает), даёт **предупреждение**, а не отказ: библиотека смотрит на атрибут `_fail_on_errors`, и без него «кейс собрался, только без картинки». Проверять расширение надо тестом на отказ, а не на успех — тест «рендер работает» был зелёным всё это время |
| 2026-09-11 | `2026-09-11-case-pdf` | `.github/workflows/quality.yml`, `pyproject.toml` | **L44:** нехватка библиотеки ОС маскируется под сломанный пакет. Без `libharfbuzz-subset0` импорт WeasyPrint печатает `DeprecationWarning`, а `filterwarnings = ["error"]` превращает его в падение **сбора тестов** — диагноз читается как «пакет несовместим», хотя не хватает системной зависимости. Локально зелено, потому что homebrew ставит её вместе с pango: третий случай «локально зелено ≠ зелено» в проекте, и первый, где разошлись не данные, а образ машины |
| 2026-09-11 | `2026-09-11-case-charts` | `src/ahrefs_cases/export/charts.py`, `templates/` | **L45:** «должно поместиться» — не проверка, а надежда: печатный лист меряется в миллиметрах полосы. Два графика в столбик дали 356 мм при печатном поле 267 мм, и решение поставить их рядом пришло из замера (`doc.pages[0]._page_box`), а не из вкуса. Заодно выяснилось, что SVG как строчный элемент тянет за собой межстрочный интервал: без `display: block` лист не сходится даже с заданной высотой |
| 2026-09-11 | `2026-09-11-case-charts` | `src/ahrefs_cases/cases/builder.py`, `src/ahrefs_cases/classify/points.py` | **L46:** продолжение L40 и L41. Данные под решение и данные под показ расходятся не только в цене, но и **в числах**: последний измеренный месяц (116 190) не равен точке Б (113 312), потому что Б — среднее по окну. Рядом на одном листе это читается как ошибка в одном из двух чисел, и правильный ответ — показать окно на кривой, а не подгонять одно число под другое |
| 2026-09-11 | `2026-09-11-case-narrative` | `src/ahrefs_cases/cases/narrative.py` | **L47:** сверка чисел в тексте слабеет там, где цифра — часть **имени**. «Ключи в топ-10» и «топ-3» уронили проверку на первом же прогоне, и соблазн был внести 10 и 3 в допустимые числа — это ослабило бы её по всему тексту ради двух названий. Правильный ход обратный: маскировать известные имена перед разбором, оставив строгость там, где стоят величины. Проверка, которая ловит собственный текст, — работающая проверка |
| 2026-09-11 | `2026-09-11-case-narrative` | `tests/test_cases_pdf.py`, `templates/` | **L48:** тест, стоящий на **месте** содержания, ломается при переезде этого содержания — и это не поломка теста, а сигнал. Проверка «на листе есть блок „Что сделали"» покраснела, когда объём работ переехал в текст; правильная формулировка спрашивает про содержание («объём работ назван»), а не про блок, в котором оно живёт |
| 2026-09-11 | `2026-09-11-case-zip` | `src/ahrefs_cases/export/archive.py` | **L49:** выдача — это ещё и место ревью. У ZIP в этом проекте две работы: отдать пачку и **дать её посмотреть**, потому что ревью черновика ТЗ не предусматривает и человек с архивом остаётся последним предохранителем. Отсюда список внутри архива с группой и пометкой «можно публиковать»: пачка без него отдаёт файлы, но не помогает решить, какие из них публиковать |
| 2026-09-11 | `2026-09-11-case-zip` | `scripts/run_collect.py`, `src/ahrefs_cases/cli/` | **L50:** гейт длины файла ловит не «распухание», а **скопление разных работ**. `run_collect.py` рос четыре поставки по одной команде и упёрся в 500 строк ровно тогда, когда в нём оказались приём, сбор, классификация и кейсы. Разрез прошёл по границе работ (команды кейсов — отдельный слой `cli`), и это дало 398 + 165 вместо 534 в одном файле |
| 2026-09-11 | `2026-09-11-api-auth` | `tests/test_api_auth.py`, `src/ahrefs_cases/api/main.py` | **L51:** приложение FastAPI без нашего `lifespan` не закрывает движок базы, и утечка всплывает **в чужом тесте**: `filterwarnings = ["error"]` превращает `ResourceWarning` в падение там, где сборщик мусора собрал соединение. Красным стал тест архива кейсов, не трогавший ни базы, ни сети. Диагноз по имени упавшего теста ведёт не туда; различитель — что именно течёт, а не где упало |
| 2026-09-11 | `2026-09-11-api-auth` | `src/ahrefs_cases/api/deps.py` | **L52:** заглушка окупается формой, а не содержанием. `current_group` из Ф1 отдавала группу `user` всем и была написана **наименее правой из возможных** — поэтому включение настоящей аутентификации не потребовало переписать ни один роутер: поменялся источник, а `require_right` остался. Заглушка, написанная всесильной, спрятала бы ровно те места, где права никто не проверял |
| 2026-09-11 | `2026-09-11-api-read` | `src/ahrefs_cases/collect/factory.py`, `src/ahrefs_cases/collect/runner.py` | **L53:** умолчание вызывающего — способ отключить предохранитель, не тронув его. `options.quota or FixtureQuota()` выглядел безобидной подстановкой по умолчанию, а означал: в живом режиме preflight сверяет смету с фикстурными 10 000. Проверка была написана, вызывалась, возвращала «можно» — и не защищала ничего. Различитель: кто решает, откуда берутся данные проверки, — конфиг или тот, кто её позвал |
| 2026-09-11 | `2026-09-11-api-read` | `tests/conftest.py` | **L54:** `dispose()` у пула закрывает **свободные** соединения, а выданное в этот момент остаётся сиротой и всплывает `ResourceWarning`ом при сборке мусора — то есть в случайном следующем тесте. Красным становится чужой тест, и «упало в архиве кейсов» указывает не туда. В тестах лечится `NullPool`, и подменять надо **сборку** движка, а не `get_engine`: её импортируют по имени, и подмена функции оставит часть кода на старом движке |
| 2026-09-11 | `2026-09-11-api-runs` | `src/ahrefs_cases/workers/queue.py` | **L55:** «выполнить сразу» и «выполнить в том же цикле» — разные вещи. Inline-реализация очереди, вызывавшая задачу прямо в обработчике, падала на `asyncio.run` внутри уже работающего цикла. Оттуда же второе: исключение задачи, поднятое наружу, делало ответ API **зависимым от бэкенда** (500 в inline, 202 в redis). Реализация интерфейса обязана совпадать с настоящей не только по результату, но и по контракту отказа |
| 2026-09-11 | `2026-09-11-api-runs` | `src/ahrefs_cases/collect/runner.py` | **L56:** фасад, повторяющий сигнатуру, ловится гейтом копипаста — и это полезно. Добавленный ради одного вызова параметр `run` продублировал список параметров в `collect_all` и `collect_projects`; правильный ответ оказался не waiver и не снимок, а **убрать причину**: фоновой задаче нужен другой вход (`collect_projects`), потому что она продолжает уже открытый прогон, а не начинает новый |
| 2026-09-11 | `2026-09-11-api-thresholds` | `src/ahrefs_cases/api/routers/`, `tests/` | **L57:** строгие предупреждения ловят чужое устаревание там, где его хуже всего искать. `HTTP_422_UNPROCESSABLE_ENTITY` объявлена устаревшей в Starlette, а `filterwarnings = ["error"]` превращает предупреждение в исключение — то есть ответ «данные не годятся» приходил пятисоткой, и только на пути обработки ошибки. Ошибочный путь тестируется реже успешного, и именно там дешевле всего спрятать такую подмену |
| 2026-09-11 | `2026-09-11-api-thresholds` | `tests/test_api_thresholds.py` | **L58:** четвёртый случай «локально зелено ≠ зелено», и снова не про код, а про **состояние**: тест зашил `started_by=1`, потому что в дев-базе пользователь №1 есть. В чистой базе CI его нет. Идентификатор, взятый из базы, отличается от идентификатора, взятого из памяти автора, ровно на один зелёный прогон |
| 2026-09-11 | `2026-09-11-api-users` | `src/ahrefs_cases/api/deps.py`, `src/ahrefs_cases/storage/models/user.py` | **L59:** роль отвечает на вопрос «кто он», а доступ — на вопрос «что ему можно», и эти вопросы расходятся быстрее, чем кажется. В CRM агентства развилка уже пройдена: там роль даёт набор прав, а личное право перекрывает её **в обе стороны** — и второе направление («этому нельзя, хотя он админ») через роль не выражается вовсе. Без личных прав первое же новое право требует либо четвёртой группы, либо правки кода |
| 2026-09-11 | `2026-09-11-api-users` | `src/ahrefs_cases/api/routers/users.py` | **L60:** перенося защиту из чужого проекта, переноси её причину, а не текст. «Нельзя разжаловать последнего администратора» и «нельзя выключить себя» выглядят перестраховкой, пока не назовёшь исход: система без администратора не управляется, а выключивший себя запирает дверь изнутри — снаружи включить её будет некому. Причина в докстринге делает защиту неудаляемой при следующем рефакторинге |
| 2026-09-11 | `2026-09-11-web-shell` | `web/src/app/nav.ts`, `web/src/auth/` | **L61:** интерфейс, который строит меню по **роли**, отменяет личные права. С Ф5.5 право выдаётся точечно поверх группы; меню по названию группы не показало бы раздел тому, кому право выдали, — и человек видел бы пустое меню при работающем API. Фронт обязан спрашивать «есть право?», а не «какая группа?» — ровно как сервер |
| 2026-09-11 | `2026-09-11-web-shell` | `web/vitest.config.ts` | **L62:** дайджест утверждений считает привязку по **добавленным строкам диффа**, а не по рабочему дереву: переименованные тесты, не попавшие в коммит, для него не существуют. Пока правка не закоммичена, «утверждение без примера» остаётся без примера — и это верно: подписывают то, что уедет в репозиторий |
| 2026-09-11 | `2026-09-11-intake-api` | `src/ahrefs_cases/workers/jobs.py`, `src/ahrefs_cases/classify/windows.py`, `scripts/run_collect.py` | **L63:** знание «откуда берутся настройки работы» живёт в вызывающем ровно до второго вызывающего. Окна точек читал CLI и передавал в сбор; появившаяся в Ф5 очередь звала тот же сбор без них — и прогон, запущенный кнопкой, покупал не то же, что прогон из консоли, под тем же именем. По отдельности оба пути выглядели исправными, поэтому различитель — тест, сравнивающий **входы двух путей**, а не проверяющий каждый |
| 2026-09-11 | `2026-09-11-intake-api` | `src/ahrefs_cases/api/routers/`, `src/ahrefs_cases/intake/gsheet_source.py` | **L64:** L57 повторился на другой константе (`HTTP_413_REQUEST_ENTITY_TOO_LARGE`), и это значит, что урок был не про `422`: любое устаревшее имя статуса при `filterwarnings = ["error"]` превращает отказ в пятисотку. Там же нашлась вторая пятисотка того же класса — сырая ошибка `httpx`, когда Google недоступен. Общее правило: **на пути отказа ответ обязан быть проверен тестом**, иначе «сервис сломан» покажут человеку вместо «откройте доступ» |
| 2026-09-11 | `2026-09-11-web-intake` | `web/src/test/render.tsx`, `web/src/auth/__tests__/login.test.tsx` | **L65:** тестовый рендер обязан давать те же провайдеры, что `main.tsx`, иначе первый экран, ходящий в API, роняет **чужой** тест: он рендерит приложение целиком, попадает на новый первый раздел меню и падает на `useQueryClient`. Диагноз по имени упавшего теста ведёт в форму входа, к которой случившееся не имеет отношения (тот же класс, что L51) |
| 2026-09-11 | `2026-09-11-web-intake` | `web/src/pages/intake/EstimatePanel.tsx` | **L66:** запрет, выраженный **исчезновением** элемента, не сообщает причину. Кнопка запуска при нехватке квоты остаётся видимой, недоступной и подписанной: пропавшую кнопку человек трактует как поломку интерфейса и идёт чинить не то, вместо того чтобы поднять лимит. Правило общее для всех блокировок, которые стоят денег |
| 2026-09-11 | `2026-09-11-intake-volume-notice` | `src/ahrefs_cases/intake/validate.py`, `src/ahrefs_cases/intake/rejections.py` | **L67:** у поля, которое **разрешено не знать**, «не разобрал» обязано стоить не дороже, чем «не сказали». Приём ронял проект целиком из-за подписи «214 ссылок» в объёме работ, хотя пустая ячейка там законна: строка с меньшей информацией принималась, а с большей — нет. Причина механическая — у приёма был один тип плохих новостей, а тип задаёт последствие. Различитель на будущее: сравнивать последствия **двух заполнений одной ячейки** (пустого и непонятого), а не проверять каждое по отдельности |
| 2026-09-11 | `2026-09-11-intake-volume-notice` | `tests/test_api_users.py` | **L68:** третий случай «зелёный по причине окружения», и впервые его создал **показ продукта**: заведённая для демонстрации учётка администратора сделала подопытного в тесте «последний администратор» не последним. Инвариант, который держится на **всей базе** (а не на строке), тестом через API не изолируется откатом транзакции: приложение ходит своими соединениями. Лечение — тест сам приводит мир к своему условию и возвращает как было, а не удаляет чужие данные |
| 2026-09-11 | `2026-09-11-docker-dev` | `Dockerfile`, `.dockerignore`, `docker-compose.dev.yml` | **L69:** объявленный, но ни разу не собранный образ — обещание, а не механизм. Компоуз с api и worker стоял в репозитории с Ф1 и выглядел рабочим; первая настоящая сборка показала, что `.dockerignore` нет вовсе (в контекст уезжали `.venv` 319 МБ под macOS и `node_modules` 372 МБ), а `pip install ".[dev,export]"` ссылается на несуществующий extra. Различитель тот же, что у любого неисполняемого объявления: у такой поставки единственный честный оракул — собрать и поднять, а не прочитать |
| 2026-09-11 | `2026-09-11-docker-dev` | `docker-compose.dev.yml` | **L70:** монтирование каталога разработчика **накрывает** то, что образ поставил внутрь. `./web:/app/web` прячет `node_modules` контейнера, и Linux пытается запустить бинарник esbuild, собранный под macOS (`darwin-arm64` против `linux-arm64`). Лечение — отдельный том поверх точки монтирования. Общее правило: всё, что ставится **внутри** образа и лежит **внутри** монтируемого пути, обязано получить свой том |
| 2026-09-11 | `2026-09-11-docker-prod` | `docker-compose.yml`, `deploy/nginx.conf` | **L71:** политику рестарта нельзя проверить, убив контейнер снаружи: `docker kill` и `docker stop` докер считает **ручной остановкой**, и `unless-stopped` на неё не реагирует намеренно. Первая проверка показала «не поднялся» и выглядела найденным дефектом. Правильный вход — уронить процесс **внутри** контейнера. Класс шире докера: проверяя автоматическое восстановление, убеждайся, что твой способ сломать не совпадает с тем, который система обязана уважать |
| 2026-09-11 | `2026-09-11-docker-prod` | `Dockerfile`, `.env.example` | **L72:** у дев- и прод-компоуза разные решения про секреты, и это не симметрия. Дев-файл держит значения (`cases/cases`) и это безобидно; боевой обязан держать **только имена** — он едет в git и на сервер. Механизм, а не договорённость: `${VAR:?причина}` не даёт компоузу стартовать без переменной и называет каждую недостающую — тихий запуск с пустым секретом подписи был бы хуже отказа |
| 2026-09-11 | `2026-09-11-tests-need-db` | `tests/conftest.py` | **L73:** пропуск честен в выводе и лжив в итоге. `253 passed, 154 skipped` и код возврата 0 читаются как «всё хорошо»: чтобы увидеть, что проверена треть, надо заметить **второе** число. Четвёртый случай «зелёного по причине окружения» (L8, L58, L68), и первый, где окружение не подменяет данные, а **убирает проверки**. Различитель: если отсутствие условия делает прогон бессмысленным, он обязан останавливаться — один раз, с командой, которой условие создаётся, а не падать сотней одинаковых красных |
| 2026-09-11 | `2026-09-11-api-charts` | `src/ahrefs_cases/api/routers/projects.py`, `src/ahrefs_cases/collect/factory.py` | **L74:** L53 повторился слово в слово на другом предохранителе. Там прогон брал `FixtureQuota()`, если ему не передали другую; здесь карточка брала `MetricSource.FIXTURE`, если ей не передали другой источник, — и в живом режиме показала бы пустые графики при целой базе. Значит урок был не про квоту, а про **класс мест**: всюду, где выбор режима принадлежит конфигу, умолчание в сигнатуре обработчика — способ отключить его, не тронув. Проверять такое надо сменой конфига: тест, меняющий аргумент, проходит и на сломанном коде |
| 2026-09-11 | `2026-09-11-api-charts` | `src/ahrefs_cases/export/charts.py`, `src/ahrefs_cases/export/html_renderer.py` | **L75:** «показывать одно и то же» держится устройством, а не намерением. Пока компоновка кривых жила внутри рендерера PDF, второй потребитель (веб-карточка) мог только повторить её — и разошёлся бы на шкале, наборе рядов или подписях, причём незаметно: оба рисунка по отдельности выглядят правильными. Лечение из двух частей: одно место компоновки и **посимвольное** сравнение в тесте. Осмотр глазами здесь не оракул |
| 2026-09-11 | `2026-09-11-web-projects` | `web/src/pages/projects/ProjectsFilters.tsx`, `web/src/pages/__tests__/` | **L76:** компонент с попапом (`Select` у Mantine) в jsdom рендерится **секундами**: файл тестов шёл 140 с и падал по таймауту, хотя проверял четыре строки таблицы. Тест, который не успевает, ничего не проверяет, и красным он выглядит как дефект продукта. Различитель — время одного рендера, замеренное отдельно: `TextInput` 18 мс против `Select` 2827 мс. Замена на чипы была нужна и по делу (пять взаимоисключающих значений, один клик вместо двух), но нашлась она через прогон, а не через вкус |
| 2026-09-11 | `2026-09-11-web-projects` | `web/src/pages/__tests__/projects.test.tsx` | **L77:** поиск по **тексту** в тесте экрана ловит первое совпадение, а подписи фильтра и данных почти совпадают («данных не хватает» на чипе и в бейдже). `findByText` нашёл чип **до** ответа сервера, и тест проверил фильтр, думая, что проверяет таблицу. Лечение из двух частей: сначала дождаться строки данных, потом читать значения по атрибуту (`data-group`), а не по подписи |
| 2026-09-11 | `2026-09-11-web-project-card` | `src/ahrefs_cases/classify/deltas.py`, `src/ahrefs_cases/api/routers/projects.py`, `web/src/pages/card/` | **L78:** у формулы, которая умеет отвечать «не знаю», второй экземпляр теряет именно это умение. Рост от нулевой базы у нас `None` — не «100 %» и не бесконечность; повтори арифметику на фронте, и экран показал бы процент там, где измерить его нельзя. То же и с подписями метрик: словарь живёт в кейсе, и копия на фронте развела бы названия одной метрики на экране и в PDF. Различитель общий: если правило умеет сказать «здесь числа нет», оно обязано быть **одно** |
| 2026-09-11 | `2026-09-11-web-project-card` | `tests/test_api_read.py`, `tests/test_api_runs.py`, `tests/test_api_intake.py`, `tests/test_api_thresholds.py` | **L79:** уборка за собой, написанная как `delete(Model)` без фильтра, стирает **чужое**. Четыре модуля тестов так чистили `MetricPoint`, `Verdict`, `Run` и `UnitsLedger` — и на общей дев-базе любой `pytest` уносил собранные ряды и вердикты вместе со своими. Нашлось показом: карточка честно сказала «не классифицирован» про проект, который часом раньше был «хорошим». Пятый случай того же класса (L8, L58, L68, L73), и первый, где тест не читает чужое состояние, а **уничтожает** его |
| 2026-09-11 | `2026-09-11-tests-own-rows` | `tests/owned_rows.py`, `tests/test_cleanup_scope.py`, `tests/test_api_*.py` | **L80:** правило «правь только своё» шире уборки: начав с `delete(Model)` без условия, оно в тот же час поймало `update(Model).values(...)` — тест замка помечал «идущим» **каждый** прогон в базе (чужие оставались такими навсегда и блокировали запуск), а тест скачивания ломал путь **каждому** артефакту, включая настоящие файлы кейсов. Различитель один на оба: у операции, меняющей строки, должно быть условие. Проверка «мои данные на месте» ловит не всё — контрольный прогон пережил удаление, но вернулся испорченным, поэтому смотреть надо и на значения |
| 2026-09-11 | `2026-09-11-charts-grouping` | `src/ahrefs_cases/export/grouping.py`, `src/ahrefs_cases/export/charts.py` | **L81:** свёртка ряда — не арифметика, а **вопрос к метрике**: поток за квартал это сумма трёх месяцев, запас — значение на конец. Одно правило на обе группы врёт в одну сторону втрое (домены) и в другую втрое же (трафик). Отсюда два следствия: список потоков берётся там, где он уже есть (`classify.series.FLOW_METRICS`), а неизвестная подпись (производный «топ-10») считается запасом — и это проверяется тестом, а не подразумевается. Побочно: координаты, в которых заданы полосы окон, обязаны сворачиваться вместе с точками, иначе полоса молча исчезает |
| 2026-09-11 | `2026-09-11-charts-grouping` | `src/ahrefs_cases/export/charts.py`, `web/src/pages/card/Charts.tsx` | **L82:** рисунок, сделанный для печати, попадает на экран с темой — и там у него нет фона. Чернила SVG выбраны под белый лист PDF (`#0b0b0b`), а веб-карточка открылась в тёмной теме: подписи значений держатся только там, где под ними светлая заливка. Нашлось не тестом и не ревью, а тем, что браузер был в тёмной теме. Правило: артефакт, общий для печати и экрана, обязан иметь объявленную подложку — либо своя светлая, либо тема параметром |
| 2026-09-11 | `2026-09-11-charts-paper` | `src/ahrefs_cases/export/charts.py` | **L83:** артефакт, опирающийся на фон, которого он не содержит, верен ровно до второго потребителя. Кривые нарисованы под белый лист: тёмные чернила, заливка затухает в «цвет полотна» — а полотна в разметке нет. Пока показывал только PDF, подразумеваемое совпадало с настоящим; веб-карточка с тёмной темой принесла своё, и подписи значений исчезли. Лечение — подложка **внутри** артефакта, а не плашка у потребителя: иначе цвет заводится дважды и на двух языках. Побочно вскрылось, что легенда срезалась границей `viewBox` всегда — рамка просто сделала это видимым |
| 2026-09-11 | `2026-09-11-charts-paper` | `Dockerfile`, `docker-compose.dev.yml` | **L84:** «продукт» и «код продукта» — разные множества. В образ уехали `src`, миграции, конфиг и скрипты, а `templates/` — нет, и `render` в контейнере падал `TemplateNotFound`: боевой образ не умел собирать кейсы, то есть делать то, ради чего сервис есть. Локально путь работал, потому что шаблон лежит рядом с кодом. Различитель тот же, что в L69: путь, ни разу не пройденный в упаковке, не существует — проверять надо не «образ собрался», а «образ делает продукт» |
| 2026-09-11 | `2026-09-11-web-runs-usage` | `src/ahrefs_cases/api/routers/runs.py`, `src/ahrefs_cases/workers/jobs.py` | **L85:** `202 Accepted` не доказывает, что задача началась. Очередь пересылает аргументы **позиционно**, а `refresh` был объявлен только-ключевым: вызов падал `TypeError` ещё до тела задачи, ответ приходил `202`, а строка прогона оставалась `queued` **навсегда** — и замок «один активный прогон» блокировал все следующие запуски до реапера (4,5 часа). Одна опечатка в сигнатуре заклинивает сервис целиком. Проверять надо не код ответа, а состояние после |
| 2026-09-11 | `2026-09-11-web-runs-usage` | `src/ahrefs_cases/workers/queue.py`, `docker-compose.dev.yml` | **L86:** «выполнить в этом же процессе» безопасно только там, где процесс ничей. `InlineQueue` поднимает задачу в потоке со своим циклом событий, а движок базы кэширован на процесс, и его соединения привязаны к циклу uvicorn — сервер получает `asyncpg InternalClientError: unknown protocol state`. В тестах не видно: там движок без пула, каждая сессия открывает своё соединение; в CLI цикл один. Значит у режима есть **граница применимости**, и место ей в докстринге рядом с кодом, а не в чьей-то памяти |
| 2026-09-11 | `2026-09-11-web-runs-usage` | `src/ahrefs_cases/storage/models/run.py`, `src/ahrefs_cases/collect/runner.py` | **L87:** поле с умолчанием, которое никто не пишет, выглядит как данные и является нулём. `Run.units_estimated` существовал с Ф1, считался в каждом прогоне и не сохранялся — экран показал бы «смета 0 → факт 1936» у всех строк. Колонка, всегда равная умолчанию, хуже отсутствующей: её читают как факт. Различитель — первый же экран, который её показывает |
| 2026-09-11 | `2026-09-11-cases-pack-api` | `src/ahrefs_cases/api/routers/cases.py`, `src/ahrefs_cases/workers/jobs.py` | **L88:** артефакт, у которого нет дороги наружу, не является выдачей. Пачка ZIP — выход сервиса по ТЗ: она собиралась прогоном, писалась на диск контейнера и считалась готовой, потому что файл существует. Забрать её человек мог только `docker cp`. Признак этого класса дыр простой: у результата есть создатель и нет получателя, и ни один тест не краснеет — каждый проверяет свою половину. Спрашивать про каждый артефакт отдельно: **кто и как его получает** |
| 2026-09-11 | `2026-09-11-cases-pack-api` | `src/ahrefs_cases/api/routers/cases.py`, `src/ahrefs_cases/export/archive.py` | **L89:** имя, содержащее дату, не является временем. Архив зовётся «кейсы-<дата>.zip», и повторная сборка того же дня **переписывает тот же файл**, не меняя имени: сортировка по имени назвала бы свежим вчерашний архив ровно в тот день, когда пачку пересобрали. Дата в имени — это подпись для человека, а не порядок для машины; порядок берётся у файловой системы |
| 2026-09-11 | `2026-09-11-web-cases` | `web/src/api/client.ts`, `web/src/pages/cases/save.ts` | **L90:** авторизация заголовком запрещает ссылку на файл. `<a href="/api/cases/1/download">` браузер отправляет **без** `Authorization` — токен живёт в `localStorage`, а не в куке, и человек получает `401` вместо кейса. Файл берётся запросом и сохраняется из памяти; следствия два, оба неочевидны: имя приходит в `filename*=utf-8''…` и без раскодирования попадает в загрузки процентами, а blob держится в памяти вкладки до `revokeObjectURL` |
| 2026-09-11 | `2026-09-11-web-cases` | `web/src/pages/CasesPage.tsx`, `web/src/pages/cases/` | **L91:** гейт длины функции поймал не «длинный компонент», а **две работы в одном**: `CasesPage` собрал состояние пачки и состояние библиотеки — 92 строки и сложность 11. Разрез прошёл по вопросам, на которые экран отвечает («что можно забрать целиком» и «что собрано»), и дал три файла по 65–97 строк вместо одного. Тот же класс, что L50 |
| 2026-09-11 | `2026-09-11-cases-latest-version` | `src/ahrefs_cases/api/routers/cases.py` | **L92:** список, куда версии **дописываются**, с каждым прогоном отвечает всё хуже на вопрос, ради которого его открывают. Библиотека кейсов сортировалась по времени и не группировалась по проекту: после девяти сборок шесть проектов дали пятьдесят строк, а нужный файл пришлось бы искать глазами. Тест этого не ловил, потому что при **одной** сборке история и рабочий список совпадают — увидеть можно только на стенде, где прогонов много. Спрашивать у каждой выдачи: на какой вопрос она отвечает и что с ней станет на сотом прогоне |
| 2026-09-11 | `2026-09-11-cases-latest-version` | `src/ahrefs_cases/api/routers/cases.py`, `src/ahrefs_cases/cli/case_commands.py` | **L93:** «самый свежий» по времени неверен там, где строки пишутся пачкой. `pack_cases` создаёт кейсы всех проектов в одной транзакции с одинаковым `created_at`, и максимум времени дал бы проекту два «свежих» кейса. Признак монотонности берётся у того, кто его гарантирует: у растущего `id`, а не у часов |
| 2026-09-11 | `2026-09-11-thresholds-in-words` | `web/src/pages/thresholds/rules.ts`, `web/src/app/nav.ts` | **L94:** право раздела — договорённость с заказчиком, а не следствие прав эндпоинта. Предпросмотр порогов в API открыт праву `read` намеренно, и из этого я вывел, что раздел «Пороги» можно показать всем; таблица доступа ТЗ говорит «Пользователь — нет». Поймал оракул `nav.test.ts`, написанный тремя поставками раньше. Вывод «раз API разрешает, значит интерфейс может» неверен всегда, когда доступ к разделу кто-то утверждал отдельно |
| 2026-09-11 | `2026-09-11-thresholds-in-words` | `web/src/pages/thresholds/RulesView.tsx` | **L95:** ноль в выключенном условии нельзя показывать числом. `guards.end_vs_peak_min_pct: 0` означает «правила нет», а «конец периода не ниже пика на 0 %» читается как работающее правило — тот же класс, что остаток квоты «0» против «неизвестен» (L30). Показывается «не задано», и это отдельная ветка, а не форматирование |
| 2026-09-11 | `2026-09-11-thresholds-preview` | `web/src/pages/thresholds/PreviewPanel.tsx` | **L96:** успокаивающая формулировка обязана знать, что она не посчитала. «Никто не сменит группу» стояло зелёным над «данных не хватает: 6» из 11 проектов — вывод о базе, сделанный по её половине. Теперь зелёное говорится только при полном счёте, иначе — «посчитано 5 из 11». Правило шире панели: любой итог, считающий подмножество, называет размер подмножества рядом с выводом |
| 2026-09-11 | `2026-09-11-thresholds-preview` | `web/src/pages/groups.ts`, `web/src/pages/projects/GroupBadge.tsx` | **L97:** словарь имён, лежащий внутри компонента, — это копия, ждущая второго читателя. Названия групп переводил `GroupBadge`, и предпросмотр порогов показал `good → poor` посреди русского экрана. Признак: значение перечисления пересекает границу экрана (приходит в другом ответе API) — значит его подпись общая и живёт отдельно от компонента |
| 2026-09-11 | `2026-09-11-thresholds-edit` | `web/src/pages/thresholds/form.ts`, `src/ahrefs_cases/classify/thresholds.py` | **L98:** «пусто» на экране и «отсутствует» в модели — разные вещи, и разница у каждого поля своя. Форма отправляла `null` за любое очищенное поле, но пороги принимают отсутствие только у двух (`float \| None`); у «конца периода не ниже пика» ноль **и есть** «выключено», а `None` — ошибка разбора. Человек, очищающий поле, хочет «выключить» — и получил бы `422`. Правило: форма, правящая чужую модель, обязана знать про каждое поле три вещи — обязательно ли оно, что значит его ноль и принимает ли оно отсутствие |
| 2026-09-11 | `2026-09-11-thresholds-edit` | `web/src/pages/thresholds/ApplyPanel.tsx` | **L99:** текст, написанный для одного шага, врёт на следующем. Плашка «версия сохранена и пока не действует» осталась на экране после активации — сообщая обратное состоянию, которое сама же и вызвала. Признак: строка, объясняющая состояние, не читает это состояние (у панели был флаг `active`, и он ей уже приходил) |
| 2026-09-11 | `2026-09-11-ci-editable-root` | `.github/workflows/quality.yml` | **L100:** предупреждение, которое нельзя погасить работой, изнашивает канал, которым сообщают о беде. Шаг ставил `-e backend` под условием «есть корневой `pyproject.toml`» — проверял одно, делал другое, и на `src`-раскладке печатал «editable-установка не удалась» каждый прогон. Читаешь его месяц, привыкаешь — и настоящую неудачу установки (mypy без рантайм-зависимостей) уже не замечаешь. Убрав первое, тем же прогоном нашёл второе того же класса: «Node.js 20 is deprecated» на v4/v5 в одном job'е из трёх. Приёмка CI поэтому смотрит не только на цвет, но и на **раздел ANNOTATIONS: он должен быть пуст** |
| 2026-09-11 | `2026-09-11-users-list` | `web/src/pages/users/rights.ts`, `src/ahrefs_cases/api/routers/users.py` | **L101:** право без происхождения — это половина ответа. Итоговый список прав показывает, что человеку можно, и прячет, почему: «админ, у которого лично отобрали `manage_users`» выглядит как обычный админ, а именно этот случай и ищут глазами. Показывать надо разницу между тем, что дала роль, и тем, что решили про человека, — и справочник ролей для этого берётся у сервера, иначе на экране заводится вторая таблица прав (тот же класс, что L97) |
| 2026-09-11 | `2026-09-11-users-list` | `web/src/pages/users/UsersTable.tsx` | **L102:** в jsdom нет раскладки, и тесты по построению слепы к обрезанию текста. Бейджи групп приезжали как «ПОЛЬЗО…» и «ВХОД ЗАКР…»: Mantine режет бейдж по ширине ячейки, а таблица отдала ширину колонке с почтой. Ни один из 89 тестов этого не видел и видеть не мог — такие дефекты ловит только снимок живого экрана |
| 2026-09-11 | `2026-09-11-users-manage` | `web/src/pages/users/ManageUser.tsx` | **L103:** «нет решения» и «решено: нельзя» — разные состояния, и галочкой они не выражаются. Личное право живёт в JSONB: отсутствие ключа значит «как в группе» и исчезнет при смене группы, `false` значит «отобрано» и смену группы переживёт. Галочка свела бы их в одно, и человек, снявший её у админа, получил бы не то, что имел в виду. Признак класса: поле, у которого **три** исхода — унаследовать, включить, выключить |
| 2026-09-11 | `2026-09-11-users-manage` | `web/src/pages/users/rights.ts` | **L104:** список, отсортированный по внутренним именам, среди переведённых подписей выглядит случайным. Права приходят по алфавиту латиницей (`change_technical_settings`, `edit_thresholds`, …), а на экране читаются как «технические настройки», «править пороги» — и я сам нажал «выдать» не у того права. Сортировать нужно по тому, что видит человек; тест, ловивший право по индексу, переписан на поиск по имени (L48) |
| 2026-09-12 | `2026-09-12-ahrefs-live-shapes` | `src/ahrefs_cases/collect/endpoints.py` | **L105:** замер одного endpoint'а не переносится на соседние по аналогии. Цена строки была выведена из `metrics-history` («10 за любое поле») и разошлась с реальностью в семь раз там, где поля дешёвые: ключевой бакет стоит 1, DR — 1, трафик — 10. Форму ответа перенесли так же, и два ключа списка из восьми оказались выдуманы (`domain_ratings`, `metrics` у объёма поиска). Признак класса: первый замер верен **и потому опасен** — он выглядит как знание о системе, будучи знанием об одной её точке |
| 2026-09-12 | `2026-09-12-ahrefs-live-shapes` | `src/ahrefs_cases/collect/fixtures/provider.py`, `src/ahrefs_cases/collect/provider.py` | **L106:** фикстура, у которой значение зависит от параметров запроса, делает вердикт зависимым от **способа покупки**. Серия строилась назад от конца запрошенного окна, и один и тот же месяц приходил разными числами: смена схемы сбора (а её выбирает цена) молча увела проект с `good` на `medium`. Значение месяца обязано быть свойством месяца — это тест на любую подделку данных: спроси одно и то же двумя разными окнами и сравни |
| 2026-09-12 | `2026-09-12-source-is-required` | `src/ahrefs_cases/classify/verdicts.py`, `scripts/run_collect.py` | **L107:** умолчание, совпадающее с режимом разработки, невидимо для тестов — потому что тесты и сами идут в этом режиме. `source: MetricSource = MetricSource.FIXTURE` трижды приводил к одному: в бою сервис читал пустоту и отвечал «данных нет» при целой базе. Третий раз чинится не вызов, а **возможность промолчать**: умолчание снято, оракул стоит на сигнатурах. Признак класса: параметр, у которого умолчание равно «как у нас на стенде» |
| 2026-09-12 | `2026-09-12-source-is-required` | `tests/test_source_is_required.py` | **L108:** оракул на **свойстве функции** переживает рост кодовой базы, оракул на списке вызовов — нет. Вызовов классификации становится больше с каждым экраном, и тест «этот вызов передал источник» устаревает в день написания; тест «у этой функции нет умолчания» держится сам и ловит четвёртый случай до того, как он случится |
| 2026-09-12 | `2026-09-12-source-is-required` | `delivery/active/STATUS.md`, `scripts/okf_sync_gate.py` | **L109:** waiver живёт ровно столько, сколько активная поставка, — значит архивировать её можно только **после** зелёного прогона. Я заархивировал тем же пушем, что и код: локальный хук подпись видел, а CI — уже нет, и гейт канона честно покраснел на два концепта. Порядок обязателен: код с видимым waiver'ом → зелёный CI → архив (в этом пуше кода уже нет, и гейту нечего судить) |
