# Verify report

**Date:** 2026-09-11
**Verifier:** human:anthony
**asserts_reviewed_by:** yes (by=human:anthony at=2026-09-11) — непривязанные
утверждения показаны словами: в настоящем браузере кнопки сохранили на диск
PDF (61 788 байт) и ZIP (267 319 байт) под кириллическими именами
**CI run:** пуш после приёмки
**Commit:** см. `git log -1`

## Shape oracles
- [x] PASS — pre-commit, 29 хуков, упавших 0
- [x] PASS — предохранители: net_loc 720 при пороге 800, файлов 11 из 25
- [x] PASS — eslint без предупреждений, tsc чист

## Behavior oracles
- [x] PASS — vitest 61 (+8), pytest 439 passed, 1 skipped
- [x] PASS — E1–E8 закрыты тестами

## Product oracles (живой стек, Chrome по CDP)
- [x] PASS — экран открыт настоящим входом: пачка, библиотека, метки публикации
- [x] PASS — «Скачать PDF» и «Скачать ZIP» сохранили настоящие файлы:
      `legal-partners.pl — Кейс.pdf` (61 788 б, `%PDF-1.7`) и
      `кейсы-2026-09-11.zip` (267 319 б, пять кейсов и список внутри)
- [x] PASS — «Пересобрать кейсы» дошла до воркера и обратно: прогон №204
      `done`, архив пересобран (новое время и размер), версии кейсов выросли
- [x] PASS — светлая и тёмная темы читаются обе (снимки сняты в каждой)

## Найдено прогоном живого экрана

**Библиотека показывала все версии подряд.** Шесть проектов дали пятьдесят
строк: версии 9, 8, 7 одного домена шли вперемешку с соседними, а на сотне
доменов первая страница досталась бы двум-трём проектам. Экран отвечал «что
когда собиралось», тогда как спрашивают у него «какой файл отправить клиенту».

Исправление написано и проверено, но выпускается **следующей поставкой**
(`cases-latest-version`): вместе с экраном оно дало 816 строк при пороге 800, и
резать по границе работ правильнее, чем поднимать порог (L50).

## Ревью рисковых мест

**Безопасность.** Скачивание идёт `fetch`'ем с заголовком `Authorization`, а не
ссылкой: прямая ссылка ушла бы без токена и вернула `401`. Кнопка «Пересобрать»
показывается по праву `run` из `/api/auth/me` — и это удобство, а не защита:
право проверяет сервер, экран лишь не обещает того, чего не даст (L61).

**Транзакция БД.** Риска нет: экран в базу не пишет — читает библиотеку и
состояние пачки, а сборку заказывает существующим прогоном.

**Интеграция.** Три внешние связи, все проверены исполнением, а не рассуждением:
API (файл доезжает с именем из `Content-Disposition`), браузер (blob сохраняется
и ссылка сразу отзывается), очередь и воркер (кнопка пересборки довела прогон до
`done`).

## Spec coverage gaps
- Версии кейса одного проекта показываются строками рядом. Исправляется
  следующей поставкой; там же тумблер «показать все версии».

## Verdict
- [x] READY FOR HANDOFF

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base HEAD~1 -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 12 code (+9 process docs) / +732/-0 (net +732) |
| commits | 1 |
| time_to_accepted_spec | 0.0h |
| rework_after_done | 0 commit(s) after first phase: handoff |
| harness_hardened | yes — web/src/pages/__tests__/cases.test.tsx (новый оракул) |
| implement_retries | 3 — гейт длины функции на `CasesPage` (разрезан на секции), скобки при снятии тумблера, prettier на двух файлах |
| verify_fails_before_green | 1 — прогон живого экрана вскрыл библиотеку всех версий |
| est_token_or_cost | n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.
