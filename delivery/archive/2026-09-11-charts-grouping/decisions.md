# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-11 · выбрал сворачивать на сервере вместо фронта — потому что рисунок строится там же, а правило свёртки зависит от природы метрики: копия показала бы сумму ссылающихся доменов за квартал, то есть втрое больше, чем их есть
- 2026-09-11 · выбрал брать список потоков из классификации (`classify.series.FLOW_METRICS`, две метрики) вместо своего в рисовальщике — потому что «поток или запас» — свойство метрики, а не графика: тот же список уже решает, как сворачивать окна точек в `classify.series.aggregate`
- 2026-09-11 · выбрал отвечать `422` на неизвестное значение вместо тихого возврата к месяцам — потому что иначе человек смотрит на месяцы, думая, что видит кварталы
- 2026-09-11 · выбрал оставить точки А и Б как есть вместо пересчёта под выбранный период — потому что они приходят из `Verdict.point_a`/`point_b`, записанных версией порогов: пересчёт под вид экрана дал бы на карточке одно число, а в PDF и в таблице проектов другое
