# Observed in production

**Поставка:** 2026-09-16-the-dash-says-which-case-it-is
**Наблюдали:** 2026-09-24 (прод поднят 24.09; живые данные — прогоны №1–4 того же дня) → 2026-09-24
**Сигнал из STATUS:** в карточке проекта без шага 2 стоит «не покупали» (на экране с b577f6c — «не собирали»), у проекта с шагом 2 и пустой историей — «нет данных»; смотреть на живом стенде

## Примеры спеки против прода

Прод, `live`, наборы «прогон-набор-А» и «калибровка-набор» (53 проекта, 10
прошли шаг 2). Признак прочерка — поле `fact_missing` в `GET /api/projects/{id}`,
экран переводит его в слово (`Reasons.tsx`).

| id | Обещано в спеке | Что в проде | Вердикт |
|---|---|---|---|
| не собирали | прочерк у проекта без шага 2 говорит «не собирали» | `tomshardware.com`, `bellroy.com` (poor, без шага 2): 4 прочерка из 4 — `not_bought` | ПОДТВЕРЖДЁН |
| прочерков нет там, где купили | у прошедших шаг 2 подтверждающие метрики с фактом | `ebsco.com` (good), `ahrefs.com` (medium): прочерков 0 | ПОДТВЕРЖДЁН |
| нет данных | шаг 2 оплачен, истории нет → «нет данных» | в наборе не встретилось: у всех 10 кандидатов история есть. На живом стенде 16.09 случай был измерен (16 прочерков, `zapier.com`) | НЕ ВСТРЕТИЛОСЬ (не расхождение) |

## Метрика: стало ли хуже

| Метрика | До мержа | После | Порог | Вердикт |
|---|---|---|---|---|
| прочерков без ответа журнала («журнал молчит») на проде | — | 0 | 0 | В пределах |

## Что сделано по результату

- [x] Расхождений нет — наблюдение закрыто

**Урок для `archive/INDEX.md`:** —
