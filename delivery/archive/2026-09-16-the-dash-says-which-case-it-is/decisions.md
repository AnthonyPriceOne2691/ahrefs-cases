# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-16 · выбрал спрашивать журнал расхода units (`units_ledger`: endpoint + домен) вместо того, чтобы завести в базе отдельную запись «метрику купили» — потому что запись пришлось бы заполнять задним числом для уже прошедших прогонов, а журнал существует с первого запроса и содержит ровно нужное: 27 доменов со строками `keywords-history`/`refdomains-history` против 71 домена, у которого есть только `metrics-history`
- 2026-09-16 · выбрал возвращать `None` («журнал про домен не знает») отдельно от пустого множества в `bought_metrics` вместо того, чтобы считать молчание журнала отказом от покупки — потому что данные, собранные до появления журнала, получили бы приговор «не покупали» на пустом месте; на стенде таких доменов сейчас 0 из 95, то есть цена решения — один `if` ради случая, который придёт с чужой базой, а не выдуманный запас
- 2026-09-16 · выбрал держать связь «условие → метрика» одной таблицей `SUPPORTING_METRICS` в `classify/rules.py` вместо разбора имени правила в роутере — потому что имена правил строятся там же (`f"{name}.{REFDOMAINS_CHECK}"`), и вторая копия разошлась бы при первом переименовании; в записанном вердикте от метрики остаётся только имя правила, больше связать их неоткуда
- 2026-09-16 · выбрал спрашивать про `KW_TOP3` для условия `kw_top10_pct` вместо проверки обеих корзин топ-10 — потому что `top3` и `top4_10` приходят одним запросом `keywords-history` и куплены всегда вместе: вторая проверка ничего не добавила бы, кроме лишнего условия
- 2026-09-16 · выбрал сделать группу заголовком блока (`blocks()` в `Reasons.tsx`, атрибут `data-conditions`) вместо колонки в таблице — потому что группа это свойство набора строк, а не каждой строки: у `cleverfiles.com` колонка печатала слово 11 раз на 11 условий (шесть «хороший», пять «средний»), а заголовков на тот же экран нужно два
- 2026-09-16 · выбрал отдельный словарь заголовков (`CONDITION_TITLES` в `web/src/pages/groups.ts`) вместо склейки «Условия » + `groupWord(group)` — потому что склейка потребовала бы второго падежа в `GROUP_LABELS`, а он там именительный и нужен таким бейджам проекта («хороший», «средний»); без этого заголовок читался бы «Условия хороший»
- 2026-09-16 · выбрал ставить условия вне групп первым блоком «Общие условия» (порядок `['', 'good', 'medium']`) вместо того, чтобы растворить их среди групповых — потому что это блокеры из `rules._blockers` (`org_traffic`, `traffic_point_b`, дыра в серии, непокупленные месяцы): они решают, судить ли вообще, и у `insufficient_data` других условий на экране просто нет — у `greenhouse.co.uk` вердикт состоит из одной такой строки
- 2026-09-16 · выбрал искать повтор слова группы ВНУТРИ блоков условий вместо поиска по всей странице — потому что в шапке карточки слово «хороший» стоит законно, один раз и про проект целиком: оракул на всю страницу запрещал бы правильное вместе с неправильным
- 2026-09-16 · выбрал писать «не собирали» вместо «не покупали» в колонке «Факт» — потому что покупаются данные Ahrefs (запрос `keywords-history` или `refdomains-history`), а строка читалась как покупка самих ссылок и ключей: «ключи же невозможно купить» (владелец). Признак в API остался `not_bought` — там он про факт оплаты запроса, и отвечает за него журнал расхода
- 2026-09-16 · выбрал снять счёт из шапки карточки (`CardHeader.tsx`) вместо того, чтобы объяснять его подсказкой — потому что это взвешенная сумма процентов и визитов без единиц (`0,4 × pct + 0,3 × abs + 0,2 × топ-3 + 0,1 × ссылки`): у `allthedifferences.com` он равен −19 745 при падении на 100 % и на 65 683 визита. Число сравнивает соседей, а в карточке одного проекта соседей нет; в сортировке списка проектов оно остаётся
- 2026-09-16 · выбрал сверять файл кейса с вердиктом ПО ЧИСЛАМ (`cases/freshness.py`) вместо сверки по `Case.verdict_id` — потому что вердикт пишется `on_conflict_do_update` по паре «проект + версия порогов»: пересчёт той же версией перезаписывает строку и сохраняет прежний `id`. Сверка по ссылке молчала ровно там, где расхождение и было: у `allthedifferences.com` `verdict_id` указывал на действующий вердикт, а числа в файле остались от фикстурного расчёта
- 2026-09-16 · выбрал не отдавать устаревший файл и называть причину словами вместо того, чтобы отдавать его с пометкой — потому что файл уходит клиенту как есть: «+69 %» под именем проекта, у которого на экране «−99,9 %». Замер по стенду: таких файлов 51 из 62, то есть пометку пришлось бы читать чаще, чем не читать
- 2026-09-16 · выбрал допуск в тысячную долю величины при сравнении чисел вместо точного равенства — потому что величины проходят через JSON и округление подписей, а `org_traffic` у Ahrefs это оценка (урок L149: оракул, сравнивающий оценку на точное равенство, судит по шуму)
- 2026-09-16 · выбрал занять место заглушкой той же высоты на ЧЕТЫРЁХ экранах сразу (`Dynamics`, `CaseLibrary`, `PackSection`, `UsagePage`) вместо правки одной «Динамики» — потому что мигание всплыло вторым заходом после Z23, и лечение одного места его уже не удержало: признак класса — строка «Загружаем…» вместо блока, который придёт
- 2026-09-16 · выбрал держать формулировку про окна одной константой `POINTS_NOTE` в `export/html_renderer.py` вместо текста на фронте — потому что та же фраза печатается в PDF (`templates/case.html.j2`), и две редакции начали бы объяснять одно и то же разными словами; оракул сверяет ответ API с константой
- 2026-09-16 · выбрал выделять на рисунке САМИ точки А и Б (`_point_mark`) вместо крайних месяцев кривой — потому что таблица показывает средние по окну, а рисунок показывал месяцы, и у `bad.org.uk` «102 478» в таблице против «68 961» на кривой читалось как ошибка сервиса. Отметка — штрих на уровне среднего внутри окна, а не кружок на линии: среднее двух месяцев на кривой не лежит, и кружок обещал бы измерение там, где его нет
- 2026-09-16 · выбрал удерживать отметку в поле графика (`min/max` по `_LEFT` и `_WIDTH - _RIGHT`) вместо рисования во всю ширину окна — потому что после свёртки в кварталы и годы окно схлопывается к краю периода: на живом просмотре отметка уехала за ось вместе с подписью, а штрих во всю ширину года растянулся через полполотна и потерял связь с точкой
- 2026-09-16 · выбрал перенести число с полосы окна к самой точке вместо подписи под осью — потому что под осью живут подписи месяцев и легенда, и «39 127» у окна Б село на «ключи в топ-10» в первой же сборке; у точки место своёью — потому что оба числа настоящие: у `cazoo.co.uk` точка Б усредняет 480 413 и 262 170, а кривая рисует каждый месяц. Подмена одного другим соврала бы, а соседство объясняет
- 2026-09-16 · выбрал ставить подпись полосы НАД ней вместо места под осью — потому что первая же сборка показала наложение: «39 127» у окна Б село на легенду «ключи в топ-10» (внизу живут и подписи месяцев, и легенда), а сверху пусто
- 2026-09-16 · выбрал вести кривую от точки А к точке Б (крайние узлы — величины вердикта, `_curve(starts_at=…, ends_at=…)`) вместо линии по крайним месяцам — потому что таблица показывает А и Б, а рисунок показывал месяцы, и у `bad.org.uk` «102 478» в таблице против «68 961» на кривой читалось как ошибка сервиса; теперь числа таблицы стоят на концах линии, а между ними она по-прежнему ломается по измеренным месяцам
- 2026-09-16 · выбрал считать потолок шкалы по точкам А и Б вместе с месяцами вместо прежнего максимума по `item.points` — потому что крайние узлы линии теперь это А и Б: у `engoo.com` точка Б равна 454 при месячном максимуме 400, и кружок вылезал за верхнюю кромку холста
- 2026-09-16 · выбрал расширить поля холста до 62 (было 54 и 46) вместо уменьшения кегля подписей — потому что ширина считается: девятизначное «3 596 464» при кегле 10 занимает 52 единицы плюс 8 на отступ, и на `ebsco.com` «440 322» не помещалось справа, переходило на другую сторону точки и ложилось на кривую
- 2026-09-16 · выбрал не рисовать полосу окна шире трети поля (`_window_band`) вместо того, чтобы рисовать её всегда — потому что после свёртки в годы окно раздувается до целого периода: на `engoo.com` полоса А занимала 296 единиц из 296 возможных, то есть закрывала рисунок целиком и переставала значить «здесь усреднено»; буква у кромки остаётся
