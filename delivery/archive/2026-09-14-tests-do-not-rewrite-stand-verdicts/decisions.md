# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**.

- 2026-09-14 · выбрал пересчитывать в тесте свою версию порогов (`NEW_VERSION`) вместо действующей — потому что пересчёт идёт по всем проектам базы: по действующей версии он переписывал 40 чужих вердиктов стенда, заменяя источник `LIVE` на `FIXTURE`, а проверяемое свойство («смена порогов бесплатна») от выбора версии не зависит вовсе
- 2026-09-14 · выбрал удалять вердикты своей версии до удаления самой версии в `_cleanup` вместо простого `delete(Ruleset)` — потому что `verdicts.ruleset_id` объявлен с `ON DELETE RESTRICT` (`storage/models/verdict.py`), а пересчёт по своей версии создаёт 40 вердиктов — по одному на каждый проект базы, — и уборка упала бы ровно там, где тест впервые что-то посчитал
- 2026-09-14 · выбрал сторожа на весь прогон (`stand_verdicts_survive_the_suite` в `tests/conftest.py`) вместо одной проверки внутри `test_api_thresholds.py` — потому что дефект принадлежит классу «тест переписывает строки, которых не создавал», а не одному тесту: следующий такой же появится в другом файле, и проверка, стоящая в этом, его не увидит (класс урока L127)
- 2026-09-14 · выбрал сравнивать источник вердиктов **существовавших до прогона** проектов вместо полного снимка таблицы — потому что новые строки тест создаёт законно и убирает сам: сторож в `tests/conftest.py` сравнивает ключи `(project_id, ruleset_id)` снимка «до», а требование «таблица не изменилась» краснело бы на каждой честной поставке — 550 тестов дают десятки таких строк, — и сторож, который врёт, выключают
- 2026-09-14 · выбрал молчание сторожа при недоступной таблице вместо падения — потому что на чистой базе CI до первой миграции `verdicts` ещё нет, и сравнивать не с чем: сторож, роняющий прогон там, где нечего стеречь, — это второй дефект вместо одного
