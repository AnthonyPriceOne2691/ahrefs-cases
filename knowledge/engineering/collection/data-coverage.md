---
type: Policy
title: Покрытие данными: «не покупали» против «нет данных»
description: Три разных состояния отсутствия данных и правило, по которому они не сливаются.
status: stable
tags: [coverage, classification, units]
generated:
  by: claude/agent
  at: 2026-09-12T00:30:00Z
implementation:
  - src/ahrefs_cases/classify/coverage.py
  - src/ahrefs_cases/collect/purchases.py
  - src/ahrefs_cases/classify/verdicts.py
  - src/ahrefs_cases/classify/rules.py
  - src/ahrefs_cases/classify/recalc.py
  - src/ahrefs_cases/collect/cache.py
---

# Три состояния, которые нельзя слить

| Состояние | Чем чинится | Что говорит сервис |
|---|---|---|
| **не покупали** — версия порогов просит месяцы **вне периода работ** (baseline до старта) | деньгами: догруз стоит не меньше 50 units на домен | вердикт `insufficient_data` с перечислением месяцев — и он **записывается** |
| **нет у Ahrefs** — месяц внутри периода работ отсутствует | ничем: столько данных есть | вердикт выдаётся, а усечённое окно названо справочной записью `point_a_months_used` |
| **дыра у Ahrefs** — месяц внутри собранного отрезка отсутствует | ничем | судит правило достоверности серии, вердикт `insufficient_data` законен |
| **истории нет вовсе** | решением человека о проекте | вердикт `insufficient_data` |

**Не куплена бывает не только дата, но и МЕТРИКА, и различает их другой
источник** (16.09.2026). Таблица выше — про месяцы: их просит версия порогов, и
чего не хватает, видно по самой серии. С метрикой так нельзя: непокупленная
история и пустая история выглядят одинаково — отсутствием точек, и по данным
они неразличимы в принципе. Отвечает журнал расхода units (`collect.purchases`,
`units_ledger`: endpoint + домен) — единственный след платежа; отдельной записи
«метрику купили» в базе нет. Различие ведёт к разным действиям и разным
деньгам: непокупленное (шаг 2 платится только кандидатам в кейсы, ~95 units на
домен) докупается, пустое — нет. Журнал про домен молчит — правильный ответ
«неизвестно», а не «не покупали».

**Граница «не покупали» проходит по периоду работ, а не по краям собранной
серии** (13.09.2026). Шаг 1 покупает период целиком — значит месяц внутри
периода, которого нет в серии, не куплен только потому, что его нет у Ahrefs.
Прежняя редакция считала такой месяц непокупленным и запрещала вердикт: живая
проверка застала девять проектов из девятнадцати без вердикта после активации
версии порогов — карточка пустая, причина нигде.

**Пересчёт не оставляет проект без вердикта, а переключение версии — без
однозначного ответа «какая действует».** Вердикт `insufficient_data` с
названными месяцами записывается всем, а действующая версия ровно одна:
ограничение базы, а не порядок вызовов (`uq_ruleset_single_active`,
13.09.2026).

**Пустота запоминается на срок от последнего настоящего запроса** (24.09.2026,
Z33). Домен, отдавший пустую историю `AHREFS_EMPTY_CONFIRMATIONS` раз подряд,
не спрашивается `AHREFS_EMPTY_RETRY_DAYS` дней (`cache.empty_since`). Прогон,
взявший пустоту из памяти, пишет исход `ok` с причиной «истории нет, проверено
…» — и в подсчёт подтверждений не входит, а время проверки берётся мимо него.
Прежняя редакция считала его «собранным»: он рвал цепочку «нет данных» подряд,
и пустой домен покупался в двух прогонах из трёх — на фикстурах бесплатно и
потому незаметно, вживую от 50 units за запрос. Засчитать его проверкой было бы
ошибкой с другой стороны: каждый прогон из памяти продлевал бы её бесконечно.
Судьбы сборки кейсов (`RunItemOutcome.CASE_…`, 24.09.2026, Z46) в подсчёт не
входят тоже: Ahrefs эта ступень не спрашивает, а строку журнала пишет каждому
проекту, пустому домену — «вердикта этой версии нет» или «данных не хватило», и
после двух «нет данных» такая строка рвала бы цепочку так же, как исход из
памяти. Собранный кейс (`ok`) в подсчёте остаётся: его собирают только проекту с
данными, а память нужна проекту без них.

**Купленный месяц принадлежит домену, а не кампании.** Кэш спрашивает
`project_id`, поэтому две кампании одного сайта платили за общие месяцы дважды
(Z8). Перенос точек между кампаниями одного домена и режима
(`cache.share_twin_points`) закрывает это без запросов к Ahrefs — но только
внутри отрезка проекта: перенос без границы растянул бы серию назад и создал бы
в ней дыру, то есть починил бы деньги ценой вердиктов.

**Вердикт помнит, по каким рядам он посчитан** (`verdicts.source`, 14.09.2026).
Источник — не свойство прогона, а свойство чисел: их кладут на один лист с
кривыми, нарисованными по рядам, и совпадение этих двух миров держалось только
на том, что человек звал `classify` и `cases` в одном режиме. На стенде, где по
проекту есть и живые, и фикстурные ряды, это дало кейс с таблицей «35 394 →
60 101» и кривой до 1 058 129 (Z10). `NULL` означает «вердикт старше поставки»
и читается как «источник неизвестен» — подставлять ему значение нельзя, это и
была бы та самая ложь; лечится бесплатным пересчётом.

**Правило живёт в расчёте вердикта, а не в обёртке.** Оно стояло в `evaluate`,
которую зовут предпросмотр и пересчёт, — а `classify` звал `compute_verdict`
напрямую и правила не видел: один проект получал «good» от классификации и «не
хватает данных» от предпросмотра. Проверку нельзя обойти, если она там же, где
считается вердикт.

Первое появилось 11.09.2026 вместе с экономией сбора: данные покупаются под ту
версию порогов, что действует **в момент сбора**. Пока история бралась с запасом
«на всякий случай», зависимости не было — теперь есть, и пересчёт обязан её
называть, а не усреднять то, что нашлось.

# Почему это опасно молча

Точка считается как среднее по тем месяцам окна, **которые есть**. Вердикт по
половине окна неотличим от настоящего, и на калибровке он объясняет расхождение
с экспертной оценкой неверно: спорить будут с порогом, а виновата нехватка
данных.

Поэтому «сколько месяцев окна реально посчитано» говорится вслух: справочные
записи `point_a_months_used` / `point_b_months_used` («точка посчитана по 1 мес.
из 2»). Группу они не меняют — как и `history_truncated`, у которого есть
допуск в один месяц и который поэтому про этот случай молчит.

# Три вопроса к кэшу, а не один

| Вопрос | Кто задаёт | Чем отвечаем |
|---|---|---|
| с какого месяца догружать непрерывную историю | шаг 1 | `next_date_from` по watermark |
| куплено ли окно точки целиком | шаги 1–2 | `window_is_covered`, решение бинарное |
| какого отрезка внутри окна не хватает | ступень кейса | `missing_span` |

Третий появился 11.09.2026 и без него ступень кейса не работала бы **молча**:
после шага 2 у позиций есть первые и последние месяцы периода, watermark
показывает последний, и «докупать нечего» — при том что середины нет. Тот же
класс, что L27: одна граница описывает только непрерывную серию.

# Кэш

Кэш закрытых месяцев — главный рычаг экономии, но не безграничный: минимум за
запрос упирает догруз в 50 units на домен, сколько бы месяцев ни догружалось.
Для окна точки решение бинарное — окно либо куплено целиком, либо покупается
заново: сузить его нельзя, цена та же.
