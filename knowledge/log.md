# Log

## 2026-09-11

- Появилась **ступень кейса**: докупает то, что показывают, и только тем, у кого
  кейс будет. Кривая позиций берётся двумя полями вместо пяти (строка 21 против
  51), стоимость трафика и DR — числами на границах. Прогон по ТЗ: 33 560 units.
- `data-coverage.md`: третий вопрос к кэшу — «какого отрезка внутри окна не
  хватает». Без него ступень кейса молчала бы: watermark считает серию с дырой
  в середине собранной.

- `collection-scheme.md`: схема выбирается **на endpoint**, а не на проект —
  у `keywords-history` строка 51 (точки дешевле в 4,5 раза), у
  `refdomains-history` строка 5 (дешевле история). Прогон по ТЗ: 28 620 units.

- Bundle развёрнут волной В2 (③ OKF). Триггер из `docs/CONTOUR_ROLLOUT.md` —
  инварианты домена перестали меняться каждый день: Ф3 закрыта, группы, пороги,
  точки А/Б, модель стоимости и схема сбора зафиксированы кодом и тестами.
- **Preflight §0 выполнен:** upstream SPEC прочитан
  (https://raw.githubusercontent.com/GoogleCloudPlatform/knowledge-catalog/main/okf/SPEC.md),
  версия `0.2` совпала с pinned в `OKF_KNOWLEDGE_BUNDLE.md` (сверка канона от
  2026-07-26). Расхождений нет, правка канона не потребовалась.
- Заведены концепты: воронка, группы и пороги, точки А/Б, модель стоимости
  Ahrefs, схема сбора, покрытие данными, первый живой прогон, карта репозитория.

- Заведён концепт **«Из чего состоит кейс»**: числа кейса — числа вердикта, и
  `build_case` серию не принимает вовсе. Оттуда же новое знание: вердикт бывает
  старше купленных данных (ступень кейса докупает DR после классификации), и
  кейс об этом говорит вслух, а не показывает неполный набор молча.

- Кейс стал файлом: HTML-шаблон A4 и PDF через WeasyPrint. Вместе с первым
  артефактом в канон вошёл **контент-запрет перед выдачей** (гео и текст,
  fail-closed по порядку вызовов) и обязательные тексты — сноска про оценку
  Ahrefs и атрибуция «рост после старта работ».
- `export` встал **выше** `cases` в контракте слоёв: рендер знает структуру
  кейса, кейс о форматах не знает вовсе.

- Кейс получил две кривые ТЗ. Вид выбран владельцем из двух собранных
  прототипов: плоские данные, объём даёт оформление — у изометрии верхняя грань
  прибавляла столбцу около тысячи визитов на шкале примера.
- Оттуда же два инварианта: кривая идёт по измеренным месяцам (сглаживания нет
  нигде) и **окна точек А и Б показаны на кривой** — последний месяц и точка Б
  различаются по построению.

- Кейс заговорил словами: текст собирается по числам структуры, и каждое число
  готового текста сверяется с ними программно. Первый же прогон поймал слабое
  место сверки — цифры внутри названий метрик («топ-10»), их пришлось
  исключить явно.
- Кейс появился в базе: `Case` с версией и `CaseArtifact` с контрольной суммой.
  Пересборка добавляет версию — перегенерация через полгода требование ТЗ.

- Ф4 закрыта: кейсы уходят пачкой. Имена по правилу ТЗ, внутри архива список с
  группой и пометкой «можно публиковать» — для человека, который смотрит ZIP
  перед публикацией (ревью черновика ТЗ не предусматривает).
- Команды кейсов переехали в `ahrefs_cases/cli`: скрипт перевалил за предел
  длины файла, и гейт был прав — в нём собрались четыре разные работы.

- Ф5 началась: сервис больше не анонимный. Вход по паролю (bcrypt), токен JWT,
  права проверяются зависимостью на роутере. Заглушка `current_group` из Ф1
  заменена настоящим источником — форма проверки не менялась.
- Убрана вторая копия правила прав: свойства `can_*` у модели пользователя.

- Появились читающие роутеры: проекты со списком и карточкой, библиотека кейсов
  со скачиванием, расход units. Все за правом `read`, у списков — потолок выдачи.
- По дороге нашлась дыра живого режима: источник остатка квоты не выбирался по
  конфигу, и прогон в `live` сверял смету с фикстурными 10 000. Теперь фабрика.

- Прогон запускается по HTTP и идёт в фоне. Очередь — интерфейс с двумя
  реализациями (`inline` и `redis`), выбор по конфигу; строку прогона создаёт
  обработчик, потому что только он знает, кто нажал кнопку (раньше все прогоны
  записывались на системного пользователя).
- Замок на второй прогон — советующая блокировка Postgres на время транзакции:
  между «активных нет» и «создал» помещается второй запрос.

- Ф5 закрыта: пороги правятся по API (сохранение и активация — за правом,
  предпросмотр открыт всем), сервис отвечает алертами про остаток units,
  упавшие прогоны и готовую пачку.
- Различие трёх групп доступа впервые видно по вызовам одного экрана: форма
  проверки прав была написана в Ф1 ровно под это.

- Управление людьми в сервисе: администратор заводит пользователей сам, пароль
  генерируется и показывается один раз, группа и **личные права** правятся по
  API. Приём с личными правами поверх роли перенесён из CRM агентства вместе с
  защитами (последний администратор, запрет выключить себя).
- Не перенесён открытый bootstrap первого администратора: у CRM под ним своя
  опора, у нас первый администратор заводится командой на сервере (урок L24).
