# Экономия units: замерено, посчитано, решено

Всё ниже — не оценки, а замеры живым ключом 10 и 12.09.2026
(`scripts/probe_units.py`, семь запросов в первый заход и семь во второй).

## Формула цены

```
стоимость запроса = max(50, строки × (1 + сумма цен полей))
```

| Поле | Цена |
|---|---|
| `org_traffic`, `org_cost`, `total_search_volume` | 10 |
| `refdomains` | 5 |
| `domain_rating`, `pages`, каждая корзина ключей (`top3`…`top51_plus`) | 1 |
| `date` | 0 |

**У каждого поля своя цена — главное открытие 12.09.2026.** Редакция от 10.09
(«10 за любое поле») выведена из единственного замеренного `metrics-history`,
где все поля дорогие, и перенесена на остальные по аналогии. На позициях она
завышала цену **в семь раз** (51 против 6 за строку), на ссылающихся доменах
занижала (5 против 6). Цена живёт в `collect/endpoints.py` таблицей
`FIELD_PRICES`, и оттуда же её берёт смета.

Минимум в 50 units существует — запрос на одну строку стоил 50 при расчётной
цене 21.

**Минимум и есть главное ограничение.** Он делает бессмысленным дробление на
мелкие запросы: три запроса по одной строке стоят 150, один запрос на 13
строк — 143.

## Что проверяли и что вышло

| Гипотеза | Результат | Вывод |
|---|---|---|
| `date_to` режет строки | **да**: окно 2 месяца = 2 строки = 50 units | точки А и Б покупаются отдельно от истории |
| кэш Ahrefs даёт скидку | **нет**: повтор при `x-api-cache: hit` стоит те же 50 | наш кэш закрытых месяцев экономит по-настоящему, чужой — нет |
| `history_grouping=yearly` | **не существует** (только daily, weekly, monthly) | число строк уменьшить группировкой нельзя |
| точечный `metrics` дешевле | **нет**: 50 за одну строку (44 за строку) | окно из двух строк за те же 50 выгоднее |
| второе поле дорого | **да**: строка 21 против 11 | `org_cost` на шаге 1 удваивает цену, а классификации не нужен |
| минимум за запрос | **есть, 50** | дробить нельзя; экономия только в сокращении числа запросов |

## Точка безразличия: 8 месяцев, то есть 9 строк

Две точки стоят 100 units всегда (два запроса по минимуму). Полная история
стоит `строки × цена строки` — ниже она посчитана для шага 1, где строка стоит
11 (одно поле `org_traffic`); у дешёвых endpoint'ов граница другая. **Строк на
одну больше, чем месяцев**: запрос отдаёт обе границы включительно, и период
18 месяцев — это 19 строк. Считать надо по
строкам, иначе граница уезжает на месяц ровно там, где выбор и происходит.

| Период работ | Строк | Точки | История целиком | Что дешевле |
|---|---|---|---|---|
| 6 месяцев | 7 | 100 | 77 | **история** (и график достаётся даром) |
| 8 месяцев | 9 | 100 | 99 | **история** |
| 9 месяцев | 10 | 100 | 110 | точки |
| 18 месяцев | 19 | 100 | 209 | точки |
| 24 месяца | 25 | 100 | 275 | точки |

### Окно точки покупается до минимума — 4 месяца

Минимум в 50 units — не только ограничение, но и **неиспользованная ёмкость**.
При одном поле строка стоит 11, значит под минимум влезает четыре строки (44),
а пятая его пробивает (55). Окно точки в два месяца и окно в четыре стоят
одинаково; беря два, мы платим за четыре и половину выбрасываем.

Поэтому запрос берёт **четыре месяца**, а точка считается по тому окну, которое
задано версией порогов. Смысл — калибровка (Ф8): переход на медиану вместо
среднего (M1) или расширение окна не потребует повторного платного прогона по
всей сотне. Перекрытия не возникает: 4 + 4 месяца требуют периода от семи, а
точками собираются периоды от девяти.

**Планировщик обязан выбирать сам**, а не следовать одной схеме: короткие
проекты дешевле собрать целиком, длинные — точками. Разница на сотне доменов
достигает десяти тысяч units в обе стороны.

## Маршрут принят 11.09.2026: история на шаге 1, точки на шаге 2

Схема выбирается не по цене одного запроса, а по тому, **сколько раз данные
понадобятся**. Точки дают ровно два окна; история той же покупкой закрывает
график в PDF (Ф4), диагностику «что просело и когда» (Ф3в), пересчёт по любым
окнам при калибровке (Ф3б, Ф8) — и снимает Z6, потому что дыры в серии нет.

Разница на домене — 109 units (209 историей против 100 точками). Купить серию
всем сразу — плюс 10 900 на сотню; докупить потом одному домену — 209.
**Точка перелома: 52 домена.** «Плохих» в списке большинство, и Ф3в про них,
значит история для всех дешевле точек.

**Правило дыры в серии смотрит только на шаг 1.** `max_gap_months` считается по
`Metric.ORG_TRAFFIC` (`classify/verdicts.py`), то есть по серии шага 1; метрики
шага 2 на блокер достоверности не влияют. Значит Z6 возникает **только** если
точками собирать шаг 1 — а деньги не там:

| Endpoint | Цена строки | Историей 19 строк | Точками | Дешевле |
|---|---|---|---|---|
| `metrics-history` (шаг 1) | 11 | 209 | 100 | точки, на 109 |
| `keywords-history` (шаг 2) | 6 | 114 | 100 | точки, на 14 |
| `refdomains-history` (шаг 2) | 6 | 114 | 100 | точки, на 14 |

Цены строк — замер 12.09.2026. Прежняя редакция называла строку ключей 51 unit
(«10 за поле») и выводила отсюда, что деньги в шаге 2; на самом деле пять
корзин стоят по единице, строка — 6, и **деньги в шаге 1**: он единственный
платится за все сто доменов.

Два маршрута, оба посчитаны `scripts/units_profile.py`
(100 доменов, 30 кандидатов, 10 кейсов, период 18 мес.):

| Маршрут | Цена | Чем платим |
|---|---|---|
| **история шаг 1 + точки шаг 2** (принят) | **28 970** | на 10 900 дороже; зато Z6 не возникает, классификация не трогается, а график, диагностика Ф3в и пересчёт окон при калибровке достаются даром |
| точки везде (`--stage1 auto`) | 18 070 | правка классификации под намеренные дыры; диагностика «плохих» платная — 70 доменов × 209 = 14 630, и маршрут становится дороже первого |

Разница ровно в шаге 1: 209 units историей против 100 точками на каждый из ста
доменов.

Допущение названо явно: «диагностика нужна по большинству плохих» — из
формулировки Ф3в. Если Ф3в окажется разовым инструментом на один домен,
перелом сдвигается и точки на шаге 1 снова выигрывают.

## Сколько стоит прогон: считает модель, а не перо

Таблица ниже **генерируется** из тех же спек, по которым пойдёт прогон:
`python3 scripts/units_profile.py --write`. Сверку держит оракул
`tests/test_units_profile.py` — подорожал endpoint, документ краснеет.

Так вышло не от любви к автоматике. Число «сколько стоит прогон по ТЗ»
переписывалось руками дважды (11.09 и 12.09) и оба раза расходилось с кодом в
тот же день, когда менялась цена: в обращении одновременно жили «27 330»,
«33 560» и «18 310», и ни одно не считалось действующей моделью.

<!-- начало таблицы: scripts/units_profile.py --write -->
Профиль ТЗ: 100 доменов, период работ 18 месяцев (19 строк — обе границы включительно).

| Ступень | Endpoint | Схема | Строка | На проект | Проектов | Units |
|---|---|---|---|---|---|---|
| шаг 1, все проекты | `metrics-history` | full_history | 11 | 209 | 100 | 20900 |
| шаг 2, кандидаты | `keywords-history` | two_points | 6 | 100 | 30 | 3000 |
| шаг 2, кандидаты | `refdomains-history` | two_points | 6 | 100 | 30 | 3000 |
| ступень кейса | `domain-rating-history` | full_history | 2 | 50 | 10 | 500 |
| ступень кейса | `keywords-graph` | серия (по назначению) | 3 | 57 | 10 | 570 |
| ступень кейса | `metrics-value` | two_points | 11 | 100 | 10 | 1000 |
| **Итого** | | | | | | **28970** |

**28 970 units на прогон, 290 на домен.**
<!-- конец таблицы -->

**Что из этого следует для заказчика.** Первичный прогон не влезает в 10 000
units — нужен боевой лимит **от 35 000**: 28 970 плюс неснижаемый запас 1000 и
место на повторы упавших доменов. Названные раньше «183 units на домен и 4,5×
экономии» неверны, как и поправка «273 на домен»: первое считалось по модели
«50 за запрос», второе — по «10 за любое поле».

**Где деньги на самом деле.** Не в шаге 2, как считалось 11.09: строка
`keywords-history` стоит 6 units, а не 51 — пять корзин по единице. Дорог
**шаг 1**, потому что он единственный платится за все сто доменов, и он идёт
историей по решению 11.09.2026 (та же покупка даёт график, диагностику
«плохих» и пересчёт по любым окнам). Две точки на шаге 1 стоили бы 100 против
209, то есть 10 900 units экономии — и ровно этих данных не хватило бы Ф3в,
графику кейса и любой калибровке окон.

## Что для этого меняется в коде

1. **Шаг 1 просит два окна вместо истории** — или историю целиком, если
   период короче точки безразличия. Выбор считается по той же формуле цены.
2. **`select` на шаге 1 — одно поле** (`org_traffic`). `org_cost` переезжает
   в шаг графика, где он и нужен.
3. **Шаг 2 тоже окнами**: подтверждающим метрикам нужны дельты, а не история.
4. **История для графика — отдельная ступень**, только для тех, у кого будет
   кейс. Появится вместе с PDF в Ф4.

Кэш, реапер, предохранитель, журнал и смета не затрагиваются: они написаны
так, что не зависят от того, сколько строк мы просим.

## Что из этого видит человек (требование к Ф6)

Точка безразличия — не внутренняя деталь планировщика, а то, что влияет на
решение оператора. Экран загрузки (Ф6) обязан показывать это до запуска:

- **у каждого проекта** — как его будут собирать и почему: «период 6 месяцев
  → берём историю целиком, 66 units» против «период 18 месяцев → берём две
  точки, 100 units». Иначе смета выглядит произвольной: два похожих проекта
  стоят по-разному, и объяснения нет.
- **в смете прогона** — разбивка по способу: сколько проектов собираем
  целиком, сколько точками, во что обходится каждая группа.
- **подсказка на границе**: проект с периодом 9–10 месяцев стоит одинаково в
  обоих режимах, но собранный целиком даёт график бесплатно. Оператору
  полезно знать, что удлинение периода до десяти месяцев меняет схему сбора.
- **после прогона** — «стоимость запуска на 100 URL» отдельным числом: это
  метрика приёмки, названная заказчиком, и она должна быть на виду, а не
  выводиться из журнала вручную.

Заказчик подтвердил, что это нужно на экране (10.09.2026).

## Чего замеры не покрыли

- Цена `keywords-history` и `refdomains-history` замерена **не была** —
  расчёт для них ведётся по той же формуле, что подтвердилась на
  `metrics-history`. `refdomains-history` документация называет отдельной
  ценой в 5; трактуем как цену строки, а не запроса, потому что занизить
  смету опаснее, чем завысить.
- Поведение при 429 и реальная скорость прогона — шаг 6 чеклиста Ф7.

## Чего эта схема не лечит: цена повторного прогона

Всё выше — про **первичный** прогон. У повторного своя арифметика, и минимум за
запрос упирает её в потолок: догруз одного нового месяца стоит те же 50 units на
домен, что и догруз восемнадцати. На сотне доменов это 5000 units за каждое
ежемесячное обновление — половина бюджета первичного прогона, притом что
запускают прогоны минимум раз в месяц шесть-семь сотрудников.

Рычаг «закрытый месяц неизменяем, догружаем только новые» продолжает работать
(50 против 209), но экономия у него не безгранична, и наша формулировка её
преувеличивала.

Возможный ответ — `POST /v3/batch-analysis`: до 100 целей за один запрос, и при
построчном биллинге сотня доменов стоила бы 1100 units вместо 5000. **Параметра
даты у него нет** — значит он даёт только срез «сейчас», то есть точку Б
проектам с текущим `period_end`, и точку А не даёт никогда. Гипотеза H6 в
`docs/FINDINGS.md`, замер на 55 units — шаг 7 чеклиста Ф7. По документации это
не решается: цену внешнего API мы уже один раз вывели из доков и ошиблись в
девять раз (урок L22).
