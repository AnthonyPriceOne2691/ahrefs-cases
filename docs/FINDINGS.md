# Реестр находок: что известно, что не проверено, что не заработает

Один документ вместо трёх мест. Уроки о системе живут в
`delivery/archive/INDEX.md` (L1–L21) — они про то, как мы работаем. Здесь —
про **продукт**: где он честно работает, где стоит на гипотезе и где заведомо
сломается, если ничего не сделать.

Ведётся по ходу. Строка отсюда исчезает только когда проверена живым прогоном
или закрыта решением заказчика — не когда «кажется, что нормально».

Обновлено: 2026-09-10, после хотфикса Z1/Z4/Z5 и работы над Z2/Z3/H1–H5.

Куда это ведёт дальше: `docs/PHASE7_CHECKLIST.md` — что проверить первым живым
ключом за один заход; `docs/CALIBRATION_QUESTIONS.md` — что решает заказчик на
калибровке, с числами и вариантами.

---

## 0. Разведка живым ключом 10.09.2026: что она изменила

Один запрос по `ahrefs.com` (33 месяца истории) ответил на пять пунктов.

| Гипотеза | Итог |
|---|---|
| Z3 — формы history-ответа | **ПОДТВЕРЖДЕНА.** Ключ `metrics`, поля `date, org_traffic, org_cost`, даты вида `2024-01-01T00:00:00Z` — разбор уже это умеет |
| H4 — помесячная сетка | **ПОДТВЕРЖДЕНА.** Все точки — первые числа месяцев |
| H3 — глубина истории | **ПОДТВЕРЖДЕНА с запасом.** Просили с 2024-01, получили 33 месяца до 2026-09 |
| Z2 — имена полей квоты | **ОПРОВЕРГНУТА.** Ответ — `{limits_and_usage}`, вложенный объект; плоских `units_limit`/`units_usage` нет. Строгая проверка сработала как надо: вердикт «остаток неизвестен», а не «остаток ноль» |
| H1 — модель стоимости | **ОПРОВЕРГНУТА, и это самое дорогое открытие проекта.** Заголовок: `estimated=693` там, где наша модель обещала 50 — в 14 раз дешевле реальности |

### Второй заход разведки: семь замеров под шесть гипотез

`date_to` режет строки (окно 2 месяца = 50 units), кэш Ahrefs скидки **не
даёт** (повтор при `hit` стоит столько же), `yearly`-группировки не
существует, точечный `metrics` дороже окна. Минимум 50 за запрос подтверждён
и оказался главным ограничением: дробить запросы бессмысленно, экономия — в
сокращении их числа. Расчёт и решение — `docs/UNITS_OPTIMIZATION.md`.

Итог того захода: 183 units на домен вместо 832 — число устарело дважды, действующее считает `scripts/units_profile.py` (290 на домен).

### H1: биллинг построчный, а не за запрос

`693 / 33 строки = 21 unit ровно` при двух полях (`org_traffic`, `org_cost`).
Похоже на «строка × (10 за поле) + 1», но одной точки замера мало: она не
отличает «21 за строку» от «фиксированная цена плюс строки» и не показывает,
есть ли минимум в 50 units.

**Чем это грозит.** Наш нынешний шаг 1 просит период работ плюс три месяца
запаса — для полуторагодового проекта это 21 строка, то есть 441 unit на
домен вместо обещанных 50. Полный прогон по ТЗ — **83 160 units**. Смета была
занижена в девять раз: preflight пропускал бы прогон, который тратит вчетверо
больше ожидаемого.

Лимиты, которыми мерили, тестовые (ключ CRM — 5,5 млн, тестовый ключ
заказчика — 10 000); боевой лимит поднимут под задачу. Поэтому вопрос не
«влезем ли», а «сколько это стоит»: «стоимость запуска на 100 URL» заказчик
назвал метрикой приёмки.

Это меняет не настройку, а **схему сбора**: см. `docs/UNITS_OPTIMIZATION.md`.

## 1. Заведомо не заработает в живом режиме

Самый важный раздел. Здесь то, что на фикстурах зелёное, а с живым ключом
сломается — не «может быть», а по расчёту.

| # | Что | Почему сломается | Что делаем |
|---|---|---|---|
| Z2 | **Ключи ответа квоты угаданы** (`units_limit`, `units_usage`) | Имена из документации, живьём не проверялись | **Перестало ломаться молча:** отсутствие ключа теперь даёт вердикт «остаток неизвестен» с именем недостающего поля, а не ноль. Ноль при fail-closed означал вечный запрет прогонов, похожий на исчерпанную квоту клиента. Сама догадка проверяется в Ф7 |
| Z3 | **Формы ответов history-endpoint'ов угаданы** (`list_key`, имена полей) | Проверено только на подменённом транспорте | **Перестало ломаться молча:** неверный `list_key` — ошибка формы с перечислением пришедших ключей, а не «нет истории»; строки без единого разобранного поля — тоже ошибка. Раньше и то и другое выглядело как молодой домен и замолкало на месяц. Догадка проверяется в Ф7 |

| Z6 | **Правило дыры в серии несовместимо со схемой «две точки» на шаге 1** (с 11.09.2026 не блокирует: шаг 1 собирается историей — см. «Маршрут» в `docs/UNITS_OPTIMIZATION.md`) | Проект, собранный точками, имеет дыру во всю середину периода: 11 месяцев при полуторагодовом периоде против допустимых 2 (`eligibility.max_series_gap_months`). `_eligibility` даёт блокер `series_gap_months`, вердикт — `insufficient_data`. Это арифметика, а не настройка: чтобы дыра не превышала G, точку надо покупать каждые G+1 месяцев, а это дороже истории целиком уже при периоде от трёх месяцев. **Везде, где точки дешевле истории, правило срабатывает обязательно** | Классификация обязана различать дыру «не покупали» и дыру «у Ahrefs нет данных»; сегодня способ сбора нигде не хранится. Решение — `delivery/active/escalation.md`. Ни один из 222 тестов этого не видит: тесты классификации строят серии сами и через сбор не идут |

| Z7 | **Первичный прогон не влезает в бюджет заказчика** | Прогон по ТЗ стоит 28 970 units (290 на домен) при названном заказчику бюджете 10 000. Прежние редакции этого числа — 18 310, 27 330, 33 560 — считались по опровергнутым моделям цены: сначала «50 за запрос», потом «10 за любое поле». Деньги не в шаге 2, как думали 11.09: строка `keywords-history` стоит 6 units, а не 51. Дорог шаг 1 — единственный, кто платится за все сто доменов | **Считает модель, а не перо:** `scripts/units_profile.py`, сверка документа — `tests/test_units_profile.py`. Заказчику нужен боевой лимит **от 35 000**. Маршрут «точки везде» стоил бы 18 070, и разница 10 900 — это названная цена графика, диагностики Ф3в и пересчёта окон при калибровке |

### Исправлено

| # | Что было | Как закрыто |
|---|---|---|
| Z4 | Первичный прогон **не влезал в бюджет заказчика**: по нашей модели он стоил 9650 units при бюджете 10 000, а неснижаемый запас был 5000 — половина бюджета. Preflight отказал бы на первом реальном запуске, и это выглядело бы как нехватка квоты у клиента | Найдено, что 3000 units из 9650 уходили на `pages-history` и `total-search-volume-history` — метрики, которых нет ни в правилах Ф3, ни в блоках кейса по ТЗ. Они выключены флагами (как DR), запас снижен до 1000. Теперь прогон стоит 6650 + запас 1000, остаётся 2350. Тест считает по тому же набору спек, что и смета, поэтому поймает и подорожание |
| Z5 | Период работ длиннее 21 месяца **молча обрезался** (`max_history_months` 24 минус запас 3): точка А бралась не от старта работ, «рост А → Б» описывал не весь срок, и узнать об этом было неоткуда | В вердикт добавлена справочная запись `history_truncated`: сколько месяцев истории не хватило. Группу она не меняет — решает человек, но теперь он видит |
| Z1 | Реапер добивал живой прогон: худший прогон шага 1 — 120 минут при пороге реапера 60 | Прогон получил собственный жёсткий лимит (`COLLECT_RUN_TIMEOUT_SEC`, 4 часа) и останавливается по нему сам — утверждение «живой прогон не идёт дольше лимита» стало правдой, а не предположением. Порог реапера обязан быть строго больше лимита, и это проверяет конфиг на старте процесса. Умолчания посчитаны от худшего случая, а не подобраны |

## 2. Гипотезы, на которых стоят расчёты

Работают, но проверены не были. Каждая — кандидат на расхождение в Ф7.

| # | Гипотеза | Цена ошибки | Где живёт |
|---|---|---|---|
| H1 | ~~Стоимость запроса = `max(50, 10 × число полей)`~~ — **опровергнута замером**, см. раздел 0. Гипотезой осталась только цена `refdomains-history` = 5: замерялся один `metrics-history` | Смета разойдётся с фактом; «стоимость запуска на 100 URL» — метрика приёмки заказчика | **Слышно сразу:** расхождение сметы с фактом больше `AHREFS_ESTIMATE_TOLERANCE_PCT` = 20 % пишется как `ahrefs_estimate_missed` с обоими числами. Модель живёт в одной функции — правка однострочная |
| H2 | Закрытым считается любой месяц, кроме текущего календарного | Если Ahrefs достраивает предыдущий месяц позже, мы навсегда сохраним неполные данные и не узнаем: повторный сбор их не перезапросит | **Инструмент готов:** `scripts/probe_next_day.py` снимает снимок месяцев и сравнивает со снимком прошлого запуска, называя вердикт по трём исходам (меняется текущий / меняется закрытый / не меняется ничего — последнее вопрос не закрывает). Нужны два запуска в разные дни: 50 units за домен, запускает человек |
| H3 | На тарифе Advanced глубина истории покрывает `max_history_months` = 24 | Просим 24 месяца, получаем меньше и считаем это свойством домена, а не тарифа | **Измеряется:** `warn_if_shallower_than_asked` считает, сколько месяцев недодали, и пишет это в лог с доменом и endpoint'ом. В вердикте то же видно как `history_truncated` |
| H4 | `history_grouping=monthly` отдаёт по одной точке на месяц | Вся арифметика точек А/Б считает месяцы; недельная сетка сломает окна незаметно | **Проверяется:** `warn_if_not_monthly` логирует любую точку не с первого числа месяца — с примером и количеством |
| H5 | Пустой ответ = у домена действительно нет истории | Опечатка в домене, неверный параметр или чужая форма ответа дают ту же пустоту | **Закрыто:** памяти о пустоте нужно `AHREFS_EMPTY_CONFIRMATIONS` = 2 пустых ответа подряд (паттерн `consecutive_zero_runs` из CRM). Цена — один лишний запрос на домен против месяца молчания о собственной ошибке |
| H6 | ~~`POST /v3/batch-analysis` биллингуется построчно~~ — **подтверждено замером 14.09.2026.** Путь — `POST /v3/batch-analysis/batch-analysis` (в v3 группа и endpoint называются одинаково; `/v3/batch-analysis` отвечает 404 и стоит 0). Цена строки — **10 units** при одном поле (`x-api-units-cost-row: 10`), и она построчная: 5 целей — 50, 6 целей — 60. Пять целей давали ровно минимум за запрос и различить «построчно» от «фиксированно» не могли; хватило шести, а не двадцати, как предполагал чеклист — 60 units вместо 200 | **Рычаг подтверждён: сотня доменов в одном запросе стоит 1 000 units вместо 5 000** (50 на домен за history-догруз). Ограничение тоже подтверждено замером: **даты в ответе нет** — только `org_traffic` числом, то есть срез «сейчас». Значит batch даёт точку Б **только** проектам, у которых период кончается текущим месяцем, и точку А не даст никогда. **Но применимость на наших данных — нулевая:** точка Б привязана к `period_end` проекта, а не к «сейчас», и на стенде (40 проектов, включая калибровочный набор) **ни у одного** период не кончается текущим месяцем — самый свежий 2025-12. Рычаг работает только для продолжающихся кампаний. Прежде чем делать поставку под batch, спросить заказчика, есть ли такие в боевом списке: без них экономия 5 000 → 1 000 units не реализуема ни на одном проекте |

| H7 | «Число ключей» из must-have ТЗ равно сумме пяти корзин распределения (`top3 + top4_10 + top11_20 + top21_50 + top51_plus`) | Если корзины не покрывают все ключи сайта (например, `top51_plus` обрезан сотней позиций), «видимость» в кейсе будет занижена — и заметить это можно только сверив с интерфейсом Ahrefs | **Не проверено.** Отдельный запрос под «число ключей» не делается: это было бы +50 units на кейс за число, которое уже должно быть в купленном распределении. Проверяется в Ф7 глазами: сумма корзин против `organic_keywords` в интерфейсе по одному домену |

| H8 | ~~`units_usage_api_key` растёт от наших запросов~~ — **проверено 13.09.2026 и исправлено.** Счётчик реален: за сутки 530 812 → 532 476 (+1 664). Но он **отстаёт**: запрос на 50 units не отразился в нём и через несколько минут | **Закрыто:** из ответа API вычитается собственный журнал за окно отставания (`budget.uncounted_spend`, `AHREFS_UNITS_COUNTER_LAG_HOURS` = 24 ч), а у `preflight` не осталось вычетов с умолчанием. Экран расхода показывает остаток как есть и рядом — сколько из него счётчик ещё не видит. Ошибка односторонняя: лишний отказ вместо прогона, оборванного на середине |

| — | **Воркспейс расходуют соседние сервисы агентства, и быстро.** За сутки 12→13.09 остаток воркспейса упал с 4 215 578 до 3 852 745 — 362 833 units не нашими запросами (наших было ~2 262) | Подтверждает решение 12.09: потолок прогона — минимум из остатка ключа и остатка воркспейса, а не остаток ключа |
| Z8 | ~~**Один домен с двумя периодами работ покупается дважды**~~ — **исправлено 14.09.2026.** Проект опознаётся тройкой `(домен, режим, начало периода)`, а ряды лежат с `project_id`: кэш «закрытый месяц неизменяем» смотрел только на свой проект, и общие месяцы двух кампаний одного сайта покупались по второму разу — 209 units на лишнюю историю по профилю ТЗ | **Закрыто в двух местах.** В плане — реестр уже заказанного (`plan._drop_twin_months`): месяц, который просит соседняя кампания того же домена и режима, второй раз не заказывается, и экономия попадает **в смету**, которую сверяет `preflight`. В базе — перенос (`cache.share_twin_points`) до и после прогона: кампания получает чужие месяцы бесплатно, но только внутри своего отрезка (`scheme.history_span`) — иначе давняя кампания растянула бы серию и создала в ней дыру. Проверено живьём на новом домене: две кампании, 3 запроса вместо 4, у обеих ряд по 12 месяцев без дыр, в отчёте «месяцев перенесено: 6» |

| Z9 | ~~**Отбор кандидатов шага 2 мерил рост иначе, чем классификация**~~ — **исправлено 14.09.2026.** Префильтр считал `последняя точка / первая`, вердикт — средние по окнам порогов. На сезонных рядах расхождение вдвое: `wineverygame.com` 0,93× против 1,86×, `wickes.co.uk` 1,03× против 1,48× при пороге 1,10× | **Закрыто:** кандидатов считает `classify.candidates.stage2_candidates` — «тот, кого действующие пороги уже не считают плохим». Модуль `collect/funnel.py` удалён: две меры одного роста — это и был дефект. Найдено калибровочным прогоном: два публично признанных победителя не могли стать `good` **ни при каких порогах**, потому что подтверждающие метрики им не покупались, а в объяснении это выглядело как «метрика не сработала» |

| Z10 | ~~**Кейс может показать таблицу и график по разным данным**~~ — **исправлено 14.09.2026.** `Verdict` не хранил источник рядов: таблица и текст брались из вердикта, кривые — из рядов названного источника, и при живых и фикстурных рядах одного проекта в один PDF попадали числа из разных миров (`wickes.co.uk`: в таблице «35 394 → 60 101», на графике 1 058 129 — разница в семнадцать раз) | **Закрыто:** вердикт хранит `source`, и кейс отказывается собираться дважды — когда источник вердикта не тот, что у рядов (включая «не записан вовсе»), и когда числа точек не воспроизводятся по этим рядам пересчётом тем же кодом (`classify.points`, окна из версии порогов вердикта). Исход `verdict_mismatch` называет причину и следующий шаг — бесплатный `classify`. Карточка проекта предупреждает о том же расхождении: она кладёт рядом ту же пару. Проверено на стенде — 14 вердиктов без источника отказались собираться поимённо |

| Z11 | ~~**Пересчёт по купленным живым рядам требует поднять живой режим**~~ — **исправлено 14.09.2026.** Флаг `--source` получили только показывающие команды (L138), а `classify`, `explain`, `preview`, `recalc` и `diagnose` брали источник из режима провайдера, хотя в Ahrefs не ходят | **Закрыто:** правило выбора источника живёт в одном месте (`cli/source.py`) и применено к **классу** читающих команд; платящие флага не имеют намеренно — они читают тем же режимом, которым покупают. Проверено на стенде: `classify --source live` при провайдере `fixture` дал 40 вердиктов с источником `live`, `cases --source live` — 10 кейсов и ноль расхождений, ни одного запроса к Ahrefs. Заодно написан оракул, обещанный поставкой L138 и не написанный тогда |

| Z12 | ~~**Прогон тестов переписывает вердикты стенда**~~ — **исправлено 14.09.2026.** `test_recalc_runs_without_touching_ahrefs` делал `POST /api/rulesets/{действующая}/recalc`, а пересчёт идёт по всем проектам базы: 40 вердиктов стенда, посчитанных по живым рядам, становились фикстурными. Правило уборки «свои строки» этого не ловило — строки не создавались, а перезаписывались | **Закрыто:** тест пересчитывает свою версию порогов и убирает её вердикты до удаления самой версии (`ON DELETE RESTRICT`). Сверх того — сторож на весь прогон (`tests/conftest.py::stand_verdicts_survive_the_suite`): источник вердиктов проектов, существовавших до прогона, обязан остаться прежним. Проверено на настоящем дефекте: со старой версией теста сторож краснеет и называет пары «было → стало» |

| H2 | ~~Закрытый месяц неизменяем~~ — **замерено 14.09.2026, и ответ не бинарный.** Снимок `ahrefs.com` через сутки: июль 4 550 157 → 4 550 **154** (−3 визита), август 5 976 774 → 5 976 **772** (−2), текущий сентябрь 4 301 827 → 4 304 125 (+2 298). То есть закрытые месяцы **меняются**, но на единицы визитов из миллионов — 0,00007 %, при том что решение о группе принимают пороги в десятки процентов | **Правило кэша остаётся.** Буквально гипотеза опровергнута, по существу — подтверждена: дрейф на шесть порядков ниже любого порога и не двигает в кейсе ни одной показанной цифры. Отступить `closed_through` на месяц назад, как советует пробник, значило бы платить 50 units за домен на каждом обновлении, чтобы поправить три визита. **Что надо сделать:** записать допуск числом (дрейф закрытого месяца < 0,01 % считается шумом) и проверять его тем же пробником, а не менять правило |

| Z13 | ~~**Пробник H2 выносит бинарный вердикт по оценочной величине**~~ — **исправлено 14.09.2026.** `scripts/probe_next_day.py` сравнивал закрытые месяцы на точное равенство: расхождение в 3 визита из 4 550 157 (0,00007 %) давало «H2 ОПРОВЕРГНУТА» и совет отодвинуть границу кэша — 50 units за домен на каждом обновлении ради трёх визитов | **Закрыто:** расхождение меряется в процентах, допуск `NOISE_PCT` = 0,01 % (на три порядка выше замеренного шума и на шесть ниже порогов, решающих группу). Дрейф в пределах допуска — отдельный исход «подтверждена по существу» с печатью максимума: молчание о нём означало бы, что следующий замер сравнивает с неверной картиной. Значимое расхождение и любой сдвиг нулевого значения опровергают правило по-прежнему |

| Z14 | ~~**Транспорт не уважает `Retry-After`**~~ — **исправлено 14.09.2026.** `ahrefs_transport._get_with_retries` повторял 429 по собственной лесенке пауз и заголовок не читал вовсе: если Ahrefs просил подождать дольше, все попытки сгорали внутри окна запрета, домен уходил в `failed`, а серия падений поднимала предохранитель и останавливала прогон целиком | **Закрыто:** пауза берётся из `Retry-After` в обеих формах RFC 9110 (секунды и HTTP-дата); негодное значение — не «ждать ноль», а прежняя лесенка; сверху потолок `AHREFS_RETRY_AFTER_MAX_SEC` = 300 с, и просьба подождать дольше даёт отказ с названной величиной, а не сон внутри запроса. Сам 429 живым ключом выжать не удалось (75 запросов, 40 параллельно — ни одного), поэтому правило проверено подставным ответом и встретится в бою впервые |

| Z15 | ~~**«Домен = проект» ломается на второй кампании**~~ — **исправлено 14.09.2026.** Найдено живым прогоном: `collect --only` сверял **количество** найденных проектов с числом названных доменов, и на сайте с двумя кампаниями падал с сообщением «в базе нет проектов: » — без единого имени. Тем же местом болели `explain` и `diagnose`: `.first()` молча показывал одну кампанию из двух, а какую именно — решал порядок строк в базе | **Закрыто:** `--only` сверяет множества доменов, а не длины списков; `explain` и `diagnose` разбирают **каждую** кампанию и печатают её период. Это тот же класс, что L142: правило «домен опознаёт проект» верно ровно до второй кампании, и чинить его надо у всех, кто принимает домен снаружи, а не у того, на ком заметили |

| Z16 | ~~**Кейс мог затереть чужой файл на диске**~~ — **исправлено 14.09.2026.** Имя файла задано ТЗ («название сайта + Кейс») и **кейс не опознаёт**: анонимные проекты одной ниши получают одинаковое «сайт в нише travel — Кейс.pdf», а с сентября 2026 — ещё и две кампании одного сайта. Архив это разводил (`_unique`), а `render_pdf` писал по имени как есть: второй кейс молча затирал первый, и на диске оставался файл с чужим проектом внутри | **Закрыто:** правило разведения имён одно на оба пути выдачи (`pdf_renderer.unique_name`, архив зовёт его же). Правил при этом два, и смотрят они в разные стороны: **чужое не затираем** (совпало имя при другом содержимом — «(2)») и **своё не дублируем** (рендер детерминирован, и тот же кейс, собранный заново, — тот же файл, а не близнец). Каждое по отдельности даёт дефект: первое без второго засыпает каталог копиями и лишает контрольную сумму артефакта смысла |

| Z17 | **Право `change_technical_settings` не проверяет ни один эндпоинт** | Оно объявлено в `api/deps._GROUP_RIGHTS`, выдаётся группе инженера, показывается на экране пользователей — и ничем не закрыто: поиск по репозиторию не находит ни одной проверки. Ровно этим правом инженер и отличается от админа, то есть **вся разница между двумя группами сейчас ни на что не влияет**. Найдено 14.09.2026 при разборе вопроса «не убрать ли роль инженера» | В самом коде на этот счёт написано прямо: «право, которого не проверяет ни один роутер, — обещание, а не доступ» (докстрока `ALL_RIGHTS`). Владелец решил 14.09.2026 **оставить три группы** — ТЗ называет именно три, и роль наполнится смыслом, когда появятся технические настройки (переключение провайдера, лимиты, ключи). До тех пор различие групп — обещание. Проверять стоит первым же экраном настроек: если он закроется правом `edit_thresholds`, роль инженера останется пустой навсегда |

| Z18 | ~~**Гейт покрытия диффа не судил ни разу**~~ — **исправлено 15.09.2026.** Шаг `Diff coverage` стоял в джобе `gates`, у которой нет Postgres, а сьют с 11.09 без дев-базы не стартует вовсе (`pytest.exit(returncode=1)` на сборе). Отчёта покрытия не появлялось, и гейт был красным **структурно** — на любом диффе, где есть изменённый Python | Четыре дня этого не было видно: каждый пуш с 11.09 нёс дифф без Python, гейт печатал «изменённых prod-файлов нет» и выходил нулём. Замер: прогон 34778321271 (13.09, зелёный) — «изменённых prod-файлов нет»; прогон 34940648465 (15.09, 25 файлов) — «coverage.json не найден». Шаг перенесён в джобу `tests`, где Postgres есть и сьют уже гоняется; покрытие берётся из его отчёта (`SKIP_TESTS=1`), второго прогона сьюта нет. Оракул — `tests/test_ci_gates_judge.py::test_suite_runner_job_has_a_database` |

| Z19 | ~~**Гейт покрытия выбрасывал причину обрыва**~~ — **исправлено 15.09.2026.** `check_diff_coverage.sh` отправлял весь вывод сьюта в `>/dev/null 2>&1` и печатал «coverage.json не найден (сьют не отработал?)» — со знаком вопроса, потому что сам не знал | pytest причину называл; её выбрасывали. Диагноз Z18 пришлось доставать сравнением с логом позапрошлого прогона CI. Теперь вывод сохраняется и последние 25 строк печатаются в самой ветке отказа. Там же закрыт соседний дефект: интерпретатор брался как `$VENV/bin/python` вслепую и в окружении без venv (джоба CI ставит проект в системный питон) гейт отдавал сырую ошибку шелла вместо суждения |

| Z20 | **Мутационный гейт не выносит вердикта** — **закрыт наполовину 15.09.2026.** В прогоне 34940648465 он зелёный при 25 изменённых файлах: «mutmut не может определить, что мутировать — гейт не судит». Причина была в конфиге: mutmut 3.x запускается из корня импортов (`src/`) и читает конфиг оттуда, а секция стояла в корневом `pyproject.toml` под ключом `paths_to_mutate` — имя из 2.x, мёртвое с 3.0 | Конфиг перенесён в `src/pyproject.toml` под ключом `source_paths`: область теперь читается, гейт видит 25 файлов. Вердикта по-прежнему нет по ДРУГОЙ причине, и она глубже: mutmut гоняет pytest из `src/mutants`, а сьют проекта предполагает корень репозитория (первым отказал сид порогов — `config/thresholds.default.yml` берётся относительно текущего каталога). Замером 15.09 подтверждено главное: **мутации до тестов доходят** — трассировка отказа идёт через `src/mutants/ahrefs_cases/…`, то есть подход рабочий. Ловушка рядом: `pythonpath = [".", "src"]` кладёт в путь импорта настоящий пакет, и при неверной настройке гейт напечатает не «не сужу», а ложное число. Доводить отдельной поставкой; пока гейт честно молчит и выходит нулём |

| Z21 | ~~**Гейт покрытия требовал покрыть удалённый файл**~~ — **исправлено 15.09.2026.** `list_changed()` брал `git diff --name-only` без фильтра, и удалённые файлы попадали в список изменённых. В отчёте покрытия их нет — гейт числил их «не исполнялся тестами вовсе» | Первый же дифф с удалением (`collect/funnel.py`, коммит fa052c2) сделал гейт непроходимым: тест писать не к чему, а `omit` пишется для живых путей. Выхода не существовало ни одного — §4.3b, и такие гейты снимают вместе с пользой. Добавлен `--diff-filter=ACMR`, у незакоммиченных правок отсечён статус `D` |

| Z22 | **Три файла CLI ниже цели покрытия 70 %** | Долг вскрыт 15.09.2026, когда гейт покрытия впервые вынес настоящий вердикт (см. Z18): `cli/collect_commands.py` 39,3 %, `cli/case_commands.py` 41,3 %, `cli/source.py` 69,2 % (последнему не хватает одной строки). Двенадцать тестов `test_cli_exit_codes.py` трогают только ранний выход по нечитаемому источнику. Отдельно найдено и закрыто: CLI проверяется запуском подпроцессом, и родительский `coverage` его не видел — включён сбор с подпроцессов (`[tool.coverage.run] parallel`, `COVERAGE_PROCESS_START` у `_run`), но это дало всего +2,7 пункта, то есть слепота замера была реальной и **малой**, а дыра в тестах — настоящей. Решение владельца 15.09: шаг в CI переведён в информационный режим (`continue-on-error`) со сроком **29.09.2026** и оракулом, который роняет сьют, когда срок выйдет (`test_the_coverage_waiver_expires_out_loud`). Закрывать поставкой `cli-commands-are-tested` |
## 3. Найденные ограничения метода (для калибровки Ф8)

Не ошибки кода — свойства выбранного способа считать. Все с числами.

| # | Находка | Числа | Следствие |
|---|---|---|---|
| M1 | Усреднение по окну **не защищает** от одиночного выброса, а делит его | Месяц с удвоением на плоской серии: +50 % при окне 2, +25 % при 4, +16,7 % при 6 — везде выше порога «средних» +10 % | Плоский проект с одним аномальным месяцем становится «средним». Защита — медиана, но это смена метода, заданного ТЗ |
| M2 | Усреднение систематически **занижает** линейный рост | Ряд, растущий на 12 % за 18 месяцев, по точкам даёт ≈ 11,3 % | Пороги ТЗ, если задавались как «последнее к первому», у нас срабатывают строже |
| M3 | Форма «слабый рост» стоит ровно на пороге | `weak_growth` ×1.12 при пороге +10 % и шуме ±3 %: 6 доменов из 12 ниже, 6 выше | Группа определяется шумом. Согласовать форму и порог — задача калибровки |
| M4 | Конфликт в самих порогах ТЗ | Приложение А требует «главную И подтверждающую» только для «хороших». Если требовать и для «средних», проект с ростом трафика +80 % без ссылочного не попадает **никуда** | Принято `medium.supporting_required: 0`; решение заказчика нужно |
| M5 | «Хороших» не бывает без шага 2 воронки | Шаг 1 приносит только трафик, `good` требует подтверждающую | Порядок «шаг 1 → префильтр → шаг 2 → классификация» обязателен |

| B6 | **UR, backlinks и «новые/потерянные домены» из must-have списка ТЗ не собираются** | History-endpoint'ов под них в разведке нет (только DR, pages, search volume), а в блоках кейса по ответам заказчика их тоже нет: гео, период, задача, что сделали, А → Б по трафику, позициям и ссылкам. Решение названо здесь, а не замолчано; вернуться к нему — когда появится читатель |

## 4. Объявленные границы (работает, но не везде)

| # | Что | Граница |
|---|---|---|
| B1 | Single-flight | Внутри процесса. Два воркера в Ф5 потребуют общего замка (Redis) |
| B2 | Google Sheet | Только опубликованная по ссылке; приватная — Ф5, когда будет где хранить креды |
| B3 | Чекпойнты | Прогон, убитый между ними, теряет до `COLLECT_CHECKPOINT_EVERY` = 10 доменов |
| B4 | Пороги заказчика | Вне репозитория (вариант D). В git — нейтральные умолчания; подставить настоящие — задача развёртывания |
| B5 | Предохранитель серии | Останавливает после N неудач подряд, но задачи, уже ушедшие в сеть, не отзываются: перелёт до `max_parallel` |

## 5. Ждём от заказчика

| # | Что | Что блокирует |
|---|---|---|
| C1 | Формула нормализации на длительность работ | Блок целиком не реализован; появится, когда придёт формула |
| C2 | Решение по M4 (подтверждающие для «средних») | Сейчас работает наш вариант, помечен в сиде порогов |
| C3 | Ключ Ahrefs (Advanced, API включён) | Ф7 целиком: Z2, Z3, H1–H4 |
| C4 | 10 доменов с экспертной оценкой | Ф8: калибровка порогов, M1–M4 |
