{
  "check_grep_gate.sh": {
    "reason": "раскладка проекта: сервис-слой не называется services/ — ядро разложено по темам intake/collect/classify/cases/export/storage/config, и инвариант «ядро не знает про обвязку» относится к ним. С канонным FILTER гейт просматривал 0 файлов, то есть был зелёным, не просудив ничего",
    "changed": "правило service-no-web: FILTER дополнен ветвью /ahrefs_cases/(intake|collect|classify|cases|export|storage|config)/; PATTERN дополнен starlette и rq (обвязка проекта — не только fastapi). Канонные ветви FILTER и PATTERN сохранены",
    "kind": "adaptation"
  },
  ".github/workflows/quality.yml": {
    "reason": "шаг «Canon payload selftest» искал объявление варианта D только в delivery/active/STATUS.md, а этот файл по правилу §2.3a не существует между поставками: CI краснел каждый раз, когда поставка уходила в архив — без ошибки исполнителя и без способа это исправить, не открыв новую поставку",
    "changed": "условие варианта B дополнено вторым источником: `grep 'stack-selftest:' delivery/STACK-ACCEPTANCE.md`. Канонная ветвь с delivery/active/STATUS.md сохранена и проверяется первой; объявление добавлено в STACK-ACCEPTANCE.md, где и так описано, где лежат каноны",
    "kind": "adaptation"
  },
  "okf_sync_gate.py": {
    "reason": "гейт написан под PR-поток: база — origin плюс base_ref. В этом проекте единственный исполнитель коммитит прямо в main (worktree: none), поэтому origin/main совпадает с HEAD и дифф пуст — гейт справедливо отказывается судить ноль файлов, но краснеет на каждом пуше без единого нарушения. Плюс сверка контура делается прогоном `pre-commit run --all-files`, где индекс пуст всегда",
    "changed": "база выбирается по потоку, сам скрипт не тронут: в pre-commit при пустом индексе — `--base HEAD~1`, при непустом канонный `--staged`; в CI на PR остаётся origin плюс base_ref, а на push в main берётся github.event.before (состояние до пуша) с откатом на HEAD~1. Оба режима скрипта канонные",
    "kind": "adaptation"
  }
}
