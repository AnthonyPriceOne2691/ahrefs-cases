# blind_error_baseline.txt — снимок grep-гейта. Генерируется --generate, НЕ руками.
# Правило: blind-error: сообщение об ошибке без контекста не локализует проблему
# Формат: <count>:<path> (path от repo-root).
# Ратчет вниз: файл проходит при count <= снимок; файл ВНЕ снимка (новый) — hard 0.
