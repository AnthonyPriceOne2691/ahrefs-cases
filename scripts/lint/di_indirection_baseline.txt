# di_indirection_baseline.txt — снимок grep-гейта. Генерируется --generate, НЕ руками.
# Правило: di-indirection: (pkg|session|ctx): Any / importlib reach-back
# Формат: <count>:<path> (path от repo-root).
# Ратчет вниз: файл проходит при count <= снимок; файл ВНЕ снимка (новый) — hard 0.
