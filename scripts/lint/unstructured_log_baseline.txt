# unstructured_log_baseline.txt — снимок grep-гейта. Генерируется --generate, НЕ руками.
# Правило: unstructured-log: значение вплавлено в текст лога вместо extra={...}
# Формат: <count>:<path> (path от repo-root).
# Ратчет вниз: файл проходит при count <= снимок; файл ВНЕ снимка (новый) — hard 0.
