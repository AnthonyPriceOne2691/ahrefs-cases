# inline_prompt_baseline.txt — снимок AST-гейта. Генерируется --generate, НЕ руками.
# Правило: inline-prompt: LLM-промпт инлайном в .py
# Формат: <count>:<path> (path от repo-root). Ратчет вниз: файл проходит при count <= снимок;
# файл ВНЕ снимка (новый) — hard 0.
