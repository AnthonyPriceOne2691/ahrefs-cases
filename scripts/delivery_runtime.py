#!/usr/bin/env python3
"""§12.6: путь, который проверяется только исполнением.

Часть `delivery_check.py` (`delivery@1.53`). Класс отказов, невидимый всем
статическим оракулам: платформа отвечает падением процесса, а не кодом ошибки.
"""

from __future__ import annotations

import fnmatch
import re
from pathlib import Path

from delivery_base import (BREAKER_EXCLUDE, DEFAULT_BREAKERS, ROOT, field,
                           is_placeholder)

# --- §12.6: путь, который проверяется только исполнением ---------------------
# Есть класс отказов, невидимый ВСЕМ статическим оракулам и необрабатываемый в
# рантайме: платформа отвечает на нарушение политики смертью процесса, а не кодом
# ошибки. Замер (iOS, четыре падения за сутки, разбор в поле): нет ключа в
# Info.plist — падение; запрос снимка сверх потолка выхода — NSException; чтение
# библиотеки под правом «только добавлять» — падение от TCC. Ни одно не возвращает
# ошибку, которую можно поймать. Сборка при этом ЗЕЛЁНАЯ во всех трёх случаях.
#
# Отсюда правило: такой путь обязан быть ИСПОЛНЕН до handoff. Не «покрыт тестом» —
# исполнен на целевой среде, потому что именно среда и выносит вердикт.
#
# Почему это отдельно от §12.5, хотя механика похожа. §12.5 доказывает, что дифф
# ПРОЧИТАН, и этого достаточно для «архитектура неудачна». Здесь чтение не
# помогает вовсе: все четыре отказа были в коде, который автор читал и считал
# верным. Рассуждение о правах — не проверка прав; замер поля прямо это назвал
# («я рассуждал о правах вместо того, чтобы их проверить»).
def declared_surfaces(status: str, key: str) -> tuple[set[str], bool]:
    """(объявленные пути, объявлено ли осознанно) из поля-объявления поверхности.

    Один разбор на два поля — `runtime_paths` (§12.6) и `model_surface` (§14.1).
    Форма у них одна по построению: список глобов либо отказ с причиной. Второй
    парсер той же формы разъехался бы на первой правке — `one-notion-one-place`,
    класс, который в этом каноне уже стоил ревизии.

    Второе значение отличает «объявили `none reason=…`» от «поля нет вовсе».
    Разница ровно та же, что у `n/a` без причины в остальном каноне: не думали и
    подумали-и-нет — разные состояния, и молчание не должно выглядеть как ответ.

    ⚠ `n/a` принимается наравне с `none`, и это починка, а не расширение вкусом:
    §14.1 пишет отказ словом `n/a`, §12.6 — словом `none`, а прежний разбор знал
    только второе. `runtime_paths: n/a reason=…` он возвращал ГЛОБОМ, то есть
    честный отказ с причиной читался как объявленный путь, не совпадающий ни с
    чем, — молчаливый пропуск там, где человек ответил.
    """
    raw = field(status, key)
    if is_placeholder(raw):
        return set(), False
    if re.match(r"(?i)^\s*(?:none|n/a)\b", raw):
        # Слово отказа без причины — то же, что его отсутствие: см. `n/a reason=`
        # у `diagnosis:`. Само по себе оно ничего не сообщает.
        return set(), bool(re.search(r"(?i)reason\s*=\s*\S", raw))
    return {p.strip() for p in raw.split(",") if p.strip()}, True


def breaker_value(status: str, name: str) -> int:
    """Порог breaker'а с учётом ЗАКОННОГО override, без дублирования принуждения.

    Override принимается по тому же правилу, что у остальных breaker'ов §3.4:
    либо строка `circuit_breakers:`, либо подписанный waiver (`reason=` +
    `by=human:`). Ругается на неподписанный override НЕ здесь, а тот же парсер,
    что и для `max_files_touched` — ключ лежит в `DEFAULT_BREAKERS`, поэтому
    ветка ошибки достаётся ему. Две реализации одного правила разъехались бы
    при первой же правке, а тут это значило бы «лимит поднят молча».
    """
    val = DEFAULT_BREAKERS.get(name, 1)
    for m in re.finditer(rf"({re.escape(name)})[ \t]*=[ \t]*(\d+)", status):
        bol = status.rfind("\n", 0, m.start()) + 1
        eol = status.find("\n", m.end())
        line = status[bol: eol if eol != -1 else len(status)]
        low = line.lower()
        if "circuit_breakers" in low or ("reason=" in low and "by=human:" in low):
            val = int(m.group(2))
    return val


def runtime_touched(paths: list[str], surfaces: set[str]) -> set[str]:
    """Какие объявленные поверхности задел дифф.

    Совпадение по префиксу И по glob: проект объявляет либо каталог
    (`ios/LashTryOn/Camera/`), либо маску (`**/Capture*.swift`). Требовать одну
    форму значило бы заставить подгонять раскладку под гейт.
    """
    hit: set[str] = set()
    for s in surfaces:
        pat = s.strip().rstrip("/")
        for p in paths:
            if p.startswith(pat + "/") or p == pat or fnmatch.fnmatch(p, s):
                hit.add(s)
                break
    return hit


def surface_named(surface: str, low: str) -> bool:
    """Названа ли поверхность в тексте блока — сама или содержательным сегментом.

    Вынесено на уровень модуля, потому что потребителей стало два (§12.6 и §14.4).
    Копия этого правила разъехалась бы молча: у неё нет ни снимка, ни порога, по
    которым расхождение было бы видно.
    """
    if surface.strip().rstrip("/").lower() in low:
        return True
    return any(tok and tok.lower() in low
               for tok in re.split(r"[/*]", surface) if len(tok) > 3)


def runtime_proof_gaps(report: str, touched: set[str]) -> list[str]:
    """Чего не хватает в записи об исполнении.

    ⚠ Прямо о пределе, как это делает §12.5 про своё: **доказательства, что
    прогон был, здесь нет и быть не может** — есть доказательство, что про
    КАЖДУЮ задетую поверхность сделано конкретное утверждение с датой и названа
    команда. Это ловит не ложь, а пропуск, и пропуск — ровно то, что случилось
    все четыре раза: путь не был исполнен НИ РАЗУ, а не исполнен плохо.
    """
    if not touched:
        return []
    block = re.search(
        r"(?ims)^#{1,4}\s*(?:исполнение\s+рисковых|runtime\s+run)\b.*?(?=^#{1,4}\s|\Z)",
        report,
    )
    if not block:
        return [
            "нет блока «## Исполнение рисковых путей» (§12.6) — дифф задел: "
            + ", ".join(sorted(touched))
            + ". Это пути, чей отказ не виден ни сборке, ни тестам — падением "
              "процесса (права, платформа) или молча (раскладка экрана). Нужен "
              "не вывод, что всё должно работать, а строка «прогнал <что>, "
              "увидел <что> at=<дата>»"
        ]
    body = block.group(0)
    low = body.lower()
    gaps: list[str] = []

    # Каждая задетая поверхность — своя строка. «Прогнал, всё ок» одной строкой
    # на три поверхности это отписка по частям, и §12.5 уже платила за то, чтобы
    # это понять: там пробой прошёл блоком с одним классом из трёх.
    # Поверхность считается названной ДВУМЯ способами: написали её саму
    # (`ios/UI/`) или назвали её содержательный сегмент (`Camera`, `Capture`).
    #
    # ⚠ Одних сегментов НЕ хватает, и это поймал собственный тест: у `ios/UI/`
    # нет ни одного куска длиннее трёх символов, поэтому удовлетворить проверку
    # было нечем ВООБЩЕ — гейт, который не пройти. Ложное срабатывание тут
    # дороже пропуска: §4.3b, такую проверку снимают с проекта за неделю вместе
    # с пользой. Сначала ищем саму строку пути, сегменты — только запасной путь
    # для глобов вида `**/Capture*.swift`.
    silent = [s for s in sorted(touched) if not surface_named(s, low)]
    if silent:
        gaps.append(
            "в блоке исполнения не названы задетые пути: " + ", ".join(silent)
            + " (§12.6) — на каждый нужна своя строка: исполнять их надо по "
              "одному, иначе отказ приходит составным и разбирается перебором"
        )
    if not re.search(r"(?i)\bat\s*=\s*\d", body):
        gaps.append(
            "в блоке исполнения нет отметки `at=<дата>` (§12.6) — прогон без "
            "даты неотличим от прогона до правки, а именно так и выглядел бы "
            "самый вероятный обход"
        )
    if "`" not in body:
        gaps.append(
            "в блоке исполнения не названа команда (§12.6) — впиши в backticks "
            "то, ЧЕМ исполнял (`-scanShot 1`, `plutil -p …`): инструмент, "
            "которого нельзя повторить, следующий раз будет выброшен, а не починен"
        )
    return gaps


# --- §14.4: дифф задел поверхность поведения модели --------------------------
# Здесь контур ТОЛЬКО спрашивает. Три требования §14.4 — снимок, оракул на путь,
# дифференциальный прогон — судят СОДЕРЖАНИЕ, и ставить их механикой раньше, чем
# в проекте работает порча промпта, запрещает сама §14.4: «проверка, проверять
# которую нечем». Замер полигона объясняет запрет числом: быстрый контур
# `local-web-agent` дал 0 из 4 на четырёх порчах — не потому, что оракулы слабы, а
# потому, что фикстуры не зависят от текста промпта по построению.
#
# Поэтому проверяется ровно то, что проверяемо сегодня: правка, задевшая
# объявленную поверхность, не уходит молча. Это ловит не плохую правку, а
# НЕНАЗВАННУЮ — тот же предел, что §12.6 называет про себя.
def model_surface_gaps(report: str, touched: set[str]) -> list[str]:
    """Чего не хватает в записи об изменении поверхности модели (§14.4).

    Заголовок блока ищется двумя формами (русской и английской) — так же, как у
    §12.6: проекты пишут отчёты на разных языках, и требование одной формы
    заголовка наказывало бы за язык, а не за пропуск.
    """
    if not touched:
        return []
    block = re.search(
        r"(?ims)^#{1,4}\s*(?:изменение\s+поверхности|model\s+surface\s+change)\b"
        r".*?(?=^#{1,4}\s|\Z)",
        report,
    )
    if not block:
        return [
            "нет блока «## Изменение поверхности модели» (§14.4) — дифф задел: "
            + ", ".join(sorted(touched))
            + ". Правка промпта или пина меняет поведение продукта при диффе в "
              "одну строку и зелёных гейтах: реализация не менялась, и молчать "
              "тут будет всё, кроме этой строки"
        ]
    body = block.group(0)
    low = body.lower()
    gaps: list[str] = []
    silent = [s for s in sorted(touched) if not surface_named(s, low)]
    if silent:
        gaps.append(
            "в блоке поверхности не названы задетые элементы: " + ", ".join(silent)
            + " (§14.4) — на каждый нужна своя строка: «поведение не изменилось» "
              "одной строкой на три элемента это отписка по частям"
        )
    if not re.search(r"(?i)\bat\s*=\s*\d", body):
        gaps.append(
            "в блоке поверхности нет отметки `at=<дата>` (§14.4) — запись без "
            "даты неотличима от записи, сделанной до правки"
        )
    return gaps


# --- §14.2a: двойник ниже модели ---------------------------------------------
# Поведенческое правило промпта держится либо исполнителем НИЖЕ модели (валидатор
# выхода, контрактная проверка, фильтр инструментов), либо самой моделью — то
# есть ничем проверяемым. Проба оси 5 назвала цену числом: инструкция «никогда
# не вводи пароль» нарушалась 6 из 6 и с правилом, и без него; безопасность
# держала механика, а правило в промпте было мёртвым с видом наличия. Обратный
# случай — правило без двойника — не виден вовсе и работает ровно до смены пина.
# Отсюда вопрос контура: где реестр «правило → двойник | advisory». Содержание
# реестра НЕ судится — та же граница, что у §14.4, и по той же причине.
def rule_enforcer_gaps(status: str, surfaces: set[str], root: Path = ROOT) -> list[str]:
    """Чего не хватает в объявлении двойников ниже модели (§14.2a).

    Вопрос задаётся только проекту, объявившему поверхность модели СПИСКОМ: без
    модели правил промпта нет, и строка там была бы ритуалом, который снимут
    вместе с правилом (§4.3b). Судится ФОРМА ответа: реестр назван и лежит
    файлом, либо отказ с причиной. Что каждое правило в реестре действительно
    несёт исполнителя — содержание, и раньше работающей порчи промпта судить его
    нечем (§14.4).
    """
    if not surfaces:
        return []
    raw = field(status, "rule_enforcers")
    if is_placeholder(raw):
        return ["поле `rule_enforcers:` в STATUS не заполнено (§14.2a) — назови реестр "
                "«правило промпта → исполнитель ниже модели | advisory» либо "
                "`n/a reason=…`. Правило, которое держит только модель, живёт пока "
                "модель его читает: проба дала 6 нарушений из 6 и с правилом, и без него"]
    if re.match(r"(?i)^\s*(?:none|n/a)\b", raw):
        if not re.search(r"(?i)reason\s*=\s*\S", raw):
            return ["`rule_enforcers: n/a` без reason= (§14.2a) — «правил в промпте нет» "
                    "и «не смотрели» обязаны различаться в тексте, а не в тишине"]
        return []
    path = raw.split()[0].strip("`\"',").split("#")[0]
    if not (root / path).exists():
        return [f"`rule_enforcers: {path}` — файла нет. Объявленный реестр без файла "
                "это снятая проверка с видом усиления (§4.6)"]
    return []


# --- §3.4a: красная черта, а не переключатель --------------------------------
# Все остальные breaker'ы считают накопление: файлы, строки, попытки, минуты.
# Этот срабатывает на ПЕРЕХОД состояния — проект стал таким, где агент дотягивается
# до необратимого, а человека в цепочке нет. Это случается один раз и мгновенно.
#
# Почему не опция с переключателем. Выключенная по умолчанию опция — это тот же
# класс, ради которого всё затевалось: механизм есть, подключён, зелёный,
# потребителя нет; про него просто забудут, и отсутствие подписи будет
# неотличимо от решения её не ставить. Включённая всегда — мешает там, где не
# нужна, и её снимут на третьем проекте (§4.3b). Поэтому ни того, ни другого:
# пока черта не перейдена, механизма как бы не существует.

#: Поверхности, которые видны по СОДЕРЖИМОМУ репозитория. Список по именам, а не
#: «любой вызов» — тот же выбор и та же цена, что у рецепта §2.7a CQG: recall это
#: список имён, он расширяется по мере встреч и обязан быть виден в диффе.
_OUTWARD_MARKERS = ("sendgrid", "mailgun", "postmark", "smtplib", "aiosmtplib",
                    "boto3.client(\"ses\"", "twilio", "telegram.bot")
_DEPLOY_MARKERS = ("deploy:prod", "environment:\n      name: production",
                   "stage: deploy")


def _deploy_surface(root: Path) -> set[str]:
    """Выкатка ищется в конфигах CI — и только там.

    ⚠ Исключение `BREAKER_EXCLUDE` сюда НЕ применяется, хотя конфиги CI в нём
    есть: джоба деплоя живёт ровно там, куда запрещено смотреть детектору
    отправки. Общее исключение ослепило бы эту половину полностью.
    """
    for rel in (".gitlab-ci.yml", ".github/workflows"):
        target = root / rel
        files = sorted(target.rglob("*.yml")) if target.is_dir() else [target]
        for f in files:
            if f.is_file() and any(m in f.read_text(encoding="utf-8", errors="ignore")
                                   for m in _DEPLOY_MARKERS):
                return {"прод"}
    return set()


def _outward_surface(root: Path) -> set[str]:
    """Отправка ищется в ПРОДУКТОВОМ коде, механика контура исключена.

    ⚠ Население сужено не вкусом, а первым же прогоном: первая редакция краснела
    НА САМОЙ СЕБЕ — список имён отправителей лежит строками в этом же файле, и
    детектор честно находил в нём `sendgrid`. Ложное красное на механике контура
    это случай, после которого проверку снимают целиком (§4.3b), и он же уже
    стоил ревизии breaker'ам объёма. Исключение то же самое и по той же причине:
    механика контура — не blast radius продукта.
    """
    for f in root.rglob("*.py"):
        if f.relative_to(root).as_posix().startswith(BREAKER_EXCLUDE):
            continue
        if any(part in {".venv", "node_modules", "__pycache__"} for part in f.parts):
            continue
        low = f.read_text(encoding="utf-8", errors="ignore").lower()
        if any(m in low for m in _OUTWARD_MARKERS):
            return {"отправка наружу"}
    return set()


def _detected_surfaces(root: Path) -> set[str]:
    """Что видно, не спрашивая человека. Остальное обязан назвать он сам.

    Две половины разнесены не по длине, а потому что у них РАЗНОЕ население —
    см. предупреждения в каждой.
    """
    return _deploy_surface(root) | _outward_surface(root)


def _signature_is_valid(status: str, root: Path) -> bool:
    """Подпись проверяется ИСПОЛНЕНИЕМ `ssh-keygen -Y verify`, а не наличием файла.

    Файл рядом — это снова «объявленный реестр без файла» наоборот: подпись,
    которую никто не проверил, неотличима от подделанной. Ключ обязан жить там,
    куда агент не дотягивается (анклав, аппаратный токен): подпись из файла в
    `~/.ssh` — это та же строка текста, только длиннее.
    """
    sig = root / "delivery" / "active" / "irreversible.sig"
    signers = root / ".github" / "allowed_signers"
    if not (sig.is_file() and signers.is_file()):
        return False
    import subprocess
    declared = field(status, "irreversible_surfaces")
    who = signers.read_text(encoding="utf-8").split()[0] if signers.read_text().split() else ""
    proc = subprocess.run(
        ["ssh-keygen", "-Y", "verify", "-f", str(signers), "-I", who,
         "-n", "irreversible", "-s", str(sig)],
        input=declared.encode("utf-8"), capture_output=True, check=False,
    )
    return proc.returncode == 0


def unsigned_irreversible_gaps(status: str, root: Path = ROOT) -> list[str]:
    """§3.4a: необратимое, до которого агент дотягивается без человека.

    Три состояния, и они обязаны различаться. Поверхность видна детектором, но
    не объявлена — молчание, и оно дороже ответа. Объявлена и не подписана —
    остановка. Объявлена и подписана — тихо.
    """
    declared, answered = declared_surfaces(status, "irreversible_surfaces")
    detected = _detected_surfaces(root)

    silent = detected - declared
    if silent and not answered:
        return [f"поле `irreversible_surfaces:` в STATUS не отвечает про "
                f"{', '.join(sorted(silent))} (§3.4a) — поверхность видно по коду, "
                "а в объявлении её нет. «Не думали» и «подумали и нет» обязаны "
                "различаться в тексте, а не в тишине"]
    if silent:
        return [f"`irreversible_surfaces:` не называет {', '.join(sorted(silent))} "
                "(§3.4a) — детектор видит это в коде. Либо назови, либо объясни "
                "в той же строке, почему это не необратимо"]
    if not declared:
        return []

    low = status.lower()
    if "max_unsigned_irreversible" in low:
        return ["порог `max_unsigned_irreversible` поднят строкой (§3.4a) — "
                "единственный breaker, у которого этого нельзя: он охраняет ровно "
                "класс «письменное разрешение подделывается агентом». Здесь waiver "
                "принимается только подписью"]
    if not _signature_is_valid(status, root):
        return [f"объявлено необратимое без человека в цепочке "
                f"({', '.join(sorted(declared))}), подписи нет или она не сходится "
                "(§3.4a). Это остановка, а не повод просить waiver: сначала "
                "`escalation.md` с двумя вариантами и ценой — поставить человека "
                "в цепочку или подписать. Подпись: `ssh-keygen -Y sign -f <ключ> "
                "-n irreversible` над строкой объявления"]
    return []


