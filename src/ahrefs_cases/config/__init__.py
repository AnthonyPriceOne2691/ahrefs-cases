"""Типизированный конфиг — единственный вход к настройкам.

Не реализовано (Ф1). Правило: `os.getenv` / `os.environ` вне этого
пакета запрещены, дефолт задаётся один раз, доступ — только `config.X`
(`getattr(config, "K", default)` прячет опечатки от mypy).

Один домен настроек = один файл: `ahrefs.py`, `storage.py`, `classify.py`,
`export.py`. Имена переменных — см. ../../../.env.example.
"""
