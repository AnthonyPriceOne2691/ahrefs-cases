"""Ruleset: версионированные пороги из БД, редактируемые из UI.

Не реализовано (Ф2). Сохранение из интерфейса создаёт **новую версию**, а не
правит текущую: вердикты привязаны к `ruleset_version` и обязаны оставаться
объяснимыми. Активна одна версия.

`config/thresholds.example.yml` — сид дефолтов для первого запуска и справочник
по структуре, не источник истины.

Пересчёт после смены порогов — явное действие (UI сначала показывает «N проектов
сменят группу»). Классификация бесплатна, API не дёргается — пересчитывать можно
сколько угодно раз.
"""
