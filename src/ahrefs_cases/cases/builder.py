"""Verdict + серии метрик → структура кейса, готовая к рендеру.

Не реализовано (Ф3). Детерминированно: один и тот же вердикт даёт один и тот же
кейс. Поле `anonymized` — часть кейса, а не решение на этапе рендера (Q12).
"""
