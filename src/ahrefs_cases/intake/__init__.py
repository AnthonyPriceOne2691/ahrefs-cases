"""Приём списка проектов: CSV, XLSX и Google Sheet → записи `Project`.

Раскладка:

- `rows.py`          — сырая таблица, одна для всех трёх источников
- `csv_source.py`    — CSV: кодировка цепочкой, разделитель по заголовку
- `xlsx_source.py`   — XLSX через openpyxl, значения приводятся к строкам
- `gsheet_source.py` — опубликованная таблица через CSV-экспорт
- `normalize.py`     — домен → канонический хост (ключ дедупликации и кэша)
- `validate.py`      — десять обязательных колонок, приведение типов, коды отказа
- `drafts.py`        — проверенная строка до записи в базу
- `upsert.py`        — создать или обновить проект, не удвоить
- `report.py`        — что принято, что отклонено и почему
- `accept.py`        — вход целиком: источник (путь, ссылка, байты) → база → отчёт
"""
