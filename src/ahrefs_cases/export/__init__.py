"""Рендер кейса в файл/страницу.

Ф3. Темы модулей:
- `html_renderer.py` — HTML-шаблон: единственный источник истины для всех форматов
- `charts.py`        — графики роста (inline SVG, без внешних зависимостей в артефакте)
- `pdf_renderer.py`  — печать HTML → PDF
- `docx_renderer.py` — только если DOCX согласован как обязательный (Q11)

Обязательная сноска в шаблоне: данные по трафику — оценка Ahrefs, не факт (Q4).
"""
